# REVUE CROISÉE A17 — **REJEU** sur correctif — incrément E18 « `companies.external_ref` reçoit son unicité »

> **Se lit AVEC `REVUE_A17_E18_2026-09-05.md`, pas à sa place.** Le premier verdict (REFUSÉ,
> trois bloquants) n'est ni amendé ni réécrit : il reste tel qu'il a été rendu, et ce fichier
> dit ce que le correctif en a fermé, ce qu'il en laisse ouvert, et ce qu'il a fait apparaître.
> **Étape 4 du pipeline (09 §3).** Le réviseur croisé n'a rien produit : il constate et il rend.
> **Aucun `git add`, `git commit` ni `git push` n'a été exécuté par cette revue** — règle posée
> le 2026-09-05 (`DECISIONS.md`, « Un réviseur qui commite son verdict est-il un lecteur ? » ;
> `ORGANISATION_AGENTS.md` §3 amendé). Ce fichier est **déposé** ; c'est le pilote qui le commite.

| | |
| --- | --- |
| **Lot / incrément** | L1 · E18 `external_ref` (PR **#34**) |
| **Branche · tête** | `lot/l1-e18-external-ref` · **`e6af20a`** |
| **Base de comparaison** | `origin/main` = **`ab6dcf5`** (merge-base = `ab6dcf5` : la branche a intégré `main`) |
| **Tête du premier verdict** | `c77a021` |
| **Date** | 2026-09-05 |
| **Réviseur** | A17 — réviseur croisé backend |
| **VERDICT** | 🔴 **REFUSÉ** — **1 bloquant résiduel** (B-1 et B-3 sont fermés) |

---

## 1. Périmètre relu — mesuré, pas déclaré

`git diff --numstat origin/main...HEAD` → **15 fichiers, +2 561 / −80.** 100 % relus.

Fait vérifié en premier, parce que c'est là que la perte de B-1 se serait reproduite :
**la fusion `e6af20a` (qui apporte L5a `#30` et la gouvernance `#44`) ne supprime RIEN.**

| Fichier | + / − | Relu |
| --- | --- | --- |
| `apps/api/drizzle/0015_unicite_companies_external_ref.sql` | +88 / −0 | intégralement (inchangé depuis `c77a021`) |
| `apps/api/schema-manifest.json` | +9 / −0 | intégralement |
| `docs/04_MODELE_DE_DONNEES.md` (§7.1) | +1 / −1 | intégralement |
| `docs/.pack-integrity.json` | +1 / −1 | intégralement |
| `packages/shared/src/errors.ts` | +74 / −0 | intégralement |
| `packages/shared/src/companies.ts` | +26 / −0 | intégralement |
| **`apps/api/src/domaines/companies/depot.ts`** | **+261 / −44** | **intégralement — dont le correctif B-2 (`63c68bc`, +55/−12)** |
| `apps/api/src/routes/companies.ts` | +22 / −5 | intégralement — **aucune ligne exécutable modifiée** (diff privé de commentaires = VIDE) |
| `apps/api/README.md` | +23 / −23 | intégralement |
| `apps/api/tests/l1-contraintes.integration.test.ts` | +220 / −1 | lu (A16 — non jugé) |
| **`apps/api/tests/l3a-companies.integration.test.ts`** | **+1 178 / −4** | **lu intégralement — dont les 378 lignes de `739e6ab`/`6a9db3c`** |
| `apps/api/tests/aide/base-l1.ts` | +55 / −1 | lu (A16 — non jugé) |
| `DECISIONS.md` | +62 / −0 | intégralement |
| `docs/ETAT.md` | +74 / −0 | intégralement |
| `docs/portes/REVUE_A17_E18_2026-09-05.md` | +467 / −0 | mon propre verdict — hors jugement |

Lectures : `11_CONTRAT_TECHNIQUE.md` §1/§2/§3/§8 · `04 §7.1` · `00_INDEX` (8 invariants) · `07 §12` ·
`DECISIONS.md` (les quatre entrées E18 des 2026-09-04 et 2026-09-05) ·
`.github/coverage-critical-paths.json` · `vitest.config.ts` · `docs/TRACABILITE_E1-E47.md`.

---

## 2. Les trois bloquants du premier verdict — vérifiés un par un

### ✅ B-1 · `docs/ETAT.md` — **FERMÉ**, et l'arithmétique tombe juste

Recompté exactement comme la première fois, contre `origin/main` = `ab6dcf5` :

| Mesure | `origin/main` | branche | écart |
| --- | --- | --- | --- |
| `wc -l docs/ETAT.md` | 2 849 | **2 923** | +74 |
| en-têtes `^## 2026-` | 82 | **85** | **+3, aucun perdu** |
| lignes retirées (`diff main → branche`, `^<`) | — | — | **0** |

- `comm -23` des deux listes d'en-têtes triées → **vide** : aucun bloc de `main` n'est absent.
- Les 3 en-têtes ajoutés sont bien ceux d'E18 (`09-03 15h10`, `09-03 17h05`, `09-05 02h00`).
- **Le bloc `## 2026-09-03 06h45 — [infra / diagnostic staging]`** : présent **une seule fois**
  (`docs/ETAT.md:2530` — **pas de doublon**), **26 lignes** (2530→2555, l'en-tête suivant est à
  2556), et **identique mot pour mot** à la version de `main` (`diff` des deux blocs extraits →
  aucune différence). `loadComposeFile` est de retour (`:2535`).
- **Place chronologique correcte** : entre le bloc précédent et
  `## 2026-09-03 08h51 — [lot L3 / porte]`, exactement là où `main` le porte.
- **Arithmétique** : 2 849 + 57 (blocs E18 du 09-03, insérés en 2796-2852) + 17 (bloc du 09-05,
  2907-2923) = **2 923.** Elle ferme.

**La fusion `e6af20a` n'a pas reproduit la perte, et c'est vérifié au-delà d'`ETAT.md`** :
`git diff --numstat origin/main...HEAD` donne **`DECISIONS.md` 62/0** et **`ETAT.md` 74/0** — zéro
suppression ; `git diff --diff-filter=DR` → **aucun fichier supprimé ni renommé**. `DECISIONS.md`
reste append-only : le texte de la prémisse du 2026-08-31 est toujours en place (`:5977`), il n'a
pas été réécrit — seul son numéro de ligne a bougé sous l'insertion de L5a.

Garde rejoué : `check-decisions.mjs` **vert** (211 entrées, toutes au format 11 §9bis). Les deux
avertissements « la date recule » **préexistent sur `origin/main`** (mesuré : même script sur
`origin/main:DECISIONS.md`, 209 entrées, **les deux mêmes reculs**) — ce n'est pas cette branche.

### ✅ B-3 · La symétrie du SIREN — **FERMÉ**, et la sonde est propre

Cinq tests livrés par A16 (`739e6ab` puis `6a9db3c`), tous `@critique`, 33 → 38 sur le fichier :

| # | Cas | Assertion qui compte |
| --- | --- | --- |
| 1 | `POST` sur le SIREN d'une fiche **archivée** (`:2215`) | `details[0].code === 'fiche_archivee'` (`:2258`), message « ARCHIVÉE » + restauration |
| 2 | `PATCH` vers le SIREN d'une fiche **archivée** (`:2282`) | même 409, `code` `fiche_archivee` (`:2309`), transaction annulée |
| 3 | SIREN d'une fiche **vivante**, `POST` **et** `PATCH` (`:2324`) | `fiche_active` (`:2356`, `:2372`) — **la moitié qui manquait** |
| 4 | `POST` — chemin dégradé (`:2472`) | 409 + `code` garantis, **`'details' in corps.error === false`** (`:2520`) |
| 5 | `PATCH` — chemin dégradé (`:2527`) | idem (`:2562`) + la candidate n'a **pas** reçu le SIREN (`:2564`) |

Les deux cas que mon premier verdict réclamait nommément (`fiche_archivee` sur le SIREN par `POST`
et par `PATCH`, plus `fiche_active` sur le cas nominal) sont là. **B-3 est fermé.**

**La sonde de chronologie (`poserLaSonde`, `:2431-2465`) est retirée proprement, et elle ne peut
pas fuir — vérifié point par point :**

- retrait par **`Reflect.deleteProperty(pool, 'query')`** (`:2461`), suivi d'une assertion
  d'absence **après** (`:2462`) ;
- assertion d'absence **avant** la pose (`:2456`, « aucune sonde antérieure ne traîne ») — et c'est
  **exactement le bon garde ici** : le projet `integration` tourne en
  **`poolOptions: { forks: { singleFork: true } }`** (`vitest.config.ts:93`), donc `src/db.js` — et
  son `pool` — est **partagé par tous les fichiers d'intégration du run**. Une sonde qui fuirait
  contaminerait les 22 autres fichiers ; ce garde la ferait rougir au fichier suivant ;
- `sonde.retirer()` est appelé dans un **`finally`** (`:2490`, `:2551`) : même si l'appel HTTP lève,
  la sonde tombe ;
- la sonde ne **simule** rien : elle `DELETE` réellement par la connexion du test et **attend le
  commit** (`:2447-2448`) avant de laisser passer la requête d'origine **inchangée** (`:2450`) ;
- elle est **discriminante** : `compteur === 0` + `SELECT` + `"companies"` + le SIREN du test en
  paramètre (`:2440-2444`) — un SIREN factice unique par test, donc pas de reconnaissance croisée ;
- **anti-vacuité** : `declenchements() === 1` (`:2499`, `:2555`) et `compterParSiren` passe de 1
  (`:2481`) à 0 (`:2500`) — un test qui ne prouverait rien rougirait ici.
- La preuve par bascule est **déclarée** dans le message de `739e6ab` (`archivee` forcé à `false`
  → exactement 4 rouges ; `code` retiré → 8 rouges). Je ne l'ai pas rejouée : elle exigerait de
  modifier du code de production, ce que mon rôle m'interdit. Les assertions anti-vacuité
  ci-dessus en sont le substitut structurel, et elles sont dans le fichier.

Une seule réserve, mineure (voir `m-6`) : le `catch` posé par le correctif B-2 avale aussi l'échec
d'assertion **de la sonde elle-même** (`:2448`). Le test survit parce que `compterParSiren === 0`
(`:2500`) est une assertion indépendante — filet réel, mais par chance et non par construction.

### 🔴 B-2 · Le correctif est **JUSTE** — mais **AUCUN test ne l'exécute**. Bloquant résiduel **B-2r**

**Ce qui est fermé, et bien fermé.** `lireFicheEnConflit` (`depot.ts:290-308`) :

```ts
  let lignes: { id: string; deletedAt: Date | null }[];
  try {
    lignes = await db.select({ id: companies.id, deletedAt: companies.deletedAt })
      .from(companies).where(eq(companies[colonne], valeur)).limit(1);
  } catch {
    return null;                                   // ← depot.ts:303
  }
```

- Le `catch` couvre **toute** la lecture : le seul `await` est dans le `try` ; ce qui reste dehors
  (`lignes[0]`, un accès de tableau) ne peut pas lever.
- **Le 409 sort sans `details`, jamais avec un `details` partiel** : `detailDeConflit` rend
  `undefined` dès `existante === null` (`:565`) — la construction du tableau est **après** ce
  `return`, il n'existe aucun chemin qui produise un `details` amputé de son `code`.
- **Aucun état n'est présumé** : `existante?.archivee === true` (`:596`, `:612`) retombe sur le
  message générique, jamais sur celui de l'archive.
- **Le chemin est le même pour `POST` et pour `PATCH`** — vérifié à la source, pas supposé :
  `insererEntreprise` (`:687`) et `mettreAJourEntreprise` (`:743`) appellent **la même**
  `traduireEchecDeContrainte`, qui appelle **la même** `lireFicheEnConflit`. Une implémentation,
  deux appelants.
- Le commentaire nomme la cause et **borne son propre remède** (`:269-285`) : la seconde connexion
  du pool prise sous transaction avortée, `max: 10` / 5 s, et le refus explicite de glisser un
  savepoint hors arbitrage. C'est la bonne réponse à 11 §8.

**Ce qui n'est pas fermé — et c'est mesuré, pas déduit.** J'ai téléchargé le rapport de couverture
**produit par la CI sur `e6af20a`** (artefact `rapport-couverture` du run `33932745763`) et lu son
`lcov.info` ligne à ligne. Lignes de `depot.ts` **jamais exécutées** par les 585 tests
d'intégration :

```
94, 95, 303, 304, 489, 490, 631, 632, 697, 698
```

**La ligne 303 est le `return null;` du `catch` de B-2.** Elle est **neuve** (`git show 63c68bc` la
porte en `+`). Les autres sont des replis défensifs préexistants (`INTERNAL_ERROR` inatteignables
`:94`/`:697`, `return false` de fin de boucle `:489`, relance `throw erreur` `:631`).

**Conséquence, dite sans détour : on peut supprimer le correctif B-2 et les 585 tests restent
verts.** Les deux tests « chemin dégradé » d'A16 ne l'atteignent pas, et ils ne le prétendent pas :
ils construisent le cas « la fiche a **disparu** » — la relecture **réussit** et ne trouve rien,
chemin déjà géré **avant** `63c68bc`. B-2 vise le cas où la relecture **échoue**. C'est exactement
l'avertissement de l'addendum de mon premier verdict, et il se vérifie.

- **Ce que ça viole** : 09 §3 étape 5 et la DoD transverse — un correctif de comportement sans test.
  C'est **mot pour mot** le motif pour lequel B-3 était bloquant, et la phrase est du fichier d'A16
  lui-même (`l3a-…:1912`) : « une branche de traduction que **personne n'exécute** disparaît sans
  bruit ». Appliquer ici une règle plus douce que celle que j'ai appliquée à B-3 serait arbitraire,
  et la règle était la mienne.
- **Rien d'autre ne le rattrape** : `apps/api/src/domaines/companies/**` n'est sous **aucun** seuil
  de couverture (voir M-5). Le job vert « couverture ≥ 90 % » ne dit rien de cette ligne.
- **Mon premier verdict le demandait explicitement** : « Un `try { … } catch { null }` … **Un test
  capable de rougir doit l'accompagner (A16)**. » Le `try/catch` est livré ; le test ne l'est pas.
- **Bloque la fusion de #34 : OUI** — et c'est le seul point qui la bloque.
- **Ce qui le ferme, et le coût est proche de zéro** — la machinerie existe déjà dans le fichier, à
  quinze lignes près, **et sans toucher une ligne de production (09 §5.6 tenu)** : reprendre
  `poserLaSonde` et, au lieu de supprimer la fiche, **faire échouer la relecture** (que la
  remplaçante lève, ou rende une promesse rejetée, quand elle reconnaît le `SELECT`). Le test doit
  alors asserter, `POST` **et** `PATCH` :
  1. statut **409** et `error.code` attendu — le contrat garanti ;
  2. `'details' in corps.error === false` — sur le **corps brut**, par le schéma strict ;
  3. **et le discriminant qui le distingue des tests 4-5 : la fiche en conflit EXISTE TOUJOURS**
     (`compterParSiren === 1`). C'est ce qui prouve que la relecture a **échoué** et non qu'elle
     n'a **rien trouvé** — sans cette assertion, le nouveau test serait un doublon des existants.

  Ôtez le `try/catch` : ce test doit rougir en 500. C'est le seul critère qui compte.

---

## 3. Constats MAJEURS

### ✅ M-4 · `docs/ETAT.md` sans bloc pour les 09-04 / 09-05 — **FERMÉ**

`docs/ETAT.md:2908` porte `## 2026-09-05 02h00 — [lot L1 / incrément E18] — étape pipeline 4/7`.
Il nomme le dernier commit vert, la branche, l'état poussé ; il récapitule ce qui a été livré depuis
le bloc du 03 17h05 (`b89dfdf`, `b713f84`, `308ed1a`) ; sa « Prochaine action » est exécutable telle
quelle par une session neuve. Format `CLAUDE.md` §8 tenu. **Plus aucune session neuve ne croirait le
défaut ① ouvert.**

### ✅ m-1 · La citation `drizzle-orm@0.44.7` — **FERMÉ**

`depot.ts:463-465` porte désormais « MESURÉ sur `drizzle-orm@0.44.7` … et **RE-MESURÉ sur `0.45.2`**
… le 2026-09-05 : les 580 tests d'intégration du run CI 33927012410 n'obtiennent leurs 409 que si
cette remontée mord ». Mesure datée et sourcée. C'est ce qui était demandé.

### 🟠 M-1 · La prémisse fausse — **fermée en substance, pas dans la forme**

Le journal du 2026-09-03 ordonnait « rectifier par une entrée datée, jamais en réécrivant celle du
jour ». **L'entrée du 2026-09-05 (`DECISIONS.md:7990`) le fait en substance** : elle écrit noir sur
blanc qu'« un **administrateur** peut poser une référence console **arbitraire** », ce qui contredit
frontalement la prémisse. Et — vérifié, c'est le point important — **rien n'a été réécrit** : les
deux occurrences fautives sont toujours là, à l'identique (`DECISIONS.md:5977` et `:7511`,
« `external_ref` n'est écrit par aucune route … — **vérifié** »). L'append-only est respecté.

**Ce qui reste** : aucune des deux occurrences ne pointe vers l'entrée du 09-05. Un lecteur qui
arrive en `:5977` lit « vérifié » sur un énoncé faux, sans rien qui le renvoie plus loin. Le
précédent maison (`DECISIONS.md:6478`, 2026-09-02) nommait explicitement la prémisse fausse.
**Rétrogradé de MAJEUR à MINEUR** (`m-5`) : l'exigence de fond est satisfaite.

### 🟠 M-2 · L'identifiant de la fiche en conflit n'est lisible **que dans une phrase française** — **TOUJOURS OUVERT, et jamais arbitré**

**Il faut lever une confusion, parce qu'elle allait faire disparaître ce constat.** Le brief de ce
rejeu présente « M-2 (qui écrit `external_ref`) » comme tranché par A01. Ce qui a été tranché le
2026-09-05 (`DECISIONS.md:7990`), c'est **le doute du §5** de mon premier verdict — « qui a le droit
d'ÉCRIRE `companies.external_ref` ». **Ce n'est pas M-2.** M-2 était, et reste :

> `depot.ts:561-575` — `detailDeConflit` met l'**état** dans `code` et l'**identifiant** dans
> `message` (`Fiche archivée existante : ${existante.id}`), alors que la doctrine
> d'`errorDetailSchema`, **dans `packages/shared/src/errors.ts:307-325`**, pose l'inverse :
> `message` « est de l'INTERFACE », « affiché TEL QUEL », et « écrire un identifiant technique dans
> `message` … revient à afficher `en_analyse` à un utilisateur ».

Recherche faite sur tout `DECISIONS.md` (`detailDeConflit`, `errorDetailSchema`, « identifiant de la
fiche », « phrase française ») : **aucune entrée n'arbitre ce point.** Les quatre entrées E18
(`:7630`, `:7653`, `:7824`, `:7990`) portent sur le code d'erreur, la fiche archivée, le contrat de
`details` et l'auteur de l'écriture — jamais sur l'endroit où vit l'identifiant. La console à qui
l'on promet un 409 « ACTIONNABLE » doit toujours extraire un UUID **par expression régulière dans
une phrase française**, laquelle a quatre variantes dans ce diff.

**Je ne rouvre pas la question de `external_ref` — elle est tranchée, option 1, et le code y est
conforme (voir §4).** Je constate que **M-2 n'a jamais été posé à A01**, et je le remets sur la
table pour qu'il ne se perde pas dans un glissement d'étiquette.
**Ne bloque pas la fusion** (le comportement préexiste au diff) ; **appelle un arbitrage A01**,
parce que `details[0].code` porte désormais l'état et qu'il n'y a plus de champ libre pour
l'identifiant sans amender `errorDetailSchema` — donc escalade 11 §8-2, pas une correction de
réviseur.

### 🟠 M-3 · `docs/TRACABILITE_E1-E47.md` — **INCHANGÉ, et je le redis pour qu'il ne se perde pas**

Fichier du gardien **A02**, à traiter à **l'étape 6**. Re-mesuré aujourd'hui sur `e6af20a` :

| Ligne | Ce que le fichier affirme | Mesuré le 2026-09-05 |
| --- | --- | --- |
| `:222` | « `errors.ts` porte **17** `ERROR_CODES` » | **22** (comptage sur le bloc `ERROR_CODES`) |
| `:100` | E18 « partiellement amorcée » — `companies.external_ref` sans plus | l'unicité est posée par `0015` |
| `:2143` | E18 — « `external_ref` **sans unicité**, `DECISIONS.md` 2026-08-31, **non tranché** » | **tranché le 2026-09-03 par Williams** |

Et la **section B** (code → exigences), dont le mode d'emploi §4 dit qu'« un artefact sans
rattachement est un **refus de lot** » : `grep` sur `0015`, `COMPANY_EXTERNAL_REF_DUPLICATE` et
`uq_companies_external_ref` → **aucun résultat**, les trois y sont toujours absents.
**Ne bloque pas la fusion ; bloque l'acceptation.** Le rattachement existe et est correct (§7) :
c'est le registre qui est en retard, pas le code qui est orphelin.

### 🟠 M-5 · Le module `companies` n'est sous **aucun** seuil — et il est **sous 90 %** là où on le mesure

**D'abord, je corrige une imprécision de mon premier verdict**, parce qu'un réviseur qui exige la
mesure se la doit à lui-même. J'avais écrit que le seuil vivait dans `vitest.config.ts:124`
(`thresholds: {}`) et dans « la liste des chemins seuillés (l. 116-120) ». C'est inexact : les
lignes 109-115 sont l'`include` de **collecte**, et `thresholds: {}` est **volontairement vide**.
Le seuil réel est appliqué par le job `coverage` de la CI via
**`.github/coverage-critical-paths.json`** et `.github/scripts/check-coverage.mjs`.

**Mais le constat, lui, tient — et il est pire que ce que j'avais dit.** Dans ce fichier versionné :

- `apps/api/src/domaines/companies/**` n'est **ni** dans `cheminsCritiques` **ni** dans
  `cheminsAttendus`. Idem pour `apps/api/src/routes/companies.ts`.
- **`companies` est le SEUL domaine de L3 absent de la liste** : `missions`, `org-units`,
  `questionnaire` et `plan-entretiens` y sont tous entrés le 2026-09-02 ; `journal`, `users`,
  `scoping` et `auth` au L2. C'est une asymétrie, pas une doctrine.
- **Le job vert « couverture ≥ 90 % (modules critiques — 09 §3) » ne dit donc RIEN de ce module.**

Chiffres **mesurés** dans le `coverage-summary.json` produit par la CI **sur `e6af20a`** :

| Fichier | lignes | instructions | fonctions | **branches** |
| --- | --- | --- | --- | --- |
| `domaines/companies/depot.ts` | 95,79 | 95,79 | 100 | **89,04** ❌ |
| `domaines/companies/service.ts` | 91,66 | 91,66 | **77,77** ❌ | **88,33** ❌ |
| `routes/companies.ts` | 97,89 | 97,89 | 100 | 100 ✅ |

**Si `companies` était inscrit, la CI serait ROUGE aujourd'hui.** Le bandeau du fichier JSON dit
précisément quoi en faire : « un module sous le seuil se corrige par des **tests**, jamais par un
rétrécissement de périmètre » — et le précédent existe (`missions` et `org-units` ont été inscrits
**sous** le seuil le 2026-09-02, CI rouge assumée, puis tenus par des tests).

**Je ne fais pas de ce point un bloquant de fusion**, et je dis pourquoi plutôt que de le laisser
entendre : `companies` n'est nommé « module critique » ni par 09 §3 (qui liste sync, crypto locale,
scoring, RBAC/propriété) ni par la note de conception L3 §4. **L'inscrire est donc une décision, et
elle appartient à A01/A02, pas à un réviseur.** Je la recommande, chiffres à l'appui. Elle éclaire
aussi B-2r : sans seuil, rien d'autre que le test manquant ne protège `depot.ts:303`.

---

## 4. Conformité à l'arbitrage A01 du 2026-09-05 (option 1) — vérifiée

L'entrée `DECISIONS.md:7990` retient : « `externalRef` reste écrit par l'API, **admin seul** ;
l'unicité suffit ; **L13 réconcilie** », avec deux bornes. Le code y est conforme :

- **`externalRef` reste accepté en entrée** : `packages/shared/src/companies.ts` l'expose en création
  et en `PATCH` — inchangé, conforme à l'option 1 (les options 2 et 3 sont écartées).
- **Les quatre routes restent `roles: ['admin']`** : `routes/companies.ts:79` (`CONFIG_ADMIN`),
  appliqué aux quatre déclarations. **Vérifié mécaniquement** : le diff de `routes/companies.ts`
  privé de ses lignes de commentaire est **vide** — aucune ligne exécutable n'a bougé, et aucune
  route n'a été ajoutée (`grep` sur `instance.(get|post|patch|put|delete)` dans les lignes `+` →
  aucun résultat).
- **L'unicité, et rien de plus** : `0015` garantit qu'une référence désigne une fiche et une seule ;
  aucun code n'a été ajouté pour « confirmer » une référence — ce qui aurait été l'option 3, écartée.
- La borne « **L13 devra confronter** » s'écrit dans le brief de L13, comme l'entrée le dit
  elle-même. **Rien à faire dans cet incrément.** Je ne rouvre pas la question.

---

## 5. Les 8 invariants, passés un par un sur le diff

| # | Invariant | Verdict | Preuve |
| --- | --- | --- | --- |
| 1 | Offline-first, UUID v7 **client**, push idempotent | **OK / sans objet** | `companies` n'est pas créable hors ligne ; le schéma de création **refuse** `id` (objet strict). Aucune génération d'UUID en SQL dans `0015` (`uuidv7`/`gen_random_uuid`/`uuid_generate` : 0 occurrence). 11 §2 tenu. |
| 2 | Aucune référence client dans le code | **OK** | Fixtures uniquement : « Entreprise factice Nu / Xi / Oméga / Bêta », `sirenFactice(...)`, `console-essai-unicite-0001`, comptes `siren-degrade-post` / `-patch`. `axion-ia.com` est la console de l'éditeur, pas un client. |
| 3 | RBAC serveur, financier admin-only, 05 §9.9 | **OK** | `CONFIG_ADMIN` sur les quatre routes, **aucune ligne exécutable modifiée** dans `routes/companies.ts` (mesuré), aucune route nouvelle. `scoping_financials` hors périmètre. §9.9 sans objet (pas de sync). |
| 4 | Aucune couleur ni taille en dur | **sans objet** | Aucun front dans le diff E18. |
| 5 | Interface 100 % français, UTC | **OK** | Les quatre messages de 409 et le garde de migration sont en français. Aucune date manipulée ; `TIMESTAMPTZ` inchangé. |
| 6 | Le terrain collecte, le siège produit | **sans objet** | Rien sur la machine terrain. |
| 7 | Correction = révision tracée, rien d'écrasé | **OK, et renforcé** | L'index n'exclut pas `deleted_at` : une archive **garde ses liens**. La descente est un `DROP INDEX` : aucune donnée perdue. Le garde de migration **refuse la montée** au lieu de dédupliquer seul. Et `DECISIONS.md` / `ETAT.md` : **0 ligne retirée** vs `main` (mesuré) — l'écart de B-1 est réparé **par ajout**, sans réécriture. |
| 8 | Sauvegarde / rien ne vit sur un seul support | **OK** — l'écart du premier verdict est levé | B-1 était de cette famille (une trace de reprise détruite par une fusion) ; le bloc est restauré, entier et unique. |

---

## 6. Interdictions 11 §2 et conventions 11 §3

**11 §2 — OK.** Mesuré sur le diff : aucune génération d'UUID v7 en SQL · `package.json` et
`pnpm-lock.yaml` **hors du diff E18** (aucune dépendance ajoutée par cet incrément ; 11 §8-1 sans
objet) · aucun Next, aucun Prisma (DDL en SQL brut versionné, `src/db/schema.ts` inchangé) · pas de
CORS · MinIO non touché · **aucun log ajouté**, donc aucune donnée personnelle dans pino · job
`gitleaks` **vert** · job « aucun test désactivé ni orphelin » **vert** (`check-no-skipped-tests.mjs`
rejoué par moi : 98 fichiers, **aucun** test désactivé).

**11 §3 — un seul écart, et c'est M-2.** Le reste est tenu, vérifié :

- enveloppe unique via `AppError` puis `toResponse()` ; aucune route ne construit d'enveloppe à la main ;
- `COMPANY_EXTERNAL_REF_DUPLICATE` présent dans `ERROR_CODES` **et** dans
  `HTTP_STATUS_BY_ERROR_CODE` — le `Record<ErrorCode, number>` rendrait la compilation rouge si l'un
  manquait : garde **structurelle**, pas déclarative ;
- Zod in/out depuis `packages/shared`, forme déclarative conservée, aucun parsage manuel ;
- **zéro `any`** : recherche sur **toutes** les lignes ajoutées du diff (`apps` + `packages`) →
  **0 occurrence** ;
- **keyset intact** : aucun `offset` dans les lignes ajoutées ; le curseur `(name, id)` n'est pas touché ;
- `snake_case` vers `camelCase` : `external_ref` en base et en SQL, `externalRef` en TS et dans
  `details[].path` — cohérent avec les chemins Zod rendus au front ;
- **le contrat du chemin dégradé est désormais VRAI** sur ce qu'il garantit (statut + `error.code`),
  ce qui était l'objet de B-2. Réserve de rédaction : voir `m-7`.

**Schéma — inchangé depuis le premier verdict, et toujours conforme.** `0015` est une transcription
littérale du 04 §7.1 amendé (l'amendement du 2026-09-03 est bien la seule ligne modifiée du 04,
vérifiée mot à mot). Garde de doublons **avant** la création de l'index ; descente sans garde, avec
restitution mot pour mot du `COMMENT ON COLUMN` de `0002`. Manifeste à jour. **Job « 6 · schema-diff
(vs fichier 04) » : SUCCESS sur `e6af20a`.** Sceau du pack : `check-pack-integrity.mjs` rejoué par
moi rend « les 12 fichiers sont conformes au sceau ».

**Gardes du dépôt rejoués par moi sur `e6af20a`** : `check-pack-integrity` OK · `check-decisions` OK
(211 entrées ; les 2 avertissements de date préexistent sur `main`) · `check-invariants` OK ·
`check-no-skipped-tests` OK (98 fichiers) · `check-tracabilite-exigences` OK (695 citations,
349 fichiers). `schema-diff.mjs` ne tourne pas localement (`DATABASE_URL` absente) — la CI l'a
exécuté, vert.

**CI sur la tête `e6af20a`** (run `33932745763`, `headSha` vérifié) : **20 jobs SUCCESS**, 1 SKIPPED
(`deploy-staging`, réservé à `main`). Zéro rouge. L'échec `shellcheck` du commit intermédiaire
`3ad3c3e` a été résolu par la fusion de `main`.

---

## 7. Code orphelin, micro-améliorations

- **Code orphelin : aucun.** `0015` se rattache à **E18** (04 §7.1 amendé, 03 M8.1, 05 §8.6) ;
  `COMPANY_EXTERNAL_REF_DUPLICATE` à **E43** et **E18** ; les tests citent E18 en tête de bloc ;
  `routes/companies.ts` porte « E19 · **E18** · E43 · E33 ». Aucune route, table ou job nouveau.
  Réserve **M-3** : la section B du registre ne les a toujours pas enregistrés — registre en retard,
  code rattaché.
- **Micro-améliorations d'étage 1 : 0 dans ce diff**, et c'est cohérent : `AMELIORATIONS.md` n'est
  pas modifié par l'incrément E18, et tout ce qui a été ajouté relève d'un arbitrage daté (quatre
  entrées `DECISIONS.md`). Le plafond de 0,5 j/lot n'est pas entamé. **Aucune ligne
  `AMELIORATIONS.md` n'était requise — donc aucune ne manque.**
- **Aucune fiche d'étage 2 implémentée par anticipation** — 11 §8-7 tenu.

---

## 8. Constats MINEURS

- **m-2 · `depot.ts:311` et `:316`** — `lireFicheParSiren` / `lireFicheParRefExterne` sont toujours
  `export` **sans aucun appelant hors du module** (recherche sur tout le dépôt, tests compris : seuls
  `:590` et `:609` ; les seules autres occurrences sont dans `dist/`, artefacts de build). Inchangé.
- **m-3 · le vocabulaire « FERMÉ » n'existe toujours nulle part comme valeur.** `errors.ts:132-133`
  promet un `code` pris dans `{ fiche_active, fiche_archivee }` — vocabulaire **FERMÉ** — mais
  `errorDetailSchema.code` reste `z.string().optional()` et les deux littéraux ne vivent que dans
  `apps/api` (`depot.ts:569`). Dans `packages/shared/src/`, les deux mots n'apparaissent **qu'en
  commentaire**. Le front doit recopier deux chaînes à la main. Le précédent maison est partagé
  (`org-units.ts`, `plan-entretiens.ts` exportent un `z.enum`). Inchangé.
- **m-4 · le `COMMENT ON COLUMN` du `@DOWN` n'est tenu que par la relecture.** `0015:86-88` restitue
  mot pour mot `0002:31-32` (vérifié à la main), mais `photographierCatalogue` compare tables,
  colonnes, contraintes et index — **pas** les commentaires (recherche de `comment` dans
  `l1-migrations.integration.test.ts` : aucun résultat). Une descente qui les perdrait resterait
  verte. Inchangé.
- **m-5 · (ex-M-1)** les deux occurrences de la prémisse fausse (`DECISIONS.md:5977`, `:7511`) ne
  pointent pas vers l'entrée du 2026-09-05 qui les corrige. Un renvoi d'une ligne suffirait — **par
  une entrée nouvelle**, jamais en réécrivant les anciennes.
- **m-6 · le `catch` de B-2 avale l'assertion de la sonde d'A16.** Si l'assertion sur
  `suppression.rowCount` (`l3a-…:2448`) échouait, l'erreur remonterait par `pool.query` jusqu'au
  `try` de `lireFicheEnConflit`, qui l'**avale**. Le test ne rougirait pas *là* ; il rougit quand
  même sur `compterParSiren === 0` (`:2500`), assertion indépendante. Filet réel, mais par chance et
  non par construction — à savoir si la sonde est réutilisée ailleurs.
- **m-7 · le contrat publié n'énumère qu'UNE cause d'absence de `details`, alors qu'il y en a
  maintenant deux.** `errors.ts:135` et `routes/companies.ts:177` disent « **absent si la fiche a
  disparu** entre la violation et la relecture » ; depuis `63c68bc`, `details` est aussi absent quand
  la relecture **échoue** (pool saturé). `depot.ts:524` est, lui, correct (« une lecture qui
  **échouerait** … dégrade le message »). Aucune conséquence pour un front (les deux causes se
  traitent pareil : « conflit constaté, fiche non nommée »), mais le texte le plus publié est le
  moins complet. Deux mots à ajouter.

---

## 9. Observations

- **o-1 · `external_ref` n'est ni normalisée ni comparée sans casse** (`refExterneSchema` : un `trim`
  et rien d'autre), là où le SIREN a sa normalisation éprouvée. « ABC1 » et « abc1 » seraient deux
  références distinctes pour un même client console. Défendable pour un identifiant opaque ;
  **à savoir avant L13**, qui doit « confronter » — c'est précisément la borne posée par l'arbitrage
  du 2026-09-05. À verser au brief de L13, pas à corriger ici.
- **o-2 · la fusion `e6af20a` est propre, et c'est vérifié, pas supposé.** Elle apporte L5a (`#30`)
  et deux PR de gouvernance (`#43`, `#44`) sans retirer une ligne nulle part. C'était le moment où
  B-1 se serait reproduit ; il ne s'est pas reproduit.
- **o-3 · l'ordre décision puis code reste respecté** : l'arbitrage du 2026-09-05 sur `external_ref`
  précède ce rejeu, et le code n'a rien anticipé de ses options écartées.
- **o-4 · la règle « le réviseur ne commite pas » est appliquée à la lettre** : aucun `git add`,
  `git commit` ni `git push` n'a été exécuté. L'arbre était propre à l'ouverture et ne porte, à la
  fermeture, que **ce seul fichier**, déposé et non commité.

---

## 10. Verdict

### 🔴 REFUSÉ — un bloquant résiduel, et un seul

| Classe | Nombre | Bloque la fusion de #34 ? |
| --- | --- | --- |
| **BLOQUANT** | **1** — B-2r (le correctif B-2 n'est exécuté par aucun test) | **OUI** |
| *(bloquants fermés)* | *2 — B-1 fermé, B-3 fermé* | *non* |
| **MAJEUR** | 3 ouverts — M-2, M-3, M-5 · *2 fermés : M-4, m-1* | non |
| **MINEUR** | 6 — m-2 à m-7 | non |
| **OBSERVATION** | 4 — o-1 à o-4 | non |

### La PR #34 peut-elle fusionner ? **NON — pas encore.** Une chose manque, une seule.

**Ce qui manque, exactement** : un test d'intégration qui fasse **ÉCHOUER** la relecture d'après
coup — pas disparaître la fiche — et qui vérifie que le 409 tient quand même. Sans lui,
`depot.ts:303` n'est exécuté par **aucun** des 585 tests (**mesuré sur le `lcov.info` de la CI, run
`33932745763`, tête `e6af20a`**), et le correctif de B-2 peut être supprimé sans qu'un seul test
rougisse. Le contrat publié dans `packages/shared` redeviendrait faux, silencieusement.

**Le remède est petit et il est balisé** : la sonde `poserLaSonde` d'A16 existe déjà et fait 34
lignes ; il s'agit de la faire **lever** au lieu de supprimer, pour `POST` et pour `PATCH`, en
assertant les trois points du §2 — 409 et `error.code` ; `details` absent sur le corps brut ; **et la
fiche en conflit toujours présente**, qui est le discriminant. **Par A16, jamais par le producteur**
(09 §5.6). Dès que ce test est vert — et rouge quand on ôte le `try/catch` —, **plus rien ne s'oppose
à la fusion** : les 20 jobs de CI sont verts, le schéma est conforme au 04, les 8 invariants passent,
il n'y a aucun code orphelin, et M-2, M-3, M-5 relèvent de l'étape 6 et d'A01, pas de la fusion.

**Et il faut dire le reste, parce que c'est l'essentiel du travail.** B-1 a été réparé **par ajout**,
sans réécriture d'historique, et l'arithmétique tombe juste au mot près. B-3 a été fermé par cinq
tests dont deux d'une qualité peu commune : la sonde de chronologie ne simule rien, elle **ordonne
deux événements réels**, elle est contenue correctement dans un projet `singleFork` où une fuite
aurait contaminé 22 fichiers, et elle porte ses propres assertions anti-vacuité. Le correctif B-2
lui-même est **juste** : il couvre toute la lecture, ne produit jamais de `details` partiel, traite
`POST` et `PATCH` par un seul chemin, et son commentaire **refuse explicitement** d'aller au-delà de
l'arbitrage (le savepoint est proposé, pas glissé). C'est du bon travail. Il lui manque la preuve que
ce dépôt exige de tout le reste : un test qui rougit.

**Désaccords à arbitrer par A01** :

1. **M-2** — où vit l'identifiant de la fiche en conflit, maintenant que `details[0].code` porte
   l'état ? **Jamais posé** : l'arbitrage du 2026-09-05 répondait au doute du §5 de mon premier
   verdict, pas à celui-ci. Le glissement d'étiquette est signalé pour qu'il ne se répète pas.
2. **M-5** — `apps/api/src/domaines/companies/**` entre-t-il dans `coverage-critical-paths.json` ?
   L'inscrire rendrait la CI rouge aujourd'hui (branches 89,04 et 88,33 ; fonctions 77,77). C'est une
   décision, avec un précédent daté du 2026-09-02.

**Pour A02, à l'étape 6** : **M-3** — les trois énoncés faux de `TRACABILITE_E1-E47.md` (`:222`,
`:100`, `:2143`) et les trois artefacts absents de sa section B.

**Doutes de spec pour `DECISIONS.md`** : **o-1** — `external_ref` n'est ni normalisée ni comparée
sans casse ; l'unicité porte donc sur la casse. À trancher **au brief de L13**, avec la borne
« L13 devra confronter » déjà posée.

---

**Rappel de rôle : je ne produis rien.** Aucun fichier de code, de test, de migration ou de
documentation du diff n'a été modifié par cette revue. Le seul fichier écrit est celui-ci, et il
n'est **pas commité** : l'arbre ne porte que son ajout, laissé au pilote.

**Signature de la revue croisée (rejeu) : A17 — 2026-09-05 — verdict REFUSÉ sur `e6af20a`,
un bloquant résiduel : B-2r.**
