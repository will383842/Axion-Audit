# REVUE CROISÉE A37 — lot **L8** (scoring) — A POSTERIORI — 2026-09-09

> **Étape 4/7 (09 §3), jouée après coup.** A02 a établi (`TRACABILITE_E1-E47.md` §O.14, R-O-3, #107)
> que **L8 est dans `main` par trois PR — #61, #65, #66 — sans revue croisée ni fiche de porte**. Huit
> arbitrages `[L8]` existent ; c'est l'étape 4 qui manquait. Réviseur croisé console A37, **lecture
> seule** — transcription par le pilote, sans amendement.
> **Périmètre** : `3a7937f` (#61, le moteur), `35c4737` (#65, couche croisée), `40948b4` (#66, les
> quatre façons de masquer un drapeau), `e8638a4` (#67) — **17 fichiers, ~5 900 lignes**. Sources relues
> **en entier** ; tests de conception lus par structure et blocs ciblés (voir §8).

---

## VERDICT : 🟠 SOUS RÉSERVE — 2 bloquantes, 6 à corriger, 4 remarques

**Les deux critères du 07 ligne L8 sont TENUS et mesurés** : les jeux de référence sont réellement
figés (attendus écrits à la main, retranscrits une seconde fois par la couche croisée), et « un drapeau
rouge n'est jamais masqué par la moyenne » tient — **les quatre correctifs de #66 éprouvés par mutation
létale, les quatre tuent**. Les réserves portent sur ce que le lot **publie à côté** du score, et sur
deux points où le moteur a tranché sans que rien ne le dise.

## A. Fidélité au barème §32.1 — CONFORME

**Jeux de référence authentiques** : `apps/api/tests/aide/scoring-jeux-de-reference.ts` porte quatre
jeux dont **chaque attendu est posé à la main avec son calcul écrit en toutes lettres**. UUID
synthétiques déterministes. La couche croisée **retranscrit les mêmes nombres en littéraux une seconde
fois** et vérifie que le fichier de jeux n'importe rien du moteur (`acceptation.test.ts:273`) — la seule
construction qui rende un attendu non « rattrapable ».

Trois cas recalculés à la main contre le texte : FIL-TPE bloc_1 **3,2** ✔ · bloc_2 fourchette = borne
basse, NC/SO hors calcul, **3,0**, ratio 0,80 ✔ · roll-up `headcount` NULL → poids 1, 164/51 = **3,22** ✔.
FIL-GC refait de bout en bout : **3,49** ✔.

## B. « Un drapeau rouge n'est jamais masqué » — TENU, et deux façons de plus

| Mutation (copie hors dépôt) | Sain | Muté | Garde qui rougit |
| --- | --- | --- | --- |
| MUT-1 seuil sur l'agrégat au lieu de chaque option | 1 drapeau | **0** | `acceptation.test.ts:1052` |
| MUT-3 `Object.is` au lieu de la coercition | 1 | **0** | `:1197` |
| MUT-2 suppression de `QUESTION_BLOQUANTE_JAMAIS_POSEE` | 1 anomalie | **0** | `:857` |
| MUT-4 déduplication « par question » | 2 propositions | **1** | `:344` |

**La cinquième — `red_flag` portant `values` ET `below` : le seuil est silencieusement désarmé.**
`bareme.ts:163-175` teste `Array.isArray(brut.values)` **en premier** et ne regarde jamais `below`.
Mesuré : question `bloquant`, `red_flag: { values: ['jamais_present'], below: 2 }`, réponse cotée **1**
→ **0 drapeau, 0 anomalie, silence total**. Or le contrat partagé (`banque-questions.ts:244-250`) dit
« **jamais les deux** » ; la doctrine de `bareme.ts:155-161` dit « un `red_flag` malformé ne s'ignore
pas, il invalide » ; et le test qui couvre le cas est **construit de sorte que la branche perdante ne
soit jamais atteinte**. Même famille que `memeValeur` (A01, 06). Gravité : à corriger, non bloquant —
la porte d'import L4 refuse la combinaison ; le résidu est celui que le moteur revendique défendre.

**La sixième — bandes non ordonnées.** `bareme.ts:206-220` valide la forme, jamais l'**ordre** ;
`coterParBandes:464-476` prend la première bande satisfaite. Mesuré : réponse 10 → score **5 au lieu de
1**, drapeau perdu, 0 anomalie. L4 contrôle l'ordre, le moteur non.

## C. Complétude et roll-up

Poids 0 ✔ · poids illisible → `NaN` ✔ · non répondue hors des deux ✔ · non communiqué hors score,
**maintenu au dénominateur** ✔. **Arbitrage A15 du 06 CONFIRMÉ** sur le résultat, avec réserve sur sa
justification asymétrique ; l'amendement candidat `cotées / (posées − sans objet)` est **nécessaire**.

## D. Orphelin et butoir §35.3

**Orphelin : oui, état déclaré** — aucune route, aucun écran. **Radar SVG : NON LIVRÉ** (07 ligne L8) —
`inventaire.ts:169-179` le marque `absent`. **L8 est livré à ~⅔ de sa ligne 07.** Pour être *utilisable*
au butoir : requête `EntreeScoring` depuis Drizzle, route admin + RBAC, persistance `block_scores` /
`unit_scores` (**impossible en l'état, R2**), filtre `missions.status`, radar, `scores.csv` dans le ZIP.
Le repli tableur du §35.3 est en revanche **réellement outillé**.

## Réserves

### R1 — BLOQUANTE — `indicatif` est vrai sur toute mission normalement échantillonnée

`moteur.ts:377-384` compte chaque question scorable comme « posée » pour **chaque unité**, y compris
celles qu'aucun plan d'entretiens ne prévoit. **Mesuré** : FIL-GC, 40 services interrogés sur 120 —
échantillonnage **conforme au §32.4** — donne `ratio = 0,267`, `indicatif = true` sur la mission
entière. Le seuil 0,60 devient un voyant allumé en permanence. **C'est le principe même d'A01 du 06**
(« un signal qui se déclenche toujours n'est plus un signal »), appliqué à un autre champ. Choix
conscient, **sans entrée `DECISIONS.md`**. Options : (a) `posees` restreint aux unités portant une
session ; (b) aux unités du plan (`work_assignments`) ; (c) statu quo écrit au brief du consommateur.
→ **A01/Williams.**

### R2 — BLOQUANTE — `seuil_fiabilite_answers` et `unit_scores.answers_count` : normés, ni implémentés ni tracés

04 l. 266-268 et 337, 11 §5 nomment **trois** clés ; `CLES_PARAMETRES_SCORING` n'en nomme que deux ;
le moteur ne produit aucun compte de réponses par (unité × bloc). **`unit_scores` du 04 ne peut pas
être écrite depuis la sortie de L8.** → implémenté, ou tracé.

### À corriger — C1 `{values, below}` · C2 bandes non ordonnées · C3 message « hors périmètre » faux pour une unité `in_scope=true` dont l'ancêtre est sorti · C4 la lecture direction/terrain (§32.1-5, « l'or du rapport ») n'est publiée qu'en cas de divergence — écart de 2,25 points → 0 publication · C5 `criticite` ajouté au contrat partagé sans trace · C6 radar absent, moteur non rattaché.

### Remarques — M1 roll-up par `headcount` propre, jamais par sous-arbre · M2 `single_choice` répondu par liste accepté · M3 doublon de question figée : la première fait foi, le drapeau peut disparaître · M4 attribution §5.6 non attestable par git.

## Croisement 09 §5.6 — respecté dans l'esprit, ÉCART dans la lettre

**La couche croisée est la meilleure relue sur ce dépôt** : elle retranscrit les attendus
indépendamment, arme le balayage d'étanchéité, double chaque garde textuelle d'une épreuve de
sensibilité. **Mais 166 des 211 tests ont été écrits par A15, auteur du moteur** (« tests écrits AVANT
le code »). Couverture par la couche croisée seule : **94,95 st / 79,72 br** — sous 90. Assumé en tête
du fichier, **pas tracé**.

## Les 8 invariants

1 sans objet · **2 OK** (0 référence, UUID synthétiques) · **3 OK** sur ce qui existe (sentinelle L2
resserrée sur `scoring/**` ; `packages/shared/src/scoring.ts` hors du périmètre balayé, à élargir) ·
4 OK par vacuité · **5 OK avec C3** · **6 OK** (0 import dans `field` et `hq`) · **7 OK** (`answers` seule,
jamais `answer_revisions`) · 8 sans objet.

## Exécution

`typecheck` 0 · eslint 0 · **scoring 211/211** · unit **1 806/1 806** · couverture `scoring/**`
**99,88 st · 96,95 br · 100 fn** · `check:tracabilite` vert · `check:no-skipped-tests` vert · `any`,
`OFFSET`, couleurs, anglais, `eslint-disable`, `.skip`, `TODO` : **0 partout**.

**Piège d'environnement** : avec un `packages/shared/dist` antérieur à L8, les 5 fichiers de test du
scoring échouent en `TypeError … 'QUESTION_FIGEE_EN_DOUBLON'`. Pas un défaut du lot ; le premier
symptôme qu'une reprise verra.

## Non vérifié

Les 1 761 lignes des quatre tests de conception, lus par structure · `acceptation.test.ts` l. 573-1433
par blocs · aucun test d'intégration ni e2e (aucun n'existe pour L8) · le seed réel
d'`estimation_params` · l'échelle au-delà de 7 200 réponses · Node v24 hors contrat.

## Désaccords à arbitrer par A01 : R1 · R2 · C1/C2 (doctrine) · §5.6 (jurisprudence).

## Doutes de spec : unité hors plan = question posée ? · direction/terrain conditionnée au seuil ? · `{values, below}` invalide ou « values d'abord » ? · roll-up par `headcount` propre ou sous-arbre ? · drapeau hors périmètre au rapport ? · amendement §32.1-3.

---

```
Signature revue croisée : A37 — 2026-09-09 — SOUS RÉSERVE.
Aucun fichier du dépôt modifié. Sondes et copies mutées hors dépôt.
```
