# REVUE CROISÉE A29 — lot L5, incrément **L5e** (cockpit) — 2026-09-09

> **Étape 4/7 (09 §3).** Réviseur croisé front A29, **lecture seule** — transcription par le pilote,
> sans amendement. **Périmètre relu** : `git diff origin/main...HEAD` (`803f0b4..b92cd23`) — 6 fichiers,
> 801 lignes, 100 % relus. Mutations sur copie hors dépôt, aucune écriture.
> **Objet** : le dernier succès de sync (critère 07 n° 1, D-6) et le jour civil des rituels
> (invariant 8, arbitrage A01 du 08). Deux commits, une main chacun : `c09fa70 [A27 seul]` (gardes,
> rouges par construction) puis `b92cd23 [A23 seul]` (production).

---

## VERDICT : 🟡 APPROUVÉ AVEC RÉSERVES — R1 bloquante (documentaire, hors code), R2 à dater

**Le code est juste, mesuré et bien gardé.** Ce qui bloquait n'est pas dans le diff : c'est un contrat
local que le cockpit lit et qu'une note de conception promettait **sous un autre nom et une autre
sémantique**. _(R1 levée depuis par A01 — `0eb9afe` — voir §10.)_

## 1. Mesures

`pnpm build` vert · garde 1 **5/5** · garde 2 **8/8** · `journee` **161/161** · `agenda+local`
**367/367** · `verify:rapide` **exit 0** · couverture `local/**` (glob critique) **98,86 l. / 97,03 br.**

## 2. Les huit questions du mandat

**A. La clé distincte — la distinction est JUSTE, le contrat était ORPHELIN → R1.**
`appliquerDescente` écrit bien `sync:since:` **à l'embarquement** (`local/ecriture.ts:295`) : réutiliser
ce curseur afficherait un faux « dernier succès » dès le jour 0. La nouvelle clé n'est écrite par
personne en L5 — conforme à D-6. **Mais `LOT_L6.md:149` promettait `sync:derniere-reussie:`, « après
chaque push OU pull abouti »** : nom divergent, sémantique divergente, **aucun test ne croise les deux**.
« Si L6a suit sa note, il écrira une clé que le cockpit ne lit pas, tous les tests resteront verts, et
la carte dira “jamais synchronisée” pour toujours. C'est exactement le mensonge programmé. »

**B. Une source pour un fait — tenu dans le cockpit, PAS dans l'application → R2.**
`app/EcranAccueil.tsx:195-214` dérive l'alerte de l'invariant 8 de `port-sync.ts:167` (`null` en dur)
pendant que `agenda/jour.ts:235` la dérive de `meta`. Invisible aujourd'hui ; **contradictoire dès le
premier push réussi de L6a** — deux écrans, deux réponses opposées. B6 réarmé, latent.

**C. Le statut reste celui du port — tenu, prouvé par mutation.** Statut forcé à `a_jour` quand la clé
existe → **1 rouge / 161**, exactement le test (d). La borne A01 est gardée par une mesure.

**D. L'emplacement — CONFORME à §34.2, tranché par le texte.** « (pastille + dernier succès + taille
d'outbox) » : une **liste de trois données** au même niveau ; rien n'y subordonne le dernier succès à
la pastille. Le `<p>` porte déjà les deux autres. Mutation (prop `derniereSync` alimentée) : **2 rouges**
sur des heuristiques — la mesure d'A27 est exacte, et ces tests **ne sont pas affaiblis** par le choix
retenu. `PastilleSync.derniereSync` n'est pas orphelin : consommé par la galerie `hq` et son test.

**E. `every` — juste, prouvé par mutation, le doute d'A23 se lève.** `every → some` : **1 rouge**,
`jour.test.ts:519` (Paris + Hô Chi Minh : rituel à 22 h le 3 à Paris = 03 h le 4 à HCM). `some`
éteindrait le rappel alors qu'un jour neuf a commencé. Sur le doute : le coût du conservateur est un
`Message` d'information sans verrou ; le pire cas inverse est une nuit de collecte non sauvegardée.
**Aucun assouplissement justifié.**

**F. `jourCivil` exporté — sans réserve.** Une copie aurait reproduit le défaut à la lettre. Le repli
`[undefined]` (`jour.ts:309-312`) n'est **pas** un désarmement de l'invariant 5 (rien n'y est affiché)
et il est **inatteignable** par l'unique appelant de production : `jour.ts:311` est la **seule** ligne
non couverte du fichier (99,22 %).

**G. Les gardes d'A27 mordent.** `slice(0,10)` restauré → **7 rouges / 8** ; slot d'écran à `null` →
**(b) seul** ; alerte nourrie par le port → **(e) seul**. La contre-épreuve croisée rougit **exactement
un** test dans chaque sens. `instantMural` est **un point fixe vérifié**, pas une approximation : un
fuseau exotique fait échouer le harnais, qui se nomme — il ne fait pas passer la garde.

**H. §5.6 — tenu.** `c09fa70` tests seuls, `b92cd23` production seule, intersection vide.

## 3. Réserves

**R1 — BLOQUANTE (documentaire).** `base.ts:155` vs `LOT_L6.md:149` — nom et sémantique. Attendu :
note L6 amendée + entrée `DECISIONS.md` (push ou pull : deux options se défendaient). E38.
**R2 — à dater et assigner**, non bloquante pour L5e, bloquante pour L6a. `EcranAccueil` second chemin
pour le même fait. Attendu : ligne datée assignée à L6a, glose de `jour.ts:230-233` rendue exacte. E38.

## 4. Remarques

m1 `jour.ts:311` branche défensive inatteignable · **m2** la garde fige le littéral
`'sync:dernier-succes:'` — **c'est ce littéral qui a épinglé le contrat** ; si A27 le remplace par
l'import, qu'il garde une assertion d'égalité · m3 la date est rendue au fuseau de mission **sans le
nommer** (étage 1 si voulu) · m4 il manque « à » entre date et heure · m5 `formes.ts:220` type
`z.string()`, `jourCivil` lève sur un fuseau illisible — préexistant, à joindre au dossier fuseaux ·
**m6 positif** : `meta` n'est pas dans `TABLES_SAUVEGARDEES` — la clé ne voyage pas dans un
`.axionbackup`, un « dernier succès » d'un appareil ne se restaure pas sur un autre · m7 `PastilleSync.
derniereSync`, à fermer par une ligne · m8 M3 (`conductedBy`) inchangé · m9 branche derrière `main`
d'un commit.

## 5. Les 8 invariants

1 OK · 2 OK · 3 OK (M3 préexistant) · 4 OK (0 CSS) · 5 OK — ISO UTC en base, fuseau de mission au
rendu, prouvé par (b) · 6 OK · 7 OK — append-only à `CLES_META`, schéma inchangé, clé absente ⇒
« jamais » · **8 AMÉLIORÉ, avec R2** — l'alerte cesse d'être aveugle, le rappel cesse de se réarmer à
minuit UTC.

11 §4 sans objet · UX 4 états 1/1, 0 couleur, français OK · 09 §5.7 aucune simplification · orphelin
aucun · étage 1 : 0, plafond non entamé.

## 6. Non vérifié

Docker arrêté (intégration, e2e, `@filrouge`) · axe-core · rendu navigateur réel · Node v24 hors contrat.

## 7. Désaccord à arbitrer par A01 : R1 seul — **push seul**, nom aligné. → tranché, voir §10.

---

```
Signature revue croisée : A29 — 2026-09-09. Aucun fichier modifié.
Mutations : C:\Users\Will\AppData\Local\Temp\a29mut, copie hors dépôt.
```

## 10. Suivi (tenu par le pilote)

| Réserve | État | Preuve |
| --- | --- | --- |
| **R1** | **LEVÉE** — A01, `0eb9afe` | Entrée `DECISIONS.md` du 2026-09-09 : **push seul**, sous le nom livré. Précédence : invariants du 00_INDEX au-dessus d'une note de conception. `LOT_L6.md` amendé (A-10, §3bis), rien supprimé en silence ; **le test qui croise les deux artefacts est assigné à A26**, pas à l'auteur du moteur |
| **R2** | **DATÉE ET ASSIGNÉE** à L6a, `LOT_L6.md` §3bis | Rectification, pas choix : la note de lot, pas le registre. Non-régression exigée : les deux écrans rendent le même verdict après un push réussi |
| m2 | A27 | à traiter au remplacement du littéral — assertion d'égalité à garder |
| m3, m4 | étage 1 | libellé « à », fuseau nommé — à la porte |
| m7 | — | pas une décision : le slot a un consommateur (`fiches-metier.tsx:269`) et un test |
