# PORTE P-A — fin des lots L0 et L1

> **Fichier de porte** au format imposé par le contrat 11 §9bis : « chaque porte produit un fichier
> `docs/portes/PORTE_<X>_<date>.md` : critères du fichier 07 copiés, cochés un à un **avec la
> preuve** (lien CI, capture, commande), verdict, signature humaine. **Le merge de la porte est
> conditionné à ce fichier commité.** C'est la trace d'audit de ton propre outil d'audit. »
>
> **Vérifications spéciales de P-A (09 §4)** : restauration de sauvegarde réussie depuis zéro
> (Postgres **et** MinIO) · migrations up/down · seed rejouable 2× identique · diff schéma-vs-04 =
> zéro écart.
>
> **ÉTAT : EN PRÉPARATION.** Cette porte ferme **L0 ET L1**. Le lot L1 n'est pas ouvert : le fichier
> est donc incomplet par construction, et il est créé maintenant pour que rien de ce qui a été
> vérifié au L0 ne soit à retrouver dans trois semaines. **Aucun verdict n'est rendu tant que L1
> n'est pas livré.**

---

## 1. Ce qui a été livré au lot L0

**Découpage L0-a / L0-b** (DECISIONS.md 2026-08-27) : la ligne L0 du fichier 07 mêle des livrables
codables et des livrables qui n'existent que sur une machine louée et un compte GitHub. Aucun critère
n'est abandonné — ils sont datés et attribués.

- **L0-a (dépôt, livré)** : monorepo pnpm 6 espaces · Compose dev/staging/prod · Caddy en domaine
  unique · pgBackRest + `mc mirror` + sauvegarde des certificats · provisionnement idempotent ·
  test de restauration en conteneur jetable · CI complète · `.env.example` exhaustif (76 variables) ·
  runbook · 40 gabarits d'agents · cadre de gouvernance.
- **L0-b (opérations, attend Williams)** : VPS, durcissement 06 §10.3, secrets réels, dépôt distant,
  Environments, premier déploiement.

---

## 2. Critères d'acceptation du lot L0 (fichier 07 §12, recopiés à l'identique)

| # | Critère | État | Preuve attendue / responsable | Sortie constatée · date · opérateur |
| --- | --- | --- | --- | --- |
| 1 | `docker compose up` = stack complète | ⬜ **NON VÉRIFIÉ** | Démon Docker arrêté sur la machine de développement (DECISIONS.md 2026-08-27). Vérifié sans démon : `docker compose config -q` **vert sur les 3 combinaisons** (dev · +staging · +prod), sans avertissement. **À exécuter : Williams** — `pnpm infra:up`, sortie attendue documentée dans `infra/README.md`. | ⬜ *à remplir : coller la sortie de `pnpm infra:up`* |
| 2 | Restauration Postgres **ET** MinIO testée depuis zéro | ⬜ **NON VÉRIFIÉ** | `infra/scripts/restore-test.sh` livré : restauration dans un conteneur **jetable**, rejeu WAL, contrôles `pg_is_in_recovery()` et jeu de tables comparé à la base vivante ; MinIO vérifié par **somme de contrôle** et relecture d'un objet témoin ; `trap EXIT` qui nettoie même en échec ; garde-fou anti-production. **À exécuter : Williams** — `pnpm infra:restore-test`. | ⬜ *à remplir : chemin du rapport `/var/log/axion/restore-test-*.log`* |
| 3 | Secrets §30.3 provisionnés | ⬜ **NON VÉRIFIÉ** | **12/12 familles de secrets** du §30.3 documentées dans `.env.example` (contrôle nominatif du gardien A02). « Provisionnés » = pose réelle en `chmod 600` sur le VPS → **Williams**, 02 §30.4-2 : « à la main par SSH, **pas par la CI** ». **Ne pas oublier la sauvegarde chiffrée du `.env` lui-même** : sans elle, un PRA restaure une infrastructure sans ses clés. | ⬜ *à remplir : `ls -l` du `.env` (root, 600) + preuve de sa sauvegarde chiffrée* |
| 4 | Déploiement staging par la CI OK | ⬜ **NON VÉRIFIÉ** | Chaîne complète livrée et corrigée après la revue croisée (l'appel à `deploy.sh` était faux, la route de santé aussi, `DEPLOY_PATH` aussi). **À exécuter : Williams** — après création du dépôt distant et des Environments. | ⬜ *à remplir : lien du run GitHub Actions* |

**Aucun de ces quatre critères n'est coché.** Trois exigent le VPS, un exige le démon Docker. C'est
cohérent avec le calendrier : la porte P-A ferme L0 **et** L1, pas L0 seul.

---

## 3. Critères d'acceptation du lot L1 (fichier 07 §12)

| # | Critère | État |
| --- | --- | --- |
| 5 | Migrations up/down propres | ⬜ lot L1 non ouvert |
| 6 | Seed rejouable 2× à l'identique | ⬜ lot L1 non ouvert |
| 7 | Diff schéma-vs-04 = **zéro écart** (manifeste 11 §7) | ⬜ lot L1 non ouvert |
| 8 | Contraintes UNIQUE actives (`answers`, SIREN partiel, `questions` code+version) | ⬜ lot L1 non ouvert |

---

## 4. Ce qui EST vérifié à ce jour, par exécution

Distinction essentielle, et c'est la leçon du lot : **ce qui suit a été exécuté**, pas relu.

| Contrôle | Commande | Résultat |
| --- | --- | --- |
| Chaîne complète | `pnpm verify` | **code 0** |
| Lint | `pnpm lint` | 0 erreur, 0 avertissement |
| Formatage | `pnpm format:check` | conforme |
| Types | `pnpm typecheck` | 6 espaces de travail, 0 erreur |
| Intégrité du pack | `pnpm check:pack` | 12/12 fichiers conformes au sceau |
| Invariants | `pnpm check:invariants` | aucune infraction mécanisable |
| Jonctions appelant → appelé | `pnpm check:jonction` | 36 scripts, 76 variables, 10 fichiers de CI — tout se rejoint. Vérifie aussi que les scripts d'infra sont appelés **avec les drapeaux qu'ils déclarent obligatoires** — c'était le défaut B-1 |
| Anti-skip | `pnpm check:no-skipped-tests` | aucun test désactivé, liste d'exceptions **vide** |
| Projets de test | `pnpm check:test-projects` | aucun test orphelin ; `--passWithNoTests` et `@filrouge` auto-péremptoires au L1 |
| Format de `DECISIONS.md` | `pnpm check:decisions` | 24 entrées, toutes au format 11 §9bis |
| Construction | `pnpm build` | 4 applications construites |
| Tests unitaires | `pnpm test:unit` | **95 verts** |
| Tests E2E | `pnpm test:e2e` | **8 verts** (chromium) |
| API à l'exécution | `node dist/server.js` + `curl` | `/v1/health` **200** · `/v1/health/ready` **503** sans Postgres · 404 au format contractuel · HSTS, CSP, `nosniff`, `Referrer-Policy` présents |
| Infrastructure (sans démon) | `docker compose config -q` | vert sur les 3 combinaisons |
| Scripts shell | `bash -n` | vert sur les 9 scripts |

---

## 5. Definition of Done transverse (09 §3)

| Ligne | État | Devient exigible à |
| --- | --- | --- |
| lint + typecheck = 0 erreur | ✅ | — |
| tous tests verts, **aucun skippé** | ✅ | — |
| couverture ≥ 90 % sur les modules critiques, **mesurée** | ➖ sans objet | **L2** (RBAC/auth), L5a (crypto), L6a (sync), L8 (scoring). `coverage-critical-paths.json` déclare les chemins attendus et **échoue si du code critique apparaît sans être déclaré**. |
| migrations up/down sur staging | ➖ sans objet | **L1 — ET dépend de L0-b.** Sans staging provisionné, cette ligne restera incochable même le lot L1 livré. C'est une dépendance croisée entre les deux moitiés de cette porte, à voir maintenant plutôt que le jour de la signature. |
| tout écran livré avec ses 4 états (§33.2) | ➖ sans objet | **L5** puis **L7-min** — aucun écran au L0 |
| axe-core vert | ➖ sans objet | **L5** — premier écran réel |
| `@filrouge` vert sur **FIL-TPE et FIL-GC** | ➖ sans objet | **L1** — les deux missions naissent en fixtures (09 §4bis). **Garde-fou posé** : `pnpm check:test-projects` échoue si L1 est livré sans test `@filrouge` couvrant les deux missions — et il ignore les COMMENTAIRES, parce qu'à sa première écriture il se satisfaisait d'une phrase de documentation. |
| README à jour | ✅ | racine + 4 applications + `infra/` + `.github/workflows/` |
| aucun TODO/FIXME sans entrée DECISIONS/AMELIORATIONS | ✅ | un seul TODO dans tout le dépôt : `smoke-test.sh` `TODO(L2)`, adossé à sa décision |
| diff schéma-vs-04 = zéro écart | ➖ sans objet | **L1** — `pnpm schema:diff` répond « NON APPLICABLE » et **se durcit automatiquement** dès que `apps/api/drizzle/` existe |

---

## 5bis. Verdicts de sécurité et d'UX novice (09 §3, étape 6)

Le 09 §3 exige que « la sécurité (A51) et l'UX novice (A54) rendent leur verdict **quand le lot les
concerne** ». Le silence n'est pas une réponse : voici lesquels concernent L0.

| Rôle | Concerné ? | État |
| --- | --- | --- |
| **A51 — sécurité offensive** | **OUI** | ⬜ **VERDICT NON RENDU.** Le lot porte le durcissement 06 §10.3, les 12 familles de secrets, la CSP, les en-têtes de sécurité et un scan ZAP volontairement non bloquant. À faire porter à A51 **avant la signature** : revue de la CSP et des en-têtes, contrôle que MinIO n'est exposé nulle part en staging/prod, relecture des garde-fous anti-production de `restore-test.sh`, et vérification que la clé SSH de l'Environment `ops` est bien restreinte côté serveur (`command=` dans `authorized_keys`) — sans quoi `ops` devient un accès de production sans approbation, soit exactement ce que sa création cherchait à éviter. |
| **A54 — recette UX novice** | **NON** | ➖ **SANS OBJET** au lot L0, vérifié et non supposé : aucun écran n'existe (les deux `App.tsx` sont des coquilles de 50 lignes). A54 intervient « à chaque fin de lot **terrain** » (09 §1) — donc **L5**, porte **P-C**, en guidé strict et mode avion. |

---

## 5ter. Invariants — ce que la machine ne peut pas vérifier

`pnpm check:invariants` couvre les invariants 1, 2 et 4 ainsi que les interdictions du 11 §2.
**Quatre invariants ne sont pas mécanisables**, et leur vérification humaine se trace ICI — c'est le
rôle d'une porte.

| Invariant | État au L0 | Où il devient vérifiable |
| --- | --- | --- |
| **3** — RBAC serveur, étanchéité financière, propriété de session §9.9 | ➖ sans objet, **vérifié et non supposé** : les seules routes livrées sont les deux sondes de santé, aucune ne touche de donnée | **L2**, porte **P-B** (tentatives d'intrusion croisées) |
| **5** — interface 100 % en français, UTC en base | ✅ respecté : libellés, messages d'erreur et commentaires en français ; `temps.ts` impose l'ISO 8601 UTC ; `TZ=UTC` porté par les images et le Compose | permanent |
| **6** — le terrain collecte, le siège produit | ✅ respecté et bien orienté : les files lourdes (rapports, LLM, exports) vivent dans `apps/worker`, jamais dans `apps/field` | **L5**, **L10** |
| **7** — toute correction = révision tracée | ➖ sans objet : aucune donnée n'existe | **L1**, **L3**, **L6** |

---

## 6. Gouvernance (11 §9bis)

- Branche `lot/l0-infra` · commits conventionnels · **`main` ne porte que la genèse** — aucun commit
  direct hors création du dépôt.
- `DECISIONS.md` : **24 entrées**, toutes horodatées. **Le format n'a PAS toujours été tenu** : au
  premier contrôle du gardien, 4 entrées sur 23 seulement étaient conformes — dont celle au nom de
  laquelle `infra/scripts/backup-caddy.sh` existe dans le dépôt. Une version antérieure de ce
  fichier affirmait « toutes au format » : c'était faux, et c'était exactement le reproche que la
  section 7 adresse à l'auto-revue. Régularisé sans réécrire l'historique (entrée de
  régularisation), puis **mécanisé** : `pnpm check:decisions` refuse désormais toute entrée hors
  format, et son exemption est lisible DANS le registre, pas cachée dans un script.
- `AMELIORATIONS.md` : étage 1 à **~0,3 j sur 0,5 j** de plafond (sceau du pack, contrôle de
  jonction). Étage 2 : **fiche A-001 proposée, NON implémentée** (11 §8.7).
- **Aucun `origin`** : les commits ne vivent que sur la machine de Williams. La garantie du §9ter est
  **dégradée** tant que le dépôt distant n'existe pas. C'est le point le plus urgent de la liste ci-dessous.

---

## 7. Le pipeline, étape par étape

| Étape | État | Signataire |
| --- | --- | --- |
| 1. Brief | ✅ ordre de lecture 11 → 02 → 06 §10.3 → 07, ligne L0 | A01 |
| 1bis. Conception | ➖ sans objet — L0 n'est pas un lot à risque (09 §3.1bis) | — |
| 2. Implémentation | ✅ A11 infra · A52 CI/CD · A55 gabarits · A01 monorepo | chefs d'équipe |
| 3. Auto-revue | ⚠️ **passée, mais elle a conclu au-delà de ce qu'elle avait mesuré** | A01 |
| 4. Revue croisée | 1ʳᵉ passe : **NON CONFORME** (7 bloquants, 12 majeurs) → corrigé → 2ᵉ passe en cours | A17 |
| 5. Tests | ✅ suite complète verte, exécutée | — |
| 6. Contrôle d'acceptation | 1ʳᵉ passe : **VETO** (4 écarts) → à rejouer après l'étape 4 | A02 |
| 7. Porte humaine | ⬜ **attend L1 puis Williams** | **Williams** |

**Un écart d'orchestration à retenir** : l'étape 6 a d'abord été lancée **en parallèle** de l'étape 4,
alors que le pipeline les ordonne. Le gardien l'a relevé (V3) et avait raison. C'est une faute d'A01,
pas du gardien — et elle est la raison pour laquelle les deux étapes sont rejouées dans l'ordre.

---

## 8. Ce qui attend Williams, par ordre de blocage

1. **Créer le dépôt privé `axion-audit` et pousser.** Tant qu'`origin` n'existe pas, plusieurs jours
   de travail ne vivent que sur une machine. Deux minutes.
2. **Démarrer Docker Desktop**, puis `pnpm infra:up` et `pnpm infra:restore-test` → critères 1 et 2.
3. **Dérouler `infra/README.md` (L0-b)** : VPS, durcissement, secrets réels en `chmod 600`,
   **sauvegarde chiffrée du `.env`**, Environments `staging`/`prod`/`ops`, protection de `main`,
   premier déploiement → critères 3 et 4.
   **Point de contrôle n°1 côté sauvegardes** : `pgbackrest stanza-create` après le premier `up`,
   sans quoi les WAL s'accumulent sans être archivés.
4. **Valider ou ajuster les `estimation_params`** (11 §5) — le contrat vous les réserve explicitement.
5. `caddy validate` sur la configuration Caddy : ni Caddy ni Docker n'étant disponibles ici, la
   syntaxe (`import` de snippets, `handle_path` + `try_files`) n'a pas pu être validée.
6. **Arbitrer la fiche AMELIORATIONS A-001** : ABSORBÉE / PHASE 2 / REFUSÉE.
7. Reconfirmer puis geler les tags d'images MinIO / `mc` / Caddy.
8. Installer Node 22 LTS localement (confort, non bloquant — la CI et les conteneurs sont déjà en 22).

---

## 9. Verdict

**VERDICT : ⬜ NON RENDU — la porte n'est pas ouverte.**

Elle ne peut pas l'être : elle ferme L0 **et** L1, et L1 n'est pas ouvert. Les quatre critères du
lot L0 attendent tous une exécution qui n'appartient pas à l'autopilote.

**Signature humaine :** ⬜ *à compléter — « validé — Williams, date »*
