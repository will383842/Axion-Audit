# CONTRÔLE D'ACCEPTATION A02 — incrément L5b — 2026-09-03

> **Étape 6 du pipeline (09 §3).** Gardien de la spécification, droit de veto.
> Commit contrôlé : `b3d8886` · branche `lot/l5b` · worktree `C:\Users\Will\Documents\_axl5b` ·
> périmètre mesuré : `git diff ce4b29b..b3d8886` = **42 fichiers, 10 515 insertions, 5 suppressions**.
> Toutes les mesures de ce dossier ont été **exécutées** le 2026-09-03 par A02, pas reprises d'un
> rapport. Aucune case n'est cochée sur déclaration.

---

## VERDICT

**ACCEPTÉ SOUS RÉSERVE.**

Trois réserves **BLOQUANTES pour le passage en porte P-C** (signature A01), trois réserves
**non bloquantes** à porter au dossier de porte. Aucune ne remet en cause le travail livré :
les trois bloquantes coûtent, ensemble, moins d'une heure et n'exigent ni arbitrage, ni juriste,
ni ligne de code fonctionnel.

**Si l'une des trois réserves bloquantes n'est pas fermée avant qu'A01 ne signe le passage en
porte, ce verdict devient un VETO** — le motif serait alors « DoD transverse non cochée
intégralement » (09 §3 étape 6), pas une opinion.

---

## 1. PÉRIMÈTRE DU LOT — ce que je coche, et ce que je refuse de cocher

Le brief d'un incrément vient du fichier 07 (ligne L5) **découpé par le 11 §6**. Recopié à
l'identique :

> **L5b** — écran 3 zones + TOUS les types de réponse (fourchette, non-communiqué inclus) +
> à-revoir/NA + notes + notes volantes + ad hoc + hors-parcours.

| # | Élément du brief L5b | Constat | Preuve |
|---|---|---|---|
| 1 | Écran 3 zones | **TENU (structure)**, disposition peinte reportée | `ecrans/entretien/{EcranEntretien,ZoneBlocs,ZoneQuestion,PanneauNotes}.tsx` + `entretien.css:77` `@media (min-width: 64rem)` ; faisceau R4 ①②③ (`EcranEntretien.test.tsx:1185-1262`), dont ③ qui mesure l'égalité du seuil JS et du seuil CSS en lisant le fichier sur disque |
| 2 | TOUS les types de réponse | **TENU côté donnée, NON ÉPROUVÉ côté saisie** | `session/valeurs.ts` : 12 formes, 100 % lignes / 92,85 % fonctions. `SaisieReponse.tsx` : 40,85 % lignes / **31,81 % fonctions** — voir §4 |
| 3 | dont mode fourchette (§27.4) | **TENU et éprouvé** | `valeurs.ts` (`range` sur number/percent/duration/money, `allow_range_snapshot`) ; `SaisieEnFourchette` exécutée 20× |
| 4 | dont non communiqué (§27.4) | **TENU** | `session/ecriture-reponses.ts:93,143,156-162,173,181` — `withheld` + `withheldReason` contrôlé contre `MOTIFS_NON_COMMUNIQUE` (liste fermée) |
| 5 | à-revoir / NA | **TENU** | `ecriture-reponses.ts:91,142,151,172` (`notApplicable` + motif attendu) ; `DialogueDrapeau.tsx` |
| 6 | notes (de question + bloc-notes de session) | **TENU** | `PanneauNotes.tsx`, `ecriture-session.ts`, `ecriture-reponses.ts` |
| 7 | notes volantes | **TENU** | `session/notes-volantes.ts` (UUID v7 client l.93, contenu chiffré) ; réserve A29 R2 fermée (le champ n'est plus vidé au succès) |
| 8 | questions ad hoc | **TENU** | `session/questions-adhoc.ts:112-113` (deux UUID v7 CLIENT, conformes au 11 §4) ; `DialogueQuestionAdHoc.tsx` |
| 9 | hors-parcours (§25.4) | **TENU** | `ecriture-reponses.ts:96,144,174` (`horsParcours`) ; `PaletteRecherche.tsx` (recherche locale plein texte) |

### Ce qui NE se coche PAS ici, et où cela se cochera

Ces éléments de la ligne L5 du fichier 07 **ne sont pas du périmètre L5b** (11 §6). Je refuse
explicitement de les cocher, et je nomme leur incrément :

| Élément du 07 (ligne L5) | Incrément | Se cochera à |
|---|---|---|
| shell Workbox, Dexie versionnée, DEK/KEK, verrouillage, pull mission, `storage.persist()` | **L5a** | déjà signé — porte P-C |
| agenda simple §25.2, proposition d'unité §25.3, 5 types de session + atelier §27.1/§28.1, entretien complémentaire §25.6 | **L5c** | porte P-C |
| validation d'entretien (guidé strict/expert §19.1, parcours express R1) | **L5c** | porte P-C |
| compression photos R2, export de secours chiffré §9.7, mise à jour applicative §31 | **L5c** | porte P-C |
| écran « Aujourd'hui » §34.2, mode avion complet iPad+PC, coupure de courant, export créé puis restauré sur un 2e appareil, test novice < 30 min (A54), mode écran partagé, police en mode avion | **critères de DÉMO du lot L5** | **porte P-C uniquement** — aucun n'est cochable sur un incrément |

---

## 2. LES DEUX SENS DE LA MATRICE E1-E47

### 2.1 Exigences → code (rien d'oublié) — voir `docs/TRACABILITE_E1-E47.md` §K.1

Couvertes par L5b : **E6, E7, E12, E13, E23, E32, E33, E37, E44 (partielle)**.
Hors périmètre, cochées ailleurs : E24, E25, E38 → L5c · E26 → L5c/L7 · E9 → L6a.

**Le sens inverse a été fait exigence par exigence, pas déduit des citations.** Le contrôle qui
avait manqué pour E32 est refait : E32 est aujourd'hui réalisée par `session/fuseau.ts` (formatage
d'un instant UTC au fuseau de la mission, 03 §22.2) et, pour sa part « devises », par
`session/valeurs.ts` (`money` + ISO 4217, défaut `EUR`) — qui ne la cite pas. **Sous-citation
relevée, non bloquante.**

### 2.2 Code → exigences (le code orphelin est REFUSÉ)

**Aucun code orphelin.** Les 29 fichiers de source livrés portent tous une ligne `Traçabilité :`.
Aucune route, aucune table, aucun job : le diff sur `apps/api/drizzle/**` et
`apps/api/schema-manifest.json` est **VIDE** (mesuré).

Deux fichiers modifiés dans `packages/shared` ont été relus au titre du 11 §8-2 (« modifier le
contrat d'ops §4 ou une convention §3 ») :

- `packages/shared/src/sync.ts` : **modification de COMMENTAIRE uniquement** (correction d'un renvoi
  de chemin cassé, C7 d'A29). Aucun champ, aucun schéma Zod touché. **Pas de déclencheur §8-2.**
- `packages/shared/src/banque-questions.ts` : ajout **additif** du champ `consigne` au retour de
  `lireAncresDeCotation` (bloquant B2 d'A29 : sur une question `scale_1_5`, la consigne consultant
  n'était rendue nulle part). Additif, `typecheck` vert sur les cinq paquets, amendement tracé en
  `DECISIONS.md` (2026-09-03). **Acceptable.**

---

## 3. RÉSERVE BLOQUANTE R1 — une QUATRIÈME glose fausse, passée sous le garde et sous la revue

**`apps/field/src/session/valeurs.ts:45-46`**

```
// Traçabilité : E13 (écran 3 zones), E30 (informations non communiquées et
// fourchettes, §27.4).
```

**Fichier 08, ligne E30** : « 3 niveaux d'audit (diagnostic cadrage / opérationnel / stratégique
groupe) alignés sur l'offre publique 8 étapes | §20.1, §20.2 ».

`valeurs.ts` type les douze formes de valeur de réponse et les valide à l'écriture. Il n'a **aucun
rapport** avec les trois niveaux d'audit. Le test arbitré (`DECISIONS.md`, 2026-09-03, « Amendement
à la règle R5 ») : *« si cette exigence disparaissait du pack, ce fichier perdrait-il sa raison
d'être, ou seulement un appelant ? »* — `valeurs.ts` ne perdrait **rien**.

**Ce qui rend ce constat certain plutôt qu'interprétatif** : dans le MÊME incrément,
`SaisieReponse.tsx:26-27` et `session/ecriture-reponses.ts:33-34` citent **E37 (« scoring
intégralement spécifié — fourchettes §27.4 »)** pour exactement ce sujet. Et 03 §32.1 traite bien
la fourchette et le non-communiqué (« le score s'évalue sur la borne BASSE » ; « exclue du
numérateur ET du dénominateur »), ce qui est le périmètre d'E37, pas d'E30.

**Pourquoi cette réserve est bloquante, et pas une pinaillerie de numérotation.** C'est la
**quatrième** occurrence de cette classe dans un seul incrément, après E7/invariant 7,
`fuseau.ts` (E13→E32) et `enregistrement.ts` (E38→E13/E6). Celle-ci a franchi :
`check:tracabilite` (RC=0, « 535 citations, aucune incohérence » — le garde ne vérifie que
l'existence du numéro, il le dit lui-même à `--angles-morts` n° 1 et n° 3), **le rejeu A29**, et la
fiche étage 2 ouverte le jour même sur ce défaut exact (`AMELIORATIONS.md:1686`). Une glose absente
est refusée ; une glose fausse est cochée — c'est pourquoi l'étape 6 existe.

**Ce qui lève R1** : que l'équipe productrice remette la glose en face de ce que `valeurs.ts` fait.
Aucun code à modifier. **Je ne dicte pas la nouvelle glose** : le choix des exigences appartient à
l'auteur, sous la règle R5 amendée.

---

## 4. RÉSERVE BLOQUANTE R2 — DoD « README de l'app à jour » : NON TENUE

`apps/field/README.md`, inchangé depuis le commit L5a `0d4daf4` :

- ligne 11 : `## État au lot L5a — le SOCLE`
- lignes 15-16 : `**Aucun écran de collecte** : l'écran d'entretien est L5b (A22), la journée et l'export de secours sont L5c (A23).`

L'incrément a livré **onze composants d'écran** et **treize modules de session**. La « Carte du
socle » n'inscrit ni `src/session/**` ni `src/ecrans/entretien/**`. Le README de l'app décrit donc
comme à venir ce qui est livré — la même forme de défaut que la fiche RGPD a corrigée sur
elle-même le 2026-09-03.

**Ce qui lève R2** : mise à jour du README (état du lot, carte des modules livrés).

---

## 5. RÉSERVE BLOQUANTE R3 — l'écart RGPD manque son amendement horodaté

Le point (a) du brief est **confirmé et qualifié**.

| Source | Texte |
|---|---|
| **arbitré** — `DECISIONS.md` 2026-09-02, « Le script d'accord de participation… », Décideur : **Williams** | « … Le rapport ne vous **attribue aucun propos nominativement**. Vous pouvez refuser de répondre à une question, **ou demander l'arrêt de l'entretien**, à tout moment… » |
| **en production** — `DemarrageEntretien.tsx:19-23`, affiché l.53 | « … elles sont **consignées sous votre nom et votre fonction**, … et ne servent à aucune évaluation individuelle. Vous pouvez ne pas répondre à une question, ou demander qu'une information ne soit pas communiquée. » |

Trois différences, pas une : la promesse de non-attribution est **inversée** ; le droit d'**arrêter
l'entretien** a disparu, remplacé par le mécanisme §27.4 ; le commanditaire passe de « votre
direction » à « votre entreprise ».

**Ce que je NE demande pas.** Je ne demande à personne de réécrire ce texte : la fiche
`AMELIORATIONS.md:1557` a raison, le rapprochement appartient au juriste, et elle porte
correctement les trois questions (a) (b) (c) à Williams avant P-E. Cette part est **hors de mon
mandat** et ne bloque pas.

**Ce que je demande, et pourquoi c'est bloquant.** Il n'existe **aucun amendement horodaté** sous
la décision du 2026-09-02. 11 §9bis : « une décision non tracée dans ce format n'existe pas » ;
09 §5.2 : « tout écart à la spec est soit refusé, soit **documenté comme amendement horodaté** —
jamais silencieux ». Une session neuve applique le protocole de reprise (11 §9ter) et lit
`DECISIONS.md` **avant** le code : elle croira que le texte arbitré est celui qui est lu aux
personnes. C'est un écart de gouvernance sur une garantie RGPD, et il se ferme en une entrée.

**Ce qui lève R3** : une entrée d'amendement horodaté sous la décision du 2026-09-02, renvoyant à
la fiche et disant que le texte en service n'est pas le texte arbitré et que l'arbitrage est
rouvert. Zéro rédaction juridique.

---

## 6. RÉSERVES NON BLOQUANTES — à porter au dossier de porte P-C

### R4 — le livrable-titre est le moins couvert, et le chiffre annoncé mesure autre chose

`apps/field/src/session/**` : **lignes 98,76 % · fonctions 98,41 % · branches 94,03 %** — remesuré,
exact. Ce glob (`.github/coverage-critical-paths.json`, entrée « Machine a etats de session
terrain ») **ne contient aucun composant d'écran**. `apps/field/src/ecrans/**` n'est sous aucun
seuil opposable, ni dans `cheminsCritiques`, ni dans `cheminsAttendus` — la « ceinture 2 » ne peut
donc pas le réclamer.

Mesure par fonction (`coverage/lcov.info`, enregistrements `FNDA`) sur `SaisieReponse.tsx`, le
fichier qui EST « TOUS les types de réponse » du brief :

| Fonction | Ligne | Exécutions | Type couvert |
|---|---|---|---|
| `SaisieDevise` | 322 | **0** | `money` |
| `SaisieDate` | 347 | **0** | `date` |
| `ChoixUnique` | 377 | **0** | `single_choice`, `multi_choice` |
| `SaisieTableau` | 495 | **0** | `table` |
| 8 des 10 `onChangement` | 111 → 259 | **0** | tous sauf `scale_1_5` |
| `SaisieReponse` | 78 | 145 | — |
| `SaisieEnFourchette` | 414 | 20 | `range` |

Fichier : **40,85 % lignes · 31,81 % fonctions**. Voisins mesurés :
`DialogueQuestionAdHoc.tsx` **14,28 % fonctions** (l'ad hoc du brief) ·
`PaletteRecherche.tsx` **53,27 % lignes** (le hors-parcours du brief) ·
`EcranEntretien.tsx` **42,3 % fonctions** · **`AccesEntretien.tsx` 0 % lignes** — c'est la porte
d'entrée de tout l'incrément depuis l'accueil, et rien ne la rend jamais.
Aucun e2e ne couvre l'écran d'entretien : `e2e/` contient `polices`, `pwa-404`, `socle`.

**Conclusion, dite sans l'arrondir : cinq des douze types de réponse ne sont rendus par aucun
test.** Ce n'est **pas** une infraction à la DoD — l'énumération de 09 §3 est « sync, crypto
locale, scoring, RBAC/propriété », et je n'exige pas ce qui n'est écrit nulle part. C'est un fait
que la porte P-C doit avoir sous les yeux **avant** la démo « 1 session de chaque type créée hors
ligne », parce que c'est là que ces cinq widgets rencontreront leur premier utilisateur.

### R5 — DoD « axe-core vert » : NON VÉRIFIABLE sur cette branche

`@axe-core/playwright@4.13.0` est en `devDependencies`, mais **aucune occurrence d'`AxeBuilder` ni
de `@axe-core/*` n'existe dans le code du dépôt** (`grep` sur `e2e/`, `apps/`, `packages/` = 0).
`e2e/accessibilite-l5a.e2e.ts`, cité par `AMELIORATIONS.md:1611`, **n'existe pas dans ce worktree**
(il vit sur `lot/l5a`). Onze composants livrés, zéro balayage. Je ne coche pas cette case ; je ne
la déclare pas non plus en échec pour l'incrément, puisqu'elle sera rejouée à la fusion et à P-C —
mais elle est **incochable en l'état**, exactement comme la note de conception L5 §5-2 l'avait
prédit.

### R6 — le fil rouge n'a pas été allongé (09 §4bis)

`@filrouge` **VERT sur FIL-TPE ET FIL-GC** : `apps/api/tests/l1-filrouge.integration.test.ts`,
5 cas, mesuré. Mais c'est le scénario **du lot L1**. 09 §4bis : « session hors ligne, cotation,
à-revoir, photo (L5) — chaque lot ne fait qu'ALLONGER le scénario, jamais le réécrire. » L5b n'y a
rien ajouté. À traiter au niveau du lot L5, avant P-C.

---

## 7. QUALIFICATION DES QUATRE POINTS OUVERTS DU BRIEF

| # | Point | Qualification A02 |
|---|---|---|
| **a** | Texte RGPD en production ≠ texte arbitré | **BLOQUANT pour P-C sur sa part traçabilité** (réserve R3) ; la part rédaction juridique est **hors périmètre agent**, correctement portée à Williams et au juriste avant P-E par `AMELIORATIONS.md:1557`. La fiche est exacte : je l'ai vérifiée ligne à ligne contre les deux textes. |
| **b** | Trois colonnes non éprouvées en jsdom | **NON BLOQUANT.** Le faisceau compte **trois** brins, pas deux, et le troisième est le bon : ③ lit `entretien.css` **sur le disque** et vérifie que le seuil du JS (`REQUETE_TROIS_COLONNES`) est celui du CSS. Ce qui reste — la mise en colonnes PEINTE — est proprement nommé, non maquillé, et renvoyé à la recette manuelle P-C écrite dans `docs/conception/LOT_L5.md` §4 (trois lignes, ancre A27, « si l'une des trois lignes échoue, P-C n'est pas franchie »). 11 §7 assume déjà cette limite pour l'appareil réel. **Reporté à P-C, comme il se doit.** |
| **c** | `vitest.config.ts` porte `thresholds: {}` | **NON BLOQUANT.** Vérifié : `thresholds: {}` est bien là, et la DoD tient par `.github/scripts/check-coverage.mjs` que j'ai **exécuté** (RC=0 sur la couverture complète). La DoD dit « mesurée » : elle l'est. Corriger `vitest.config.ts` toucherait le contrat d'ops (11 §8-2) → fiche étage 2 PROPOSÉE et **non implémentée** (`AMELIORATIONS.md:1734`), ce qui est exactement la conduite exigée par 09 §5.9. **Rien à reprocher.** |
| **d** | `pnpm verify` n'exécute pas le projet `interface` | **NON BLOQUANT — et le maillon suivant a été mesuré.** `pnpm verify` enchaîne `test:unit` + `test:integration` : le projet `interface` (**29 fichiers**, soit TOUS les tests d'écran de L5b) n'y passe pas. **Mais la CI, elle, les exécute** : le job `coverage` de `.github/workflows/ci.yml` lance `pnpm test:coverage` = `vitest run --coverage`, qui exécute les trois projets (commentaire l.965-967, vérifié). Le merge reste donc bloqué si un test d'écran rougit. `check:test-projects` confirme la captation : **81 fichiers — interface 29 · unit 32 · integration 17 · playwright 3**, RC=0. Le correctif de `verify` arrive de `lot/l3-suite` ; les deux violations ESLint `ECRITURE_DEXIE` annoncées à la fusion avec `lot/l5a` **ne se produisent pas sur cette branche** (`pnpm lint` RC=0) et relèvent de la fusion, pas de cet incrément. |

---

## 8. LES 8 INVARIANTS — contrôlés un par un

| # | Invariant | Verdict | Preuve |
|---|---|---|---|
| 1 | Offline-first, **UUID v7 côté client**, push idempotent | **OK** | `uuidv7()` sur les 4 sites de création : `ecriture-reponses.ts:136`, `ecriture-session.ts:103`, `notes-volantes.ts:93`, `questions-adhoc.ts:112-113`. Aucun `randomUUID`/`uuidv4`/`gen_random_uuid` dans le code livré. Aucune migration, donc aucune fonction SQL de génération v7. |
| 2 | Aucune référence client dans le code | **OK** | Aucun nom propre d'entreprise dans les 42 fichiers. Le script d'accord désigne le commanditaire par « votre entreprise » (le texte arbitré disait « votre direction ») — générique dans les deux cas. `check:invariants` marque INV-2 « NON APPLIQUÉ » (non mécanisable) : contrôle fait **à la main**. |
| 3 | RBAC serveur, financier en routes admin, écritures de sync réservées au propriétaire §9.9 | **SANS OBJET** pour L5b (aucune route, aucun code serveur livré) — reste porté par L2 (mesuré vert au §9) et L6a. |
| 4 | Aucune couleur / taille en dur, tokens uniquement | **OK** | `entretien.css` : **0** occurrence de `#hex`, `rgb(`, `rgba(`, `hsl(` sur 418 lignes ; **106** `var(--)`. Aucun `style={{}}` ni littéral de couleur ajouté dans les `.tsx`. Les seuils de mise en page sont en `rem` et le fichier explique pourquoi (les media queries ne lisent pas les variables CSS). |
| 5 | Interface 100 % en français, horodatages UTC | **OK** | Relevé exhaustif des `libelle=`/`titre=`/`aria-label=`/`placeholder=` des onze composants : **tous en français**. `session/fuseau.ts` formate un instant UTC au fuseau de la mission, à l'affichage seulement, sans `new Date()`. |
| 6 | Le terrain collecte, le siège produit | **OK** | Aucune génération lourde livrée côté terrain. |
| 7 | Toute correction = révision tracée, rien d'écrasé en silence | **OK** | `ecriture-reponses.ts:184` : `revision: existante === null ? 1 : existante.revision + 1`. Réserve A29 R2 fermée : une note saisie n'est plus effacée. `validerValeurPourQuestion` refuse en français **sans écrire**, la valeur valide en base reste intacte. |
| 8 | Sauvegarde terrain, sync ≥ 1×/j, export de secours testé | **HORS PÉRIMÈTRE L5b** — porté par **L5c** (export `.axionbackup`, entrée `cheminsAttendus` `apps/field/src/backup/**`, statut « non livré ») et **L6**. `enregistrement.ts` a d'ailleurs **cessé** de citer E38 : correction R5, juste. |

---

## 9. DoD TRANSVERSE (CLAUDE.md §5 / 09 §3) — cochée ligne à ligne, avec la commande

| # | Ligne de DoD | État | Commande et sortie |
|---|---|---|---|
| 1 | lint + typecheck stricts = 0 erreur | **COCHÉE** | `pnpm lint` → **RC=0** · `pnpm typecheck` → **RC=0** (5 paquets : shared, ui, api, field, hq, worker) |
| 2 | tous les tests verts, **aucun test skippé** | **COCHÉE** | `npx vitest run --coverage` (3 projets) → `Test Files 78 passed (78)` · `Tests 1710 passed (1710)` · **RC=0**. Détail : unit+interface 61 fichiers / 1402 cas ; integration 17 fichiers / 308 cas. `node scripts/check-no-skipped-tests.mjs` → RC=0, « aucun test désactivé (81 fichiers analysés) ». `grep .skip/.todo/.only` sur le code L5b → **0**. |
| 3 | **couverture ≥ 90 % sur les modules critiques — MESURÉE** | **COCHÉE** | `node .github/scripts/check-coverage.mjs` → **RC=0**, « Tous les modules critiques atteignent 90 % — couverture MESURÉE ». Modules L5 : `apps/field/src/session/**` **98,76 / 98,76 / 98,41 / 94,03** (lignes/instr./fonctions/branches) · `apps/field/src/local/**` **97,83 / 97,83 / 98,97 / 93,00**. **Réserve de lecture R4** : ce périmètre exclut les écrans. |
| 4 | migrations up/down exécutées sur staging | **SANS OBJET pour l'incrément** | Diff **VIDE** sur `apps/api/drizzle/**` et `schema-manifest.json` : L5b n'introduit aucune migration. Reste **dû au lot** à P-C. |
| 5 | tout écran livré avec ses **4 états** (§33.2) | **COCHÉE avec réserve** | `EcranEntretien` : `Squelette` (chargement), `PastilleSync` + « Hors ligne — la collecte continue », `Message ton="alerte"` + « Enregistrement local en échec », état vide « aucun entretien ouvert ». `EcranNouvelEntretien` : les 4. `PaletteRecherche` : « Aucune question ne correspond ». `PanneauNotes` : « Aucune note volante dans cet entretien ». `ZoneBlocs`/`ZoneQuestion`/`AccesEntretien` : zones et bloc de navigation, non soumis à §33.2. **Réserve : le rendu de ces états n'est pas éprouvé partout (R4).** |
| 6 | **axe-core vert** | **NON VÉRIFIÉE — R5** | Aucun `AxeBuilder`, aucun `@axe-core/*` dans le code du dépôt. Case **incochable** en l'état. |
| 7 | `@filrouge` vert sur **FIL-TPE ET FIL-GC** | **COCHÉE** | `apps/api/tests/l1-filrouge.integration.test.ts` — 5 cas verts, dont « la mission FIL-TPE tient à l'échelle micro » et « la mission FIL-GC tient à l'échelle grand compte ». **Réserve R6 : non allongé par L5b.** |
| 8 | **README de l'app à jour** | **NON TENUE — R2** | `apps/field/README.md:11,15-16` — voir §4. |
| 9 | aucun TODO/FIXME sans entrée DECISIONS/AMELIORATIONS | **COCHÉE** | `git diff ce4b29b..HEAD -- apps/ packages/ \| grep '^+.*TODO\|FIXME\|XXX\|HACK'` → **0 résultat**. |
| 10 | **diff schéma-vs-04 = zéro écart** | **NON REJOUÉE localement** | `node scripts/schema-diff.mjs` → « PostgreSQL injoignable — ECONNREFUSED 127.0.0.1:5432 ». **Atténuation mesurée** : le delta de schéma de L5b est vide (ligne 4). Rejoué en CI, et à P-C. |

### Critères propres au gardien

| Critère | État | Preuve |
|---|---|---|
| Matrice E1-E47 à jour **dans les deux sens** | **FAITE** — `docs/TRACABILITE_E1-E47.md` §K | avec l'écart K.2-a (R1) |
| Code orphelin | **AUCUN** | 29 fichiers source, 29 lignes `Traçabilité :` ; 0 route, 0 table, 0 job |
| Plafond étage 1 (0,5 j / lot) | **RESPECTÉ** | Deux fiches étage 1 pour L5a/L5b, dont une explicitement « documentaire, le compteur du plafond n'est pas mouvementé » (`AMELIORATIONS.md:1602`) et une dont les deux constats sont **relevés et NON corrigés** (`:1609`) |
| Aucune fiche étage 2 implémentée avant arbitrage (11 §8-7) | **RESPECTÉ** | Les deux fiches ouvertes par L5b (contrôle de traçabilité `:1686` ; `thresholds: {}` `:1734`) sont marquées **PROPOSÉE** et ne sont pas implémentées. Vérifié : `vitest.config.ts` porte toujours `thresholds: {}`. |
| Verdict **A51** (sécurité) | **NON RENDU** | Le lot ne touche ni route, ni jeton, ni crypto (aucun code serveur livré). Le seul angle sécurité/RGPD est le texte d'accord, correctement routé vers un juriste. **Acceptable pour l'incrément** ; A51 reste dû au lot à P-C. |
| Verdict **A54** (UX novice) | **NON RENDU — attendu à P-C** | 09 §1 place la recette A54 « à chaque fin de LOT terrain », pas à chaque incrément ; la recette est nommée dans `docs/conception/LOT_L5.md` §4. **Légitime.** |
| Format `DECISIONS.md` | **CONFORME** | `node scripts/check-decisions.mjs` → RC=0, « 131 entrée(s), toutes au format 11 §9bis ». 10 entrées `[L5b]`. |

---

## 10. CE QUE JE N'AI PAS PU VÉRIFIER, ET QUI DOIT ÊTRE DIT

1. **`diff schéma-vs-04`** — PostgreSQL n'est pas levé sur ce poste (`ECONNREFUSED 127.0.0.1:5432`).
   Atténué par la mesure du delta vide, non remplacé par elle.
2. **Migrations up/down sur staging** — hors de ma portée ; sans objet pour l'incrément.
3. **La disposition en trois colonnes PEINTE** — jsdom n'évalue pas les media queries d'une feuille
   de style. Cette observation ne se remplace pas : appareil réel, P-C, A27/A54.
4. **Le texte RGPD** — aucun test ne peut relire un engagement juridique. Juriste, avant P-E.
5. **Node 22 vs Node 24** — toutes mes mesures ont tourné sous **Node v24.19.0**, alors que 11 §1
   épingle **Node 22 LTS** et que `package.json` déclare `>=22.11.0 <23` ; pnpm a émis
   « Unsupported engine » à chaque appel. Tout est vert malgré cela, mais ce n'est **pas** la
   version de référence : la CI, elle, tourne sur la version épinglée.
6. **Les deux violations ESLint `ECRITURE_DEXIE`** annoncées à la fusion avec `lot/l5a` — elles
   n'existent pas sur cette branche (`pnpm lint` RC=0). Je ne peux ni les confirmer ni les infirmer
   avant la fusion.

---

## 11. DOUTES DE SPEC À PORTER EN `DECISIONS.md`

1. **Le glob de couverture critique doit-il inclure `apps/field/src/ecrans/**` ?** Le dépôt a
   corrigé trois fois la même asymétrie (« un seuil qui mesure le dépôt mais pas la route mesure la
   moitié qui ne décide de rien » — `scoping`, `users`, puis `local/`). A29 a **écarté** la
   correction ici, avec un motif recevable (« l'orchestration d'écriture vit DANS le composant — à
   extraire un jour, pas ici »). Mais le motif décrit précisément l'asymétrie. C'est un arbitrage
   A01, pas un veto : la DoD n'écrit pas « écrans ».
2. **Quelle exigence porte §27.4 (fourchette et non communiqué) ?** Le fichier 08 ne rattache §27 à
   aucune E. Le dépôt a choisi **E37** (§32.1 traite bien les deux) ; `valeurs.ts` a choisi E30.
   Une ligne d'arbitrage éviterait la prochaine divergence.
3. **09 §4bis exige que chaque lot ALLONGE `@filrouge`.** À quel incrément le segment L5 est-il
   dû — L5b, L5c, ou à la porte P-C ? Le pack ne le dit pas.

---

## 12. SIGNATURE

Critères du lot (périmètre L5b, 11 §6) : **9/9 tenus**, dont 1 avec réserve de preuve (types de
réponse : donnée tenue, saisie non éprouvée — R4).
Traçabilité exigences → code : **E6, E7, E12, E13, E23, E32, E33, E37 couvertes · E44 partielle**.
Traçabilité code → exigences : **code orphelin : aucun** · **1 glose fausse : E30 sur `valeurs.ts`**.
8 invariants : **1 OK · 2 OK · 3 sans objet · 4 OK · 5 OK · 6 OK · 7 OK · 8 hors périmètre**.
Interdictions 11 §2 : **aucun écart** (pas de Next, pas de Prisma, pas de CORS, pas d'UUID SQL, pas
de secret versionné, pas de test skippé, aucune donnée personnelle en log ajoutée).
DoD transverse : **7 cochées · 1 sans objet · 1 non tenue (README) · 1 non vérifiable (axe-core)** ;
la ligne 10 (diff schéma) non rejouée localement, delta vide mesuré.

**VERDICT : ACCEPTÉ SOUS RÉSERVE.**
Bloquantes pour le passage en porte P-C : **R1** (glose E30), **R2** (README), **R3** (amendement
horodaté sous la décision RGPD du 2026-09-02).
Non bloquantes, à porter au dossier de porte : **R4** (couverture des widgets de saisie — cinq
types jamais rendus), **R5** (axe-core absent), **R6** (fil rouge non allongé).

**Si R1, R2 ou R3 n'est pas fermée avant la signature d'A01, ce verdict devient un VETO**, au motif
de 09 §3 étape 6 : « la Definition of Done transverse est cochée intégralement ». Le veto se
lèverait alors par la correction, ou par une décision écrite d'A01 ou de Williams — jamais par le
temps qui passe.

**Conformité + traçabilité — A02, 2026-09-03, sur `b3d8886`.**
