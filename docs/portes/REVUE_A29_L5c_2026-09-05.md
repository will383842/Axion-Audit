# REVUE CROISÉE A29 — incrément L5c — 2026-09-05

> **Étape 4 du pipeline (09 §3).** Réviseur croisé de l'équipe front. **Je n'ai produit aucune
> ligne du code relu, et je n'en produis aucune ici** (09 §1, §5.6) : ce fichier est le seul que
> j'écris.
> Commit relu : `f20b7a0` · branche `lot/l5c` · worktree `C:\Users\Will\Documents\_axl5c`.
> **Périmètre mesuré** : `git diff 5ef8ef2..f20b7a0` = **29 fichiers, 6 480 insertions,
> 11 suppressions** — 100 % relus.
> `5ef8ef2` est le point de fusion `lot/l5a` → `lot/l5c` ; le `lot/l5b...lot/l5c` du brief
> retombe sur `4e1f35e` (deux bases de fusion, avertissement `git`) et fait entrer 33 fichiers
> de L5b qui ne sont pas de cet incrément. La base retenue est celle qui isole L5c.
> Toutes les mesures citées ont été **exécutées** ce jour. Ce qui est déduit est écrit « déduit ».
> Hors périmètre, et écartés en conséquence : `ecrans/journee/EcranAgenda.test.tsx` et
> `EcranAujourdhui.test.tsx`, **non suivis par git** (travail A27 en cours).

---

## VERDICT

**REFUSÉ.**

**4 bloquants · 8 majeurs · 11 mineurs · 15 observations.**

Le refus porte sur le **câblage**, pas sur le domaine. Les modules `agenda/**` et `sauvegarde/**`
sont parmi les mieux écrits du dépôt : le port d'écriture est respecté sans une seule exception,
la machine à états n'est jamais redéclarée, la clé du `.axionbackup` dérive réellement du mot de
passe et le test le prouve réellement, aucun jeton n'est inventé, aucun `any`, aucune
simplification de crypto. Le domaine est là.

Ce qui n'y est pas, c'est l'accès à ce domaine. **Trois des gestes que le fichier 07 nomme
explicitement pour L5 n'ont aucun point d'entrée dans l'application** : terminer un entretien,
rouvrir un entretien terminé, restaurer une sauvegarde. Ils sont écrits, testés, et appelés par
personne. Le quatrième bloquant est celui que 03 §33.2 me charge nommément de refuser plutôt que
de remarquer : trois écrans sur quatre sont livrés sans leurs quatre états.

Aucun des quatre ne demande d'arbitrage, ni de dépendance, ni de ligne de spécification nouvelle.

---

## 1. LE PÉRIMÈTRE DU BRIEF, LIGNE À LIGNE

Brief L5c, recopié du 11 §6 :

> **L5c** — agenda + proposition d'unité + 5 types de session + atelier + entretien complémentaire
> + validation d'entretien (strict/expert, express R1) + compression photos R2 + export de secours
> + mise à jour SW §31.

| # | Élément | Constat | Preuve |
|---|---|---|---|
| 1 | Agenda §25.2 | **TENU** | `ecrans/journee/EcranAgenda.tsx`, `agenda/sessions.ts`, anti-collision non bloquante (`sessions.ts:216-255`) |
| 2 | Proposition d'unité §25.3 | **TENU** | `agenda/unites.ts:90` — `proposee`, `proposedBy`, `inScope: true`, UUID v7 client l.99 |
| 3 | Les 6 `kind` dont atelier | **TENU** | `sessions.ts:46-63`, `modeApplicable` l.77 (03 §32.6-1 : « complémentaire » est un mode) |
| 4 | Entretien complémentaire §25.6 | **TENU** | `sessions.ts:107,131-135,164` (`linkedReviewAnswerId` + garde de cohérence) |
| 5 | Démarrage pré-rempli en un tap §34.2 | **TENU** | `EcranAujourdhui.tsx:177-185` → `memoriserSessionCourante` + vue `entretien` |
| 6 | **Terminer ≠ valider §19.1** | **NON ATTEIGNABLE** | domaine complet (`agenda/validation.ts`), **zéro appelant** — B1 |
| 7 | Validation groupée §19.1 V2.10 | **CÂBLÉE, SANS MATIÈRE** | `EcranFinDeJournee.tsx:188` ; sa source `jour.ts:221` filtre `status === 'termine'`, état inatteignable — B1 |
| 8 | Parcours express R1 §29 | **TENU, avec une borne fausse hors cas nominal** | `agenda/pilote.ts` — M2 |
| 9 | Compression photos R2 §29 | **BIBLIOTHÈQUE SEULE** | `sauvegarde/photos.ts` : aucun appelant, aucun chemin de capture — M6 |
| 10 | Export de secours 11 §4 | **EXPORT TENU · IMPORT NON ATTEIGNABLE** | `sauvegarde/sauvegarde.ts` — B2 |
| 11 | Fin de journée §34.2-2 | **TENU pour la 1ʳᵉ mission seulement** | `EcranFinDeJournee.tsx:153` — B3 |
| 12 | Bandeau de mise à jour §31-1 | **TENU** | `BandeauMiseAJour.tsx` ; `activationPermise()` par défaut refus (L5a) |
| 13 | Grille UX §33 — 4 états | **1 écran sur 4** | B4 |

---

## 2. CONSTATS BLOQUANTS

### B1 — Aucun écran ne termine, ne rouvre ni ne déverrouille une session

`apps/field/src/agenda/validation.ts:92` `terminerSession` · `:108` `rouvrirSession` ·
`:124` `validerSession` · `:139` `deverrouillerSession`.

**Mesuré** :

```
grep -rn "terminerSession\|rouvrirSession\|deverrouillerSession\|validerSession\b" \
     apps/field/src --include=*.tsx --include=*.ts | grep -v agenda/validation
  → aucun résultat
grep -rn "status: 'termine'" apps/field/src | grep -v agenda/validation.ts
  → aucun résultat
```

Le seul « Terminer » de tout `apps/field` est `EcranFinDeJournee.tsx:350`, « Terminer la journée ».

**Ce que ça viole** : 03 §19.1 règle V2.10 (« Terminer l'entretien est le geste À CHAUD de fin de
rencontre » — l'entretien « reste ROUVRABLE librement par son auteur ») · 03 §33.7, critère de
porte V2.10 : « un entretien TERMINÉ reçoit une note additionnelle sans révision PUIS est validé
en groupe » · `LOT_L5.md` §1, qui donne « terminer ≠ valider » à L5c et l'interdit à L5b
(« Ne livre PAS : […] terminer/valider ») · 07 ligne L5.

**La conséquence en chaîne, et c'est elle qui rend le constat bloquant** : `jour.ts:221`
construit `aValider` en filtrant `status === 'termine' && valideeLe === null`. Aucun code ne pose
`termine`. Donc `aValider` est structurellement vide, donc `sessionsValidablesEnGroupe`
(`EcranFinDeJournee.tsx:117`) rend toujours `[]`, donc la **validation groupée** — le livrable-titre
du §34.2-2 et de la V2.10 — n'a aucune matière atteignable ; et le badge « Terminée, à valider »
(`EcranAujourdhui.tsx:137`) ne peut jamais s'afficher.

Le module de domaine est excellent et son test l'est aussi (`validation.test.ts`, écrit avant le
code). Il lui manque un bouton.

**Bloque la fusion ET la porte P-C.**

### B2 — L'import de sauvegarde n'a aucun point d'entrée

`apps/field/src/sauvegarde/sauvegarde.ts:336` `importerSauvegarde`.

**Mesuré** : `grep -rn "importerSauvegarde" apps/field/src` → la déclaration, et
`sauvegarde.test.ts`. Rien d'autre. `grep -rn "axionbackup\|EXTENSION_SAUVEGARDE"` dans
`ecrans/` et `app/` → un commentaire, aucun `<input type="file">`, aucun geste de restauration.

**Ce que ça viole** : 07 ligne L5, critère d'acceptation **« export de secours créé puis restauré
sur un 2ᵉ appareil »** · invariant 8 (« export de secours chiffré **disponible et testé** ») ·
11 §4 (« Import = validation Zod du fichier + fusion par UUID ») · `LOT_L5.md` §4, E2E `@critique`
« export de secours créé puis **restauré sur un 2ᵉ profil navigateur**, données identiques ».

**Et un effet que je signale parce qu'il touche un arbitrage rendu ce jour** : le champ
`RapportImport.avertissement` (`sauvegarde.ts:411-414`) est la mise en œuvre de la décision
`DECISIONS.md` 2026-09-05, option (c) : « restaurer les LIGNES et **ANNONCER** le nombre
d'opérations non réinjectées ». Sans écran d'import, **cette annonce n'est faite à personne**.
L'arbitrage est appliqué dans le domaine et non tenu dans le produit. Je ne conteste pas
l'arbitrage — je constate qu'il n'aboutit pas.

Accessoirement, le texte de l'avertissement promet plus qu'il ne peut : « ils repartiront au
prochain envoi complet de la mission ». Il n'existe aucun mécanisme d'« envoi complet » : les
lignes restaurées n'ont pas d'op d'outbox, et `appliquerDescente` n'en fabrique jamais
(`local/ecriture.ts:274-276`). La phrase honnête est celle que la décision écrit elle-même.

**Bloque la porte P-C** (le critère 07 est indémontrable en démo).

### B3 — « Fin de journée » ne traite que la PREMIÈRE mission embarquée

`apps/field/src/ecrans/journee/EcranFinDeJournee.tsx:153`

```ts
const missionId = journee.missions[0]?.mission.id ?? null;
```

`journee.missions` est la liste de **toutes** les missions de l'appareil (`agenda/jour.ts:170-203`,
`lireMissions` ne filtre que les supprimées). Les étapes ① sync et ② sauvegarde ne portent donc que
sur `missions[0]`. La synthèse affichée juste au-dessus, elle, additionne bien **toutes** les
missions (`:256-268` : à-revoir et éléments en attente cumulés sur `journee.missions`).

Sur deux missions embarquées, l'auditeur lit « 12 éléments de collecte en attente », presse « un
seul geste », et l'écran conclut « Ce qui a été fait » — sans que la seconde mission ait été ni
synchronisée ni sauvegardée, et sans le dire.

**Ce que ça viole** : invariant 8 (« aucune donnée ne vit sur un seul appareil > 24 h ouvrées ») ·
03 §34.2, qui fonde le cockpit sur « **toutes missions embarquées confondues** » · `LOT_L5.md`
§3.6, « le bouton ne doit pas mentir ».

**Bloque la fusion.**

### B4 — Trois écrans sur quatre sans leurs quatre états

**Mesuré**, écran par écran :

| Écran | chargement | vide | erreur | hors ligne |
|---|---|---|---|---|
| `EcranAujourdhui.tsx` | ✓ | ✓ | ✓ | ✓ |
| `EcranAgenda.tsx` | ✓ | ✓ | **✗** | **✗** |
| `EcranPilote.tsx` | ✓ | ✓ | ✓ | **✗** |
| `EcranFinDeJournee.tsx` | ✓ | ✓ | ✓ | **✗** |

Référence interne, mesurée de la même façon : `app/EcranAccueil.tsx` (L5a) et
`ecrans/entretien/EcranEntretien.tsx` (L5b) portent les **quatre**. Le standard de la maison est
donc établi, et L5c est en dessous.

`EcranAgenda` mérite une ligne de plus : ses trois `useLiveQuery` (`:86-90`, `:96-100`, `:102-112`)
n'ont **aucun `try/catch`**, contrairement aux trois autres écrans de L5c. Une lecture locale qui
rejette laisse la valeur à `undefined`, donc l'écran en squelette **indéfiniment**. C'est
exactement le mode de défaillance que le test `@critique` d'`EcranAccueil.test.tsx` — « si la
lecture locale REJETTE, l'écran affiche une erreur en français (cause + action), **sans tomber** »
— a été écrit pour nommer sur L5a. Le même défaut est reconduit ici.

**Ce que ça viole** : 03 §33.2, mot pour mot — « **Critère de revue croisée A29** et de recette
A54 : un écran sans ses 4 états ne passe pas » · `LOT_L5.md` §3.7 · 03 §33.7 (critère P-C :
« 4 états vérifiés écran par écran ») · DoD transverse `CLAUDE.md` §5.

Mon mandat ne me laisse pas le classer autrement : c'est un refus, pas une remarque.

**Bloque la porte P-C.**

---

## 3. CONSTATS MAJEURS

### M1 — `versionSchemaLocal` est écrit dans l'en-tête et jamais relu

`sauvegarde/format.ts:84` déclare le champ ; `format.ts:27-33` le justifie longuement : sans lui,
« une sauvegarde faite par une version PLUS RÉCENTE de l'application s'importerait "au mieux" dans
une plus ancienne, **en silence** », et 05 §31-1 l'interdit.

**Mesuré** : `grep -rn "versionSchemaLocal" apps/field/src` → l'écriture (`sauvegarde.ts:262`), le
schéma, le commentaire. **Aucune lecture.** `importerSauvegarde` ne contrôle que `versionFormat`
(`sauvegarde.ts:346`).

Le champ est un ajout au format que 11 §4 ne nomme pas ; il a été assumé au nom d'une garde qui
n'existe pas. Deux issues cohérentes : écrire la garde, ou retirer le champ. La troisième — le
garder tel quel — laisse dans le format une promesse que le code ne tient pas.

### M2 — R1 : le « 3 étapes visibles » n'est vrai que dans le cas nominal

`agenda/pilote.ts:248-251` `visibleEnExpress` masque les étapes trivialement satisfaites et replie
`rapport` sur `analyse`. Sur une mission express complète, le calcul rend bien
`collecte, analyse, livraison`, et `pilote.test.ts:132,140` le vérifie. Très bon parti : le « 3 »
du pack devient une vérification plutôt qu'un nombre recopié.

Mais `trivialementSatisfaite` (`:187-199`) dépend des mesures. **Déduit du code, non couvert par un
test** :

- `questions: 0` sur une mission express → `preparation` redevient visible → **4 étapes visibles** ;
- `unites: 0` sur une mission express → `cadrage` redevient visible → **4 étapes visibles** ;
- et `estParcoursExpress` (`:122-128`) accepte `unites: 0` (`unites <= UNITES_MAX_EXPRESS`), alors
  que 03 §29 R1 dit « structure **mono**-unité ».

`pilote.test.ts:171,178` éprouve précisément ces deux cas — mais sur `.etapes`, jamais sur
`.etapesVisibles.length` (mesuré : `grep -n "etapesVisibles" pilote.test.ts` → l.132 et l.140,
toutes deux sur le cas express complet). La borne que le test protège n'est donc pas protégée là où
elle casse.

Conséquence à l'écran : `EcranPilote.tsx:138-142` affiche alors « Cette mission tient en **4**
étapes : **structure mono-unité** et diagnostic de cadrage » — faux sur les deux points.

Ce constat n'est **pas** la question secondaire tracée dans `DECISIONS.md` (« LESQUELLES des trois
étapes »), qui reste ouverte et légitimement ouverte. Il porte sur le NOMBRE, que la décision dit
justement vouloir vérifier plutôt que recopier.

**Réponse à la question 5 du brief** — « un auditeur peut-il se retrouver avec un pilote condensé
sur une mission qui ne le mérite plus ? » : **non par croissance**, et c'est bien vu. `mesurer`
(`EcranPilote.tsx:52-72`) recompte à chaque rendu sur les lignes locales, unités `proposee`
comprises (seules `fusionnee` et supprimées sont exclues) : une mission qui grandit perd l'express
immédiatement, et une validation humaine survit à la sortie de l'express (`pilote.ts:279-281`,
testé). **Mais oui par périmètre partiel** : `entretiens` ne compte que les sessions **présentes
sur cet appareil**. Sur une mission à plusieurs auditeurs, après L6b, un appareil peut garder un
pilote condensé pendant que la mission dépasse les 3 entretiens ailleurs. À nommer, pas forcément à
corriger : le §34.2 veut un cockpit 100 % local.

### M3 — Aucun filtre de propriété (§9.9) sur la journée ni sur la validation groupée

`agenda/jour.ts:176` appelle `depotSessions.duJour({missionId, fuseau})` ; ce dépôt (L5a,
`local/depots/sessions.ts:90-110`) ne filtre **pas** sur `conductedBy`.
`agenda/validation.ts:161` `sessionsValidablesEnGroupe` n'interroge que la machine à états, jamais
la propriété.

L'identité est pourtant disponible et déjà utilisée dans cet incrément : `session/auditeur.ts`
(`identiteAuditeurSchema.id`), lu par `EcranAgenda.tsx:125`.

Latent aujourd'hui — sans L6b, l'appareil ne détient que ce que son porteur a embarqué. Il cesse de
l'être au premier pull delta : le cockpit listera les sessions des collègues alors que 03 §34.2
pose que « son tableau de bord ne porte que sur **SON** périmètre (§18.2) », et « Fin de journée »
proposera de les valider — produisant des ops que le serveur rejettera en `forbidden`
(05 §9.3, §9.9). **Invariant 3.**

Le poser maintenant coûte un prédicat ; le poser après L6b coûte une journée d'ops rejetées.

### M4 — « Un échec n'annule pas les suivants » n'est tenu que pour l'export

`EcranFinDeJournee.tsx:138-146` promet : « LE geste unique : synchroniser, sauvegarder, valider —
dans cet ordre, et **sans qu'un échec n'annule les suivants** ».

Dans le code :

- ① `portSyncInerte.synchroniserMaintenant` (`:159`) — **pas de `try/catch`** ;
- ② l'export (`:172-181`) — **protégé** ;
- ③ `validerEnGroupe` (`:189`) — **pas de `try/catch`**.

Et `validation.ts:200-212` ne collecte que les refus de la machine : un `reecrireSession` qui lève
(échec Dexie, échec de chiffrement) **interrompt la boucle** et rejette. Le `catch` global
(`:199-203`) affiche alors « Le rituel n'a pas pu aller à son terme. Aucune donnée n'a été perdue »
— sans dire quels entretiens ont été validés et lesquels ne l'ont pas été, alors que certains l'ont
réellement été.

**Réponse à la question 6 du brief** : la règle n'est donc vraie **dans le code** que pour
l'étape ②. Elle est vraie *par accident* pour ① et ③ tant que le port est inerte et que Dexie ne
tombe pas ; `LOT_L5.md` §3.6 annonce que « **L6a remplace cette implémentation** », et elle
deviendra fausse au premier port réseau. Dernier point : le commentaire justifie l'ordre par « on
ne modifie pas avant d'avoir protégé » — or la validation ③ s'exécute **aussi** quand l'export ② a
échoué ou quand le mot de passe était vide. Continuer est conforme au brief ; c'est la
justification écrite qui ne décrit pas ce que fait le code.

### M5 — Le mot de passe de la sauvegarde n'est jamais vérifié

`EcranFinDeJournee.tsx:168-177` : le seul contrôle est `motDePasse.trim() === ''`.
`exporterSauvegarde` dérive ensuite une clé de **n'importe quelle** chaîne
(`sauvegarde.ts:132-138` → `deriverKek`).

Une faute de frappe produit donc un `.axionbackup` parfaitement formé, annoncé « Sauvegarde
chiffrée produite », et **définitivement inouvrable** — découvert le jour de la restauration,
c'est-à-dire le jour où l'appareil est perdu.

L'appareil peut vérifier hors ligne, avec ce qui existe déjà : `deriverKek(mdp, sel du coffre)`
puis `ouvrirCoffre(kek, dekEnveloppee)` (`local/coffre.ts`), le chemin qu'`EcranDeverrouillage`
de L5a emprunte à chaque déverrouillage.

**Invariant 8** : l'export doit être « disponible **et testé** ». Un export dont on ne sait pas
s'il s'ouvre n'est pas testé.

### M6 — R2 est livré comme bibliothèque : il n'existe aucun chemin photo dans l'application

**Mesuré** : `compresserPhoto` (`sauvegarde/photos.ts:141`) — zéro appelant, zéro test.
`grep -rn "entite: 'attachment"` sur tout `apps/field/src` → un seul écrivain de pièces jointes,
`session/notes-volantes.ts` (L5b), avec `kind: 'note'`. Aucun champ de fichier image, aucune
capture, nulle part.

Deux textes livrés reposent pourtant dessus :

- `EcranAujourdhui.tsx:61-65` annonce à l'auditeur, dans l'**état hors ligne**, la capacité
  « **Photographier**, annoter, signaler un point à revoir » ;
- 03 §34.2-2 exige que la synthèse de fin de journée compte « **photos en attente de sync** » ;
  `EcranFinDeJournee` ne les compte pas, et ne le peut pas.

**Réponse à la question 10 du brief — vrai trou ou trou de mesure ?** Les deux, mais le vrai trou
domine, et il précède l'autre. Le `_note_glob` de `.github/coverage-critical-paths.json` dit vrai
sur `jsdom` (ni `createImageBitmap` ni `OffscreenCanvas`) mais conclut faux : « le reste se vérifie
en E2E et à la recette d'appareils (A27) ». **Aucun E2E ne peut l'atteindre**, puisqu'aucun code de
production ne l'appelle.

Ce qui le prouve, dans les deux sens :

- `grep -rn "compresserPhoto" apps/field/src --include=*.tsx` → **0** ;
- couverture mesurée ce jour, lignes NON couvertes de `photos.ts` : **100-164** — c'est-à-dire
  exactement `versBlob()` et `compresserPhoto()`, les deux seules fonctions non pures.
  `dimensionsCibles`, la règle pure, est à **100 %**.

Ce qui le lèverait : un chemin de capture livré, puis une mesure sous Playwright (environnement à
canvas) plutôt que sous `jsdom`. Tant qu'il n'existe pas, le chiffre de 26,76 % ne mesure pas un
outillage manquant : il mesure du code que rien n'atteint.

### M7 — Le régime déclaré pour `App.tsx` est dépassé, et l'amendement n'est pas dans `DECISIONS.md`

`LOT_L5.md:29-36`, amendement du 2026-09-05 : `App.tsx` rejoint `vues.ts` « sous le même régime —
**strictement append-only, un `case` par écran** dans `ContenuCourant` ».

**Réponse à la question 9 du brief, mesurée** sur `git diff 5ef8ef2..f20b7a0` :

- `vues.ts` : **conforme** — 4 lignes ajoutées, plus une ligne de commentaire de position modifiée
  (voir O8), qui est le marque-page laissé pour L5c lui-même ;
- `App.tsx` : **NON conforme**. Outre les quatre `case` attendus, L5c **modifie une ligne
  existante** (`import type { ReactNode }` devient `import { useEffect, useRef, type ReactNode }`),
  **ajoute une fonction** `useVueInitiale()` d'environ 26 lignes, et **modifie le corps d'`App()`**
  en y insérant `useVueInitiale();`.

Ce n'est pas append-only, et ce n'est pas « un `case` ». Sur le seul fichier que la note vient de
déclarer partagé avec A22, c'est le point exact où une collision se paie.

**Et la trace manque** : `grep -n "VUE_INITIALE\|vue initiale\|App.tsx" DECISIONS.md` → aucune
entrée L5c. L'arbitrage vit dans une note de conception et dans un message de commit.
`CLAUDE.md` §7 : « Une décision non tracée dans ce format n'existe pas » ; 11 §8-2 range la
modification d'une convention parmi les escalades.

Je ne conteste pas le **contenu** : `ecrans/journee/vue-initiale.ts` est une bonne pièce, la
fonction est pure, la reprise instantanée (03 §17.4) est explicitement préservée par la double
condition `vueAtterrissage === VUE_INITIALE && profondeurPile === 1`, et sept tests la gardent. Je
conteste le régime et l'absence d'entrée.

Défaut technique associé : dans `useVueInitiale`, `void aUneMissionEmbarquee(base).then(...)`
**sans `.catch()`**. Une lecture locale qui rejette produit un rejet non géré — le mode de
défaillance exact qui rend `EcranAccueil.test.tsx` rouge aujourd'hui, reproduit dans le fichier
partagé.

### M8 — `deverrouillerSession` efface l'horodatage de validation, sans trace

`agenda/validation.ts:151` remet, en une écriture, `status` à `en_cours`, `valideeLe` à `null` et
`endedAt` à `null`.

Rien, nulle part, ne conserve qu'un entretien **a été** validé, ni quand. `valideeLe` est une
donnée d'audit qui remonte au siège par `ecrireLocal` : le push effacera aussi la trace côté
serveur.

L'entrée `DECISIONS.md` du 2026-09-05 arbitre que le **motif** n'est pas persisté, et argue que
« l'invariant 7 reste tenu par ailleurs, puisque toute correction de **RÉPONSE** qui suit le
déverrouillage incrémentera `answers.revision` ». C'est exact pour `answers`. Ça ne dit rien de la
ligne `interviews`, dont un champ est ici mis à `null` en écrasement direct.

**Invariant 7** : « rien n'est jamais silencieusement écrasé ou supprimé ». Je n'attaque pas
l'arbitrage rendu ; je constate qu'il ne couvre pas ce champ, et que le champ, lui, part au siège.

---

## 4. CONSTATS MINEURS

- **m1 · `EcranAgenda.tsx:56-60`** — `versUtc` interprète le créneau au fuseau de **l'appareil**
  (`new Date(saisieLocale)`), alors que l'affichage repasse au fuseau de la **mission**
  (`formaterHeure`) et que `depotSessions.duJour` découpe le **jour civil** au fuseau de mission.
  Une session planifiée depuis le siège pour une mission lointaine peut donc ne pas apparaître le
  bon jour. Le champ le dit à l'auditeur (`:462`), ce qui atténue beaucoup — mais rien ne le trace.
  À porter dans `DECISIONS.md` plutôt qu'à deviner. À noter : le garde outillé
  `HORLOGE_DE_L_APPAREIL` ne voit que `new Date()`, pas `new Date(x)`.
- **m2 · `EcranPilote.tsx:127`** — `key={vue.titre}` : clé React sur le titre de mission. Deux
  missions homonymes produisent des clés dupliquées. `mission.id` est disponible et déjà utilisé
  ainsi en `EcranAujourdhui.tsx:332`.
- **m3 · `EcranFinDeJournee.tsx:117,189`** — le profil passé est la constante `PROFIL_PAR_DEFAUT`
  et non le profil réel de l'auditeur, pourtant persisté (`session/auditeur.ts`,
  `identiteAuditeurSchema.profil`) et lu à deux pas (`EcranAgenda.tsx:125`). Sans effet aujourd'hui
  — les deux profils autorisent `valider` depuis `termine` — mais 03 §19.1 fait du profil un
  réglage par utilisateur, et c'est le genre d'écart qui devient faux sans bruit.
- **m4 · `EcranFinDeJournee.tsx:85-92`** — `deposerFichier` appelle `URL.revokeObjectURL(url)`
  **synchroniquement** après `lien.click()`, et n'insère jamais le lien dans le document. Sur
  WebKit — l'iPad est la cible dure (03 §22.1) — le téléchargement peut ne pas avoir démarré à la
  révocation, et l'écran annonce alors « Sauvegarde chiffrée produite » pour un fichier qui
  n'atteint pas le disque. **Déduit**, non mesurable sous `jsdom` : à ajouter à la recette
  d'appareils A27 de la porte P-C, seul endroit où ce constat peut se trancher.
- **m5 · `EcranFinDeJournee.tsx:382`** — `Message ton="succes"` pour la validation, y compris quand
  `validees.length === 0` et que des refus existent.
- **m6 · `sauvegarde.ts:409`** — `lignesRestaurees: enregistrements.length` compte les lignes
  **soumises**, pas écrites : `appliquerDescente` en conserve (`ecriture.ts:272,281,288`) sans le
  rendre. Le chiffre sur-annonce. La glose du champ (`:317`) le sait (« écrites **ou proposées** ») ;
  l'écran d'import, lui, ne le saura pas.
- **m7 · `EcranAgenda.tsx:132,143-146`** — `dureeMin` peut valoir `NaN` (champ vidé) ;
  `planifierSession` le neutralise (`:155`) mais `chevauchements` le reçoit tel quel, et
  `sessions.ts:200-201` produit alors une borne `fin` non numérique — toutes les comparaisons
  deviennent fausses et l'anti-collision signale un chevauchement pour toute session postérieure.
  Avertissement seulement, jamais bloquant : c'est ce qui le maintient en mineur.
- **m8 · `photos.ts:141-156`** — `compresserPhoto` ré-encode en JPEG **même** quand
  `dejaSousLaBorne` est vrai, et ne compare jamais `octetsApres` à `octetsAvant`. Une capture
  d'écran PNG — pièce d'audit courante — devient un JPEG possiblement plus lourd et dégradé, sans
  original conservé (R2, « originaux non conservés »). Ce n'est pas contraire à R2, qui n'anticipe
  pas le cas : fiche `AMELIORATIONS.md`, pas correctif unilatéral.
- **m9 · `BandeauMiseAJour.tsx:14-17`** — l'en-tête annonce « le bouton est **DÉSARMÉ** tant qu'une
  session est en cours » ; le bouton n'est pas `disabled` (`:68`), il est cliquable puis refusé.
  Le comportement se défend mieux que la formule ; c'est le commentaire qui doit suivre le code.
  Par ailleurs `activationPermise()` est lu au rendu (`:63`), sans réactivité.
- **m10 · `EcranFinDeJournee.tsx:66`** — `CLE_DERNIER_RITUEL` est une clé `meta` littérale, hors du
  registre `CLES_META` de `local/base.ts`. Le choix est **juste** ici (L5c ne doit pas écrire dans
  le fichier de L5a) ; il mérite d'entrer au registre à la fusion, sinon deux endroits nomment des
  clés `meta`.
- **m11 · `agenda/jour.ts:235`** — `rappelFinDeJournee` est exporté, testé (4 cas,
  `jour.test.ts:502-535`) et **appelé par aucun écran**. 03 §34.2-2 : « rappel discret sur le
  cockpit tant que le rituel du jour n'est pas fait ». Le cockpit ne le rend pas. Code orphelin
  **et** exigence non rendue — deux lignes dans `EcranAujourdhui` la ferment.

---

## 5. OBSERVATIONS

- **O1 · La suite est rouge, et la déclaration d'`ETAT.md` est exacte — je la confirme par mesure.**
  `pnpm test:unit` → **1265/1265 verts**. `pnpm test:interface` → **590/591**, l'unique échec étant
  `apps/field/src/app/EcranAccueil.test.tsx` — « @critique si la lecture locale REJETTE… ».
  `pnpm lint` → **2 erreurs**, toutes deux dans `apps/field/src/session/enregistrement.ts:92,104`
  (règle « écriture Dexie »). `pnpm typecheck` → **0**.
  Les trois sont **hors périmètre L5c** : `git diff 5ef8ef2..f20b7a0` ne touche ni `EcranAccueil`
  ni son test — dernier commit du test : `0f72e88`, L5a — ni `enregistrement.ts`, qui est de L5b.
  Elles **restent bloquantes pour la fusion vers `main`** — `CLAUDE.md` §5, « tous les tests
  verts » — sans être imputables à cet incrément.
- **O2 · Le port d'écriture : aucune infraction, et c'est mesuré, pas déduit.** La règle ESLint
  `ECRITURE_DEXIE` couvre bien tout `apps/field/src/**` — `eslint.config.js:303-318`, `ignores`
  limité à `horloge.ts`, `ecriture.ts`, `base.ts` et aux tests. Elle **tourne**, puisqu'elle
  attrape les deux écritures de L5b. Toutes les écritures de L5c passent par `ecrireLocal`
  — `agenda/ecriture-session.ts:76`, `sessions.ts:143`, `unites.ts:102` — ou par `ecrireMeta`.
- **O3 · Aucune seconde table de transitions.** Grep sur `TRANSITIONS_SESSION` et `peutTransiter`
  dans `apps/field/src` et `packages` : `TRANSITIONS_SESSION` n'existe qu'en
  `session/machine.ts:77` ; `agenda/validation.ts` l'interroge cinq fois par `peutTransiter` et
  propage le motif de refus tel quel, sans jamais le reformuler. C'est exactement la conduite que
  09 §5.7 protège, et A23 dit vrai sur ce point.
- **O4 · La clé de `.axionbackup` dérive bien du MOT DE PASSE, et le test le prouve vraiment.**
  Question 4 du brief, vérifiée : `sauvegarde.test.ts:217-239` exporte sous `coffreNeuf 11`, puis
  installe une base neuve avec `coffreNeuf 99` — sel de KEK différent **et** `creerDekEnveloppee`
  neuve, donc DEK réellement différente — et n'apporte que le mot de passe. Si la clé du fichier
  venait de la DEK, ce test seul tomberait. Le sel est neuf à chaque export
  — `sauvegarde.ts:245`, garanti par `:256-267`. Aucune sentinelle personnelle dans le sérialisé
  complet, en-tête compris — `:284-286` ; le mot de passe n'y figure pas — `:289-296`.
  **Le test prouve ce qu'il annonce.**
- **O5 · Un point de vigilance sur l'en-tête en clair** : `enTete.libelleAppareil`
  — `sauvegarde.ts:254-260` — est du texte libre saisi par l'utilisateur. Aucune donnée personnelle
  n'y entre **par le code** ; rien n'empêche un auditeur de nommer sa tablette « iPad de Jean
  Dupont ». À nommer à la recette A54, pas à corriger dans le code.
- **O6 · Pas d'AAD sur le chiffrement du payload** — `sauvegarde.ts:248`. L'en-tête n'est donc pas
  authentifié : `missionId`, `creeLe` et `operationsIncluses` sont modifiables sans que le
  déchiffrement s'en aperçoive. C'est **cohérent avec tout le dépôt** — aucun `additionalData` dans
  `local/coffre.ts` — et la fiche **A-008** est ouverte sur ce sujet depuis L5a. Je ne rouvre pas ;
  je note que ce site en fera partie le jour de l'arbitrage.
- **O7 · Croisement 09 §5.6.** `agenda/validation.test.ts` et `sauvegarde/sauvegarde.test.ts` sont
  écrits par A23, auteur du code testé. La limite est **déclarée en tête de chaque fichier** et
  dans `ETAT.md` — c'est la bonne conduite, et elle mérite d'être dite. Elle n'est pas dans
  `DECISIONS.md` : `CLAUDE.md` §7 et 11 §8-5 en font une escalade traçable, pas une note de
  fichier. La tension avec `CLAUDE.md` §4 — TDD *avant* le code sur la machine à états — est réelle
  et se résout par une entrée, jamais par une exception tacite. **Non bloquant** dès lors qu'A26 et
  A27 passent derrière, ce qui est engagé.
- **O8 · `vues.ts`** : quatre lignes ajoutées, **plus** une ligne de commentaire existante modifiée
  — « à compléter par A23 » devient « A23 ». Append-only respecté dans l'esprit ; je le note pour
  être exact, pas pour l'opposer.
- **O9 · `AMELIORATIONS.md` ne porte aucune ligne L5c.** Plafond 0,5 j : sans objet, rien n'est
  déclaré, et le diff ne contient effectivement pas de confort ajouté hors mandat — à une exception
  près, la règle de vue initiale, arbitrée en note de conception plutôt que journalisée, voir M7.
- **O10 · Couverture, mesurée par moi** — `vitest --coverage` sur `agenda/`, `sauvegarde/` et
  `ecrans/journee/` : 7 fichiers, 106 tests verts.

  | Module | lignes | branches | fonctions |
  |---|---|---|---|
  | `agenda/**` | 99,22 | 87,19 | 100 |
  | `sauvegarde/sauvegarde.ts` | **98,90** | **94,87** | **100** |
  | `sauvegarde/format.ts` | **100** | **100** | **100** |
  | `sauvegarde/photos.ts` | **26,76** | 100 | **33,33** |
  | `ecrans/journee/**` | 1,18 | — | — · A27 en cours |

  Les chiffres publiés par A23 dans `_note_glob` sont **exacts au centième**. Rien n'a été arrondi
  en sa faveur.
- **O11 · `.github/coverage-critical-paths.json`** : la correction de nom `apps/field/src/backup/**`
  vers `apps/field/src/sauvegarde/**` est faite, motivée dans `_note_glob` **et** dans `$motif`, et
  **rien d'autre n'a bougé** — le diff se lit comme une entrée retirée de `cheminsAttendus` et deux
  ajoutées à `cheminsCritiques`, sans une ligne de plus. Le partage en deux entrées, une par glob,
  est justifié par la mécanique de `check-coverage.mjs` et suit le précédent `scoping` / `users`.
  L'exclusion de `photos.ts` est nommée **avec son chiffre** — c'est la bonne conduite ; c'est sa
  **conclusion** que je conteste, en M6, pas sa transparence.
- **O12 · Invariant 4 — zéro couleur, zéro taille en dur.** `journee.css` n'utilise que **24**
  propriétés personnalisées, **toutes définies** dans `packages/ui/src/tokens.css` : confrontation
  faite une à une, aucun jeton inventé — les quatre du rapport d'auto-revue ont bien disparu.
  Grep des littéraux hexadécimaux, `rgb`, `hsl`, `px`, `rem` et `em` sur `journee.css` → **aucun
  résultat** ; idem sur les `.tsx` de L5c, et aucun style en ligne. Le rouge d'alerte reste un ton
  distinct — `Message ton="alerte"` contre `"avertissement"`, arbitré par `jour.ts:125-151`.
- **O13 · Invariant 5** — 100 % français dans les libellés livrés, relecture intégrale. UTC :
  `maintenant()` partout, `formaterHeure` au fuseau de mission à l'affichage — la seule entorse est
  l'entrée de créneau, m1.
- **O14 · Interdits `CLAUDE.md` §2** — aucun `any`, aucun `@ts-ignore`, aucun `TODO` ni `FIXME`,
  aucun `console.*`, aucun CDN, aucune donnée personnelle dans un message technique. Mesuré par
  grep sur les trois répertoires. Une seule assertion de type non prouvée : `sauvegarde.ts:312`,
  `as unknown as EnregistrementDescendant`, encadrée par **deux gardes réelles** — charge validée
  contre `SCHEMA_CHARGE`, `clientUpdatedAt` exigé sous peine de `SauvegardeIllisibleError` — et
  motivée sur vingt lignes. **Je l'accepte** : écrire ici sept schémas Zod d'index fabriquerait la
  seconde description qui dérive.
- **O15 · Ce que je ne re-signale pas, et que je tiens pour bien arbitré** — `DECISIONS.md`
  2026-09-05, ratifié le même jour : la réinjection de l'outbox reportée à L6a · la note d'unité
  §25.3 non offerte plutôt que jetée · le motif de déverrouillage vérifié et non persisté. Les
  trois raisonnements tiennent, et le troisième — « un formulaire qui ne demande pas est honnête ;
  un formulaire qui demande et oublie ne l'est pas » — est exactement la bonne lecture de
  l'invariant 7. Une seule réserve, en M8 : l'arbitrage sur le motif ne couvre pas l'effacement de
  `valideeLe`, qui est un autre champ, et qui remonte au siège.

---

## 6. LES 8 INVARIANTS SUR LE DIFF

| # | Invariant | Verdict | Appui |
|---|---|---|---|
| 1 | Offline-first, UUID v7 client, push idempotent | **OK** | aucun appel réseau dans `agenda/**` ni `sauvegarde/**` ; `uuidv7()` en `sessions.ts:140` et `unites.ts:99` ; `opId` conservé à l'export — `format.ts:105-124` — ce qui préserve la déduplication `processed_ops` |
| 2 | Aucune référence client dans le code | **OK** | aucun nom propre ; `nomFichierSauvegarde` désigne la mission par son UUID — `format.ts:174-177` ; fixtures de test explicitement fictives |
| 3 | RBAC serveur ; écritures de sync réservées au propriétaire §9.9 | **ÉCART** | M3 — ni `jour.ts` ni `validation.ts` ne filtrent sur `conductedBy` |
| 4 | Aucune couleur ni taille en dur, alerte en rouge distinct | **OK** | O12 — confrontation `journee.css` ↔ `tokens.css`, 24 sur 24 |
| 5 | 100 % français ; UTC en base, fuseau de mission à l'affichage | **OK avec réserve** | O13 ; réserve m1 sur l'entrée de créneau |
| 6 | Aucune génération lourde sur la machine terrain | **OK** | `jour.ts` et `pilote.ts` ne comptent que des lignes locales ; aucun scoring, aucune agrégation multi-auditeurs ; la compression photo est l'exception que `LOT_L5.md` §3.5 autorise nommément |
| 7 | Rien d'écrasé silencieusement | **ÉCART** | M8 — `valideeLe` remis à `null` sans trace. Par ailleurs **très bien tenu** : l'import réutilise `appliquerDescente`, qui protège les lignes portant une op non appliquée et arbitre par `clientUpdatedAt` — `ecriture.ts:280-290` — testé dans les deux sens |
| 8 | Sauvegarde terrain, alerte au-delà de 24 h | **ÉCART** | B2, B3, M5. L'alerte locale, elle, fonctionne et ne ment pas — `jour.ts:196` vers `evaluerAlerteSauvegarde` |

**Réponse à la question 1 du brief — l'invariant 7, écriture par écriture.**

| Écriture de L5c | Verdict |
|---|---|
| Validation groupée — `validation.ts:210` | **OK** : n'écrit que `valideeLe` ; ne touche jamais `answers` ; une session refusée n'arrête pas les autres (réserve M4 sur l'échec d'écriture) |
| Fin de journée — `EcranFinDeJournee.tsx:194` | **OK** sur la donnée ; l'écrasement est une clé `meta`, non synchronisée |
| Restauration d'un `.axionbackup` sur base non vide | **OK, et c'est le meilleur endroit du lot** : `appliquerDescente` conserve toute ligne portant une op non appliquée, puis arbitre par `clientUpdatedAt` ; `versEnregistrement` **exige** `clientUpdatedAt` et refuse le fichier sinon — `sauvegarde.ts:303-307` — parce qu'un `undefined` y désarmerait l'arbitrage |
| Compression photo — `photos.ts` | **conforme à R2** : « originaux non conservés » est la règle du pack, pas un oubli. Réserve m8 sur le ré-encodage inutile d'une image déjà sous la borne |
| Déverrouillage — `validation.ts:151` | **ÉCART — M8** |

---

## 7. CONTRAT DE SYNC 11 §4 — les points touchés par L5c

L6 n'est pas ouvert ; seuls les points que cet incrément touche sont jugés.

| Point 11 §4 | Verdict | Appui |
|---|---|---|
| Format d'op : `op_id`, `entity`, `action`, `client_updated_at` | **OK** | écrit par `ecrireLocal`, L5a ; L5c ne le contourne jamais ; entités employées : `interview` et `org_unit_proposal`, toutes deux au contrat |
| Lots de 100 max, ordre de file préservé | **OK** | `validerEnGroupe` est délibérément séquentiel et non parallèle, motif écrit — `validation.ts:188-191` |
| `processed_ops` et déduplication | **OK** | `opId` conservé tel quel dans la sauvegarde — `format.ts:105-109` ; le raisonnement est juste, et rare |
| Question ad hoc en UNE op | **sans objet** — L5b | — |
| `.axionbackup` : clé dérivée du **mot de passe**, pas de la DEK | **OK, ET PROUVÉ** | O4 |
| `.axionbackup` : en-tête `format_version`, `mission_id`, `device_label`, `created_at`, `kdf` | **OK avec deux ajouts** | `format.ts:77-92` ; `operationsIncluses` est utilisé — `EcranFinDeJournee.tsx:177` ; `versionSchemaLocal` ne l'est pas — **M1** |
| `payload` = données locales **plus** outbox, AES-256-GCM | **OK** | `sauvegarde.ts:209-252` ; les sept tables miroirs du 05 §9.1 |
| Import = validation Zod, fusion par UUID, op locale plus récente jamais écrasée | **CODE OK, PRODUIT ABSENT** | logique conforme et testée ; **aucun appelant** — B2 |

---

## 8. « SIMPLIFICATIONS TEMPORAIRES » — 09 §5.7

**Aucune détectée.** Ni la synchronisation ni la cryptographie n'ont été allégées pour avancer.

Le port de sync inerte n'en est pas une : c'est un choix **antérieur** — `LOT_L5.md` §3.6 — et L5c
l'affiche honnêtement plutôt que de le verdir. `versEtatPastille` — `EcranAujourdhui.tsx:75-87` —
traduit `indisponible` en `hors-ligne` et **jamais** en `synchronise`, avec le motif écrit ; la
phrase « la synchronisation n'est pas encore disponible dans cette version » — `:345-346` — est
celle que la note demandait. Le compte d'opérations en attente, lui, est lu dans la file réelle
— `jour.ts:188` — donc vrai par construction, comme `LOT_L5.md` §3.3-② l'exige.

C'est le point où cet incrément est le plus solide, et il faut le dire aussi nettement que les
quatre bloquants.

---

## 9. CODE ORPHELIN

Au sens du contrôle A02 — code vers exigence — rien n'est orphelin : chaque fichier porte une ligne
`Traçabilité` citant des exigences E1-E47 existantes, et `check:tracabilite` ne relève **aucun**
écart sur les fichiers **suivis** de L5c. Ses deux refus C2 portent sur `EcranAgenda.test.tsx` et
`EcranAujourdhui.test.tsx`, tous deux **non suivis par git** — travail A27 en cours, hors périmètre.

Au sens opérationnel — du code livré que rien n'atteint — j'en relève **sept** :

| Symbole | Fichier | Exigence qu'il devait servir |
|---|---|---|
| `terminerSession` | `agenda/validation.ts:92` | 03 §19.1 V2.10 — B1 |
| `rouvrirSession` | `agenda/validation.ts:108` | 03 §19.1 V2.10 — B1 |
| `validerSession` | `agenda/validation.ts:124` | 03 §19.1 — B1 |
| `deverrouillerSession` | `agenda/validation.ts:139` | 03 §19.1, profil expert — B1 |
| `importerSauvegarde` | `sauvegarde/sauvegarde.ts:336` | 11 §4, critère 07 — B2 |
| `compresserPhoto` | `sauvegarde/photos.ts:141` | 03 §29 R2 — M6 |
| `rappelFinDeJournee` | `agenda/jour.ts:235` | 03 §34.2-2 — m11 |

Aucun n'est à supprimer : tous sont à **brancher**. C'est la même phrase pour les sept, et c'est ce
qui rend ce refus réparable en peu de lignes.

---

## 10. CE QUI DOIT ÊTRE CORRIGÉ AVANT LA FUSION — ordre proposé

1. **B1** — brancher « Terminer », « Rouvrir » et « Déverrouiller » sur l'écran d'entretien ou sur
   le cockpit. Sans lui, rien en aval ne se démontre.
2. **B3** — boucler « Fin de journée » sur **toutes** les missions embarquées, et rendre un
   résultat par mission.
3. **B4** — les quatre états sur `EcranAgenda`, `EcranPilote` et `EcranFinDeJournee` ; et le
   `try/catch` manquant sur les trois `useLiveQuery` d'`EcranAgenda`.
4. **B2** — un point d'entrée de restauration, qui affiche `RapportImport.avertissement` ; et
   corriger la phrase « ils repartiront au prochain envoi complet », qui promet un mécanisme
   inexistant.
5. **M5** — vérifier le mot de passe contre le coffre avant de produire la sauvegarde.
6. **M8** — ne pas effacer `valideeLe` sans trace, ou dire dans `DECISIONS.md` pourquoi c'est
   acceptable.
7. **M1** — lire `versionSchemaLocal` à l'import, ou retirer le champ de l'en-tête.
8. **M3** — filtrer sur `conductedBy` dans `jour.ts` et dans `sessionsValidablesEnGroupe`.
9. **M2** — étendre l'assertion `etapesVisibles.length` aux cas « zéro unité » et « zéro
   question », et trancher `unites <= 1` contre « mono-unité » par une entrée `DECISIONS.md`.
10. **M4** — protéger les étapes ① et ③ du rituel, et rendre le détail de ce qui a échoué.
11. **M6** — décider : livrer un chemin de capture photo, ou retirer « Photographier » de
    `CAPACITES_HORS_LIGNE` et déclarer R2 comme non livré au dossier de porte. **L'un ou l'autre,
    pas le statu quo.**
12. **M7** — une entrée `DECISIONS.md` pour la règle de vue initiale et pour le régime réel
    d'`App.tsx` ; ajouter le `.catch()` manquant.
13. **m1 à m11** — au fil de l'eau ; m4 part à la recette d'appareils A27, m8 en fiche
    `AMELIORATIONS.md`.

**Bloquent la fusion** : B1, B3, B4 — plus les rouges hérités d'O1, qui ne sont pas de cet
incrément mais qui rendent la branche rouge.
**Bloquent la porte P-C** : B1, B2, B4, et M6 tant que la démonstration « une session de chaque
type, photos en attente de sync » n'a pas de matière.

---

## 11. DÉSACCORDS À ARBITRER PAR A01

1. **M6** — « compression photos R2 » est-elle livrée par une bibliothèque sans appelant ? Je dis
   non. Si A01 dit oui, alors `EcranAujourdhui.tsx:61-65` doit cesser d'annoncer « Photographier »
   à l'auditeur, et le fichier de porte doit porter R2 comme **non livré**.
2. **M2** — `estParcoursExpress` accepte zéro unité là où 03 §29 dit « mono-unité ». Lecture large
   ou borne à corriger ? La question est distincte de celle, déjà ouverte, des trois étapes.
3. **M7** — le régime d'`App.tsx` : l'amendement du `LOT_L5.md` dit « un `case` par écran » et le
   code y ajoute un crochet. Faut-il élargir l'amendement, ou déplacer le crochet hors du fichier
   partagé ? A22 relit ce fichier avec l'ancienne règle en tête.
4. **O7** — la déviation 09 §5.6, A23 ayant écrit les tests de son propre code au titre du TDD,
   mérite-t-elle une entrée `DECISIONS.md` ? Je pense que oui, et que la déclaration en tête de
   fichier, si honorable soit-elle, n'en tient pas lieu.

---

## 12. DOUTES DE SPEC POUR `DECISIONS.md`

- Le créneau d'agenda se saisit-il au fuseau de **l'appareil** ou à celui de la **mission** ?
  03 §34.2 dit « heure locale du site » pour l'affichage, et le pack ne dit rien de la saisie — m1.
- « Photographier » : quel écran porte la capture, et sous quel `attachments.kind` ? Le 04 le
  prévoit, aucun incrément ne le réclame — M6.
- Un entretien déverrouillé doit-il conserver la trace de sa validation précédente ? Le 04 n'a
  qu'un champ pour l'état courant — M8.
- Un `.axionbackup` produit par un schéma local plus récent : refus, ou import au mieux ? 05 §31-1
  interdit la seconde branche « en silence », pas nécessairement en toutes circonstances — M1.

---

## SIGNATURE

**Revue croisée — étape 4 du pipeline — A29 — 2026-09-05.**

**Verdict : REFUSÉ** — 4 bloquants, 8 majeurs, 11 mineurs, 15 observations.
Périmètre : `git diff 5ef8ef2..f20b7a0`, 29 fichiers, **100 % relus**.

Ce refus n'est pas un jugement sur le travail : le domaine livré est solide, honnête sur ses
limites, et il tient les invariants là où c'était le plus difficile — le port d'écriture, la
machine à états, la fusion à l'import, la dérivation de clé. Ce qui manque est le fil entre ce
domaine et l'auditeur. Sept symboles attendent un appelant, et trois écrans attendent deux états.

**Je n'ai produit aucune ligne de code, aucun test et aucun correctif.** Le seul fichier que
j'écris est celui-ci — `git status` le montrera, et c'est la condition de ce que je viens de dire.
