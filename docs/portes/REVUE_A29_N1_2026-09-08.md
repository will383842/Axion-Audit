# REVUE CROISÉE A29 — chantier N1 (ancres co-visibles, iPad paysage) — 2026-09-08

> **Étape 4/7 du pipeline (09 §3).** Réviseur croisé front A29, **lecture seule** — il ne produit rien
> (09 §1) ; la présente fiche est la transcription de son rapport par le pilote, sans amendement.
> **Worktree** : `C:/Users/Will/Documents/_axn1`, branche `fix/n1-ancres-paysage`, rebasée sur
> `origin/main` après le squash de #97. **Périmètre relu : `git diff origin/main...HEAD`, 6 fichiers,
> 1 218 insertions / 54 suppressions, 100 % relus.** Le mandat annonçait `9a7aff7` ; la revue s'est
> terminée sur **`ae9736f`** (entrée A01 docs-only, +43 l.), lue et intégrée : elle arbitre la question
> §17.4 que le mandat posait et délègue nommément deux points, traités en R-4 et R-7.
> **Ordre de lecture** (09 §5.8) : `CLAUDE.md` → 03 §33.3, §17.4, §33.6, M3.1 → les six entrées
> `DECISIONS.md` (quatre du 07, deux du 08 signées A01) → `RECETTE_NOVICE_L5_2026-09-07_REJEU.md` §2.2
> → `ATTESTATION_A20_L5B_2026-09-08.md` → le diff, fichier par fichier.

---

## VERDICT : 🟠 CONFORME SOUS RÉSERVE

**Une seule réserve bloquante (R-1), triviale à fermer.** Le correctif lui-même est juste, mesuré, et
la garde qui le protège mord — reprouvé par A29 lui-même, par mutation.

---

## 1. Constats bloquants

### R-1 — BLOQUANT — `docs/ETAT.md:4985` et `:4987` : le bloc qui fait foi est faux sur deux points et périmé sur un troisième

Point D du mandat : **oui, la formulation fausse a été propagée**, dans le seul endroit que le squash
n'effacera pas.

- `:4985` « la barre d'actions perd `sticky` » — **faux** : `entretien.css:245-247` conserve
  `position: sticky; bottom: 0` sur `.axn-question__navigation`. La barre est **scindée**, c'est le
  fond de l'arbitrage `ae9736f`. Le message du `wip:` `28e0b28` portait la même erreur ; il
  disparaîtra au squash, **pas ce bloc**.
- `:4987` « la zone centrale prenait la première cellule libre » — **faux** : ce sont
  `RappelHorsLigne` et les `Message` d'erreur (`EcranEntretien.tsx:750-789`, enfants directs de la
  grille) qui la prenaient ; la zone centrale, décalée, **tombait en colonne 3**. Le commentaire CSS
  (`entretien.css:109-117`) le dit correctement — c'est ETAT.md qui a dérivé.
- Périmé : « Tests rouges connus : les 4 gardes N1 », « NON PROUVÉ », « étape 2/7 », « relancer A21 » —
  les quatre gardes sont **vertes** et l'étape courante est 4/7. **Section engagée** : CLAUDE.md §8 /
  11 §9ter — « le dernier bloc fait foi », mis à jour « à CHAQUE changement d'étape ».

**Fermeture** : un bloc ETAT.md de clôture (≤ 25 l.) disant le vrai mécanisme et l'état vert.
→ **FERMÉE** par le bloc du 2026-09-08 13h30 (ce commit).

## 2. Constats à corriger avant l'étape 6

### R-2 — `e2e/fixtures/champ-de-vision.ts:483-495` : `exigerDansLeChampDeVision` implémente le critère qu'A01 a ÉCARTÉ, et n'est appelé nulle part

`grep` : **0 consommateur** hors du fichier pour la fonction, pour `TOLERANCE_PX`, et pour six types
exportés (`MesureChampDeVision`, `MesureCoVisibilite`, `EtapeCoVisibilite`, `BoiteMesuree`,
`Debordement`, `EtatDansLeChamp`). Son message d'échec « doit être lisible SANS défiler » (:491) est
**littéralement l'option a)** écartée par l'entrée A01 du 2026-09-08. Une assertion prête à l'emploi
qui rétablirait le critère refusé est un piège pour le prochain agent, et du code orphelin
(CLAUDE.md §4 étape 6). **Remède** : retirer, ou commenter l'interdiction. → **A26**.

### R-3 — `champ-de-vision.ts:402-434` : la règle de recouvrement est écrite deux fois, et les deux versions ont divergé

Le fichier proclame « une seule règle, jamais deux qui dériveraient » (:183-185). La règle de
recouvrement est pourtant recopiée : `recouvrements()` (:302-332) et son jumeau en ligne dans
`mesurerChampDeVision` (:402-434), qui **omet la branche :329** (nœud non collant). Portée : les deux
assertions du test sont alimentées par le balayage (le critère A01 reste bien mesuré) ; ce sont les
relevés « sans geste » (:872-887) — ceux attendus pour comparer avec A54 — qui sous-déclarent.
→ **A26**.

### R-4 — `ZoneQuestion.tsx:270` : `role="toolbar"` promet un contrat clavier que le code n'honore pas

Délégué par A01 (`ae9736f`). **Sur la couverture partielle** (5 boutons sur 7) : aucun écart — un
`toolbar` + un `group` nommé est une composition licite. **Sur le contrat clavier** : vrai défaut,
**préexistant** — aucun `tabIndex`/`onKeyDown`/`ArrowLeft` dans le fichier (pas de roving tabindex),
et `session/raccourcis.ts:120-126` lie ↑↓ à la navigation entre questions puis **désarme** ce
raccourci sur un contrôle : dans cette « barre d'outils », les flèches ne font rien. Un lecteur
d'écran annonce « barre d'outils », l'utilisateur essaie des flèches inertes. `axe-core` n'a aucune
règle là-dessus : **vert n'atteste rien ici**. **Remède** : roving tabindex, ou `role="group"` (qui ne
promet rien) — « la seconde option coûte un mot et supprime le mensonge ». → **A21**.

### R-5 — `entretien.css:124` : les pistes latérales deviennent `minmax(0, 1fr)` sans `min-width: 0` sur l'item

`.axn-entretien__zone--centre` a `min-width: 0` (:71) ; `.axn-entretien__zone--laterale` ne l'a pas.
Un item de grille garde `min-width: auto` : si le min-content d'une colonne latérale dépasse ~246 px,
l'item déborde sa piste. Les notes perdent 74 px (−23 %) et portent des `textarea`. A21 déclare le
débordement à 0 ; A29 ne l'a **pas revérifié** et constate l'asymétrie. → **A21**.

### R-6 — `entretien.css:264-268` : l'élargissement de « Suivant » excède le remède N1, sans ligne `AMELIORATIONS.md`

La bascule grille → flex imposait de remplacer `grid-column: -2 / -1` ; `margin-inline-start: auto`
suffisait. `flex: 1 1 auto` + `max-width: var(--taille-largeur-contenu-etroit)` ajoute un
élargissement de confort que ni N1 ni §17.4 n'exigent. Sous 09 §4bis, le canal est l'étage 1
(compteur L5b ~0,12 j / 0,5 j — la marge est là, c'est la trace qui manque). → **A21** choisit
(revenir au strict, ou garder + ligne étage 1 écrite par le pilote).

### R-7 — La constance des sept boutons n'est mesurée par rien, et A01 vient d'en faire une propriété à GARDER

`grep` : **zéro test** cite « Actions sur la question », « Navigation entre les questions » ou
`toolbar`. §17.4 est un critère de P-C ; une propriété que l'arbitre déclare à garder et que rien ne
mesure retombera au premier refactor. **Garde due**, écrite par un agent qui n'a pas produit
`ZoneQuestion.tsx` (09 §5.6). → **A26**.

## 3. Remarques (non bloquantes)

- **R-8** — angle mort non déclaré : `document.elementFromPoint` **ignore** `pointer-events: none` ; un
  voile décoratif opaque serait invisible à l'instrument — un **faux vert**. Latent (0 occurrence de
  `pointer-events` dans le dépôt). Trois lignes de commentaire dans l'en-tête. → **A26**.
- **R-9** — la marge verte tient à peu : une barre d'outils collante opaque d'**une** rangée (56 px)
  laisse la garde **verte** ; sur cinq rangées, rouge. L'écran a de l'ordre d'**une rangée de
  réserve** en iPad paysage privé, sur une émulation optimiste. → **au rejeu A54 sur iPad réel**.
- **R-10** — `28e0b28 [A21 seul]` embarque `docs/ETAT.md`, journal du pilote. Séparation
  production/test parfaite ; l'étiquette couvre un fichier qui n'est pas d'A21. Le squash l'efface.
- **R-11** — `apps/hq/src/app/collision-classes.test.ts:27` et `:114` citent
  `.axn-question__actions .axn-bouton`, sélecteur disparu. Chaîne d'exemple : test vert et vrai,
  citation périmée. → **A26**.
- **R-12** — la colonne gauche perd 26 px ; `.axn-blocs__libelle` tronque en `ellipsis` sans `title`
  (`ZoneBlocs.tsx:112`). Préexistant, aggravé d'environ 10 %. Nom accessible complet, donc pas un
  écart §33.6. → fiche étage 1, hors N1.

## 4. Réponses aux questions du mandat

| Question | Réponse A29 |
| --- | --- |
| Le prédicat correspond-il **littéralement** au critère A01 ? | **Oui**, et il erre du bon côté : boucle `CRANS = [1..5]` × 4 combinaisons, `echecsGeometriques` puis `echecsRegardables` assertés **séparément**. Deux durcissements déclarés (tout chevauchement compte sans test d'opacité ; un `dd` plus large que la fenêtre = coupé). Un instrument plus strict ne produit qu'un **faux rouge**, jamais un faux vert. |
| `PAS_DE_BALAYAGE_PX = 8` peut-il sauter une fenêtre < 8 px ? | **Oui, et c'est sans danger** : le critère est **existentiel**, échantillonner ne peut que réduire l'ensemble trouvé — rougir à tort, jamais verdir à tort. `scroll-behavior` et `scroll-snap` : 0 occurrence. |
| Recouvrement sans test d'opacité — trou ? | **Non, c'est le bon défaut** (conservateur). Le vrai trou est R-8. |
| `@critique`, français, sans skip ? | Oui. `check:no-skipped-tests` : 181 fichiers, aucun. |
| §17.4 et l'ordre DOM — écart ? | **Lecture légitime, confirmée sur le texte**, avec un argument que l'entrée A01 ne fait pas : garder Précédent avant les outils à la tabulation aurait exigé un `order` CSS **découplant l'ordre DOM de l'ordre visuel** — WCAG 2.4.3 et 1.3.2 cassés. L'arrangement retenu est **celui que l'accessibilité impose**. §33.6 n'est pas entamé. |
| Invariant 4 — chaque déclaration ajoutée est un jeton ? | **Oui, vérifié** : un seul `px` dans le diff CSS, **dans un commentaire**. Quatre jetons appelés, tous définis (`tokens.css:84, 95, 98, 99`). Tactile : `--taille-controle-hauteur-large` = 56 px ≥ 44, sur les **deux** groupes. |
| Invariant 1 — mesuré réseau coupé ? | **Oui, mécaniquement** : `ouvrirLaQuestionAEchelle` appelle `passerEnModeAvion`, qui **assert** `navigator.onLine === false` ET l'échec d'un `fetch` réel. `RappelHorsLigne` est donc rendu : la configuration exacte où la grille glissait est celle qui est mesurée. |
| Les six entrées intactes ? | **Confirmé au diff** : `git diff --name-only origin/main...HEAD -- packages/ui` → **0**. |
| 246 px suffisent-ils pour l'arbre FIL-GC ? | **La question ne se pose pas** : la colonne gauche porte `ZoneBlocs`, pas l'arbre d'unités (rendu nulle part dans `ecrans/entretien/`). Ce que la réduction affecte : R-12 et R-5. |

## 5. Les 8 invariants sur ce diff

| # | Verdict |
| --- | --- |
| 1 offline-first | **OK** — défaut offline-only, garde réseau coupé avec assertion. |
| 2 aucune référence client | **OK** — libellés génériques, en fixture. |
| 3 RBAC / propriété | Sans objet. |
| 4 aucune couleur/taille en dur | **OK** — un `px` en commentaire ; 4 jetons définis. |
| 5 français, UTC | **OK**. |
| 6 rien de lourd sur le terrain | **OK** — CSS et DOM ; le balayage est du code de test. |
| 7 rien d'écrasé | **OK** — `radio:checked` → `toHaveCount(0)` ; `chercherCoVisibilite` repose le défilement. |
| 8 sauvegarde terrain | Sans objet. |

## 6. Croisement 09 §5.6 — la preuve qu'A20 réclamait, et qu'A20 n'a pas eue sur L5b

| Commit | Étiquette | Fichiers | Nature |
| --- | --- | --- | --- |
| `bf4f510` | — | `DECISIONS.md`, `docs/ETAT.md` | docs seuls |
| `e280595` | `[A26 seul]` | `champ-de-vision.ts`, `hors-ligne-l5.e2e.ts` | **test seul — zéro production** |
| `c239ecb` | — | `DECISIONS.md` | docs seuls |
| `28e0b28` | `[A21 seul]` (wip) | `ZoneQuestion.tsx`, `entretien.css`, `docs/ETAT.md` | production + docs — **zéro test** |
| `9a7aff7` | `[A21 seul]` | `entretien.css` | production seule |
| `ae9736f` | — | `DECISIONS.md` | docs seuls |

**Aucun commit ne mêle production et test.** C'est exactement l'artefact que l'attestation A20 du
2026-09-08 déclarait manquant sur les deux premiers tours de L5b. **Ici, §5.6 est démontrable ET
démontré**, par commit et par fichier. Seule nuance : R-10.

## 7. Ce qu'A29 a mesuré lui-même

Node local **v24.19.0**, hors contrat — à confirmer par la CI sur Node 22. `pnpm build` avant toute
mesure, exit 0.

- `pnpm verify:rapide` → **EXIT=0** (14 gardes, eslint, prettier, tsc ; 76 fichiers, 1 160 tests, 0 skip).
- `playwright hors-ligne-l5 --grep "N1|ancres se LISENT"` → **5 passed** (4 gardes N1 + R1).
- `playwright accessibilite-toutes-vues-l5` → **19 passed**, dont « entretien (réseau coupé, 4ᵉ état) ».
- 8 fichiers e2e sauf `pwa-servie` → **66 passed**. `pwa-servie` : Docker indisponible, sans rapport.

**La garde mord-elle ? Reprouvé par mutation** sur l'artefact construit (`dist/`, ignoré par git),
restauré et vérifié par `cmp` :

| Mutation | Résultat |
| --- | --- |
| A — barre d'outils collante opaque d'**une** rangée | **4/4 verts** (→ R-9) |
| B — retour à la grille d'origine `17rem minmax(0,1fr) 20rem` | **paysage privé ROUGE + paysage partagé ROUGE** |
| C — barre d'outils collante sur cinq rangées | **paysage privé ROUGE** |
| restauration (`cmp` identique) | **4/4 verts** |

**Les deux leviers sont protégés par la garde** : levier 2 par B, levier 1 par C. C'est la propriété
qui compte le plus, et elle tient.

## 8. Ce qu'A29 n'a pas pu vérifier

1. Le débordement horizontal à 0 déclaré par A21 (l'exigeait d'écrire du code de test — 09 §1).
2. Les chiffres de mise en page d'A21 un à un ; la conclusion qu'ils soutiennent est prouvée par
   les 4 gardes et les mutations B et C.
3. Node 22 — la CI fait foi.
4. `pwa-servie.e2e.ts` (Docker absent).
5. **L'iPad réel** — ni barre d'URL Safari, ni clavier, ni encoches. **Cette revue ne coche pas le
   critère de porte** ; elle atteste que la garde est due, juste et verte.

## 9. Désaccords à arbitrer par A01 : aucun. Doutes de spec pour `DECISIONS.md` : aucun.

R-4 et R-7 sont des points délégués par A01 et rendus tranchés ; R-6 relève d'`AMELIORATIONS.md`.

---

```
Signature revue croisée : A29 — 2026-09-08
Verdict : CONFORME SOUS RÉSERVE — bloquant R-1 seul ; R-2 à R-7 à corriger ou tracer avant
l'étape 6 ; R-8 à R-12 en remarques.
git status --porcelain (worktree _axn1) → vide · (dépôt principal) → vide
```

## 10. Suivi des réserves (tenu par le pilote) — après le REJEU A29 sur `70bdcb5`

| Réserve | Main | État au rejeu | Preuve |
| --- | --- | --- | --- |
| R-1 | pilote | **FERMÉE** | bloc ETAT 13h30 : le vrai mécanisme (scission ; `RappelHorsLigne`) |
| R-2 | A26 | **FERMÉE** | `exigerDansLeChampDeVision` supprimée, 4 types dé-exportés, `TOLERANCE_PX` 5 consommateurs, interdiction écrite `:75-84` citant A01 — aucun orphelin |
| R-3 | A26 | **FERMÉE** | `grep elementFromPoint` → **une** occurrence (`:347`) ; pas nul refusé `:389` |
| R-4 | A21 | **FERMÉE** | `role="group"`, « Outils de la question » ; `grep toolbar` → 0 hors commentaire. Nom **juste** |
| R-5 | A21 | **FERMÉE — faux positif d'A29** | Grid §6.6 : deux conditions cumulatives pour un minimum automatique (`overflow: visible` ET piste `auto`) ; `overflow-y: auto` tombe la 1ʳᵉ, `minmax(0, 1fr)` tombe la 2ᵉ. L'item ne pouvait déborder ni avant ni après ; `min-width: 0` est documentaire et inoffensif |
| R-6 | A21 | **FERMÉE sur son objet** | `margin-inline-start: auto` seul ; aucun fichier docs touché ; compteur L5b ~0,12 j inchangé. **Mais → R-13** |
| R-7 | A26 | **REQUALIFIÉE → FERMÉE** (`c893586`, non-rétraction bornée au seul bouton `droite`, sens `reference`/`releve` explicité, `group` nommés gardés, 5 mutations rouges, 12/12) | lecture « bord droit » **confirmée** (§17.4:301 ; M2 ; boîte [673,765] ⊂ [580,765]) ; pas d'entrée DECISIONS (application, pas choix). **Trou** : `:1111` exemption non bornée et générique — ferme par non-rétraction (`b.largeur >= a.largeur - TOLERANCE_PX`) restreinte au seul bouton `bord: 'droite'` |
| R-8 | A26 | **FERMÉE (documentaire)** | en-tête `:62-73`. **Arbitrage : NE PAS armer la sonde** — faux rouges sur les voiles transparents, trou latent (0 occurrence), cohérence avec R-2. `pointer-events` entre dans les greps de revue d'A29 ; forme mécanique = garde nouvelle → A20/A01 |
| R-9 | A54 | inchangée, non aggravée | bande collante = une rangée, `min-height` 3,5 rem ; cran 5 paysage privé dégagé à 496 px. iPad portrait au repos : ancres 4-5 **recouvertes** par la navigation (conforme, une position existe) — à mettre sous les yeux d'A54 |
| R-10 | — | constaté | `1d18947` production seule, `70bdcb5` test seul |
| R-11 | A26 | **FERMÉE** | `.axn-question__navigation .axn-bouton` existe ; unit 7/7 |
| R-12 | — | inchangée | `ZoneBlocs.tsx` non touché ; étage 1 hors N1 |
| **R-13** _(neuve)_ | pilote | **À CORRIGER → rectifiée ici** | voir §11 |
| **R-14** _(neuve)_ | A20 | remarque | `e2e/**/*.ts` n'est typechecké par **aucune** garde (`pnpm typecheck` saute la racine). A29 l'a fait à la main : `npx tsc -p tsconfig.json --noEmit` → EXIT=0. Remède : ajouter au script `typecheck` — chaîne de vérification → proposé à A20, non implémenté |

## 11. R-13 — RECTIFICATION du fait consigné dans `1d18947`

Le message de `1d18947` écrit : « Après : Suivant 184,86 × 56 px » et « la barre d'origine donnait à
Suivant ~112-137 px, **donc aucune régression** ». **Les deux sont inexacts**, mesuré par A29 avec
l'annotation `mesure §17.4` de la garde elle-même, iPad paysage privé :

| Bouton | Largeur | Bord droit |
| --- | --- | --- |
| `Suivant` (questions 1 et 2) | **92 px** | 765 px |
| `Terminer l'entretien` (question 3) | **185 px** | 765 px |

**184,86 px est le bouton _Terminer_, pas _Suivant_.** Et l'état d'avant chantier
(`git show da7e8c9:…/entretien.css:168` → `repeat(auto-fit, minmax(7rem, 1fr))`) garantissait un
**plancher de 7 rem = 112 px** à chaque bouton. Après le retour au strict, il n'y a plus de plancher :
**la cible du geste le plus fréquent perd 20 à 45 px** par rapport à l'avant-N1. « Aucune régression »
n'est donc **pas établi**. Ce qui tient : la position verticale (y = 742–798 / 810), qui est ce que
§17.4 écrit ; 92 × 56 px ≥ 44 px (§19.2) ; bord droit inchangé.

**A29 ne demande pas de plancher** — `min-width: 7rem` serait une taille en dur sans jeton (invariant 4)
et le pack n'écrit pas cette largeur. Si A20 juge les 20-45 px gênants au pouce : ligne étage 1 avec un
jeton, arbitrage A20/A01. **Un commit ne se réécrit pas ; le fait est rectifié ici.**

## 12. Remarques mineures du rejeu (aucune action exigée)

- `:1019` — `/^(Suivant|Terminer l’entretien)/` ne couvre pas « Fin de session »
  (`EcranEntretien.tsx:357`). Faux rouge possible, jamais faux vert.
- La garde §17.4 tourne sur un seul appareil (iPad paysage) ; les quatre gardes N1 couvrent les deux
  orientations. Choix de coût déclaré.
- Rien ne fixe `role="group"` ni les deux `aria-label` — une ligne, à l'occasion (proposée à A26).
- Le commentaire `entretien.css:126-128` attribue à `overflow-y` un mérite qu'il partage avec
  `minmax(0, 1fr)` ; la protection est double, pas simple.

```
Rejeu A29 — 2026-09-08 — sur 70bdcb5 : CONFORME SOUS RÉSERVE, aucun bloquant.
Exécuté par A29 : pnpm build EXIT=0 · tsc -p tsconfig.json --noEmit EXIT=0 (couvre e2e/) ·
verify:rapide EXIT=0 (unit 71 fichiers / 1 806 · interface 76 / 1 160) · hors-ligne-l5 12/12 ·
accessibilite-toutes-vues-l5 19/19 · collision-classes 7/7. Aucun fichier modifié par A29.
```
