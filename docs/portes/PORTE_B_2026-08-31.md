# PORTE P-B — fin du lot L2 (auth, RBAC, étanchéité financière, CRUD users, `activity_log`)

> **Fichier de porte** au format imposé par le contrat 11 §9bis : critères du fichier 07 copiés,
> cochés un à un **avec la preuve** (lien CI, capture, commande), verdict, signature humaine.
> **Le merge de la porte est conditionné à ce fichier commité.**
>
> **Vérification spéciale de P-B (09 §62)** : « Tentatives d'intrusion croisées : chaque rôle essaie
> d'accéder aux données des autres (dont financier) + push sur la session d'un autre auditeur —
> **zéro fuite, zéro écriture croisée exigés** ».

---

> ## ⚠️ ÉTAT : MONTÉ, NON SIGNÉ — ET LA RAISON N'EST PAS LA MODESTIE
>
> **Ce dossier est écrit par A01, qui est l'auteur d'une partie du code qu'il évalue.** La chaîne de
> signature (09 §1) ne prévoit aucune diagonale : le contrôle d'acceptation appartient au **gardien
> A02**, et la porte à **Williams**. Un dossier de porte auto-signé n'est pas une porte, c'est une
> relecture avec un titre.
>
> **Ce que ce fichier est** : l'inventaire des preuves, mesurées et datées, avec ce qu'elles
> établissent **et ce qu'elles n'établissent pas**. **Ce qu'il n'est pas** : un verdict.
>
> **Et il ouvre sur une mauvaise nouvelle qu'il faut lire avant les tableaux** : _deux des trois
> membres du critère spécial P-B ne sont pas prouvables aujourd'hui, et pas faute d'effort — faute
> d'objet._ Voir le **§4**, qui est la vraie question posée à Williams.

---

## 1. Ce qui a été livré au lot L2

| Tâche | Contenu                                                                                              | Auteur du code | Auteur des tests (09 §5.6) |
| ----- | ---------------------------------------------------------------------------------------------------- | -------------- | -------------------------- |
| T1    | Socle d'autorisation : politique déclarée `config.acces`, **présence vérifiée AU DÉMARRAGE**           | agent de lot   | agent croisé               |
| T2    | Auth JWT : login, refresh **rotatif**, détection de réutilisation, logout                              | agent de lot   | agent croisé               |
| T3    | CRUD users (7 routes déclarées, **8 enregistrées**), garde-fou reset §9.7, `habilitated_at` §34.4      | agent de lot   | agent croisé               |
| T4    | `activity_log` : journalisation, **aucune donnée personnelle**                                         | agent de lot   | agent croisé               |
| T5    | Route financière `/v1/scoping/:id/financials` + **balayage sentinelle**                                | A17            | A16                        |

**148 tests de L2** — 119 d'intégration (`l2-auth-routes` 29 · `l2-crochets` 38 · `l2-journal` 11 ·
`l2-users` 41) et 29 unitaires (`socle` 12 · `crochets` 6 · `jetons` 6 · `quota` 5).

---

## 2. Preuve d'exécution — CI de `main`

**Run `33339483715` sur `63fcc26`, 2026-08-30 : les 20 jobs en `success`**, dont
`4 · integration (postgres, redis, minio)`, `couverture ≥ 90 % (modules critiques — 09 §3)`,
`6 · schema-diff (vs fichier 04)`, `aucun test désactivé ni orphelin`,
`8 · deploy-staging / Coolify → déploiement vérifié` et `ZAP baseline (staging)`.
→ https://github.com/will383842/Axion-Audit/actions/runs/33339483715

**Sur les deux « skipping » que rapporte `gh pr checks` sur une PR** : c'est **un job**, pas un test —
`8 · deploy-staging`, conditionné par `ci.yml:1322` (`github.ref == 'refs/heads/main'`). Il ne
s'exécute pas sur une PR **par construction**, et il est **vert sur `main`**. Vérifié en outre qu'il
n'existe dans le dépôt **aucune désactivation conditionnelle à l'exécution** : `skipIf` / `runIf` /
`.skip()` / `todo(` n'apparaissent que dans les **fixtures du garde anti-skip lui-même**, et les
trois sorties `return` anticipées trouvées à la main sont **un helper de fermeture et deux sorties de
boucle d'attente**, aucune dans un corps de test. Question soulevée par un pair, close par mesure.

---

## 3. Critères d'acceptation du lot L2 (fichier 07, recopiés à l'identique)

| #   | Critère du 07                                        | Preuve mesurée                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Ce que la preuve **n'**établit **pas**                                                                                                                                                                                                                                                                     |
| --- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------|
| 1   | **Tests rôle × route exhaustifs**                    | Le mot « exhaustif » est porté par une **propriété**, pas par une liste : le crochet `onRoute` peuple `app.registreAcces`, et **trois tests énumèrent le registre, pas ce à quoi on a pensé** — le méta-test anonyme (`l2-crochets:665`), la confrontation de la matrice `/v1/users` au registre (`l2-users:543`), le balayage sentinelle (`aide/sentinelle-financiere.ts`). Une route déclarée **sans politique empêche l'API de démarrer** (`socle.test.ts`), donc l'omission n'est pas improbable, elle est impossible. Preuve concrète que le registre voit ce qu'une matrice écrite à la main manque : **`HEAD /v1/users`, fabriquée par Fastify, n'est écrite dans aucun fichier** et est pourtant testée (401/403/200).                                                                                                                                                       | **Le méta-test anonyme ne balaie que `GET` et `HEAD`.** `POST`/`PATCH`/`DELETE` ne sont pas balayés anonymement par le registre : ils sont couverts par la matrice `/v1/users`, confrontée au registre **mais bornée à ce préfixe**. Il n'existe **aucun balayage 4 rôles × tout le registre × toutes les méthodes**. |
| 2   | **Un consultant ne voit pas les missions d'autrui**  | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **NON PROUVABLE AUJOURD'HUI, ET PAS PAR NÉGLIGENCE.** Voir §4.1.                                                                                                                                                                                                                                             |
| 3   | **Un token consultant ne lit JAMAIS une donnée financière** | **Le critère le mieux prouvé du lot, par quatre ceintures.** La 4ᵉ est le balayage sentinelle : on sème un cadrage aux montants **improbables**, on appelle **toutes les routes du registre** avec les jetons `consultant`/`analyste`/`lecteur`/anonyme (**l'admin est délibérément exclu** : il a le droit, 03 §34.1), et on exige qu'aucun corps ne contienne une sentinelle. Le test n'assère pas seulement l'absence de fuite, il **assère sa propre validité** : `parametresNonCartographies` vide (le balayage n'a pas tapé dans le vide), la route financière du produit **présente dans le registre** (le balayage a un objet), et `couverture.exerces > 0` (**au moins un appel a rendu 2xx et un corps a été lu** — sans quoi il serait « vert par vacuité », mot du test). Un second test **injecte** une fausse fuite au format français pour prouver que le détecteur est **sensible**. Deux tests `@critique`, donc non skippables. | Le balayage prouve l'étanchéité **sur les routes qui existent** — il y en a **une** portant `financier: true` dans le produit. Sa force est d'être **automatiquement étendue** à toute route future ; ce n'est pas la même chose qu'une force déjà exercée sur beaucoup de routes.                       |
| 4   | **Push sur la session d'autrui → `forbidden`**       | —                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | **NON PROUVABLE AUJOURD'HUI.** Voir §4.2.                                                                                                                                                                                                                                                                    |
| 5   | **Reset refusé si outbox non vide signalé**          | Implémenté et testé. La lecture est **volontairement conservatrice** : « outbox non vide » = dernier `sync_log.outbox_remaining > 0` **OU aucune sync connue de l'appareil** — parce que _la sync du portable ne dit RIEN de ce qui dort dans l'outbox de la tablette_. Le filtre `isNotNull(outbox_remaining)` écarte les lignes qui ne portent pas la donnée, au lieu de les compter pour zéro.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                | Le signal repose sur `sync_log`, que **seul L6 alimentera réellement**. Aujourd'hui la table est écrite par les tests, pas par un terrain. **Le garde est juste, sa source ne l'est pas encore.**                                                                                                            |

---

## 4. LE POINT DUR — deux membres du critère P-B n'ont pas d'objet, et il faut le dire avant de signer

Le critère spécial de P-B (09 §62) a **trois** membres. Le premier — l'accès financier croisé — est
prouvé (§3, critère 3). Les deux autres ne le sont pas, **et la cause est structurelle**.

### 4.1 « Un consultant ne voit pas les missions d'autrui »

La politique de route `type: 'mission'` **existe** et fonctionne. Mais elle **ne fait pas ce que le
critère demande**, et son propre code le dit sans ambiguïté (`politique.ts`, en-tête) :

> _« La politique de route dit QUI ENTRE, pas CE QUE LE SQL RAMÈNE. L'isolation "un consultant ne
> voit pas les missions d'autrui" est portée par le DÉPÔT (jointure obligatoire sur `mission_users`),
> pas par ce crochet. Deux garde-fous, deux natures ; confondre les deux, c'est croire qu'une porte
> fermée trie le courrier. »_

Et le test du banc l'écrit à son tour, en refusant le piège : un consultant appelant
`/essai/missions/<uuid quelconque>` reçoit **200**, avec ce commentaire — _« Ces politiques
garantissent l'identité, PAS la propriété de la ligne. Un test qui attendrait 403 ici confondrait les
deux garde-fous. »_

**Mesuré : `mission_users` n'apparaît dans AUCUN code de production.** Sept occurrences dans le
dépôt, **toutes en commentaire**. Il n'existe ni route missions, ni dépôt missions — c'est L3.

> **Conclusion, et elle est désagréable : le critère 2 ne peut pas être coché, et il ne peut pas non
> plus être refusé. Il n'a pas d'objet.** Le cocher serait exactement le défaut que ce dépôt poursuit
> depuis trois jours — **une mesure vraie sur ce qu'elle observe, qui répond à une autre question que
> celle posée**. La porte cocherait « le consultant ne voit pas les missions d'autrui » sur la foi
> d'un garde qui, de son propre aveu écrit, ne regarde pas les missions.

### 4.2 « Push sur la session d'un autre auditeur → `forbidden` »

Même forme, même cause. `type: 'proprietaire_session'` **vérifie l'identité, pas la propriété** ; la
propriété de session du 05 §9.9 vit dans le **service de sync**, qui est **L6**. La table
`sync_sessions` **n'existe pas** dans les migrations (`sync_log` existe, c'est autre chose), et il n'y
a **aucune route de push**. Rien à traverser, donc rien à refuser.

### 4.3 Une conséquence à ne pas laisser passer : le balayage sentinelle est **pré-désarmé** pour L3

Le balayage cartographie trois paramètres : `id` → un cadrage **réellement semé** ; `missionId` et
`sessionId` → **deux UUID littéraux semés nulle part**. C'est légitime aujourd'hui, parce que ces deux
noms ne servent qu'aux bancs d'essai, qui ne lisent aucune ligne.

**Mais l'en-tête du module promet exactement le contraire pour demain** — _« la route
`/v1/missions/:id/interview-plan` ajoutée demain oblige son auteur à semer une mission réelle, sans
quoi le balayage rougit »_. Or le jour où L3 ajoute une route de produit portant `:missionId`, le
paramètre sera **déjà cartographié** vers une ligne inexistante : `parametresNonCartographies` restera
vide, la route rendra 404, elle sera comptée « non exercée », et `couverture.exerces > 0` restera
satisfait **par la seule route de cadrage**. Le balayage serait vert sur une route qu'il n'a jamais
traversée — **le défaut que son propre en-tête dit refuser de reproduire**.

**Ce n'est pas un défaut d'aujourd'hui, c'est une amorce datée.** Elle se ferme en retirant
`missionId` et `sessionId` de la cartographie le jour où une route de produit les porte, pour que le
garde exige une ligne réelle. **À porter au brief de L3**, pas à corriger ici : aujourd'hui le
retirer rendrait le balayage rouge sur les bancs, ce qui serait un garde qui crie à tort.

### 4.4 Ce que je propose, sans le décider

**La décision appartient à Williams**, parce qu'elle porte sur le **périmètre d'une porte**, pas sur
du code. Trois options, et je donne mon avis :

1. **Signer P-B sur ce qui a un objet, et DATER les deux membres restants** — le critère 2 rejoué à
   la livraison de L3 ; le critère 4 à P-D (L6). Les deux entrent dans le fichier de porte concerné
   avec **le présent §4 en pièce**, pour que personne ne les redécouvre.
   → **C'est ce que je recommande.** Rien n'est coché à faux, rien n'est oublié, et la séquence du
   pack est respectée.
2. **Refuser P-B jusqu'à L3 et L6.** Cohérent, mais contredit le pack lui-même, qui place P-B à
   « Fin L2 » : la porte attendrait deux lots qu'elle est censée précéder.
3. **Amender le critère du 09 §62.** Possible, mais c'est un amendement de spec, et il ne se décide
   pas dans un fichier de porte.

---

## 5. Ce que le lot a livré EN PLUS, et qui n'est dans aucun critère

Le gardien A02 doit lire dans les **deux sens** (09 §3-6). Sens code → exigences, trois éléments :

- **`lireUtilisateur` (`users/depot.ts:170`) est DU CODE MORT** — zéro appelant dans tout le dépôt.
  Écrit pour un `GET /v1/users/:id` **jamais câblé** : le CRUD a une liste et pas de lecture unitaire.
  **C'est du code orphelin au sens du §6 : soit la route manquante est câblée, soit la fonction est
  supprimée.** Relevé par l'agent croisé, pas par l'auteur.
- **`PATCH /v1/users/:id` pose `usageProfile: 'expert'` sur un compte NON habilité**, alors que
  03 §19.1 décrit le mode expert comme celui d'un auditeur habilité. **Épinglé sans être tranché**,
  ce qui est le bon geste : c'est une question de spec, elle va en `DECISIONS.md` avant la porte.
- **Cinq `throw incoherenceInterne()` inatteignables dans `users/service.ts`** (L211, L262, L317,
  L363, L456). Ils se déclenchent quand une écriture du dépôt rend `null` ; or la frontière du service
  est déjà scellée en amont par `avant === null → NOT_FOUND`. **La moitié « dépôt » est prouvée, la
  moitié « service » reste scellée — et c'est celle qui peut régresser.** Question de conception, pas
  trou de test.

---

## 6. Rectifications que je dois à ce dossier — deux messages de commit de `main` sont faux

Un message de commit sur `main` ne se réécrit pas. **La trace juste est donc ici, et nulle part
ailleurs.**

- **`63fcc26` dit « ses 39 tests » ; le contenu intégré en porte 41.** Chiffre périmé de quelques
  heures au moment où j'ai rédigé le message. **Le compte exact est 41.**
- **`daa1c86` a perdu tous ses identifiants techniques.** J'ai rédigé le corps du message dans une
  chaîne shell entre guillemets **doubles** en y laissant des accents graves : le shell les a
  interprétés comme des substitutions de commande et a **effacé** `@testing-library/react`,
  `afterEach(cleanup)`, `globals: true`, `vitest.setup.interface.ts`, `.test.tsx` et
  `@vitejs/plugin-react`. Le message reste lisible, mais **amputé de ce qu'il nomme**.
  _Cause : j'avais utilisé un heredoc pour `63fcc26` — qui a fonctionné — et je ne l'ai pas refait
  pour celui-ci._ Le contenu intégral et exact de ce qui a été perdu vit dans l'en-tête de
  `vitest.setup.interface.ts`, qui est le vrai document de ce défaut.

---

## 7. Un constat mesuré, qui vaut mieux ici qu'ailleurs — la règle de croisement §5.6

Sur les six trouvailles majeures des dernières 24 heures, **cinq viennent de quelqu'un d'autre que
l'auteur du code**, et **aucune d'une relecture** :

| Trouvaille                                                                          | Trouvée par                                 |
| ----------------------------------------------------------------------------------- | ------------------------------------------- |
| Le garde de l'invariant 7 exemptait la porte d'écriture                              | l'agent qui écrivait **ses** tests          |
| La pagination keyset bouclait indéfiniment (microsecondes perdues par `toISOString`) | l'agent qui **implémentait** T3, en exécutant |
| Les ancres Excel (CRLF) refusées par un contrôle **bloquant**                        | l'agent qui testait **L4**                  |
| Le `DEFAULT 'answer'` qui recréait le défaut qu'il fermait                            | un **garde** posé par un tiers              |
| La correspondance NAF structurellement impossible                                    | l'agent qui **implémentait** L3a            |
| Le 26ᵉ fichier de test sans nettoyage DOM (25 sur 26 l'appelaient)                   | l'agent qui testait **les composants**      |

**Toutes par exécution, par un garde, ou par un regard qui n'avait pas écrit la chose.** C'est la
justification la plus solide que ce dépôt ait produite pour la règle §5.6 — et elle est **mesurée, pas
argumentée**. Elle dit aussi ce qui manquera le jour où les sessions de recroisement se ferment.

---

## 8. Question ouverte portée à cette porte — la séquence L3 vs P-B

`lot/l3a-companies` existe et est verte **alors que le fichier 09 place `| P-B | Fin L2 |` entre L2 et
L3**. Constat soulevé par un pair, tracé dans `DECISIONS.md` le 2026-08-31. **A01 a gelé L3a** (aucun
commit, aucune fusion L3 avant signature) ; **A01 ne s'est PAS accordé le droit de décider que la
séquence peut glisser.** Cette décision est posée ici, telle quelle, à Williams.

---

## 9. Verdict

| Signataire        | Rôle                                                        | Verdict                                                       | Date       |
| ----------------- | ----------------------------------------------------------- | ------------------------------------------------------------- | ---------- |
| **A01**           | montage du dossier, inventaire des preuves                  | **NON SIGNATAIRE — auteur d'une partie du code évalué**       | 2026-08-31 |
| **A02** (gardien) | contrôle d'acceptation + matrice E1-E47 dans les **deux** sens | ⬜ **À RENDRE**                                                | —          |
| **Williams**      | la porte, et l'arbitrage du §4.4 et du §8                   | ⬜ **À RENDRE**                                                | —          |

---

## 10. CONTRÔLE D'ACCEPTATION DU GARDIEN A02 — 2026-08-31, REJEU DE LA PORTE

> **Ce que cette section est.** Le contrôle d'acceptation de l'étape 6 (09 §3), rendu par le gardien
> A02. **Je n'ai repris aucune case des §1 à §9** : le dossier qui précède est monté par A01, qui est
> l'auteur d'une partie du code qu'il évalue, et il le dit lui-même en tête. Ce que j'ai fait est
> l'inverse d'une relecture : j'ai **rejoué les mesures** et je publie **les miennes**, avec la
> commande ou le numéro de run qui les produit.
>
> **En cas de divergence entre une case des §1-§9 et le présent §10, c'est le §10 qui porte le
> verdict du gardien** — le pack ne confie cet état à personne d'autre (09 §1). **Rien n'est effacé
> au-dessus** : ce dépôt ne réécrit pas ce qui a été écrit, il date ce qui change.
>
> **Ce que cette section n'est pas** : la porte. La porte est à Williams (09 §1, `CLAUDE.md` §10).

### 10.0 — Conditions de la mesure, à lire AVANT les chiffres

| Élément | Constat |
| --- | --- |
| **Commit contrôlé** | **`main` = `62193b8`**, confirmé sur `origin` par `git ls-remote --heads origin` → `62193b810eab1c8988f47d7884bbde892de297a3 refs/heads/main`. C'est le commit que la porte juge. |
| **L'arbre de travail n'est PAS `main`** | Le répertoire de travail est tenu par **une autre session**, sur `lot/l2j-arbitrages-porte-b` (`ba9f258`), avec des modifications **non commitées** dans `apps/api/tests/l2-users.integration.test.ts`, `DECISIONS.md` et `docs/ORGANISATION_AGENTS.md`. **Je n'y ai rien touché** et je n'ai **pas** lancé la suite d'intégration (collision de conteneurs). Mes mesures statiques sont donc prises par `git show main:<fichier>`, **jamais sur l'arbre**, et je le dis parce qu'une mesure a le droit d'être bonne, pas celui d'être crue sans son contexte. |
| **Mon propre travail** | Worktree isolé `C:/Users/Will/Documents/_axporteb`, branche `porte/b-controle-a02` créée depuis `main`. **Aucun code, aucun test modifié.** Ni poussé, ni fusionné. |
| **Preuve d'exécution** | **Run CI `33344097876`, sur `62193b8`** — relu job par job par moi (`gh run view --job … --log`). **Ce n'est PAS le run cité au §2 du dossier.** |

#### 10.0 bis — PREMIÈRE DIVERGENCE, ET ELLE EST DE FORME : le §2 prouve un autre commit

Le §2 du dossier atteste le run **`33339483715` sur `63fcc26`**. `63fcc26` est à **cinq commits**
derrière le `main` que la porte juge, et **l'un de ces cinq porte du code** : `daa1c86` — le design
system, **24 composants**, `composants.css` (1 069 l.), 26 fichiers de test, et l'entrée du projet
vitest `interface`. Les quatre autres (`3601dfa`, `e846442`, `9a3998a`, `62193b8`) sont de la
documentation et de l'état.

**Le dossier prouve donc l'exécution d'un arbre qui n'est pas celui qu'il présente à la porte.** Le
verdict d'exécution ne change pas — j'ai relu le run du bon commit et il est **vert sur ses 20 jobs**
— mais la règle de ce dépôt vaut ici comme ailleurs : _une mesure vraie sur ce qu'elle observe, qui
répond à une autre question que celle posée._ **La preuve du §2 est remplacée par la mienne.**

Run **`33344097876`**, `62193b8`, 20 jobs `success`, dont :
`0 · jonction` · `invariants` · `aucun test désactivé ni orphelin` · `1 · lint` · `2 · typecheck` ·
`2bis · build` · `3 · unit` · `4 · integration` · `5 · e2e` · `6 · schema-diff` ·
`couverture ≥ 90 %` · `7 · constructibilité des 4 images` (×5) ·
`8 · deploy-staging / Coolify → déploiement vérifié` · `ZAP baseline (staging)`.
→ https://github.com/will383842/Axion-Audit/actions/runs/33344097876

**Ce que je confirme du §2, en revanche, et par ma propre commande** : il n'existe **aucune**
désactivation de test dans le dépôt.
`git grep -nE "\.skip\(|skipIf|runIf|\.todo\(|it\.only|describe\.only" main -- apps packages e2e scripts`
rend **9 lignes**, toutes dans les commentaires et le motif de `scripts/check-no-skipped-tests.mjs`,
dans un commentaire de `check-test-projects.mjs`, et dans les fixtures d'injection de
`garde-fous-invariants.test.ts`. **Aucune dans un corps de test.** Le job `anti-skip` :
« **60 fichier(s) de test analysé(s), aucun test désactivé** », liste d'exceptions vide.

---

### 10.1 — LES CINQ CRITÈRES DU FICHIER 07 POUR L2, RECOCHÉS PAR MOI

> Recopiés à l'identique de `docs/07_PLAN_TESTS_RISQUES.md`, ligne **L2** : « Tests rôle × route
> exhaustifs ; un consultant ne voit pas les missions d'autrui ; un token consultant ne lit JAMAIS
> une donnée financière ; push sur la session d'autrui → `forbidden` ; reset refusé si outbox non
> vide signalé ».

| # | Critère | Verdict A02 | Ce que J'AI mesuré — et ce que ma mesure n'établit PAS |
| --- | --- | --- | --- |
| 1 | **Tests rôle × route exhaustifs** | 🟡 **COCHÉ SOUS RÉSERVE — R-B1** | **Le mécanisme est le bon, et je le confirme par lecture du code, pas du commentaire.** Le registre `app.registreAcces` est peuplé par `onRoute` ; `apps/api/src/auth/socle.test.ts` prouve qu'une route sans politique **empêche le démarrage** ; `apps/api/tests/l2-users.integration.test.ts:543` confronte la matrice au registre — **`ACTES` compte 7 entrées** (`git show main:… \| grep -c "    nom: '"` → **7**) **+ `HEAD /v1/users` nommée explicitement = 8 routes enregistrées**, ce qui est bien le chiffre du §1. **MAIS la limite qu'A01 énonce, je l'ai vérifiée à la ligne et elle est exacte** : `l2-crochets.integration.test.ts:672` porte `if (methode !== 'GET' && methode !== 'HEAD') continue;`. **J'AJOUTE UNE PRÉCISION QU'IL NE DONNE PAS, ET ELLE EST À SON AVANTAGE** : le balayage sentinelle, lui, appelle **toutes les méthodes** du registre avec **quatre porteurs** (consultant, analyste, lecteur, anonyme) — il TRAVERSE donc la matrice complète ; ce qu'il n'ASSÈRE pas, c'est le refus (il n'assère que l'absence de sentinelle). Le balayage « 4 rôles × tout le registre × toutes les méthodes » **est donc à trois lignes d'exister**, ce qui rend la réserve peu coûteuse. **Ce que ma mesure n'établit pas** : que les 8 routes soient les bonnes ; le pack n'en nomme qu'une (`GET /v1/users`), les sept autres vivent d'arbitrages `DECISIONS.md`. |
| 2 | **Un consultant ne voit pas les missions d'autrui** | ⬛ **SANS OBJET — DATÉ À LA LIVRAISON DE L3** | **Vérifié moi-même. Je confirme la conclusion d'A01 et je RÉCUSE la mesure qui la porte — voir §10.4.** Aucun dépôt missions, aucune route missions : `git grep -n "proprietaire_session\|'mission'" main -- apps/api/src` rend **11 lignes**, dont **aucune n'est une route de produit** — deux déclarations de type dans `politique.ts`, deux `case` du crochet, deux `exigerParametre`, une constante d'énumération du schéma, et **quatre bancs d'essai dans `socle.test.ts`**. La politique dit qui entre ; le dépôt dirait ce qui sort ; le dépôt n'existe pas. Périmètre arbitré par Williams le 2026-08-31 (107ᵉ entrée `DECISIONS.md`, option 1 du §4.4). |
| 3 | **Un token consultant ne lit JAMAIS une donnée financière** | ✅ **COCHÉ — et c'est le critère le mieux tenu du projet** | **Quatre mesures, toutes de moi.** ① `.github/coverage-critical-paths.json` soumet **DEUX** globs au seuil de 90 % (`apps/api/src/domaines/scoping/**` **et** `apps/api/src/routes/scoping.ts`) ; le job `couverture` du run `33344097876` imprime « **OK — Etancheite financiere · lines 100.00 % · statements 100.00 % · functions 100.00 % · branches 100.00 %** » **et** « **OK — Route financiere `GET /v1/scoping/:id/financials` · 100,00 % sur les quatre métriques** ». ② Le balayage sentinelle (`apps/api/tests/aide/sentinelle-financiere.ts`, relu intégralement) **assère sa propre validité** avant de conclure : `parametresNonCartographies` vide, route financière **présente au registre**, `couverture.exerces > 0`. ③ La contre-épreuve de sensibilité existe, format français compris. ④ La ceinture 3 se prouve **par retrait des œillères** — `balayerSources([])` doit dénoncer le dépôt financier lui-même, faute de quoi son silence ne vaut rien. Deux tests `@critique`, donc non skippables, et l'anti-skip est vert sur 60 fichiers. **Ce que ma mesure n'établit pas** : le balayage prouve l'étanchéité **sur une seule route de produit** portant `financier: true`. Sa force est d'être automatiquement étendue ; ce n'est pas une force déjà exercée. |
| 4 | **Push sur la session d'autrui → `forbidden`** | ⬛ **SANS OBJET — DATÉ À LA PORTE P-D** | **Vérifié par trois commandes, et le §4.2 est EXACT sur les trois.** `git grep -n "CREATE TABLE sync" main -- apps/api/drizzle` → **une seule ligne : `0007_transverse.sql:45 CREATE TABLE sync_log`**. `sync_sessions` n'apparaît **nulle part** dans le code — 2 occurrences dans tout le dépôt, `DECISIONS.md:4917` et le §4.2 ci-dessus. `git grep -niE "'/v1/sync\|/sync/push\|processed_ops" main -- apps/api/src/routes apps/api/src/domaines` → **aucun résultat**. Rien à traverser, donc rien à refuser. |
| 5 | **Reset refusé si outbox non vide signalé** | 🟡 **COCHÉ SOUS RÉSERVE — R-B2** | **Implémenté et éprouvé, je l'ai lu.** `apps/api/src/domaines/users/depot.ts:475` filtre `isNotNull(syncLog.outboxRemaining)` — les lignes qui ne portent pas la donnée sont **écartées**, jamais comptées pour zéro ; `service.ts:516` : `etats.length === 0 \|\| etats.some(e => (e.outboxRemaining ?? 0) > 0)` — **« aucune sync connue » vaut refus**, ce qui est la lecture conservatrice. Le test `@critique` existe et ferme le vrai piège : `l2-users.integration.test.ts:1157` — « **refuse quand un SECOND appareil a un outbox non vide, même si le plus récent est à jour** ». **Ce que ma mesure n'établit pas, et c'est la réserve** : `sync_log` n'est alimentée aujourd'hui **que par les tests**. Le garde est juste ; **sa source n'existera qu'à L6**. Le critère est tenu **au niveau du serveur**, pas au niveau du système. |

**Bilan des critères : 1 coché sans réserve · 2 cochés sous réserve · 2 sans objet, datés par
l'arbitrage de Williams · 0 non satisfait.**

---

### 10.2 — TRAÇABILITÉ E1-E47 DANS LES DEUX SENS

#### 10.2.1 — L'INSTRUMENT LUI-MÊME EST PÉRIMÉ, ET C'EST LA PREMIÈRE CHOSE À DIRE

`docs/TRACABILITE_E1-E47.md` est le fichier que le gardien est censé utiliser. Son §A.quinquies et
son §B.11 déclarent leur périmètre : « **les 161 fichiers suivis par git au commit `ce5b912`** » et
« `1c56759..HEAD`, 61 commits ». Ils ont été écrits à `46f547b`. **Trois commits de `main` sont
postérieurs, dont DEUX portent du code** — et ce sont les deux plus gros du lot :

| Commit | Ce qu'il livre | Présent dans §B.11 ? |
| --- | --- | --- |
| `63fcc26` | **T3 — le CRUD users** : `domaines/users/{depot,service,mots-de-passe}.ts`, `routes/users.ts`, `packages/shared/src/users.ts`, 41 tests d'intégration | **NON** |
| `daa1c86` | **Le design system** : 24 composants, `composants.css` (1 069 l.), `superposition.ts`, `utilitaires.ts`, 26 fichiers de test, projet vitest `interface` | **NON** |

**Conséquence mesurable, et elle est gênante.** Le §B.11.1 du fichier de traçabilité s'intitule
« **LES SIX ROUTES** (contrôle 11 §8.6) » et conclut « **Six routes livrées, six rattachées, zéro
orpheline** ». `main` en enregistre **quatorze** (les 6 + les 8 de `/v1/users`). Et le §A.quinquies
écrit noir sur blanc, sous la table qui « fait foi » : « **Ce que la ligne L2 du fichier 07 exigeait
et qui n'est PAS livré : le CRUD users (tâche T3)… La porte P-B ne peut pas cocher ce critère.** »
**C'est faux depuis `63fcc26`.**

**Je ne fais pas grief à A01 de cet écart** — il est le produit mécanique d'un document daté. **Je
constate que le sens 2 n'a jamais été passé sur les deux plus gros apports que cette porte juge**, et
que le §5 du dossier le présente pourtant comme fait (« Sens code → exigences, trois éléments »).
**Je le passe donc ici.**

#### 10.2.2 — SENS 1 : EXIGENCES → CODE (rien d'oublié ?)

| Exigence de la ligne L2 (07 §12) | Code qui la porte, nommé par moi | Verdict |
| --- | --- | --- |
| Auth JWT login / refresh rotatif / détection de réutilisation / logout | `apps/api/src/domaines/auth/{routes,service,depot,mots-de-passe,jetons-rafraichissement}.ts` · `apps/api/src/auth/{jetons,identite,contexte}.ts` | ✅ livré · couverture **98,22 %** lignes |
| RBAC middleware (rôles) | `apps/api/src/auth/{politique,depot}.ts` — crochet ③ et registre `onRoute` | ✅ livré · couverture **97,29 %** lignes |
| **Propriété de session §9.9** | `apps/api/src/auth/politique.ts:91` — le TYPE existe, **aucune route ne l'emploie** | ⬛ **vocabulaire seul** — le comportement est L6 (§10.4) |
| Étanchéité financière (`scoping_financials`, admin only) | `apps/api/src/domaines/scoping/financiers.depot.ts` · `apps/api/src/routes/scoping.ts` | ✅ livré · **100 %** sur les 4 métriques |
| CRUD users + garde-fou reset §9.7 | `apps/api/src/domaines/users/**` · `apps/api/src/routes/users.ts` · `packages/shared/src/users.ts` | ✅ livré · couverture **97,36 %** lignes |
| **Règle d'affectation §34.4** | `habiliterUtilisateur` POSE `habilitated_at` ; **le REFUS d'affectation `mission_users` si NULL n'est écrit nulle part** — il n'a pas d'objet (§10.4) | ⬛ **la lecture est livrée, le refus est L3** |
| `activity_log` | `apps/api/src/domaines/journal/{depot,service}.ts` · `packages/shared/src/journal.ts` | ✅ livré · couverture **93,33 %** lignes · le garde `check:porte-journal` est **désormais câblé en CI** (`ci.yml:505`) : la réserve §I.2 du fichier de traçabilité est **LEVÉE, vérifiée par moi** |

**Aucune exigence dont L2 avait la charge n'est oubliée.** Deux ne sont pas tenues et ne peuvent pas
l'être ici (propriété de session, refus d'affectation) : elles sortent avec la même date que les
critères 2 et 4.

#### 10.2.3 — SENS 2 : CODE → EXIGENCES (le contrôle anti-orphelin), passé sur `46f547b..62193b8`

`git diff --name-status 46f547b main` : **78 fichiers**, dont **69 de code ou de test**. Rattachement
établi par moi en confrontant l'en-tête `// Traçabilité :` à `docs/TRACABILITE_E1-E47.md` — **et les
en-têtes ont été RELUS, pas pris pour argent comptant** :

| Groupe livré | Nb | Rattachement établi par le gardien |
| --- | ---: | --- |
| `apps/api/src/domaines/users/{depot,service,mots-de-passe}.ts` | 3 | **E45** (pilotage humain : habilitation) · **E33** (sécurité : mot de passe engendré, Argon2id) · **E42** (journal) — cohérent |
| `apps/api/src/routes/users.ts` — **7 routes + `HEAD` engendrée par Fastify** | 1 | **E45, E43.** Le pack (05 §22) n'écrit que « CRUD /v1/users » ; **les 7 verbes sont documentés au titre du 11 §8.6** dans l'en-tête du fichier ET dans `apps/api/README.md` (relu : les 7 y figurent, une ligne chacune). **Conforme** |
| `packages/shared/src/users.ts`, extensions de `auth.ts` / `errors.ts` | 3 | **E43** — conventions d'API, `ERROR_CODES` |
| `packages/ui/src/composants/*.tsx` — **24 composants** | 24 | **E27** (design moderne, charte, WCAG AA), **E44** (UX/UI 2026-2027), **E13** (écran 3 zones) pour six d'entre eux. **Vérifié fichier par fichier** : les 24 portent une ligne de traçabilité, et `pnpm check:tracabilite` confirme en CI « **365 citations, 242 fichiers, aucune incohérence** ». Périmètre autorisé par 09 §6 (« en parallèle A21 pose `packages/ui` + `/design` ») |
| `packages/ui/src/{composants.css, tokens.css}`, `superposition.ts`, `utilitaires.ts` | 4 | **E27 / E44** — miroir des tokens, gardé par `tokens.test.ts` (109 cas), qui échoue si les fichiers divergent |
| `vitest.setup.interface.ts` + projet vitest `interface` | 1 | **E36** (exécutable par lots) — les trois paquets de test React sont arbitrés par Williams le 2026-08-31 |
| 26 fichiers `*.test.tsx` + `l2-users.integration.test.ts` | 27 | Outillage de test des groupes ci-dessus |

**Résultat du sens 2 : deux orphelins au sens du `CLAUDE.md` §4 étape 6 — un connu, UN QUE JE
TROUVE.**

##### ORPHELIN 1 — `lireUtilisateur` : IL EST TOUJOURS LÀ AU COMMIT QUE LA PORTE JUGE

`git grep -n "lireUtilisateur" main -- apps packages` : **`apps/api/src/domaines/users/depot.ts:170`
est la SEULE occurrence de ce symbole exact** dans tout `main`. Les quinze autres lignes portent
`lireUtilisateurAuthentifie` ou `lireUtilisateurPourEcriture`, qui sont d'autres fonctions, toutes
deux appelées. **Zéro appelant. C'est un orphelin, et il l'est au commit `62193b8`.**

**Je sais qu'un correctif existe. Je refuse de le compter, et voici pourquoi — c'est mesuré, pas
procédurier :**

1. `git ls-remote --heads origin` rend **8 références** : `main` et sept branches `lot/…`.
   **`lot/l2j-arbitrages-porte-b` n'en fait PAS partie.** `CLAUDE.md` §8 est explicite :
   _« la durabilité vit sur `origin` : **un commit non poussé n'existe pas** »._
2. `gh run list --branch lot/l2j-arbitrages-porte-b` rend **zéro run**. Le correctif n'a **jamais été
   exécuté par la CI**.
3. `ba9f258` ajoute `GET /v1/users/:id` **sans modifier** `apps/api/tests/l2-users.integration.test.ts`
   (`git diff --name-status main ba9f258` → 4 fichiers, dont aucun test). Or le méta-test de la
   matrice compare le registre à `ACTES`, qui compte **7 entrées** : **avec une neuvième route
   enregistrée et `ACTES` inchangé, ce test ne peut que rougir.** L'arbre de travail montre d'ailleurs
   ce fichier **en cours d'édition** au moment où j'écris.

**Verdict : l'orphelin est constaté à la porte. Le correctif est en cours ; il n'est ni poussé, ni
exécuté, ni complet. → RÉSERVE R-B3.**

##### ORPHELIN 2 — `reinitialiserCachePreparation` : CELUI QUE PERSONNE N'AVAIT VU

`apps/api/src/dependances.ts:349` :

```
/** Vide le cache — réservé aux tests, qui ne doivent pas hériter d'un verdict voisin. */
export function reinitialiserCachePreparation(): void {
```

`git grep -n "reinitialiserCachePreparation" -- . ':!apps/api/dist'` rend **UNE SEULE LIGNE : sa
propre déclaration.** Aucun appelant — **y compris parmi les tests**, c'est-à-dire parmi ceux à qui
son commentaire dit qu'elle est réservée.

**C'est le même défaut que `lireUtilisateur`, d'un cran au-dessus** : `lireUtilisateur` était du code
muet ; celui-ci est du code **qui affirme un consommateur inexistant**. Un lecteur qui relit
`dependances.ts` conclut que la sonde de préparation est isolée entre tests. Elle ne l'est pas. C'est,
mot pour mot, _un garde-fou qui annonce plus qu'il ne fait_ — la famille que ce dépôt traque. Il date
du lot L0-b (le fichier est inventorié au §9.4 de `PORTE_A`) : **il a déjà traversé une porte.**
→ **RÉSERVE R-B4.**

##### Pourquoi la machine ne les a vus ni l'un ni l'autre — et pourquoi ce n'est PAS un défaut de la machine

`check:graphe-modules` détecte le **module** que rien n'importe, pas le **symbole** que rien n'appelle.
Il le déclare lui-même, angle mort n° 6 :

> _« Granularité MODULE, pas SYMBOLE : un module importé pour une seule de ses dix fonctions est
> "atteint". Les neuf autres sont du code mort que ce contrôle ne voit pas. `noUnusedLocals` couvre le
> local, pas l'exporté. »_

**Le garde est honnête sur sa portée, et c'est précisément ce qui permet au gardien de savoir où
chercher à la main.** J'ai donc balayé les **224 symboles exportés** de `apps/*/src` et
`packages/*/src` et confronté chacun au reste du dépôt ; après retrait des faux positifs structurels
(réexports par baril de `packages/shared`, tables Drizzle du fichier 04 déclarées pour les lots à
venir, types consommés par inférence), **il reste exactement les deux ci-dessus.**

##### Ce que le sens 2 rend de POSITIF, et qu'il faut dire avant les réserves

- **La soupape s'est refermée toute seule, comme elle le promettait.**
  `scripts/modules-en-attente.md` portait `apps/api/src/http/pagination.ts` ; le module est désormais
  consommé par `GET /v1/users`, et la règle 2 (« une entrée dont le module est désormais atteint →
  REFUS ») **a réclamé le retrait de la ligne**. Le registre est vide, et c'est le garde qui l'a exigé,
  pas la mémoire de l'auteur. **C'est le premier garde-fou de ce dépôt qui se désarme correctement.**
- `check:porte-journal` est **câblé en CI** (`ci.yml:505`) : la réserve §I.2 du fichier de traçabilité
  est levée, vérifiée par moi, sortie « **activity_log : porte d'écriture unique (183 fichiers
  balayés)** ».
- `check:tracabilite` est vert sur **365 citations / 242 fichiers**, avec **4 lignes exemptées** sur un
  plafond de 12.

---

### 10.3 — DEFINITION OF DONE TRANSVERSE (`CLAUDE.md` §5), LIGNE PAR LIGNE, MESURÉE

| Ligne de la DoD | Verdict A02 | Mesure — la mienne |
| --- | --- | --- |
| lint + typecheck stricts = **0 erreur** | ✅ | Run `33344097876` : `1 · lint` **success**, `2 · typecheck` **success**, `2bis · build (sources)` **success**. Le job `lint` enchaîne `check:pack`, `check:decisions`, `check:executabilite`, `eslint --max-warnings=0` et `prettier --check`. ⚠️ **Non rejoué sur mon poste** : l'arbre de travail n'est pas `main` et appartient à une autre session. |
| tous les tests verts, **AUCUN test skippé** | ✅ **et je publie le décompte** | `3 · unit` : **14 fichiers, 386 tests** · `4 · integration` : **17 fichiers, 296 tests** · `5 · e2e` : **36** — **718 au total**, tous `passed`, RC=0. `anti-skip` : **60 fichiers analysés, aucun test désactivé**, exceptions vides. `check:test-projects` : **60 fichiers, tous captés (interface 26 · unit 14 · integration 17 · playwright 3), aucun orphelin**. ⚠️ **Voir R-B5** : le projet `interface` (26 fichiers) **n'a aucun job de CI dédié** et **n'est pas dans `pnpm verify`**. |
| **couverture ≥ 90 %** sur les modules critiques — **MESURÉE** | ✅ **MESURÉE : six modules, quatre métriques, 24 chiffres** | Job `couverture` du run `33344097876` ; le seuil est refusé s'il diffère de 90. **RBAC / socle** (`apps/api/src/auth/**`) : lignes **97,29** · stmts **97,29** · fonctions **100,00** · branches **92,86**. **Domaine auth** : **98,22 / 98,22 / 100,00 / 94,20**. **Étanchéité financière (dépôt)** : **100 / 100 / 100 / 100**. **Route financière** : **100 / 100 / 100 / 100**. **Journal** : **93,33 / 93,33 / 100,00 / 95,24**. **Domaine `users`** : **97,36 / 97,36 / 91,67 / 93,33**. Sortie du job : « **Tous les modules critiques atteignent 90 % — couverture MESURÉE.** » ⚠️ **Trou de mesure DÉCLARÉ, que je reprends à mon compte — R-B6** : `packages/shared/**` est rapporté à **0,00 %** (les tests exécutent le JS compilé) ; `journal.ts` et surtout **`redaction.ts`** — la politique de redaction pino, c'est-à-dire l'invariant « aucune donnée personnelle dans les logs » — **ne sont soumis à AUCUN seuil**. |
| migrations up/down **exécutées sur staging** | ⬜ **NON VÉRIFIÉ PAR LE GARDIEN** | **Je ne coche pas cette case, et je dis exactement pourquoi.** (a) Mon mandat est en lecture seule : je n'ai ni créé ni détruit de base sur `audit-staging`. (b) Le job `8 · deploy-staging` **ne joue aucune migration** : il vérifie l'empreinte du script distant, puis sonde `/api/v1/health/ready` → 200. (c) **Le job émet lui-même cet avertissement, que je cite** : « _Preuve de PRISE D'EFFET seulement — aucune étiquette OCI de révision n'existe sur ce conteneur, donc le COMMIT réellement cloné n'est pas vérifié_ ». Ce qui EST prouvé : `6 · schema-diff` applique les migrations sur un PostgreSQL neuf et rend **ZÉRO ÉCART**. **Ce n'est pas « sur staging ».** → **R-B7** |
| tout écran livré avec ses **4 états** (§33.2) | ⬛ **sans objet — exigible à L5** | Vérifié, non supposé : `apps/field/src` et `apps/hq/src` ne contiennent que `App.tsx` et `main.tsx` (rapport de couverture du run `33344097876`). **Aucun écran livré.** Les **primitives** des 4 états existent en revanche (`EtatVide`, `EtatErreur`, `EtatHorsLigne`, `Squelette`), ce qui est le bon ordre. ⚠️ La page `/design` que 09 §6 confie à A21 **n'existe pas** — constat, pas réserve. |
| **axe-core vert** | ⬜ **IMPOSSIBLE À COCHER — L'OUTIL N'EXISTE PAS DANS CE DÉPÔT** | **C'est la case que j'étais chargé d'éprouver, et le piège est réel.** Quatre mesures : (a) `axe` n'apparaît dans **aucun `package.json`** du dépôt ; (b) `git grep -c "axe-core" main -- pnpm-lock.yaml` → **aucune occurrence** ; (c) aucun paquet `axe*` dans `node_modules` ; (d) `grep -ri axe .github/` ne rend **qu'une case à cocher, `.github/pull_request_template.md:46`** — soit **une case de formulaire dont l'outil n'est pas installé, et qu'un rédacteur de PR peut cocher de bonne foi**. Les seules autres occurrences du dépôt sont **trois commentaires** de `packages/ui`. La ligne est « sans objet » à L2 (aucun écran) et c'est vrai — **mais rien, absolument rien, ne porte l'obligation d'installer l'outil avant L5** : ni fiche `AMELIORATIONS.md` (vérifié : **zéro** mention), ni entrée `DECISIONS.md`, ni mécanisme équivalent aux `cheminsAttendus` du seuil de couverture. **Une case cochable sans son outil est exactement le défaut que ce dépôt traque.** → **RÉSERVE R-B8** |
| `@filrouge` vert sur **FIL-TPE ET FIL-GC** | ✅ | `apps/api/tests/l1-filrouge.integration.test.ts` — **5 tests, verts**, relevés par moi dans le journal du job `4 · integration` du run `33344097876`. ⚠️ **Ce que ma mesure n'établit pas** : le scénario **n'a pas été allongé par L2**, alors que 09 §4bis veut qu'il grandisse à chaque lot. Il reste celui de L1. Constat, non réserve : L2 n'ajoute aucune étape de parcours terrain. |
| README de l'app à jour | 🟡 **COCHÉ SOUS RÉSERVE** | **Contrôlé sur ce qui est contrôlable** : `apps/api/README.md` documente les **7 routes `/v1/users`** une par une, les 3 routes d'auth, la route financière et les 2 sondes — confronté ligne à ligne au registre, il est à jour de `main`. `packages/ui/README.md` est mis à jour par `daa1c86`. **Je n'ai pas relu tous les README ligne à ligne** : ce n'est pas mécanisable, et je préfère le dire. ⚠️ Sur `ba9f258`, `GET /v1/users/:id` est ajoutée **sans** mise à jour du README — hors périmètre de cette porte, à porter à la suivante. |
| aucun TODO/FIXME sans entrée `DECISIONS`/`AMELIORATIONS` | 🟡 **COCHÉ SOUS RÉSERVE — R-B9** | **Recomptés par moi sur tout le code suivi hors `docs/` : trois, exactement comme à P-A.** `apps/api/scripts/db-generate.mjs:108` et `:112` sont des **marqueurs de gabarit** écrits dans une migration vide — conformes. `infra/scripts/smoke-test.sh:141` porte `TODO(L2)` et **son entrée `DECISIONS.md` existe** (2026-08-27) : la lettre de la DoD est donc tenue. **Mais l'entrée dit : « à activer au lot L2 (auth/RBAC), quand `/v1/auth/login` existera ».** `/v1/auth/login` existe depuis `acee96e`. **L2 se termine sans que l'étape ait été activée : l'entrée qui légitime ce TODO est arrivée à échéance, et la case reste verte.** Une case satisfaite par sa lettre et défaite par son objet. |
| **diff schéma-vs-04 = zéro écart** | ✅ | Job `6 · schema-diff` du run `33344097876` : « **✓ diff schéma-vs-04 : ZÉRO ÉCART.** », doublé de `check:schema-inventaire` (aucun objet hors des familles surveillées). **L2 n'ajoute aucune migration** — `git diff --name-only 46f547b main -- apps/api/drizzle` est **vide** : la case est tenue par non-régression. |

**Bilan DoD : 5 lignes cochées sans réserve · 3 cochées avec réserve · 1 sans objet · 2 NON COCHÉES**
(migrations sur staging, axe-core).

---

### 10.4 — VERDICT SUR LE §4 DU DOSSIER : LA CONCLUSION TIENT, UNE DES MESURES NON

**Sur le fond, A01 a raison, et je le dis avant la nuance.** Les critères 2 et 4 n'ont pas d'objet. Je
l'ai établi par mes propres commandes (§10.1, lignes 2 et 4) : aucun dépôt missions, aucune route
missions, aucune table `sync_sessions`, aucune route de push, et les deux types de politique
concernés ne sont exercés que par des bancs d'essai. **Cocher ces deux critères aurait été le défaut
que le dossier dit vouloir éviter ; refuser la porte pour eux aurait contredit le pack, qui place P-B
à « Fin L2 ».** L'option 1 du §4.4, arbitrée par Williams, est la bonne.

**Sur la mesure qui porte le §4.1 : elle est fausse, et je la corrige.** Le dossier écrit :

> _« **Mesuré : `mission_users` n'apparaît dans AUCUN code de production.** Sept occurrences dans le
> dépôt, toutes en commentaire. »_

Ma mesure — `git grep -o "mission_users\|missionUsers" main -- . ':!docs' ':!DECISIONS.md' | wc -l` :

| Emplacement | Occurrences | Nature |
| --- | ---: | --- |
| **Total hors `docs/` et `DECISIONS.md`** | **39** | — |
| `apps/api/drizzle/*.sql` | **12** | **DDL EXÉCUTABLE** — `CREATE TABLE mission_users`, trois contraintes, `DROP TABLE`, `CREATE INDEX`, `DROP INDEX` |
| `apps/api/schema-manifest.json` | **8** | manifeste de comparaison de schéma — **donnée, pas commentaire** |
| `apps/api/src/**` | **10** | **8 en commentaire** · **2 en CODE** : `db/schema.ts:416-417`, `export const missionUsers = pgTable('mission_users', …)` |
| `apps/api/scripts/seed.mjs` | **3** | **CODE** — liste de tables et `INSERT INTO mission_users` |
| `apps/api/tests/**` | **5** | fixtures et messages d'échec |

**« Sept, toutes en commentaire » ne décrit ni le dépôt, ni `apps/api/src`.** La table **existe** en
base, elle est **déclarée en Drizzle**, elle est **semée**. Ce qui n'existe pas — et c'est la seule
chose qui compte pour le critère — c'est **une requête de produit qui joigne dessus**. La formulation
juste est : _`missionUsers` est déclarée et jamais lue ; aucun dépôt ne la joint ; aucun module ne
l'importe (vérifié par balayage des 224 symboles exportés)._

**Pourquoi je m'arrête sur sept mots.** Parce qu'un dossier de porte est relu par quelqu'un qui n'a
pas la commande sous les yeux, et parce que ce dépôt a passé trois jours à établir qu'une mesure
approximative qui soutient une bonne conclusion **reste une mesure approximative**. La conclusion est
maintenue ; **sa preuve est remplacée par celle-ci.** → **R-B10 (rédactionnelle).**

---

### 10.5 — VERDICT SUR LE §4.3 : L'ANALYSE TIENT, ET ELLE EST TROP ÉTROITE

**Ce que j'ai vérifié.** `apps/api/tests/l2-crochets.integration.test.ts:1078-1082` :

```
valeursDeParametre: {
  id: cadrageAvecFinancier,
  missionId: '018f0000-0000-7000-8000-00000000aaaa',
  sessionId: '018f0000-0000-7000-8000-00000000bbbb',
},
```

`git grep -n "00000000aaaa\|00000000bbbb"` rend **exactement ces deux lignes** dans tout le dépôt : ces
UUID **ne sont semés nulle part**. Et la chaîne de conséquences qu'A01 décrit est exacte — je l'ai
suivie dans le moteur : `substituer()` ne remonte un manquant que si le nom **n'est pas dans la
table** ; une route rendant 404 est classée `non_exerce` ; **aucune anomalie n'est levée pour un
`non_exerce`** ; et `couverture.exerces > 0` reste satisfait par la seule route de cadrage. **Le
balayage serait vert sur une route qu'il n'a jamais traversée. A01 a raison.**

**Ce qu'il n'a pas vu, et qui rend son correctif insuffisant.** Sa proposition est de _retirer
`missionId` et `sessionId` de la cartographie le jour où une route de produit les porte_. **Cela ne
ferme que deux tiers du trou.** Le troisième paramètre cartographié est **`id`**, et il pointe vers un
**cadrage** (`cadrageAvecFinancier`). Or `:id` est le nom que **toute** route REST donne à sa clé
primaire. Le jour où L3 livre `GET /v1/missions/:id` ou `GET /v1/companies/:id` — et
`lot/l3a-companies` est déjà écrite —, `parametresNonCartographies` restera vide **parce que `id` est
cartographié**, la route rendra 404 sur un UUID de cadrage, elle sera comptée « non exercée », et le
balayage sera vert sans avoir rien traversé. **Le paramètre le plus dangereux est celui qui est déjà
« bien » cartographié, pas ceux qui sont mal cartographiés.**

**Verdict : l'analyse d'A01 n'exagère pas — elle sous-estime.** L'amorce n'est pas « `missionId` et
`sessionId` » : c'est **la cartographie par NOM de paramètre**, qui confond deux ressources dès que
deux routes appellent leur clé `id`. La fermeture correcte n'est pas de retirer deux lignes, c'est de
**cartographier par (gabarit de route, paramètre)**, ou d'exiger que toute route du registre soit
**exercée ou explicitement refusée**, jamais `non_exerce` en silence. **À porter au brief de L3, avec
cette précision.** → **R-B11 (datée, non bloquante aujourd'hui).**

---

### 10.6 — CE QUI TIENT, ET QU'IL FAUT LIRE AVANT LES RÉSERVES

Une revue qui ne trouve que des défauts n'est pas une revue. Six choses tiennent, et je les ai
mesurées moi-même :

1. **La couverture des modules critiques est MESURÉE, pas déclarée** — six modules, quatre métriques
   chacun, **24 chiffres tous ≥ 90 %**, et le seuil lui-même est verrouillé (la CI refuse une valeur
   différente de 90). C'est la première fois que cette ligne de DoD est autre chose qu'une intention.
2. **L'étanchéité financière est le critère le mieux prouvé du projet.** Quatre ceintures, deux
   `@critique`, **100 % sur les quatre métriques et sur les deux globs**, et un balayage qui **assère
   sa propre validité** avant d'assérer l'absence de fuite.
3. **La soupape du graphe des modules s'est refermée d'elle-même.** `pagination.ts` est sorti de
   `modules-en-attente.md` parce que le garde l'a exigé, pas parce que quelqu'un s'en est souvenu.
4. **Aucun test désactivé, nulle part** — vérifié par ma propre commande et pas seulement par le
   garde : les seules occurrences de `.skip` / `runIf` / `todo` vivent dans le garde anti-skip et ses
   fixtures.
5. **`check:porte-journal` est câblé** : l'invariant 7 a désormais un garant mécanique qui s'exécute
   réellement (183 fichiers balayés), et la réserve §I.2 du fichier de traçabilité tombe.
6. **La règle de croisement a produit, encore ici, ce que la relecture n'a pas produit.** Je n'en fais
   pas un argument : je constate que **les deux orphelins de ce lot ont été trouvés par balayage de
   symboles, jamais par lecture** — la mienne comprise.

---

### 10.7 — LES RÉSERVES, NUMÉROTÉES, ET CE QU'ELLES BLOQUENT EXACTEMENT

| # | Réserve | Bloque la porte ? | Ce qu'elle exige |
| --- | --- | --- | --- |
| **R-B1** | Aucun balayage **4 rôles × tout le registre × toutes les méthodes** n'assère le refus ; le méta-test anonyme s'arrête à `GET`/`HEAD` | **Non** | Le balayage sentinelle traverse déjà cette matrice ; il suffit d'y ajouter l'assertion d'autorisation. **Étage 1, avant P-C.** |
| **R-B2** | Le garde-fou §9.7 lit `sync_log`, alimentée **uniquement par les tests** jusqu'à L6 | **Non** | Rejeu du critère 5 **à la porte P-D**, sur une source réelle. À joindre au dossier P-D avec le §4. |
| **R-B3** | **`lireUtilisateur` est orphelin au commit jugé.** Le correctif n'est **ni sur `origin`, ni exécuté par la CI**, et il ajoute une route sans mettre à jour la matrice de test | **OUI, formellement** | La porte ne peut être signée que sur un commit de `main`. **Soit** le correctif est poussé, vert en CI et fusionné, **soit** `62193b8` est jugé avec son orphelin. **Seul point qui touche la lettre du `CLAUDE.md` §4 étape 6 : « le code orphelin est REFUSÉ ».** |
| **R-B4** | **`reinitialiserCachePreparation` (`dependances.ts:349`) est orphelin**, et son commentaire affirme un consommateur inexistant | **Non** | Le brancher dans les tests qui sondent la préparation, ou le retirer. **Une ligne d'`AMELIORATIONS.md` suffit à le tracer** ; l'ignorer, non. |
| **R-B5** | Le projet vitest `interface` (**26 fichiers**) n'a **aucun job de CI dédié** et n'est **pas dans `pnpm verify`** — il ne s'exécute que par ricochet du job `couverture` | **Non** | Ajouter `test:interface` à `pnpm verify`. Aujourd'hui, un `pnpm verify` **vert** ne dit rien de 26 fichiers de test. |
| **R-B6** | `packages/shared/**` est rapporté à **0,00 %** de couverture : **`redaction.ts`** — l'invariant « aucune donnée personnelle dans les logs » — **n'est sous AUCUN seuil** | **Non** | Défaut d'outillage **déjà déclaré** dans `coverage-critical-paths.json` — mais déclaré n'est pas tracé : **aucune fiche `AMELIORATIONS.md` ne le porte.** À ouvrir. |
| **R-B7** | **« Migrations up/down exécutées sur staging » n'est pas vérifiée** — le job de déploiement ne joue aucune migration et avertit lui-même que le commit en service n'est pas vérifié | **Non pour L2** (aucune migration livrée par L2) | Le dire à Williams **avant** la signature, pas après. Redevient bloquante au premier lot qui livre une migration. |
| **R-B8** | **`axe-core` n'est installé nulle part**, alors que la DoD l'exige et qu'une **case à cocher** l'annonce dans le gabarit de PR | **Non pour L2** (aucun écran) | **Poser dès maintenant l'obligation**, sur le modèle des `cheminsAttendus` du seuil de couverture : une entrée qui fait **rougir la CI** si un écran est livré sans l'outil. Sinon la case sera cochée à L5 comme elle peut l'être aujourd'hui : de bonne foi, et à vide. |
| **R-B9** | `TODO(L2)` de `infra/scripts/smoke-test.sh:141` : son entrée `DECISIONS.md` dit « **à activer au lot L2** », et **L2 se termine sans activation** | **Non** | Activer l'étape `login` du smoke test, ou re-dater l'entrée. Une entrée de registre échue rend la case de DoD verte **sans objet**. |
| **R-B10** | La mesure du §4.1 (« sept occurrences, toutes en commentaire ») **est inexacte** — 39 occurrences, dont au moins 17 en code exécutable | **Non** | Rédactionnelle. La conclusion du §4.1 est maintenue ; **le §10.4 remplace sa preuve.** |
| **R-B11** | Le désarmement du balayage sentinelle **est plus large que le §4.3 ne le dit** : `id` est cartographié vers un cadrage, donc **toute** future route `:id` est pré-désarmée | **Non** | Au brief de L3, **avec la précision du §10.5** : cartographier par (gabarit, paramètre), ou refuser le `non_exerce` silencieux. |
| **R-B12** | **`docs/TRACABILITE_E1-E47.md` est périmé de trois commits** : son §B.11 annonce « six routes, zéro orpheline » quand `main` en enregistre quatorze, et son §A.quinquies affirme que le CRUD users n'est pas livré | **Non** (le §10.2.3 y supplée) | Reporter le §10.2.3 dans le fichier de traçabilité. **Tant que ce n'est pas fait, l'instrument du sens 2 ment sur son propre périmètre.** |

---

### 10.8 — CE QUE JE N'AI PAS PU VÉRIFIER, ET POURQUOI

> **Trois contrôles de ce dépôt échouent délibérément quand ils n'ont rien pu vérifier. C'est le
> standard, et il vaut pour moi.**

1. **La suite d'intégration rejouée sur mon poste.** Interdite par mon mandat : une autre session
   travaille dans `apps/api/tests/` et nous nous disputerions les conteneurs. Je m'appuie donc sur le
   run CI **du bon commit**, relu job par job — ce qui est une preuve d'exécution, mais **pas la
   mienne**.
2. **`lint`, `typecheck` et `format:check` rejoués localement.** Même cause : l'arbre de travail n'est
   pas `main`. Les trois jobs sont verts sur `62193b8` ; je n'ai pas de mesure indépendante.
3. **Les migrations up/down sur le staging** — R-B7. Mandat en lecture seule, et le job de déploiement
   ne les joue pas.
4. **La règle de croisement 09 §5.6.** Comme aux deux passes précédentes : **tous les commits portent
   le même auteur git**. Les fichiers de test **déclarent** leur auteur croisé
   (`l2-users.integration.test.ts` : « écrit par le testeur d'intégration, qui n'a produit AUCUNE des
   lignes testées ») — **c'est une déclaration, pas une preuve**, et elle vaut mieux que le silence.
5. **Tous les README relus ligne à ligne.** J'ai confronté `apps/api/README.md` au registre des routes ;
   je n'ai pas fait de même pour les cinq autres. Ce n'est pas mécanisable, et je préfère le dire.
6. **Le contenu du scan ZAP baseline.** Le job est vert ; je n'ai pas lu son rapport.
7. **Le comportement réel du staging.** Je n'y ai pris aucun accès à cette passe.

---

### 10.9 — CE QUE LE DOSSIER D'A01 AFFIRME ET QUE LA MESURE NE SOUTIENT PAS

**Quatre points, par ordre décroissant de gravité. Aucun ne renverse le dossier ; tous en corrigent la
preuve.**

1. **§2 — la preuve d'exécution porte sur un autre commit.** Run `33339483715` sur `63fcc26`, à cinq
   commits de `main`, dont un porte 24 composants et 26 fichiers de test. **Remplacée par le run
   `33344097876` sur `62193b8`** (§10.0 bis). Verdict inchangé, preuve changée.
2. **§4.1 — « sept occurrences, toutes en commentaire » est faux** : 39 occurrences, dont au moins 17
   en code exécutable (DDL, déclaration Drizzle, `INSERT` du seed, manifeste). **La conclusion tient,
   la mesure non** (§10.4).
3. **§5 — « le gardien doit lire dans les deux sens ; sens code → exigences, trois éléments ».** Le
   sens 2 n'avait **jamais** été passé sur `63fcc26` (T3, 41 tests) ni sur `daa1c86` (24 composants) :
   le fichier de traçabilité arrête son périmètre à `46f547b`. **Les trois éléments listés sont les
   trois constats de la revue croisée de T3, pas un contrôle anti-orphelin.** Je l'ai passé (§10.2.3),
   et **il rend un orphelin de plus** que les trois éléments annoncés.
4. **§1 — « 148 tests de L2 ».** Le compte omet
   `apps/api/src/domaines/auth/jetons-rafraichissement.test.ts` (**9 tests**, T2 — indiscutablement
   L2), relevé par moi dans le journal du job `3 · unit`. **Le chiffre est d'au moins 157.**
   Sous-comptage, donc sans conséquence sur le verdict — mais ce dossier rectifie déjà deux chiffres de
   commit à son §6, et celui-ci va dans le même registre.

**Et ce que le dossier affirme que la mesure SOUTIENT, contre l'attente** : le §4.3 est exact dans son
mécanisme et **trop prudent dans sa portée** (§10.5) ; le §2 est exact sur l'absence de désactivation
de test, vérifiée par ma propre commande ; le §3 critère 3 est exact sur ses quatre ceintures, et les
chiffres de couverture le dépassent — **100 %**, là où le dossier n'annonçait rien de chiffré.

---

### 10.10 — VERDICT DU GARDIEN

> 🟡 **ACCEPTÉE SOUS RÉSERVE** — 12 réserves numérotées au §10.7, **dont UNE SEULE bloque
> formellement : R-B3.**

**Ce qui est acquis, et que je ne réserve pas** : les cinq critères du fichier 07 sont tranchés (1
coché, 2 cochés sous réserve, 2 sans objet **datés par l'arbitrage de Williams du 2026-08-31**) ; la
couverture des six modules critiques est **mesurée** et tient ses 24 chiffres ; le diff schéma-vs-04
est à zéro ; aucun test n'est désactivé ; `@filrouge` est vert sur les deux échelles ; et la
traçabilité est passée **dans les deux sens** au §10.2, y compris sur les deux commits que
l'instrument officiel ne couvrait pas.

**Ce qui bloque, et c'est étroit.** **R-B3** : `CLAUDE.md` §4 étape 6 dit « **le code orphelin est
REFUSÉ** », sans nuance. Au commit que la porte juge, `lireUtilisateur` est orphelin. Le correctif
existe, il est juste, Williams l'a arbitré — **mais il n'est ni sur `origin`, ni passé en CI, et il
ajoute une route sans mettre sa matrice de test à jour.** Un gardien qui compterait un correctif non
poussé ferait précisément ce que ce dépôt refuse : accepter une affirmation à la place d'une mesure.
**Deux issues, et elles ne sont pas de mon ressort** : pousser `lot/l2j-arbitrages-porte-b`, obtenir
la CI verte et fusionner **avant** la signature — ou signer sur `62193b8` en actant l'orphelin. **Je
recommande la première** : le travail est fait, il lui manque un `git push` et un run.

**Ce que ce verdict ne dit pas.** Il ne signe pas la porte. Il ne ratifie pas non plus le §8 (séquence
L3 vs P-B), qui n'est pas un point de conformité : `lot/l3a-companies` est **gelée sur `1a6bf5f`**, je
l'ai vérifié (`git ls-remote`), et la décision appartient à Williams.

| Signataire | Rôle | Verdict | Date |
| --- | --- | --- | --- |
| **A01** | montage du dossier, inventaire des preuves | **NON SIGNATAIRE — auteur d'une partie du code évalué** | 2026-08-31 |
| **A02** (gardien) | contrôle d'acceptation + matrice E1-E47 dans les **deux** sens | 🟡 **ACCEPTÉE SOUS RÉSERVE** — R-B1 à R-B12, **R-B3 seule bloquante** | 2026-08-31 |
| **Williams** | la porte, et l'arbitrage du §8 | ⬜ **À RENDRE** — la porte reste à lui (09 §1, `CLAUDE.md` §10) | — |

_Contrôle rendu sur `main` = `62193b8`, run CI `33344097876`, depuis la branche
`porte/b-controle-a02` (non poussée, non fusionnée). **Aucun code, aucun test modifié par ce
contrôle.**_
