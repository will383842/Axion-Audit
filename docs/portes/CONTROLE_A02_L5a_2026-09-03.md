# CONTRÔLE D'ACCEPTATION A02 — INCRÉMENT L5a

> **Gardien de la spécification (A02)** · pipeline 09 §3, **étape 6/7** · 2026-09-03 (UTC)
> **Objet du contrôle** : `lot/l5a` @ **`aaafcc5269ac8440f3c78f75e2bea2fc198ce12d`**, PR **#30** vers `main`.
> **Mandat** : lecture seule sur le code. Aucun `checkout`, aucun commit de production, aucune PR
> ouverte ni fusionnée. Ce fichier et la section **M** de `docs/TRACABILITE_E1-E47.md` sont les deux
> seuls écrits de cette passe.
> **Ordre de lecture appliqué** (`CLAUDE.md` §0, ligne L5) : `11` (§1, §2, §3, §4, §6, §7, §8, §9bis)
> → `03` (§17.4, §19.1, §22.1, §33.2, §33.3) → `01` §20.4 → `05` (§9.1-9.9, §31) → `06` §10 →
> `07` ligne L5 → `docs/conception/LOT_L5.md` → `docs/08_TRACABILITE.md` (E1-E47, intégral).
> **Le pack entier n'a pas été chargé** (09 §5.8).

---

## 0. RÈGLE DE PREUVE APPLIQUÉE DANS TOUT CE DOSSIER

Une preuve est **un chemin `fichier:ligne`**, **une commande exécutée avec sa sortie**, ou **un
numéro de job CI avec son verdict**. Un fichier qui existe prouve qu'il existe ; il ne prouve pas
qu'il fonctionne. Tout ce qui n'a pas été exécuté par moi est écrit « **non exécuté par moi** ».

**AVERTISSEMENT SUR MES PROPRES MESURES LOCALES.** Les hooks et la machine de cette passe tournent
sur **Node v24.19.0** (`node -v`), alors que `package.json` épingle `"engines": {"node": ">=22.11.0
<23"}` conformément au 11 §1. **Tout vert obtenu localement répond donc à côté de la question.** Je
le signale à chaque ligne concernée, et je ne l'accepte jamais comme preuve suffisante à lui seul :
la seule mesure qui engage est celle de la CI, qui tourne sur **Node v22.23.2** (relevé dans le
journal du job « couverture » : `node: v22.23.2`, image `/opt/hostedtoolcache/node/22.23.2/x64`).
Mes exécutions locales servent de **corroboration indépendante**, pas de substitut — et l'une d'elles
reproduit le chiffre de la CI à la deuxième décimale près (§6.3), ce qui vaut mieux qu'une confiance.

---

## 1. LE PÉRIMÈTRE MESURÉ — sur quel arbre exactement ce contrôle se tient

**Un contrôle ne se tient pas sur un arbre qui bouge.** Commit contrôlé, nommé une fois pour toutes :

|  |  |
| --- | --- |
| **Tête contrôlée** | `aaafcc5269ac8440f3c78f75e2bea2fc198ce12d` |
| Sujet | `merge: main (L3 #26, gouvernance #29) dans lot/l5a — R-L5a-9 fermee par la mesure` |
| Branche | `lot/l5a`, poussée (`git status` = *clean*) |
| PR | **#30** — `state: OPEN`, `mergeable: MERGEABLE`, `mergeStateStatus: CLEAN` |
| **Base du diff** | **`508ae159ab29a8111e073f0c96097ec31eb4dde0`** = `origin/main` |
| Vérification de la base | `git merge-base origin/main aaafcc5` → `508ae15` : `origin/main` est **entièrement contenu** dans la tête. `git diff 508ae15 aaafcc5` mesure donc exactement ce que L5a **ajoute** à `main`, sans rien lui imputer de L3 |
| Première ligne de l'incrément | `0d4daf4` (`wip(l5a): socle PWA terrain…`), parent `fa30be1` (P-B signée) |
| Commits propres à L5a | **14** (`git log --oneline origin/main..aaafcc5`), dont **12 sans fusion** |

> **Note de méthode.** Le `main` local du worktree est resté à `042fe76` (L3) ; `origin/main` est à
> `508ae15` (#29). Mesurer contre le `main` local aurait imputé à L5a deux fichiers de gouvernance
> (`docs/journal/2026-09-03.md`, `docs/portes/PORTE_DESCOPE_2026-09-15.md`) qui ne sont pas les
> siens. **Toutes les mesures ci-dessous sont contre `origin/main` = `508ae15`.**

### 1.1 Volume, en fichiers et en lignes

| Périmètre | Fichiers | Insertions | Suppressions |
| --- | ---: | ---: | ---: |
| **Total** `git diff --numstat 508ae15 aaafcc5` | **71** | **+16 337** | **−2 361** |
| Hors `pnpm-lock.yaml` | 70 | +11 651 | −398 |
| **Code seul** (`apps/`, `packages/`, `e2e/`) | **60** | **+11 090** | **−319** |

Répartition des 71 fichiers : `apps/field/src/local` **25** · `apps/field/src/app` **12** ·
`apps/field/src/local/depots` **5** · racine `apps/field` **4** · `e2e` **3** ·
`packages/shared/src` **2** · `apps/field/sw` **2** · `apps/field/src/session` **2** ·
`apps/field/src` **2** · `apps/field/scripts` **2** · un chacun pour `scripts/modules-en-attente.md`,
`package.json`, `eslint.config.js`, `infra/README.md`, `infra/caddy/fronts.static.caddy`,
`docs/ETAT.md`, `docs/conception/LOT_L5.md`, `DECISIONS.md`, `AMELIORATIONS.md`,
`apps/field/public/.gitignore`, `pnpm-lock.yaml`.

Un seul fichier supprimé : `e2e/pwa-404.e2e.ts`, **remplacé** par `e2e/pwa-servie.e2e.ts` (§4.4).

### 1.2 Ce que le périmètre ne contient PAS — vérifié, pas supposé

```
$ git diff --name-only 508ae15 aaafcc5 -- apps/api apps/hq apps/worker
(sortie vide)

$ git diff --name-only 508ae15 aaafcc5 | grep -iE 'migration|\.sql|schema-manifest'
apps/field/src/local/migration.test.ts        <-- migration LOCALE Dexie, pas SQL
```

**Aucune ligne de serveur, aucune migration SQL, aucun manifeste de schéma.** C'est le fait qui
commande §6.4 (migrations) et §6.10 (diff schéma-vs-04).

---

## 2. LA FUSION DE `main` DU MATIN — les deux fichiers en conflit, recomptés

Le bloc `ETAT.md` du 2026-09-03 11h30 annonce quatre conflits, dont deux de code. **Je les ai
recomptés, je ne les ai pas crus.**

### 2.1 `packages/shared/src/index.ts` — additif, rien de perdu

| Révision | Exports | Contenu |
| --- | ---: | --- |
| `508ae15` (`origin/main`) | 19 | `errors, auth, banque-questions, pagination, temps, env, redaction, motifs, journal, scoping, users, companies, missions, org-units, questionnaire, plan-entretiens, assignments` |
| `41b36a3` (l5a avant fusion) | 11 | les mêmes moins les 7 modules L3, **plus `sync`** |
| **`aaafcc5` (tête)** | **20** | **les 19 de `main` + `export * from './sync.js';` (l. 26)** |

**Verdict : fusion additive, aucun export perdu.** Les modules L3 absents de la branche (`motifs`,
`companies`, `missions`, `org-units`, `questionnaire`, `plan-entretiens`, `assignments`) sont tous
présents à la tête ; le `sync` de la branche est ajouté en fin de baril. Corroboré par
`pnpm --recursive --stream typecheck` → **6 projets `Done`, code 0**.

### 2.2 `.github/coverage-critical-paths.json` — 13 chemins, seuil intact à 90

| Révision | `seuilMinimalPourcent` | `cheminsCritiques` | `cheminsAttendus` |
| --- | ---: | ---: | ---: |
| `508ae15` (`main`) | **90** | **11** (7 L2 + 4 L3) | 5 (dont `[L5a] apps/field/src/crypto/**`) |
| `41b36a3` (l5a) | **90** | **9** (7 L2 + **2 L5a**) | 4 |
| **`aaafcc5` (tête)** | **90** | **13** = 7 L2 + 4 L3 + **2 L5a** | **4** |

Les 13 chemins de la tête, extraits programmatiquement du JSON :

```
[L2] apps/api/src/auth/**                     [L3 (L3b)] apps/api/src/domaines/missions/**
[L2] apps/api/src/domaines/auth/**            [L3 (L3c)] apps/api/src/domaines/org-units/**
[L2 (T5)] apps/api/src/domaines/scoping/**    [L3 (L3d)] apps/api/src/domaines/questionnaire/**
[L2 (T5)] apps/api/src/routes/scoping.ts      [L3 (L3d)] apps/api/src/domaines/plan-entretiens/**
[L2] apps/api/src/domaines/journal/**         [L5a] apps/field/src/local/**
[L2 (T3)] apps/api/src/domaines/users/**      [L5a] apps/field/src/session/**
[L2 (T3)] apps/api/src/routes/users.ts
```

**Verdict : 11 + 2 = 13, aucune entrée perdue, aucun seuil relâché.** Le bandeau du fichier interdit
d'en retirer une, et la règle « la CI refuse toute valeur différente de 90 » est intacte — le journal
du job de couverture montre la garde qui la fait respecter (`if [ "$seuil" != "90" ]`).

La 5ᵉ entrée `cheminsAttendus` de `main` — `[L5a] apps/field/src/crypto/**` — a **disparu à bon
droit** : c'est l'entrée *attendue* que L5a devait consommer. L'arborescence réelle est
`apps/field/src/local/**` et non `src/crypto/**` ; le mode d'emploi du fichier autorise nommément
cette correction (« en corrigeant le glob si l'arborescence réelle diffère », message d'erreur de la
ceinture 2 relevé dans le journal du job). **Ce n'est pas une perte, c'est une consommation.**
Restent 4 attentes : `[L5c] backup/**`, `[L6a] field/src/sync/**`, `[L6a] api/src/sync/**`,
`[L8] api/src/scoring/**`.

### 2.3 R-L5a-9 — **fermée par la mesure, vérifiée par moi et non crue sur parole**

La réserve annonçait que les cinq décisions `[L5a]` du 2026-09-02, écrites à l'origine sur
`lot/l3-suite`, devaient survivre à la fusion. Recompte :

```
$ grep -c '^## .*\[L5a\]' DECISIONS.md            (tête aaafcc5)   -> 11
$ git show 41b36a3:DECISIONS.md | grep -c ...     (branche seule)  ->  6
$ git show 508ae15:DECISIONS.md | grep -c ...     (main seul)      ->  5
```

**6 + 5 = 11. Aucune entrée dupliquée, aucune perdue.** Les cinq de `main` sont présentes aux lignes
**6735** (Argon2id — escalade), **6759** (aucun AAD sur AES-GCM), **6780** (état `validé` sans
colonne au 04), **6801** (liste fermée + `supprimeLe`/`answerId`), **6848** (profil OWASP confirmé
par Williams). Les six de la branche sont aux lignes **7365, 7388, 7408, 7427, 7446, 7467**.

Contrôle structurel indépendant : `wc -l DECISIONS.md` = **7 495** = 7 364 (`main`) + 131, et les six
entrées de branche occupent exactement 131 lignes (5 504 → 5 634 chez `41b36a3`).
`node scripts/check-decisions.mjs` → **193 entrées, toutes au format 11 §9bis**, code 0.

> **Ce que je retiens de la note de méthode du bloc ETAT 11h30**, et qui mérite d'être conservé :
> « Aucune ligne perdue et structure détruite sont compatibles. » La première résolution par *hunks*
> passait le contrôle multi-ensembles et coupait deux entrées de leurs champs `Décideur` /
> `Impact spec`. C'est `check:decisions` qui a refusé, pas la vigilance. **R-L5a-9 : FERMÉE.**

---

## 3. SENS 1 — EXIGENCES → CODE (ce que le 11 §6 confie à L5a, ligne par ligne)

Le brief de l'incrément est le **11 §6**, ligne L5a, mot pour mot :

> « **L5a** — shell PWA offline (Workbox) + Dexie (schéma local versionné) + DEK/KEK + verrouillage
> + pull mission + `storage.persist()`. »

Six items. Complétés par `docs/conception/LOT_L5.md` §1 (colonne « Livre ») et §2 (interfaces
publiées), validés à l'étape 1bis.

| # | Item 11 §6 | Code livré | Preuve | Verdict |
| --- | --- | --- | --- | --- |
| **1** | **shell PWA offline (Workbox)** | `apps/field/sw/service-worker.ts` (`precacheAndRoute(self.__WB_MANIFEST)` l. 45 ; `NavigationRoute` avec `denylist: [/^\/api\//]` l. 55-57) · `apps/field/scripts/build-sw.mjs` (`injectManifest` de `workbox-build`, l. 31/62) · `apps/field/vite.config.ts` (manifeste + `theme-color` injectés depuis les jetons) · `apps/field/src/app/service-worker-client.ts` | e2e CI `pwa-servie.e2e.ts` : **12 tests `@critique` verts sur DEUX piles Caddy** — `/sw.js` en JS non mis en cache, `/manifest.webmanifest` installable avec icône ≥ 192, `/apple-touch-icon.png` en PNG, et les 4 chemins non livrés qui rendent 404 sans HTML déguisé (job `5 · e2e`, run 33737000867) | ✅ **TENU** |
| **2** | **Dexie (schéma local versionné)** | `apps/field/src/local/base.ts` (`SCHEMA_LOCAL` liste ordonnée, `VERSION_SCHEMA_LOCAL`, `BaseTropRecenteError` — **aucun chemin n'appelle `delete()`**) · `apps/field/src/local/formes.ts` (les 9 tables 05 §9.1) | `base.test.ts` **11 tests** · `migration.test.ts` **8 tests** dont 3 `@critique` : « v1→v2 avec outbox non vide : mêmes 121 ops, même ordre » et « **une montée qui ÉCHOUE ne perd rien** ». Exécutés par moi : verts | ✅ **TENU** |
| **3** | **DEK / KEK** | `apps/field/src/local/coffre.ts` (Argon2id `hash-wasm` → KEK ; DEK AES-256 **non extractable** ; `verrouiller()` ferme le coffre ; `reenvelopperDek` ne touche aucune donnée) · `coffre-appareil.ts` · `enveloppe.ts` · `jetons.ts` (refresh token **chiffré dans Dexie**, 11 §3) | `coffre.test.ts` **23** · `coffre-appareil.test.ts` **17** (dont `@critique` « après changement, les enveloppes des lignes sont IDENTIQUES » = la DEK n'a pas été re-chiffrée) · `enveloppe.test.ts` **14** · `jetons.test.ts` **8**. Budget 11 §4 « dérivation < 1 s » **mesuré en CI** (`accessibilite-l5a.e2e.ts:199`, vert) | ✅ **TENU** (réserve non bloquante **NB6**) |
| **4** | **verrouillage** | `apps/field/src/app/verrou.ts` (227 l. — `DELAI_INACTIVITE_MS` 15 / 60 min, Wake Lock, `verrouillerMaintenant`), câblé par `app/contexte.tsx:220` | **AUCUNE.** Aucun fichier de test n'existe pour ce module ; couverture v8 **0,00 % de lignes** (mesure §6.3). Aucun e2e n'avance des minuteurs de 15 ou 60 min | 🔴 **LIVRÉ, NON PROUVÉ** — réserve **BLOQUANTE B1** |
| **5** | **pull mission** | `apps/field/src/local/embarquement.ts` : `embarquerMission()` prépare le stockage puis **refuse** avec le motif `premier_pull_indisponible` | **DESCOPÉ vers L6a**, tracé : `DECISIONS.md` l. **7446** (« le premier pull est descopé de L5a vers L6a, qui livre l'endpoint serveur qu'il consomme »), décideur A01. Le refus est testé (`EcranAccueil.test.tsx:224`, `@critique` « tant que le premier pull n'existe pas, l'écran le DIT et n'offre aucun bouton ») | 🟡 **DESCOPÉ, DOCUMENTÉ** — pas un veto ; réserves **NB1** et **NB2** |
| **6** | **`storage.persist()`** | `apps/field/src/local/stockage.ts` (persist / estimate / seuils, `'indisponible'` quand l'API manque) · `embarquement.ts` (**refus ⇒ mission NON embarquée**) · `app/EcranStockage.tsx` (l'écran qui guide, 05 §31-2) | `stockage.test.ts` **22 tests** · `EcranStockage.test.tsx` **7 tests** dont 3 `@critique`. Couverture mesurée `stockage.ts` **96,92 %**, `EcranStockage.tsx` **100 %** | ✅ **TENU** |

### 3.1 Les interfaces publiées par `LOT_L5.md` §2 — le contrat envers L5b et L5c

C'est l'autre moitié du sens 1 : un socle se juge aussi sur ce qu'il **promet à qui vient après**.

| Interface promise (§2) | Livrée | Preuve |
| --- | --- | --- |
| `src/local/base.ts` — `BaseLocale`, 9 tables, `VERSION_SCHEMA_LOCAL` | ✅ | `base.test.ts` 11 tests |
| `src/local/ecriture.ts` — `ecrireLocal` (UNE transaction `[table,'outbox']`) · `appliquerDescente` (**jamais** `outbox`) | ✅ | `ecriture.test.ts` **15** · `descente.test.ts` **8** ; couverture **100 %** lignes, 96,77 % branches |
| `src/local/coffre.ts` — `deriverKek`, `ouvrirCoffre`, `dechiffrer<T>(e, ZodType<T>)`, `reenvelopperDek`, `verrouiller` | ✅ | signatures conformes ; 23 tests |
| `src/local/horloge.ts` — `maintenant()`, `reglerDecalage(serverTime)` | ✅ | `horloge.test.ts` **11** ; **seul module autorisé à lire l'horloge** (§5, invariant 1c) |
| `src/local/depots/*.ts` — `duJour()`, `parSession()`, `rechercher(texte)` | ✅ | 4 dépôts, `depots.test.ts` **23** ; **100 %** lignes sur les quatre |
| `src/session/machine.ts` — 4 états, `TRANSITIONS_SESSION`, `peutTransiter(…, profil)` | ✅ | `machine.test.ts` **55 tests** (4 états × actions × 2 profils, terminer ≠ valider) |
| `src/app/verrou.ts` — `useVerrou()`, `DELAI_INACTIVITE_MS` 15/60 | ⚠️ livrée, **non éprouvée** | voir **B1** |
| `src/sync/port.ts` — `PortSync` déclaré, implémenté par L6a | ✅ **déplacé** en `src/local/port-sync.ts` | motif écrit dans l'en-tête (l. 17-27) : `apps/field/src/sync/**` est le glob **réservé** à L6a dans `coverage-critical-paths.json` ; y poser un port déclaratif ferait rougir la CI pour une raison étrangère à sa qualité. Tracé au compteur étage 1 (`AMELIORATIONS.md`, ligne L3b-d). `port-sync.test.ts` **13 tests** ; couverture **100 %** |
| `packages/shared/src/sync.ts` — `ENTITES_SYNC`, `operationSchema`, `lotPushSchema`, `reponsePushSchema`, `reponsePullSchema` | ✅ | 212 lignes ; `RESULTATS_OP = ['applied','duplicate','superseded','forbidden','error']` (l. 156) — **conforme mot pour mot au 05 §9.3 et au 11 §4** |
| `packages/ui` — L5 **compose**, ne recrée rien | ✅ | les 3 écrans importent `Bouton, Message, PastilleSync, ZoneEtat, EtatErreur, Squelette` de `@axion/ui` ; **aucun composant nouveau dans `apps/field`** |
| `app/vues.ts` — registre **append-only**, une ligne par écran | ✅ | 3 vues L5a (`deverrouillage`, `stockage`, `accueil`), emplacements L5b et L5c marqués par commentaire (l. 46-47) |

**Rien de ce que le 11 §6 et la note §2 confient à L5a n'a été oublié.** Un item est descopé avec
trace (le premier pull), un item est livré sans preuve (le verrou).

---

## 4. SENS 2 — CODE → EXIGENCES (le contrôle anti-orphelin)

**Règle appliquée** (`CLAUDE.md` §4-6) : tout artefact livré se rattache à une exigence **E1-E47** ou
à une fiche `AMELIORATIONS.md`. **Le code orphelin est REFUSÉ.**

### 4.1 Balayage exhaustif des 60 fichiers de code

```
$ for f in $(git diff --name-only 508ae15 aaafcc5 -- apps packages e2e | grep -E '\.(ts|tsx|mjs|css)$');
    do grep -q 'Traçabilité' "$f" || echo "SANS GLOSE: $f"; done
SANS GLOSE: apps/field/src/app/coquille.css
SANS GLOSE: packages/shared/src/index.ts
```

**Deux fichiers seulement sur 60**, et aucun des deux n'est orphelin :

- `apps/field/src/app/coquille.css` — porte sa justification en clair, l. 4-6 : « Aucune valeur
  littérale : couleurs, espacements, rayons et tailles viennent TOUS des jetons de `packages/ui`
  (**invariant 4**) ». Se rattache à **E44** et à l'invariant 4. **Rattaché.**
- `packages/shared/src/index.ts` — **une seule ligne ajoutée** (`export * from './sync.js';`). Baril
  d'exports, pas un module. **Rattaché** par le module qu'il expose (**E7**).

Le garde-fou outillé confirme sur l'ensemble du dépôt :
`node scripts/check-tracabilite-exigences.mjs` → **680 citations, 344 fichiers, aucune incohérence**,
code 0 (exécuté par moi).

### 4.2 Les gloses ouvertes une par une contre le libellé officiel

Le garde n'est qu'un contrôle **lexical** — il le dit lui-même dans ses angles morts : « il ne
distingue pas un rattachement JUSTE d'un rattachement FAUX ». C'est mon travail. J'ai donc **rouvert
la section citée** pour chaque glose non triviale. C'est exactement le défaut trouvé sur L5b (une
citation d'E30 pour un module relevant d'E37).

| Fichier:ligne | Glose | Libellé officiel (`08_TRACABILITE.md`) | Section rouverte | Verdict |
| --- | --- | --- | --- | --- |
| `EcranAccueil.tsx:31` | **E26** « alertes actives sur les manques » | E26 · « Alertes actives sur les manques (centre d'alertes, **8 types**, cliquables, acquittement motivé) » → **01 §20.4** | **01 §20.4 ouvert** : la liste des 8 types côté auditeur contient nommément « **sync en échec ou muette** » | ✅ **JUSTE** — l'alerte « aucune sync depuis 24 h » EST l'un des huit types |
| `app/vues.ts:21` | **E13** « écran 3 zones, enregistrement continu » | E13 → **M3.1, §17.4** | **03 §17.4 ouvert** : porte la règle « rouvrir l'app = revenir exactement à la question en cours ». Le fichier la sert par `meta.vueCourante` (en-tête l. 15-19) | 🟡 **MINCE MAIS JUSTE** — l'écran 3 zones est L5b ; c'est **§17.4**, moitié d'E13, qui est servie. Défendable, pas faux |
| `depots/reponses.ts:11` | **E13**, **E12** | E12 · « Entretiens par interlocuteur, pause/reprise, **à revoir**, notes, ad hoc » | code : `parSession`, filtre `flagReview === 1` (l. 115), exclusion des `supprimeLe` | ✅ **JUSTE** — c'est la lecture indexée qui sert « à revoir » |
| `depots/questions.ts:20` | **E11** « questionnaire généré et figé par mission » | E11 · « Génération automatique du questionnaire depuis le profil client, **figé par mission** » → M2 | code : lecture du `*_snapshot` figé + `rechercher()` hors-parcours §25.4 | ✅ **JUSTE** — la moitié « figé par mission » d'E11, celle que le terrain consomme |
| `horloge.ts:24` | **E32** « fuseaux, devises, interface française », **E9** | E32 · « Audits monde entier : **fuseaux horaires**… » · E9 · « multi-consultants, **sync sans conflit** » | 05 §9.4 : `client_updated_at` est le juge du *last-write-wins* | ✅ **JUSTE** sur les deux |
| `session/machine.ts:37` | **E24** « validation obligatoire de chaque étape » | E24 · « Validation OBLIGATOIRE de chaque étape (verrous, **profils guidé strict/expert**, dérogations tracées) » → §19.1 | `machine.test.ts` : 4 états × actions × **2 profils**, terminer ≠ valider | ✅ **JUSTE** |
| `contrat-sync.ts:14` | **E7** « remontée continue dès qu'il y a du réseau » | E7 → §9.3, §22.1 | 05 §9.3 : contrat d'op et 5 résultats | ✅ **JUSTE** |
| `local/*.ts` (15 fichiers) | **E33** « sécurité / RGPD » | E33 · « Sécurité/RGPD : **chiffrement local**… » → §10 | 06 §10.5 : « IndexedDB chiffré » | ✅ **JUSTE** |
| `sw/service-worker.ts:29`, `main.tsx:11`, `service-worker-client.ts:18` | **E17** « stack imposée » | E17 · « Stack imposée : Hetzner, Docker, PostgreSQL, Fastify, **React+Vite PWA, Dexie**… » | — | ✅ **JUSTE** |
| `accessibilite-l5a.e2e.ts:39-41` | **E36**, **E43** | E36 « exécutable par lots avec critères » · E43 « exécutabilité autopilote » | le fichier porte la dérogation `@axe-core/playwright` (11 §8-1) et les budgets 11 §4 | ✅ **JUSTE** |

**Aucune glose fausse trouvée sur L5a.** Deux sont *minces* (`vues.ts`, et par ricochet
`depots/reponses.ts`, qui citent E13 alors que l'écran d'entretien est L5b) ; dans les deux cas la
**section** citée par E13 — §17.4 — est réellement servie par le code présent. Je les signale sans en
faire une réserve : une glose mince qui pointe la bonne section n'est pas une glose fausse.

### 4.3 Artefacts sans glose `E<n>` mais rattachés — inventaire nominatif

| Artefact | Rattachement | Preuve |
| --- | --- | --- |
| `apps/field/scripts/build-sw.mjs` | E6, E17 — construction du SW | en-tête l. 3-13 |
| `apps/field/scripts/build-icones.mjs` | **Fiche A-009** (`AMELIORATIONS.md` l. 1629) « L'icône de la PWA terrain est PROVISOIRE » + E44 | fiche + `DECISIONS.md` l. 7388 |
| `apps/field/public/.gitignore` | artefact de construction (les icônes générées ne sont pas versionnées) | — |
| `apps/field/sw/tsconfig.json` | outillage du `typecheck` du SW (`apps/field/package.json`, script `typecheck`) | — |
| `eslint.config.js` (+95 l.) | **outillage des interdits** réclamés par `LOT_L5.md` §4 (« Interdits outillés »), réserve **R-L5a-5** de la revue A29 | §5, invariant 1a |
| `infra/caddy/fronts.static.caddy` | ouverture des chemins PWA, conséquence directe du bloquant **A29 B1** | `DECISIONS.md` l. 7365 |
| `package.json` (+1) | `@axe-core/playwright 4.13.0` | `DECISIONS.md` l. **6706** (dépendances de test, « silence vaut accord ») |
| `scripts/modules-en-attente.md` | soupape du garde anti-orphelin ; 5 modules L5a entrés **et sortis** le même jour | `check:graphe-modules` code 0 |
| `docs/ETAT.md`, `docs/conception/LOT_L5.md`, `DECISIONS.md`, `AMELIORATIONS.md` | gouvernance 11 §9bis / §9ter | — |

### 4.4 Le seul fichier supprimé — substitution, pas perte

`e2e/pwa-404.e2e.ts` (2 blocs) → `e2e/pwa-servie.e2e.ts` (**6 blocs, 12 tests exécutés** sur deux
piles Caddy). L'en-tête du nouveau fichier (l. 5-31) porte le motif complet : L0 avait **fermé** ces
chemins tant qu'aucune PWA n'existait ; L5a livre la PWA, donc le fichier « a changé de sens, comme
son en-tête d'origine l'annonçait, et de nom, parce que l'ancien mentait ». **La moitié de la
garantie L0 est conservée** — les 4 chemins non livrés (`/hq/sw.js`, `/hq/manifest.webmanifest`,
`/service-worker.js`, `/hq/service-worker.js`) rendent toujours 404 sans HTML déguisé, et c'est
testé. Arbitrage tracé : `DECISIONS.md` l. 7365.

### 4.5 VERDICT ANTI-ORPHELIN

> **CODE ORPHELIN : AUCUN.** Les 60 fichiers de code et les 11 fichiers de configuration et de
> gouvernance du périmètre se rattachent tous à une exigence E1-E47 ou à une fiche
> `AMELIORATIONS.md` nommée. **Aucun *scope creep* silencieux détecté.**
>
> Deux artefacts *anticipent* légitimement l'aval, et le disent : `port-sync.ts` (implémentation
> **inerte** que L6a REMPLACE) et `service-worker-client.ts` (l. 15 : « Le bandeau lui-même est L5c ;
> ce module lui fournit l'état et l'action »). L'un et l'autre sont des **pièces de socle**, pas des
> fonctionnalités d'étage 2 anticipées : le 11 §8-7 n'est pas en cause.

---

## 5. LES 8 INVARIANTS (`CLAUDE.md` §1), un par un, avec la preuve

### Invariant 1 — Offline-first, **UUID v7 côté client**, push idempotent — ✅ **TENU**

**a) UUID v7 applicatif.** Les deux seuls sites de génération d'identifiant du front :

```
apps/field/src/local/coffre-appareil.ts:27   import { uuidv7 } from 'uuidv7';
apps/field/src/local/coffre-appareil.ts:93       await ecrireMeta(base, CLES_META.appareil, uuidv7());
apps/field/src/local/ecriture.ts:33          import { uuidv7 } from 'uuidv7';
apps/field/src/local/ecriture.ts:177             opId: uuidv7(),
```

**Aucun `crypto.randomUUID()`, aucune fonction SQL v7** — et ce n'est plus une consigne : L5a a
**outillé** l'interdit dans `eslint.config.js` (constante `UUID_APPLICATIF`, deux sélecteurs
`no-restricted-syntax`). La constante est **ré-étalée dans les trois blocs** de la couche terrain,
avec le motif écrit l. 64-86 : en configuration à plat, un bloc ultérieur qui redéclare
`no-restricted-syntax` **remplace** la règle au lieu de s'y ajouter — sans cette précaution,
`apps/field/src/**` aurait perdu **en silence** la garde UUID. Piège **mesuré** (« vérifié sur un
fichier sonde, qui ne remontait que deux erreurs sur quatre »), pas supposé.

**b) L'UI lit toujours IndexedDB (05 §9.2-3).** Le service worker exclut `/api` de tout cache
d'exécution : `sw/service-worker.ts:57` → `denylist: [/^\/api\//]`. `LOT_L5.md` §3.1 en donne le
motif : un `StaleWhileRevalidate` sur l'API fabriquerait une seconde source de vérité.

**c) L'horloge.** Un seul module lit l'heure de l'appareil : `local/horloge.ts` (l. 50, 86, 91, 104).
Le seul autre `new Date(...)` du front — `depots/sessions.ts:55` — prend **un argument** : c'est une
conversion pour `Intl`, pas une lecture d'horloge, et la règle ESLint le dit explicitement
(sélecteur `NewExpression[callee.name='Date'][arguments.length=0]`, message l. 137-140 :
« `new Date(valeur)` AVEC argument reste autorisé »).

**d) Idempotence.** `packages/shared/src/sync.ts` porte `operationSchema` avec `opId: z.uuid()` et
les 5 résultats du 05 §9.3 ; `TAILLE_LOT_PUSH_MAX` fixe les lots de 100 (11 §4). Le push est L6a.

**e) L'écriture atomique.** `ecrireLocal` écrit ligne + op dans **une** transaction Dexie ;
`appliquerDescente` **n'inclut pas** `outbox` dans sa transaction, donc toute écriture y lèverait.
La garantie est structurelle, pas déclarative. `ecriture.test.ts` **15**, `descente.test.ts` **8**,
couverture **100 %** lignes.

### Invariant 2 — Aucune référence client dans le code (fixtures exceptées) — ✅ **TENU**

```
$ grep -rn -i 'axion-ia|client pilote|nom du client' apps/field/src apps/field/sw --include=*.ts --include=*.tsx | grep -v '.test.'
(sortie vide)
```

Seule occurrence d'un nom de mission : `EcranAccueil.test.tsx:59` → `titre: 'Mission fictive FIL-TPE'`
— **fixture canonique 09 §4bis**, dans un fichier de test. Exception prévue par l'invariant.

### Invariant 3 — RBAC serveur, financier admin, écritures de sync au propriétaire — ✅ **SANS OBJET, ET SANS ENTORSE**

```
$ git diff --name-only 508ae15 aaafcc5 -- apps/api apps/hq apps/worker
(sortie vide)
```

**L5a ne livre aucune route serveur.** Ce que je vérifie néanmoins, parce que c'est là que le terrain
peut préparer une entorse : le contrat d'op partagé prévoit bien le résultat `forbidden` du 05 §9.9
(`packages/shared/src/sync.ts:156`), et son commentaire (l. 151-153) impose que l'op sorte « vers un
état *rejetée* VISIBLE, **jamais rejouée silencieusement** ». `DECISIONS.md` l. 7427 grave la règle
jumelle côté descente : « une ligne dont l'op est en ÉCHEC n'est jamais écrasée », **testée**
(`descente.test.ts`, ajout du commit `2c754b2`).

### Invariant 4 — **Aucune couleur ni taille en dur, tokens uniquement** — ✅ **TENU, ET PROUVÉ EN NAVIGATEUR**

```
$ grep -rn '#[0-9a-fA-F]{3,8}\b' apps/field/src apps/field/sw apps/field/scripts apps/field/index.html | grep -v '.test.'
(sortie vide)

$ grep -rn '[0-9]+px|[0-9]+rem' apps/field/src --include=*.tsx --include=*.ts | grep -v '.test.'
(sortie vide)
```

`coquille.css` : **118 lignes, zéro littéral** — `var(--couleur-surface-fond)`,
`var(--espacement-3)`, `var(--typo-taille-lg)`, `var(--taille-bordure-epaisseur)`… Le fichier ne
*définit* aucun jeton, il n'en consomme que (l. 4-6).

Deux endroits où une couleur aurait dû se dupliquer, et où elle ne l'a pas fait :

- `index.html:12` → `<meta name="theme-color" content="%COULEUR_THEME%">`, **substitué à la
  construction** depuis les jetons par `vite.config.ts`. Motif écrit l. 8-11 : « le HTML statique ne
  peut pas lire une variable CSS, mais ce n'est pas une raison pour dupliquer la charte ».
- `scripts/build-icones.mjs` : les icônes PWA sont peintes depuis `COULEURS_CHARTE` de `packages/ui`
  (l. 24-25), jamais depuis un littéral.

**Preuve d'exécution, et c'est elle qui compte** (un jeton déclaré mais non appliqué ne prouve rien) :
e2e CI `socle.e2e.ts:51` « front terrain › applique les tokens de la charte, **jamais une couleur en
dur** » et `socle.e2e.ts:80` « porte une couleur de thème **injectée depuis les tokens** » — les deux
vertes, run 33737000867.

### Invariant 5 — Interface 100 % en français, horodatages UTC — ✅ **TENU**

- `apps/field/index.html:4` → `<html lang="fr">`, avec le motif (césure, synthèse vocale, lecteurs
  d'écran). e2e `socle.e2e.ts:40` « démarre, **s'annonce en français** » — vert.
- Libellés relevés, français et orientés action : `'Aucune mission sur cet appareil'`
  (`EcranAccueil.tsx:214`), `'Les données locales n'ont pas pu être lues'` (l. 206),
  `'Stockage de l'appareil'`, `'Déverrouiller'` (`vues.ts:43-45`).
- **UTC** : `horloge.ts:86` → `new Date(Date.now() + decalageMs).toISOString()`. Le fuseau de mission
  n'apparaît qu'à l'affichage, `depots/sessions.ts:55` (`Intl` sur une valeur ISO).

### Invariant 6 — Le terrain collecte, le siège produit — ✅ **TENU**

Ce que L5a calcule sur l'appareil : ses **propres lignes** (compteurs de session
`depots/sessions.ts:151`, à-revoir `depots/reponses.ts:115`, alerte « sync muette » locale). Ce
qu'il ne calcule pas : aucun scoring, aucune agrégation multi-sessions, aucun assemblage de
questionnaire, aucun DOCX, aucun LLM. La frontière est écrite au `LOT_L5.md` §3.5 et le code s'y
tient — `apps/field` n'importe rien de `apps/api`.

### Invariant 7 — Toute correction = révision tracée, rien n'est écrasé — ✅ **TENU**

```
$ grep -rn '\.delete\(|\.clear\(|deleteDatabase' apps/field/src --include=*.ts --include=*.tsx | grep -v '.test.'
apps/field/src/app/service-worker-client.ts:98    return () => abonnes.delete(abonne);   <-- Set JS
apps/field/src/local/base.ts:376                  await base.meta.delete(cle);           <-- meta, non synchronisée
```

**Aucune ligne d'entité n'est jamais supprimée.** La suppression est logique (`supprimeLe`), et
`depots/reponses.ts:62-66` le dit sans détour : « La ligne, elle, reste en base — invariant 7,
`delete_soft` ». `base.ts` en tire une conséquence forte, l. 19-25 : sur une base créée par une
version plus récente, le réflexe « supprimer et repartir propre » détruirait des réponses non
synchronisées ; le module lève `BaseTropRecenteError` et **aucun chemin n'appelle `delete()`** sur la
base. Testé (`base.test.ts`, dont un cas de preuve : « Dexie 4 n'émet PAS `VersionError` sur une base
plus récente — la version IndexedDB ne suffit pas »).

### Invariant 8 — Sauvegarde terrain : sync ≥ 1×/jour + export de secours testé — 🟡 **PARTIELLEMENT SANS OBJET, ET C'EST ÉCRIT**

**L'export de secours `.axionbackup` (11 §4) est un livrable de L5c, pas de L5a** (11 §6, ligne L5c ;
`LOT_L5.md` §1, colonne « Ne livre PAS » de L5a : « aucun export de secours »). **Cette moitié de
l'invariant n'a donc pas encore d'objet ici**, et je le dis plutôt que de la cocher à vide.

Ce que L5a livre **et qui relève déjà de l'invariant 8**, avec sa preuve :

- l'alerte « aucune sync depuis 24 h », **calculée localement** — `EcranAccueil.test.tsx:261`,
  `@critique` « une réponse non synchronisée sur un appareil jamais synchronisé ⇒ **l'alerte de
  l'invariant 8 est visible** » ;
- **le refus de mentir** : `port-sync.ts` rend `{ statut: 'indisponible' }` et **jamais une pastille
  verte** ; testé par `EcranAccueil.test.tsx:241` (« un état de sync qui ne verdit pas sans
  serveur »). C'est le point le plus important de l'incrément au regard de l'invariant 8 : une
  pastille qui verdit sans serveur coûte une journée d'entretiens.

---

## 6. LA DoD TRANSVERSE (`CLAUDE.md` §5) — dix lignes, chacune avec sa preuve

### 6.1 lint + typecheck stricts = 0 erreur — ✅

| Source | Commande | Résultat |
| --- | --- | --- |
| **CI (Node 22.23.2)** | job `1 · lint` | **pass** (1 min 0 s) |
| **CI** | job `2 · typecheck` | **pass** (43 s) |
| Moi (Node 24 — corroboration) | `pnpm build:packages` puis `npx eslint . --max-warnings=0` | code **0**, aucune sortie |
| Moi (Node 24 — corroboration) | `pnpm --recursive --stream typecheck` | **6 projets `Done`**, code **0** |

### 6.2 Tous les tests verts, **AUCUN test skippé** — ✅

Mesures sur **`aaafcc5`**, runs CI **33737000867** (pull_request) et **33736955968** (push) — les deux
`conclusion: success`, `head_sha` vérifié égal à la tête contrôlée.

| Suite | Fichiers | Tests | Job |
| --- | ---: | ---: | --- |
| `unit` | 39 | **923** | `3 · unit` pass |
| `interface` | 29 | **466** | exécuté dans le job `couverture` |
| `integration` | 23 | **563** | `4 · integration` pass (5 min 12) |
| **Sous-total vitest** | **91** | **1 952** | job couverture : `Test Files 91 passed (91)` · `Tests 1952 passed (1952)` |
| `e2e` Playwright | 4 | **45** | `5 · e2e (chromium)` → `45 passed (12.7s)` |
| **TOTAL** | **95** | **1 997** |  |

Dont **L5a en propre**, exécuté par moi (Node 24) et recoupé au journal CI :
`npx vitest run --project unit apps/field` → **14 fichiers / 257 tests** ;
`npx vitest run --project interface apps/field` → **3 fichiers / 19 tests**. **276 tests de socle.**

Anti-skip : `node scripts/check-no-skipped-tests.mjs` → « **aucun test désactivé** (95 fichiers de
test analysés) », code 0 (exécuté par moi) ; job CI « aucun test désactivé ni orphelin (11 §2 /
09 §5.7) » **pass**. **Liste d'exceptions vide, et elle l'est restée.**

> **LE POINT SIGNALÉ PAR LE BLOC `ETAT.md` DU 06h35 — RÉPARÉ, MAIS PAS PAR L5a, ET LE RC=0 QU'IL
> ANNONCE NE PROUVE PAS CE QU'IL PRÉTEND.** Vérifié en ouvrant les trois révisions :
>
> | Révision | `verify` lance-t-il `test:interface` ? |
> | --- | --- |
> | `41b36a3` (l5a, **là où le RC=0 a été mesuré**) | **NON** — séquence finale `test:unit && test:integration && test:e2e` |
> | `508ae15` (`origin/main`, correctif venu de L3 — `DECISIONS.md` l. 7162) | **OUI** |
> | **`aaafcc5` (tête)** | **OUI** — `… && pnpm test:unit && pnpm test:interface && pnpm test:integration && pnpm test:e2e` |
>
> **Conclusion en trois points, sans arrondir.** ① Le défaut structurel **est réparé à la tête**, et
> `verify:rapide` l'est aussi. ② La réparation vient de la **fusion de `main`** (correctif L3), pas
> d'un geste de L5a : `git diff 508ae15 aaafcc5 -- package.json` ne montre qu'**une** ligne,
> `@axe-core/playwright`. ③ **Le « RC=0, 1 000 tests » du bloc 06h35 ne couvre donc AUCUN des 29
> fichiers `.test.tsx`**, y compris les 3 écrans de L5a — le bloc le dit lui-même (« Ne pas lire ce
> RC=0 comme *toute l'interface est verte* : il ne le dit pas »), et il a raison de le dire.
> **Ce qui répare la preuve, ce n'est pas ce bloc : c'est le job CI `couverture` sur `aaafcc5`**, qui
> lance `pnpm test:coverage` = **tous** les projets vitest. Relevé dans son journal : 29 occurrences
> du tag ` interface `, dont les trois écrans L5a, **tous verts**. La preuve existe ; elle n'est
> simplement pas celle qu'annonçait le bloc.

### 6.3 Couverture ≥ 90 % sur les modules critiques — ✅ **MESURÉE, ET RECOUPÉE**

**Mesure de référence : CI, job « couverture ≥ 90 % (modules critiques — 09 §3) », run 33737000867,
`pass` (5 min 28), sur `aaafcc5`, Node 22.23.2.** Verdict final imprimé : « *Tous les modules
critiques atteignent 90 % — couverture MESURÉE.* » Les deux entrées L5a, telles que la CI les
imprime :

| Glob L5a | lines | statements | functions | branches |
| --- | ---: | ---: | ---: | ---: |
| `apps/field/src/local/**` (« Couche locale terrain — coffre DEK/KEK, port d'écriture, base Dexie versionnée ») | **97,87 %** | **97,87 %** | **99,01 %** | **93,69 %** |
| `apps/field/src/session/**` (« Machine à états de session terrain — terminer ≠ valider ») | **97,87 %** | **97,87 %** | **100,00 %** | **94,12 %** |

**Contre-mesure indépendante, exécutée par moi** (`npx vitest run --project unit --project interface
--coverage --coverage.reportsDirectory=./coverage-a02 apps/field`, puis agrégation par glob depuis
`coverage-summary.json`) :

```
apps/field/src/local/**    (17 fichiers)   lines 97.87 %  (1149/1174)   branches 93.69 %  (282/301)
apps/field/src/session/**  ( 1 fichier )   lines 97.87 %  (  92/94  )   branches 94.12 %  ( 16/17 )
```

**Les deux mesures coïncident à la deuxième décimale.** Le chiffre n'est ni déclaré ni recopié : il
est reproduit par deux chemins indépendants. Les 11 autres globs (L2, L3) sont verts au même job.

> **Ce que cette mesure NE couvre PAS, et que le seuil ne pouvait pas voir.** La même contre-mesure,
> étendue au troisième répertoire de `apps/field/src`, donne `apps/field/src/app/**` (8 fichiers) à
> **40,11 % de lignes (292/728)**, détail par fichier :
>
> ```
> app/EcranStockage.tsx          100,00 %      app/verrou.ts                    0,00 %   (227 l.)
> app/EcranDeverrouillage.tsx     98,66 %      app/contexte.tsx                 0,00 %   (318 l.)
> app/EcranAccueil.tsx            80,64 %      app/navigation.ts                0,00 %   ( 94 l.)
>                                              app/service-worker-client.ts     0,00 %   (139 l.)
>                                              app/vues.ts                      0,00 %   ( 58 l.)
>                                              App.tsx                          0,00 %   ( 89 l.)
>                                              main.tsx                         0,00 %   ( 36 l.)
> ```
>
> `apps/field/src/app/**` **n'est ni dans `cheminsCritiques` ni dans `cheminsAttendus`** : la
> ceinture 2 de la CI (« module critique LIVRÉ mais NON SOUMIS AU SEUIL ») n'avait rien à réclamer,
> et elle ne s'est pas tue à tort. **Mais 961 lignes de socle sortent de cet incrément sans aucun
> test unitaire ni d'interface**, dont `verrou.ts` — livrable nommé au 11 §6. Voir **B1**.
>
> **Nuance mesurée et due** : `App.tsx`, `main.tsx`, `contexte.tsx`, `vues.ts` et `navigation.ts`
> **sont** parcourus de bout en bout par les 4 balayages axe-core (qui créent un coffre et
> déverrouillent réellement l'application) ; Playwright n'étant pas instrumenté dans le rapport v8,
> leur 0 % **sous-estime** la réalité. `verrou.ts`, lui, ne l'est par rien : aucun e2e n'avance un
> minuteur de 15 ou 60 minutes, et aucun test unitaire n'existe.

### 6.4 Migrations up/down exécutées sur staging — ⚪ **HORS PÉRIMÈTRE L5a, PORTÉ PAR R-L3-2-bis**

**Je ne la recompte pas contre L5a, et je dis pourquoi.**

1. `git diff --name-only 508ae15 aaafcc5` ne contient **aucune** migration SQL ni manifeste de schéma
   (§1.2) : L5a n'a rien à faire monter ni descendre.
2. La ligne est portée, pour l'ensemble du dépôt, par la réserve **R-L3-2-bis rattachée à L0** —
   `DECISIONS.md`, entrée du **2026-09-03**, l. 7284 (« [L3] *Migrations up/down exécutées sur
   staging* : la porte L3 attend-elle la remise en état de staging ? »).
3. Le mécanisme, lui, tourne : job CI **`6 · schema-diff (vs fichier 04)` = pass** sur `aaafcc5`, qui
   porte les trois garde-fous de migration levés à la seconde passe L3 (descente sur base vide, cycle
   up → down → re-up, descente de `0014` sur base **peuplée**).
4. Le bloc `ETAT.md` 11h30 note que `main` reste rouge sur `8 · deploy-staging` — **hors périmètre
   L5a**, et couvert par la même réserve.

### 6.5 Tout écran livré avec ses 4 états (03 §33.2) — ✅ **pour les 3 écrans du socle**

**03 §33.2 relu** : « **vide** (message qui dit quoi faire) · **chargement** (skeletons aux dimensions
finales — jamais de spinner plein écran) · **erreur** (cause + action, français clair) · **hors
ligne** (pastille discrète + rappel des capacités locales) ».

| Écran | vide | chargement | erreur | hors ligne |
| --- | --- | --- | --- | --- |
| **`EcranAccueil`** | `.test.tsx:210` « aucune mission ⇒ titre *aucune mission* + explication de ce qui manque (03 §17.6) » | `.test.tsx:198` « avant que la lecture locale réponde, un statut occupé — jamais un écran blanc » | `.test.tsx:280` `@critique` « si la lecture locale REJETTE, erreur en français (cause + action), sans tomber » | `PastilleSync` l. 226, testée l. 241/261 ; texte l. 115 « la collecte continue normalement hors ligne » |
| **`EcranStockage`** | `.test.tsx:141` « espace non mesurable ⇒ ni alerte ni occupation, une explication en français » | `.test.tsx:109` `@critique` « persistance INCONNUE ⇒ statut occupé, AUCUNE alerte » | `.test.tsx:155/163` `@critique` « persistance REFUSÉE ⇒ alerte française avec guidage » + `EtatErreur` l. 80 | l'écran **est** le guidage hors ligne (05 §31-2) |
| **`EcranDeverrouillage`** | **sans objet, motivé** | `.test.tsx:118` « pendant la dérivation, l'écran le montre et refuse une seconde soumission » | `.test.tsx:146` `@critique` « mot de passe refusé ⇒ erreur française, **sans le mot de passe dedans** » | `AIDE_HORS_LIGNE` l. 27-29 |

Le seul écran qui ne compose pas `ZoneEtat` est `EcranDeverrouillage`, et il **écrit son raisonnement
avant qu'on le lui demande** (l. 18-20) : « le chargement est porté par la coquille, l'erreur est
ci-dessous, **il n'y a pas d'état vide (l'écran EST le contenu)** et le hors ligne est le mode
NOMINAL — d'où la mention explicite plutôt qu'une pastille ». **J'accepte** : §33.2 exige quatre états
servis, pas quatre composants posés, et l'absence est motivée par écrit.

### 6.6 axe-core vert — ✅ **RÉSERVES R-B8 / R-L5a-6 FERMÉES, PAR EXÉCUTION**

`R-B8` (ma réserve de la porte P-B) et `R-L5a-6` (revue A29) disaient la même chose : `axe-core`
n'existait nulle part, donc la case était **incochable**, et « cochable à vide, ce que ce dépôt
refuse ». Elle est fermée, et je la coche sur **exécution**, pas sur installation :

```
✓ 2 [chromium] accessibilite-l5a.e2e.ts:158 › écran de déverrouillage : premier usage, aucune violation axe   (674ms)
✓ 1 [chromium] accessibilite-l5a.e2e.ts:164 › écran de déverrouillage : coffre existant, aucune violation axe (886ms)
✓ 3 [chromium] accessibilite-l5a.e2e.ts:173 › écran Aujourd'hui : état vide, aucune violation axe             (711ms)
✓ 4 [chromium] accessibilite-l5a.e2e.ts:184 › écran Stockage de l'appareil : aucune violation axe             (954ms)
✓ 5 [chromium] accessibilite-l5a.e2e.ts:199 › budget de dérivation : coffre + déverrouillage < 1 s            (504ms)
```

(job `5 · e2e (chromium)`, run 33737000867, sur `aaafcc5`.) **Les trois écrans livrés sont balayés,
et le balayage tourne dans un moteur de rendu réel** — le fichier explique l. 25-28 pourquoi un
balayage hors navigateur « rendrait un vert qui répond à une autre question ».

### 6.7 `@filrouge` vert sur **FIL-TPE ET FIL-GC** — ✅

| Fichier | Tests | Missions couvertes |
| --- | ---: | --- |
| `apps/api/tests/l1-filrouge.integration.test.ts` | 5 | FIL-TPE (l. 72) **et** FIL-GC (l. 92, 119), plus le cloisonnement des deux (l. 158) |
| `apps/api/tests/l3-filrouge.integration.test.ts` | 11 | les 4 étapes L3 **paramétrées sur les deux fixtures** (l. 350), plus « FIL-TPE garde ses 3+1 unités et ses 30 questions ; FIL-GC ses 150+1 et ses 135 » (l. 677) |

Les deux fichiers **verts** au job `4 · integration` sur `aaafcc5` :
`✓ integration apps/api/tests/l3-filrouge.integration.test.ts (11 tests) 5192ms` ·
`✓ integration apps/api/tests/l1-filrouge.integration.test.ts (5 tests) 8834ms`.

**Fait mesuré, sans valeur de réserve** : le fil rouge est un parcours **serveur** sur deux missions
canoniques (09 §4bis) ; il ne traverse pas le socle terrain, et rien au 07 ni au 09 ne l'exige. Je le
consigne pour qu'on ne lise pas ce vert comme une preuve portant sur `apps/field`.

### 6.8 README de l'app à jour — 🟡 **À JOUR SAUF UNE PHRASE, DEVENUE FAUSSE PAR LA FUSION**

`apps/field/README.md` (+65 l.) est substantiel et exact sur l'essentiel : carte des 13 modules du
socle, les deux règles de socle outillées, ce que le socle **refuse** et pourquoi. **Une phrase est
fausse à la tête** :

```
apps/field/README.md:46
  - `embarquerMission()` prépare le stockage puis **refuse** le premier pull : il dépend de L3d
    (figeage du questionnaire), non livré.
```

**L3d EST livré.** `apps/api/src/domaines/questionnaire/` existe (assembleur, dépôt, service),
`042fe76` est dans `main` avec le tag `v0.l3`, la porte `PORTE_L3_2026-09-02.md` est signée, et le
glob `[L3 (L3d)] apps/api/src/domaines/questionnaire/**` est au seuil de couverture à **100 %**. Le
vrai motif du refus est ailleurs, et les deux autres sources l'écrivent correctement :
`DECISIONS.md` l. 7446 (« l'obstacle restant est côté API », endpoint L6a) et `embarquement.ts:24-27`
(« L3d (figeage du questionnaire) est livré ; l'obstacle restant est l'endpoint serveur, que L6a
livre et consomme »). **Le README a gardé la version d'avant la fusion.** → réserve non bloquante
**NB3**.

### 6.9 Aucun TODO / FIXME sans entrée — ✅ **pour L5a**

```
$ grep -rn 'TODO|FIXME|XXX:' apps/field/src apps/field/sw apps/field/scripts \
      packages/shared/src/sync.ts e2e/accessibilite-l5a.e2e.ts e2e/pwa-servie.e2e.ts
(sortie vide)
```

**Zéro marqueur introduit par L5a.** La réserve **R-B9** (3 marqueurs hérités, tous adossés à une
décision) reste inchangée et hors périmètre.

### 6.10 diff schéma-vs-04 = zéro écart — ✅

Job CI **`6 · schema-diff (vs fichier 04)` = pass** (36 s) sur `aaafcc5`. Et le fait qui rend ce vert
non ambigu : **L5a ne touche aucun fichier de schéma** (§1.2), donc l'écart ne peut être ni créé ni
masqué par cet incrément. Le seul « schéma » livré est **local** (Dexie, `local/base.ts` +
`local/formes.ts`), hors du champ du manifeste `schema-manifest.json`.

### 6.11 Récapitulatif DoD

| # | Ligne | Verdict |
| --- | --- | --- |
| 1 | lint + typecheck = 0 | ✅ |
| 2 | tests verts, aucun skippé | ✅ (1 997 tests, 0 skip) |
| 3 | couverture ≥ 90 % modules critiques, mesurée | ✅ (97,87 / 97,87 — recoupée) |
| 4 | migrations up/down sur staging | ⚪ hors périmètre L5a — **R-L3-2-bis, rattachée à L0** |
| 5 | 4 états par écran | ✅ (3 écrans) |
| 6 | axe-core vert | ✅ (R-B8 / R-L5a-6 fermées) |
| 7 | `@filrouge` sur FIL-TPE **et** FIL-GC | ✅ |
| 8 | README à jour | 🟡 **NB3** |
| 9 | aucun TODO/FIXME sans entrée | ✅ |
| 10 | diff schéma-vs-04 = zéro | ✅ |

**8 cochées · 1 hors périmètre et tracée · 1 sous réserve non bloquante.**

### 6.12 Mes critères propres (gabarit A02)

| Critère | Verdict | Preuve |
| --- | --- | --- |
| Matrice E1-E47 à jour **dans les deux sens** | ✅ | §3, §4, et la section **M** ajoutée à `docs/TRACABILITE_E1-E47.md` |
| Plafond étage 1 (0,5 j / lot) respecté | 🟡 **NON VÉRIFIABLE** | le tableau « Compteur du plafond étage 1 » d'`AMELIORATIONS.md` (l. 23-31) **n'a aucune ligne L5a**. Un déplacement d'étage 1 est pourtant tracé — mais sur la ligne **L3b-d** (« port de sync L5a déplacé hors du glob réservé à L6a »). → **NB5** |
| Aucune fiche étage 2 implémentée avant arbitrage (11 §8-7) | ✅ | A-008 (AAD) et A-009 (icône) sont **proposées**, non implémentées ; A-009 livre un **défaut provisoire tracé**, pas la fonctionnalité |
| Verdict **A51** (sécurité) présent si le lot le concerne | 🔴 **ABSENT** | `grep -rn 'A51\|A54' DECISIONS.md docs/portes/ docs/ETAT.md \| grep -i 'l5a'` → **vide**. `ls docs/portes/` : `VERDICT_A51_L3_2026-09-02.md`, `VERDICT_A51_SECURITE_2026-08-31.md` — **aucun sur L5a**, alors que L5a est l'incrément de crypto du projet → **BLOQUANTE B2** |
| Verdict **A54** (UX novice) présent si le lot le concerne | ⚪ sans objet | le critère 07 « test novice < 30 min » porte sur le **lot L5 complet** (parcours guidé strict), pas sur un socle sans écran de collecte |

---

## 7. LES INTERDICTIONS DU 11 §2 / `CLAUDE.md` §2 — grep à l'appui

| Interdiction | Verdict | Preuve |
| --- | --- | --- |
| Fonction **SQL** de génération UUID v7 | ✅ aucune | aucun `.sql` au périmètre ; règle ESLint `UUID_APPLICATIF` (sélecteur `Literal[value=/uuid_generate_v7\|uuidv7…/i]`) **active sur `apps/field/src/**`** |
| **Next.js** | ✅ absent | `grep -rn 'next\.js\|from .next' apps/field packages/shared/src/sync.ts` → seule occurrence : `apps/field/package.json:6`, « **JAMAIS Next.js (11 §2)** » dans la description. Aucun `"next"` dans aucun `package.json` |
| **Prisma** / ORM générateur de schéma | ✅ absent | `grep -n '"@prisma\|"prisma"' package.json apps/*/package.json packages/*/package.json` → vide |
| **CORS** | ✅ absent | aucun `cors` en dépendance ; aucun en-tête `Access-Control-*` dans `infra/caddy/fronts.static.caddy` ; les deux fronts et l'API restent sous le même domaine |
| **MinIO** exposé publiquement | ✅ sans objet | aucune occurrence de `minio` ni du port 9000 dans le périmètre |
| **Donnée personnelle dans les logs** | ✅ **structurellement impossible ici** | `grep -rn 'console\.\|logger\.\|pino' apps/field/src apps/field/sw` (hors tests) → **aucune occurrence**. Le front ne journalise rien. Règle jumelle côté index local : §7.1 |
| **Secret versionné** | ✅ | job CI **`gitleaks` (bloquant — 02 §30.4-5) = pass** sur `aaafcc5` ; aucun `.env` au périmètre |
| **Test désactivé / skippé** | ✅ | `check-no-skipped-tests` code 0 sur 95 fichiers, **liste d'exceptions vide** ; job CI dédié `pass` |
| Dépendance hors liste 11 §1 (§8-1) | ✅ **tracée** | `fake-indexeddb 6.2.5` et `@axe-core/playwright 4.13.0` → `DECISIONS.md` l. **6706**, règle « silence vaut accord » du 2026-08-31, **amendement horodaté du 11 §1** déclaré. `workbox-build 7.4.1`, `workbox-precaching`, `workbox-routing` = **famille Workbox 7 déjà épinglée**, même version — pas un ajout hors liste. `dexie-react-hooks` est nommé au 11 §1 |
| Route non listée §8 / §24.2 (§8-6) | ✅ sans objet | aucune route livrée |

### 7.1 L'étanchéité de l'index local — le contrôle que 06 §10.5 impose et que rien n'oblige à faire

`LOT_L5.md` §3.2 pose la règle jumelle de la *redaction* pino : « **aucune donnée personnelle ni
contenu de réponse dans un index local** ». Elle est vérifiée par balayage, pas par relecture :
`apps/field/src/local/etancheite.test.ts` — **29 tests**, dont :

- l. 452 « le scénario a produit des lignes dans plusieurs tables (**le balayage ne porte pas sur le
  vide**) » — le test qui empêche le test de passer par vacuité ;
- l. 487 `@critique` « aucune sentinelle n'apparaît non plus **après normalisation de casse** » ;
- l. 502 « les colonnes en clair sont **celles de la liste fermée du §3.2** » ;
- l. 542-622 **contrôle positif** : `personName`, `personEmail`, `value`, `note`, `participants`,
  contenu de note volante et `payload` d'op se **déchiffrent** bien — donc ils étaient chiffrés ;
- l. 609 `@critique` « les lignes **DESCENDUES du siège** sont chiffrées au repos comme les lignes
  saisies ».

La liste fermée a été **amendée deux fois le 2026-09-02**, chaque fois par une décision au format
(`DECISIONS.md` l. 6801 et 7446), et la note de conception §3.2 porte les deux amendements datés.
Aucune des colonnes ajoutées (`supprimeLe`, `answerId`, `answerType`, `criticality`,
`orgUnits.parentId`) n'est une donnée personnelle : **la liste reste fermée**. ✅

---

## 8. RÉSERVES

### 8.1 Réserves **BLOQUANTES** (2)

#### **B1 — `apps/field/src/app/verrou.ts` : 227 lignes d'un livrable nommé au 11 §6, sans aucun test**

- **Sections engagées** : **11 §6** (ligne L5a : « … + **verrouillage** + … ») · **05 §9.7**, règle
  V2.10 en deux temps : « 15 min d'inactivité HORS session ; **pendant une session `en_cours`, le
  délai est porté à 60 min** — l'inactivité se mesure sur TOUTE interaction (tactile, clavier,
  scroll), et le **Screen Wake Lock** maintient l'écran éveillé […] **Bouton de verrouillage manuel
  d'un geste** sur toutes les vues terrain » · `LOT_L5.md` §2, ligne `src/app/verrou.ts`.
- **Exigences** : **E33** (sécurité / RGPD), **E23** (novice < 30 min) — les deux citées par le
  fichier lui-même, l. 30.
- **Preuve de l'écart** (mesurée, §6.3) :
  - couverture v8 de `apps/field/src/app/verrou.ts` : **0,00 % de lignes** ;
  - `ls apps/field/src/app/` : **aucun `verrou.test.ts`** ;
  - `grep -rn 'useVerrou' apps/field/src e2e` : **un seul appelant**, `app/contexte.tsx:220` — lui
    aussi à **0,00 %** ;
  - aucun des 45 tests Playwright n'avance un minuteur de 15 ou 60 minutes (les 4 balayages axe
    chargent l'application, ils ne la laissent pas inactive un quart d'heure).
- **Pourquoi c'est bloquant.** Le verrou n'est pas un confort : c'est le seul rempart quand la
  tablette est posée ou volée, et son défaut le plus coûteux — se déclencher **en entretien** — est
  précisément celui que la règle des 60 min existe pour éviter. Un minuteur non testé est un minuteur
  dont personne ne connaît le comportement au changement d'onglet, à la reprise après veille, ou
  quand `sessionActive` bascule. **L5b et L5c vont construire sur ce socle** : le prouver maintenant
  coûte un fichier de test à minuteurs simulés ; le prouver plus tard coûtera un incident en mission.
  C'est exactement l'angle mort que le seuil de couverture ne pouvait pas voir,
  `apps/field/src/app/**` n'ayant jamais été déclaré.
- **Ce qui la lève** : un fichier de test écrit par **un autre agent que A24** (09 §5.6 — A26 ou A27)
  couvrant au minimum les deux délais 15 / 60, la bascule quand `sessionActive` change, la remise à
  zéro sur *toute* interaction, `verrouillerMaintenant`, et le comportement quand `wakeLock` est
  indisponible. **Ou** l'inscription motivée de `apps/field/src/app/verrou.ts` dans
  `cheminsCritiques` — mais alors avec un chiffre au-dessus de 90, jamais avec un glob élargi.
- **Ce n'est pas un veto** : aucune section du pack n'écrit « le verrou doit être testé », et le plan
  de tests de `LOT_L5.md` §4 — **validé à l'étape 1bis par A01 et par moi** — ne le listait pas. La
  faute est partagée, et je l'inscris aussi à mon débit (§10.5).

#### **B2 — Aucun verdict A51 sur l'incrément qui livre TOUTE la crypto locale du produit**

- **Sections engagées** : 09 §1 (A51, revue de sécurité) · **06 §10.5** (« IndexedDB chiffré ») ·
  **05 §9.7** (architecture DEK/KEK) · **11 §8-4** (« toucher à la sécurité/crypto autrement que
  spécifié » n'est jamais autopiloté) · gabarit A02 (« verdicts A51 présents quand le lot les
  concerne »).
- **Preuve de l'écart** : `grep -rn 'A51\|A54' DECISIONS.md docs/portes/ docs/ETAT.md | grep -i 'l5a'`
  → **sortie vide**. `ls docs/portes/` : `VERDICT_A51_L3_2026-09-02.md`,
  `VERDICT_A51_SECURITE_2026-08-31.md` — **aucun sur L5a**.
- **Pourquoi c'est bloquant.** L5a est *l'*incrément de sécurité du projet, et il contient au moins
  quatre points qu'une revue A51 doit voir plutôt que de découvrir à P-C :
  1. les **paramètres Argon2id** ont été *choisis par l'implémenteur* (profil OWASP `m = 47 104 Kio,
     t = 1, p = 1`) parce que le pack fixe l'algorithme et pas les paramètres — escaladés, confirmés
     par Williams (`DECISIONS.md` l. 6848), mais **jamais examinés par A51** ;
  2. **AES-GCM sans AAD** : une enveloppe déchiffrable l'est *quelle que soit la ligne où on la
     colle* (fiche **A-008**, étage 2, non arbitrée) ;
  3. le **refresh token de 30 jours** est stocké dans Dexie sous la DEK (`local/jetons.ts`) ;
  4. la **liste fermée** des colonnes en clair a été **élargie deux fois en une journée** — chaque
     élargissement est motivé et tracé, mais une liste qui s'élargit deux fois mérite un regard qui
     n'est pas celui de ceux qui l'ont élargie.

  La revue croisée A29 a fait un travail sérieux (5 bloquants, 10 réserves) — mais A29 est la revue
  **de code**, pas la revue **de sécurité**, et 09 §1 les distingue.
- **Ce qui la lève** : un `docs/portes/VERDICT_A51_L5a_<date>.md`, **ou** l'arbitrage écrit d'A01
  reportant explicitement la revue A51 à la porte P-C avec les quatre points ci-dessus en pièce
  jointe. **Un report écrit me suffit ; un silence, non.**

### 8.2 Réserves **NON BLOQUANTES** (7)

| # | Réserve | Preuve | Ce qui la lève |
| --- | --- | --- | --- |
| **NB1** | **`LOT_L5.md` §1 n'a pas été amendé pour le descope du premier pull.** Le tableau §1 promet toujours que L5a livre « embarquement d'une mission (**premier pull, complet**) », alors que `DECISIONS.md` l. 7446 le descope vers L6a. La décision elle-même ne mentionne que « `LOT_L5.md` §3.2 amendé, daté » | `docs/conception/LOT_L5.md` §1, ligne L5a, colonne « Livre » — inchangée : `git diff 508ae15 aaafcc5 -- docs/conception/LOT_L5.md` ne touche que §3.2 | une ligne d'amendement daté au §1, comme celle déjà posée au §3.2 |
| **NB2** | **La décision de descope déclare « Impact spec : aucun sur `/docs` », mais le 11 §6 EST dans `/docs` et il liste « pull mission » pour L5a.** Le champ sous-estime l'impact | `DECISIONS.md` l. 7446 (champ `Impact spec`) vs `docs/11_CONTRAT_TECHNIQUE.md` §6, ligne L5a | une entrée complémentaire portant l'amendement horodaté du 11 §6, ou une reformulation du champ |
| **NB3** | **`apps/field/README.md:46` est faux à la tête** : « il dépend de L3d (figeage du questionnaire), **non livré** ». L3d est livré, tagué `v0.l3`, porte signée | §6.8 | remplacer par le motif exact déjà écrit en deux autres endroits (endpoint L6a) |
| **NB4** | **`local/contrat-sync.ts` : un commentaire faux, et un import qui le suit.** L. 4-10 : « `packages/shared/src/sync.ts` […] n'est **PAS encore** ré-exporté par le baril `index.ts` : ce fichier appartient à un autre agent ». **Faux dès le premier commit de l'incrément** : `git log -S"export * from './sync.js';" -- packages/shared/src/index.ts` désigne **`0d4daf4`**, c'est-à-dire L5a lui-même. Conséquence : l. 29 et 41 importent par `'../../../../packages/shared/src/sync.js'` alors que `formes.ts:38` importe correctement par `'@axion/shared'`. Le paquet s'exporte par `./dist/index.js` (son `package.json`) : **le même paquet est donc résolu par deux chemins dans la même app** — la source pour le contrat de sync, le `dist` pour le reste. Sans effet aujourd'hui (`formes.ts` n'importe que des **types**), mais deux instances de `ENTITES_SYNC` ou d'un schéma Zod ne sont pas la même valeur | `contrat-sync.ts:4-10, 29, 41` · `packages/shared/src/index.ts:26` · `packages/shared/package.json` (`exports`) | remplacer les deux imports par `'@axion/shared'` et supprimer le commentaire — ou corriger le commentaire si le détour est voulu, et dire pourquoi |
| **NB5** | **Aucune ligne L5a au compteur du plafond étage 1.** Le tableau d'`AMELIORATIONS.md` (l. 23-31) s'arrête à `L3b-d`. Le seul geste d'étage 1 identifiable de L5a (port de sync déplacé hors du glob réservé à L6a) est compté sur la ligne **L3b-d**. Le plafond 0,5 j/lot est donc **invérifiable** pour L5a — je ne peux ni le cocher ni le contester | `AMELIORATIONS.md` l. 23-31 | une ligne `L5a` au compteur, avec le consommé |
| **NB6** | **Le budget A28 « chiffrement < 50 ms/écriture » (11 §4) n'est mesuré nulle part.** Seule la dérivation < 1 s l'est. Il porte pourtant sur `ecrireLocal`, cœur de L5a | `grep -rn '50 ms\|50ms' apps/field/src e2e` → aucune assertion ; `accessibilite-l5a.e2e.ts:198` ne teste que la dérivation | une mesure, au même endroit que l'autre budget. **Limite déjà nommée et acceptée** : la machine de CI n'est pas un iPad ; le relevé sur tablette réelle reste dû à A27 à P-C (11 §7) |
| **NB7** | **Le contournement `ChampTexte` sans nature « secret » n'est documenté sur aucune fiche de cette branche.** `EcranDeverrouillage.tsx:12-16` compose directement les classes du design system parce que `ChampTexte` retire `type` de ses propriétés. La fiche qui le décrit — **A-011** — existe sur `lot/l7a`, pas ici (`git show origin/lot/l7a:AMELIORATIONS.md \| grep A-011` → l. 1626). Sur `lot/l5a` prise seule, le contournement est muet | `AMELIORATIONS.md` de la branche : A-009 → **A-012**, sans A-010 ni A-011 | rien à faire si `lot/l7a` fusionne ; à noter si l'ordre change. **Le trou de numérotation A-010/A-011 n'est PAS une perte** : les deux numéros sont pris sur `lot/l7a`, vérifié |

### 8.3 Écarts **non documentés** (motifs de veto potentiels) — **AUCUN**

J'ai cherché spécifiquement les écarts qui n'auraient **ni** entrée `DECISIONS.md` au format, **ni**
fiche `AMELIORATIONS.md`. Tout ce que j'ai trouvé de divergent est tracé :

| Divergence | Trace |
| --- | --- |
| Premier pull non livré (11 §6) | `DECISIONS.md` l. 7446, décideur A01 |
| Paramètres Argon2id choisis, non lus dans le pack | l. 6735 (escalade) + l. 6848 (confirmé Williams) |
| AES-GCM sans AAD | l. 6759 + fiche **A-008** |
| État `validé` sans colonne au 04 | l. 6780, obligation transmise à L6a |
| Liste fermée §3.2 élargie deux fois | l. 6801 et 7446 + note §3.2 amendée et datée |
| Icône PWA provisoire | l. 7388 + fiche **A-009** |
| `fake-indexeddb`, `@axe-core/playwright` hors 11 §1 | l. 6706, amendement horodaté du 11 §1 |
| `pwa-404` → `pwa-servie` (inversion d'un garde L0) | l. 7365 (revue A29, bloquant B1) |
| Port de sync hors du glob `src/sync/**` | compteur étage 1 (ligne L3b-d) + en-tête `port-sync.ts:17-27` |
| Instabilité CI `l0-restauration` | fiche **A-012** |

**Pas de veto.** Le droit de veto s'exerce sur l'écart **non documenté** ; ici la documentation est
présente, et souvent meilleure que ce que la règle exige.

### 8.4 Deux arbitrages que `LOT_L5.md` §5 exigeait **avant la première ligne de code**, et qui n'ont pas de trace

Ce n'est pas un veto — le résultat livré est conforme au pack dans les deux cas — mais c'est un
manquement à l'étape **1bis**, et il me concerne puisque j'ai co-validé la note.

`LOT_L5.md` §5 pose **six** points « à arbitrer avant la première ligne de code ». Recherche
exhaustive dans `DECISIONS.md` :

| Point §5 | Sujet | Tracé ? |
| --- | --- | --- |
| **§5-1** | `vite-plugin-pwa` hors 11 §1 → proposition : `workbox-build` en `injectManifest`, aucune dépendance nouvelle | ❌ **aucune entrée**. La proposition a été **implémentée** (`scripts/build-sw.mjs:31`). Je juge le résultat **conforme** (`workbox-build 7.4.1` appartient à la famille « Workbox 7 » du 11 §1, même version que `workbox-precaching`/`workbox-routing`) — mais la note disait « **Tranché avant L5a, pas pendant** » |
| **§5-2** | `axe-core` absent du dépôt | ✅ `DECISIONS.md` l. 6706, et cité nommément (« la note L5 §5-2 ») |
| **§5-3** | qui écrit `packages/shared/src/sync.ts` | ❌ **aucune entrée** — et c'est le plus gênant : `local/contrat-sync.ts:4` **affirme** « arbitrage A01 sur le point `LOT_L5.md` §5-3 ». `CLAUDE.md` §7 : « **Une décision non tracée dans ce format n'existe pas.** » Le code cite donc un arbitrage qui n'existe pas au registre. Le **résultat** reste conforme (11 §4 définit le format d'op ; l'`outbox` de L5a devait déjà l'avoir), seule la trace manque |
| **§5-4** | le port inerte ira-t-il jusqu'à P-C ? — **« A02 doit dire si le critère 07 *écran Aujourd'hui, sync par mission* est cochable dans cet état »** | **JE RÉPONDS AU §9.1** |
| **§5-5** | défaut applicatif d'`interviews.mode` | sans objet à L5a (L5c / L6a) |
| **§5-6** | « parcours express R1 » — devine interdite, Williams | **toujours ouvert**, hors périmètre L5a |

→ consigné comme doute de spécification (§9.2-2), pas comme réserve bloquante : deux entrées
`DECISIONS.md` rétrospectives ferment le sujet.

---

## 9. CE QUE LE GARDIEN DOIT DIRE, ET LES DOUTES À PORTER

### 9.1 Réponse au point `LOT_L5.md` §5-4 — la question m'était nommément adressée

> « **A02 doit dire si le critère 07 *écran Aujourd'hui, sync par mission* est cochable dans cet
> état**, ou s'il descend en L6b comme l'habilitation est descendue en L3d. »

**Réponse : NON, il n'est pas cochable — et il ne l'est pas parce qu'il n'appartient pas à L5a.**

1. **Le critère est un critère de LOT, pas d'incrément.** Le 07, ligne L5, colonne démo, demande
   « écran *Aujourd'hui* §34.2 (**agenda agrégé**, à-revoir, **sync par mission**) ». L'agenda est
   L5c (11 §6), la sync est L6a/L6b. Le cocher à L5a serait le cocher à vide.
2. **Ce que L5a livre est le bon comportement, et il faut le dire aussi.** `portSyncInerte` rend
   `{ statut: 'indisponible' }`, l'écran l'affiche tel quel, et **il est testé** que la pastille ne
   verdit pas (`EcranAccueil.test.tsx:241`). C'est l'honnêteté que `LOT_L5.md` §3.6 réclamait : « une
   pastille qui verdit sans serveur, c'est exactement le garde-fou qui annonce plus qu'il ne fait ».
   **Je valide ce choix sans réserve.**
3. **Conséquence pour la porte P-C** : le critère « écran *Aujourd'hui*, sync par mission » **ne peut
   pas être coché tant que L6a/L6b ne sont pas livrés**. Soit P-C se tient après L6b, soit le critère
   descend explicitement à la porte suivante — **c'est un choix de séquence produit : il appartient à
   Williams**, et je le porte au §9.2-1.

### 9.2 Doutes de spécification à porter en `DECISIONS.md`

1. **[L5a → P-C, Williams]** Le critère 07 ligne L5 « écran *Aujourd'hui* §34.2 (agenda agrégé,
   à-revoir, **sync par mission**) » est incochable avant L6b. **P-C se tient-elle après L6b, ou le
   critère descend-il à la porte suivante ?** (Précédent : l'habilitation descendue de L2 vers L3d.)
   *Devine interdite : la réponse change l'ordre des lots.*
2. **[L5a → A01]** Deux arbitrages exigés par `LOT_L5.md` §5 **avant** le code n'ont pas d'entrée :
   §5-1 (construction du service worker) et §5-3 (qui écrit `packages/shared/src/sync.ts`) — ce
   dernier étant **cité comme existant** par `local/contrat-sync.ts:4`. Deux entrées rétrospectives,
   au format, ferment le sujet et rendent le commentaire vrai.
3. **[L5a → A01]** L'entrée du 2026-09-02 qui descope le premier pull déclare « Impact spec : aucun
   sur `/docs` » alors que **11 §6, ligne L5a, liste « pull mission »**. Faut-il un amendement
   horodaté du 11 §6, et l'amendement du `LOT_L5.md` §1 qui va avec (NB1) ?
4. **[gouvernance → A01]** `apps/field/src/app/**` (961 lignes, dont le verrou de 05 §9.7) n'est ni
   dans `cheminsCritiques` ni dans `cheminsAttendus` de `coverage-critical-paths.json`. **La ceinture
   qui détecte les « modules critiques livrés hors seuil » ne peut réclamer que ce qui a été prévu.**
   Faut-il déclarer `apps/field/src/app/verrou.ts` comme module critique (sécurité 05 §9.7), ou
   assumer que le seuil ne couvre pas la coquille ? *La réponse conditionne B1.*

---

## 10. CE QUE JE N'AI PAS PU VÉRIFIER, ET QUI DOIT ÊTRE DIT

Un garde honnête sur sa portée vaut mieux qu'un garde qui rassure.

1. **Le mode avion RÉEL sur iPad** — non exécuté par moi, non exécutable en CI. Limite **déjà assumée
   au 11 §7** (« les service workers sous iOS ne sont PAS couverts par Playwright — le mode avion
   réel se rejoue à la main aux portes P-C et P-E »). Reste dû à A27.
2. **Les budgets sur matériel cible** — la dérivation < 1 s est mesurée sur un *runner* GitHub, pas
   sur un iPad. Le fichier e2e le dit lui-même (l. 34-37). Le chiffrement < 50 ms/écriture n'est pas
   mesuré du tout (**NB6**).
3. **`pnpm verify` complet, `pnpm test:integration` et `pnpm test:e2e` : non exécutés par moi.** Ils
   demandent Docker (Testcontainers : Postgres, Redis, MinIO) et un `build` complet. Je m'appuie sur
   les jobs CI `4 · integration` et `5 · e2e`, verts sur la tête exacte, et je le dis plutôt que de
   laisser croire que j'ai tout rejoué.
4. **Mon environnement local est hors contrat** (Node v24.19.0 contre `>=22.11.0 <23`). Tout ce que
   j'ai exécuté localement — lint, typecheck, 276 tests de socle, la contre-mesure de couverture,
   `check-no-skipped-tests`, `check-tracabilite-exigences`, `check-decisions`, `check-invariants`,
   `check-graphe-modules` — est une **corroboration**, jamais une preuve autonome. Que ma
   contre-mesure de couverture reproduise le chiffre CI **à la deuxième décimale** (97,87 / 97,87 /
   99,01 / 93,69) est le meilleur argument que j'aie que les deux environnements disent la même chose
   **sur ce point précis**. Il ne s'étend pas au reste.
5. **Ma part dans B1.** Le plan de tests de `LOT_L5.md` §4 ne listait aucun test de verrou, et j'ai
   co-validé cette note à l'étape 1bis. **B1 est autant un défaut de ma validation qu'un défaut de
   l'implémentation.** Je l'inscris à mon débit et je le signale pour les notes de conception L5c et
   L6 : *un module nommé au 11 §6 doit avoir une ligne dans le plan de tests de sa note.*
6. **La revue croisée A29** a porté sur `1892df3`. Les commits ultérieurs — `2c754b2` (correctif du
   rejeu), `92944eb` et `d589b03` (A28) — ne touchent que du **code de test**, `formes.ts`
   (commentaire) et `build-sw.mjs` (précache de l'icône iOS), tous couverts par le rejeu tracé à
   `DECISIONS.md` l. 7467. **La chaîne de revue est intacte** ; je l'ai vérifiée par
   `git show --stat` sur les quatre commits, je ne l'ai pas supposée.

---

## 11. VERDICT

> # 🟡 ACCEPTÉ SOUS RÉSERVE
>
> **Réserves bloquantes : 2** — **B1** (`app/verrou.ts` : 227 lignes d'un livrable nommé au 11 §6,
> couverture mesurée 0,00 %, aucun test d'aucune sorte) et **B2** (aucun verdict A51 sur l'incrément
> qui livre toute la crypto locale du produit).
> **Réserves non bloquantes : 7** (NB1 → NB7).
> **Écarts non documentés : AUCUN. Code orphelin : AUCUN. Pas de veto.**

**Ce que je signe** (09 §1, chaîne de signature) : la **conformité** de l'incrément L5a au pack et sa
**traçabilité dans les deux sens**, sous les deux réserves bloquantes ci-dessus. Je ne signe ni
l'auto-revue (A24), ni la revue croisée (A29), ni la fin d'incrément (A20), ni le passage en porte
(A01), ni la porte (Williams).

**Ce que j'ai trouvé de meilleur, et qui mérite d'être dit** : cet incrément refuse trois fois de
mentir là où mentir aurait été plus simple — le port de sync qui rend `indisponible` plutôt qu'une
pastille verte, l'embarquement qui distingue « le stockage est prêt » de « les données sont là », et
le garde de mise à jour dont le défaut est `null` (« je ne sais pas ») et non `false` (« vas-y »).
Les deux règles de socle — une seule horloge, un seul port d'écriture — ne sont pas des consignes :
elles sont **outillées** dans ESLint, avec trois blocs disjoints parce que deux blocs se seraient
effacés l'un l'autre *en silence*, et ce piège a été **mesuré sur un fichier sonde** avant d'être
refermé. C'est ce niveau d'exigence qui rend les deux réserves ci-dessus visibles : sur un incrément
moins rigoureux, `verrou.ts` à 0 % serait passé inaperçu au milieu du bruit.

**Prochaine action attendue dans la chaîne de signature** :

1. **A20** (chef d'équipe front) affecte **B1** à **A26 ou A27** — jamais à A24, qui a écrit le
   module (09 §5.6) ;
2. **A01** tranche **B2** (verdict A51 sur L5a, ou report écrit à P-C) et pose les **deux entrées
   `DECISIONS.md`** du §9.2-2, plus l'arbitrage du §9.2-4 ;
3. **A01** signe alors le **passage en porte** ;
4. **Williams** tient la porte **P-C** et arbitre les fiches d'étage 2 en attente — **A-008** (AAD),
   **A-009** (icône de charte), **A-012** (instabilité `l0-restauration`) — ainsi que le doute de
   séquence du §9.2-1.

**Aucune PR n'a été ouverte ni fusionnée par cette passe. Aucune ligne de code n'a été modifiée.
Aucune ligne datée antérieure de `docs/TRACABILITE_E1-E47.md` n'a été réécrite.**

---

**Signature conformité + traçabilité : A02, gardien de la spécification — 2026-09-03.**
Contrôle tenu sur `aaafcc5269ac8440f3c78f75e2bea2fc198ce12d`, et sur rien d'autre.
