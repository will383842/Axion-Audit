# REVUE CROISÉE A29 — lot L5, incrément **L5d** (invariant 5 à l'écran) — 2026-09-08

> **Étape 4/7 du pipeline (09 §3).** Réviseur croisé front A29, **lecture seule** — il ne produit rien
> (09 §1) ; transcription par le pilote, sans amendement.
> **Périmètre relu** : `git diff origin/main...HEAD` (base `cde4f10`, head `874eefb`) — **7 fichiers,
> 811 insertions, 12 suppressions, 100 % relus.** Aucun fichier du dépôt modifié par A29 ; les
> mutations ont été jouées sur une copie hors dépôt, détruite après usage.
> **Origine du lot** : `ATTESTATION_A20_L5B_2026-09-08.md` — « un module d'invariant contourné par son
> appelant est un **défaut de lot**, pas une coquille ».

---

## VERDICT : 🟠 CONFORME SOUS RÉSERVE — 1 bloquante (R1), 4 à corriger, 5 remarques

Le correctif **ferme réellement les trois défauts** nommés par A20 et A02, et A29 l'a prouvé **par
mutation, pas par lecture**. Ce qui manque n'est pas la correction : **c'est la garde de ce qu'A22 a
inventé pour la faire.**

---

## 1. Les trois commits, et le croisement 09 §5.6 — TENU

| Commit | Étiquette | Fichiers | Nature |
| --- | --- | --- | --- |
| `c324119` | `[A26 seul]` | 2 | **100 % test** — garde rouge par construction |
| `4b0737b` | `[A24 seul]` | 1 | **100 % test** — amendement du test qui verrouillait le défaut |
| `874eefb` | `[A22 seul]` | 4 | **100 % production** |

**Zéro mélange production/test dans un même commit.** C'est précisément ce qu'A20 n'a pas pu attester
sur L5b (`d97b27b` mêlait 157 lignes d'e2e et de la production). Sur L5d, l'attribution est décidable
**par commit et par fichier**. Rien à redire.

## 2. Réserves

### R1 — 🔴 BLOQUANTE — le seul choix de conception de l'incrément n'est gardé par rien

`apps/field/src/session/fuseau.ts:74` · CLAUDE.md §4 étape 2 (TDD) · 09 §3 étape 4 · DoD §5.

Le repli « UTC nommé » — ce qu'A22 soumet explicitement à la revue — n'est vérifié par **aucune
assertion**.

**Mesure 1, couverture** (projet `interface`, dossier `journee`) :
`fuseau.ts` — **61,53 % de branches**, lignes non couvertes `17-19, 28-30, 74`. **Sous le seuil DoD.**

**Mesure 2, décisive** — mutation supprimant `MENTION_UTC` (`return enUtc;`), le repli redevenant un
UTC nu présenté comme heure du site :

```
Test Files  69 passed (69)
     Tests  1231 passed (1231)
```

> **La correction de L5d peut être défaite intégralement sans qu'un seul test rougisse.**

Et la branche est **vivante** : sonde `throw` → atteinte **6 fois**, sur `EcranFinDeJournee` au premier
rendu (`journee` encore `null`). **Traversée à chaque rendu, jamais assertée.**

**Pourquoi bloquante** : « un incrément qui corrige *une forme d'appel que rien ne gardait* ne peut pas
livrer une forme neuve que rien ne garde. Le motif se reproduit dans le même incrément qui le corrige. »

**Levée** : un commit `[A26 seul]` ou `[A24 seul]` — **jamais A22** (09 §5.6) — couvrant
`formaterDateHeureMission` sur ses branches : fuseau `null`, fuseau `''`, ISO illisible, **et la
présence de la mention**. Le module est pur, trois `it` suffisent.

### R2 — haute — L5d introduit le premier point de défaillance APRÈS une écriture commise

`sauvegarde.ts:391-398` et `:547` · `EcranRestauration.tsx:183-185` · invariant 7 lu à l'envers ·
invariant 8 (chemin de secours) · 03 §33.2.

Sur `origin/main`, entre `ecrireMeta(…cleEmbarquement…)` et le `return`, **aucune opération faillible**.
L5d y insère `await lireIdentiteMission(...)` : `get` Dexie + déchiffrement AES-GCM + parse Zod. Le
repli existe trois lignes plus haut (`if (ligne === undefined)`) — mais **l'échec de déchiffrement n'y
est pas routé**.

**Mutation** — `lireIdentiteMission` lève : `Unable to find an element with the text:
/sauvegarde restaurée/i` (×14). L'écran ne dit **jamais** le succès et tombe dans `classerEchec`, qui
affiche :

> « Vérifiez le mot de passe et le fichier, puis réessayez. **Rien n'a été modifié.** »

**C'est faux** : `appliquerDescente` a écrit la mission, `ecrireMeta` a posé la marque d'embarquement.
**L'auditeur qui vient de perdre sa tablette lit « rien n'a été modifié » sur un appareil où tout est
restauré.**

A22 avait écarté le `try/catch` (« une branche qu'aucun test honnête ne peut atteindre »). **A29 : il a
raison sur l'atteignabilité, et c'est le mauvais critère.** Le défaut est **l'ordre des effets** :
_une lecture de confort ne doit pas pouvoir annuler le compte-rendu d'une écriture déjà commise._
**Levée : 2 lignes.**

### R3 — à corriger — un fuseau IANA invalide fait PLANTER le rendu ; la console s'en protège, le terrain non

`fuseau.ts:70-71` (via `formaterDateHeure:29`) · le contre-exemple : `apps/hq/src/format/dates.ts:23-30`.

```
"Pacific/Kiritimati" => 07/09/2026 12:30
null / ""            => 06/09/2026 22:30 (heure UTC)
"Europe/Pariss"      => LEVE: RangeError Invalid time zone specified
"UTC+2"              => LEVE: RangeError Invalid time zone specified
```

`fuseauMission` vient de la base locale (`local/formes.ts:220` : `z.string()`, **aucune contrainte
IANA**), alimentée par un `.axionbackup` produit par un **autre appareil** ; 04:47 dit `timezone TEXT`.
Une `RangeError` pendant le rendu React **fait exploser l'écran de restauration — le chemin de secours**.

Le validateur existe et est importable (`packages/shared/src/temps.ts:134`, mémoïsé) ; la console
l'utilise. **Asymétrie franche entre les deux fronts sur la même règle, au détriment de celui qui n'a
ni réseau ni support.** Le risque **préexiste** sur `EcranAgenda.tsx:324` et `EcranAujourdhui.tsx:119` :
L5d ne le crée pas, il **l'étend** à l'écran de restauration, qui en était exempt.

### R4 — à corriger — C1 est aveugle à l'appel multi-ligne, et le code corrigé d'A22 EST multi-ligne

`invariant5-appelants.test.ts:80` et `:113-121`. `FUSEAU_DESARME` s'applique **ligne par ligne**. La
même mutation, réécrite sur 4 lignes (formatage Prettier standard, qui scinde dès 100 caractères),
passe **verte** alors que le désarmement est là. Et `EcranRestauration.tsx:366-369` — le code corrigé —
est lui-même multi-ligne.

**Aggravant** : la section « CE QUE CETTE GARDE NE VOIT PAS » (`:35-45`) est honnête sur deux angles
morts (`?.` et `{iso}` interpolé) — **et ne mentionne pas celui-ci**. _Une garde qu'on croit plus large
qu'elle n'est vaut moins qu'une garde qu'on sait étroite._

### R5 — à corriger — trois registres muets sur L5d

- **`AMELIORATIONS.md`** : aucune ligne. Or L5d livre trois choix d'étage 1 (repli UTC nommé, message
  de repli mission, règle d'unanimité multi-mission). Plafond 0,5 j **non mesurable**, donc non
  attestable — pas « dépassé », **non contrôlable**.
- **`DECISIONS.md`** : A22 écrit « Escalade à A01 » **dans un message de commit**. Ce n'est pas le canal
  (§7). En l'état, **l'escalade n'existait pas**. _(Fermée depuis : entrée A01 du 2026-09-08.)_
- **`docs/ETAT.md`** : aucun bloc L5d alors que trois étapes ont été franchies. **Responsabilité du
  pilote.**

### R6 — fusionnée avec R1 — branches de repli non exercées

| Branche | Fichier:ligne | Atteinte ? |
| --- | --- | --- |
| `fuseauDesMissions` — fuseaux divergents | `EcranFinDeJournee.tsx:97` | **non** |
| `lireIdentiteMission` — mission absente | `sauvegarde.ts:395` | **non** |
| repli UTC — mention rendue | `fuseau.ts:74` | traversée, **jamais assertée** |

## 3. Les sept questions du mandat — réponses

**A. Le repli « UTC nommé » : CONFORME, et ce n'est pas une troisième voie.** A29 tranche **pour** A22,
mais **pas sur son argument**. Le fait qui tranche est dans le dépôt : `apps/hq/src/format/dates.ts:34-47`
fait **exactement le même repli, avec la même mention**. La console — celle qu'A26 cite comme le modèle
vertueux — le pratique déjà. **A22 n'a pas inventé une troisième voie : il a rejoint celle de l'autre
front.** Le pack ne traite pas le cas « fuseau inconnu » ; entre taire l'instant, le rendre sans cadre
et le rendre en nommant son cadre, **seul le troisième ne ment pas**. Aucune branche n'utilise le
fuseau de l'appareil (vérifié par lecture **et** par mutation).

**B. La signature : le blocage est réel, mais PARTIEL — A22 avait une voie qu'il n'a pas vue.**
Le test `peripherie-entretien.test.ts:634-638` bloque bien, et le réécrire lui est interdit. **Mais le
coût exact est de 7 erreurs TS : 5 en production, 2 dans le test.** Et
`formaterDateHeureMission(iso, string | null)` est **déjà** incompatible avec `string | undefined` :
A22 pouvait **router les cinq appelants de présentation vers le couple `*Mission`** (+1 fonction
~10 lignes) et **laisser les deux aînées intactes** — le test serait resté vert **sans une ligne
modifiée**. Ce qui aurait quand même justifié d'escalader : cela change **ce que cinq écrans affichent
pendant leur chargement**, ce qui excède le périmètre borné par A20.

> **Conclusion : l'escalade est fondée, son motif doit être rectifié.** Pas « impossible sans réécrire
> un test », mais « possible sans toucher aux tests, au prix d'un changement d'affichage sur cinq
> écrans qui dépasse le périmètre ». **Un blocage devient une décision de périmètre** — ce qu'A01 sait
> trancher.

**C. Sauvegardes anciennes : lisibles.** `VERSION_FORMAT_SAUVEGARDE` inchangée, aucun octet de
`format.ts` ni de la crypto modifié. Sans ligne de mission → repli correct **mais jamais exécuté par un
test**. **Désaccord sur le `try/catch`** → R2.

**D. La garde d'A26 fait ce qu'elle prétend**, éprouvée par 5 mutations (M1 le défaut d'origine, M2
l'UUID nu, M3 l'ISO brut, M4 le contournement par `resolvedOptions().timeZone`, M7 le repli sur
`undefined`) : **toutes rougissent, et nomment le bon endroit** avec fichier:ligne et le décalage de
**jour civil**, pas d'heure. Deux angles morts : R4 et r4.

**E. L'amendement d'A24 : durcissement net, aucune perte.** 14 `it(` et 13 `@critique` avant comme
après, aucun skip. La mutation M2 (retour à l'UUID) fait rougir son fichier : **la ligne est une garde
active.** Et 377 est franchement durcie.

**F. La règle d'unanimité multi-mission : non éprouvée, mesuré.** Sonde : la branche `fuseaux.size > 1`
(`EcranFinDeJournee.tsx:97`) **n'est atteinte par aucun test**. A22 est honnête (« raisonnée, pas
éprouvée »). Acceptable **si et seulement si** R1 est traitée — même garde.

**G. Croisement §5.6 : tenu et démontrable** (§1).

## 4. Les trois constats hors périmètre — verdict d'A29

1. **`agenda/jour.ts:252-253`, jour civil en UTC : hors périmètre ACCEPTÉ, mais à tracer.** Ce n'est pas
   l'invariant 5 mais **l'invariant 8** : le rappel se réarme à minuit UTC, soit **14 h locale sur une
   mission à UTC+14** — un rituel fait le matin couvre la matinée du lendemain. Incohérence interne
   notable : `local/depots/sessions.ts` **sait** faire ce calcul au fuseau de mission — c'est le motif
   même de son inscription dans `MODULES_AUTORISES`. **Le dépôt sait, et cet appelant-ci ne s'en sert
   pas.** Fiche `AMELIORATIONS.md` étage 2 attendue.
2. **L'UUID dans `nomFichierSauvegarde` : hors périmètre, et l'arbitrage est déjà écrit dans le code.**
   `format.ts:167-172` : « la mission est désignée par son UUID, pas par son titre » — mettre le titre
   exporterait **un nom de client sur une clé USB**, bien pire que 36 caractères hexadécimaux.
   **L'UUID n'est pas toléré malgré l'invariant 5 : il est exigé par l'invariant 2.** Rien à changer ;
   doute à clore en `DECISIONS.md` _(fait : entrée A01 du 2026-09-08)_.
3. **Le désarmement par `?.` sur cinq emplacements : hors périmètre cohérent**, mais ne se ferme
   qu'avec B. A26 les nomme lui-même comme angle mort assumé : **un travail identifié, pas un impensé.**

## 5. Les 8 invariants

| # | Verdict |
| --- | --- |
| 1 offline-first | **OK** — aucun réseau ; le titre vient de la base locale, pas d'une requête |
| 2 aucune référence client | **OK** — fixtures fictives ; `nomFichierSauvegarde` **renforce** l'invariant |
| 3 RBAC / propriété | sans objet |
| 4 aucune couleur/taille en dur | **OK** — grep sur les lignes ajoutées → **0** |
| 5 français / fuseau de mission | **ÉCART FERMÉ, non consolidé** — les 3 défauts fermés et prouvés par mutation ; réserves R1 et R3. **L'écart d'A02 (7/8 + 1) est levé sur le fond** |
| 6 le terrain collecte | **OK** — un `get` + un déchiffrement + un parse |
| 7 rien d'écrasé silencieusement | **ÉCART (R2)** — les données sont intactes ; **le compte-rendu peut nier une écriture commise** |
| 8 sauvegarde terrain | **OK sur le format, dégradé sur le chemin** — R2 et R3 fragilisent l'écran de secours |

**11 §4** : sans objet. **UX §33.2** : 4 états inchangés, aucun retiré. **09 §5.7** : aucune
simplification temporaire, aucun `TODO`/`FIXME`, aucune dépendance ajoutée. **Code orphelin** : aucun.

## 6. Mesures exécutées par A29

`pnpm build` EXIT=0 · `vitest interface journee+entretien` **247/247, 0 skip** · `vitest unit session/`
EXIT=0 · `pnpm verify:rapide` EXIT=0 (**1167/1167** interface) · suite `apps/field` sur copie
**1231/1231** · `git show --stat` ×3 · `git status --porcelain` vide sur les deux worktrees.

## 7. Ce qu'A29 n'a pas pu vérifier

Node **v24.19.0 hors contrat** — les résultats `Intl` (dont la sonde `RangeError` de R3) sont à
confirmer en CI sous Node 22 · **Docker arrêté** : `pnpm verify` complet non joué, la CI fait foi ·
**iPad physique / service worker iOS** (11 §7) : non joué, rien ici ne le remplace · couverture DoD
complète non attestée (mais **`fuseau.ts` à 61,53 % de branches est un chiffre mesuré, sous le seuil**)
· axe-core et fil rouge non joués.

## 8. Désaccords à arbitrer par A01

1. **La signature (point B)** — l'escalade est fondée, le motif à rectifier. Le changement d'affichage
   pendant le chargement sur 5 écrans entre-t-il dans L5d, ou ouvre-t-il un L5e ?
2. **Le `try/catch` de R2** — A22 assume l'absence ; A29 la refuse sur le principe de l'ordre des
   effets. 2 lignes.
3. **R1 est-elle bloquante ?** A29 la tient pour telle et maintient.

## 9. Remarques mineures

r1 `'Tablette fictive dorigine'` (apostrophe manquante) · **r2** le test « aucun UUID nu » est rédigé
comme une règle générale mais n'est appliqué qu'à un écran — appliqué à `EcranFinDeJournee` après un
export il rougirait **à tort** ; sa formulation excède son champ · **r3** divergence de libellé entre
les fronts : `(UTC)` en console, `(heure UTC)` au terrain · **r4** `FORMATAGE_DIRECT` attrapera à tort
un futur `Number.toLocaleString('fr-FR')`, qui sert pourtant l'invariant 5 — faux positif latent ·
**r5** le brief situait la rectification « 8/8 → 7/8 » dans `CONTROLE_A02_PC_2026-09-07.md §6.2` ; elle
est en réalité dans `RECOCHE_A02_R1_2026-09-07.md:248`.

---

```
Signature revue croisée : A29 — 2026-09-08
Verdict : CONFORME SOUS RÉSERVE — R1 bloquante ; R2-R5 à corriger ; r1-r5 en remarques.
Aucun fichier du dépôt modifié par A29.
```

## 10. Suivi des réserves (tenu par le pilote)

| Réserve | Main | État |
| --- | --- | --- |
| **R1** (bloquante) | A26 | en cours — garde du repli UTC + R6 |
| **R2** | A22 | en cours — router l'échec vers le repli existant |
| **R3** | A22 | en cours — `fuseauIanaValide` de `packages/shared` |
| **R4** | A26 | en cours — C1 lit le fichier entier, ou nomme l'angle mort |
| **R5** | pilote | **FERMÉE** — entrée A01 du 2026-09-08 (`DECISIONS.md`), fiche `AMELIORATIONS.md` et bloc ETAT posés par ce commit |
| r2 | A26 | en cours — la garde doit **nommer** l'exclusion des noms de fichiers (exigé par A01) |
| B / signature | A01 | **TRANCHÉ** — `string \| null` requis ; **A26** amende le test, A22 route les appelants |
| constat 1 (jour civil UTC) | — | fiche étage 2, correctif autorisé par A01, incrément séparé |
