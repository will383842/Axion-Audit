# CONTRÔLE D'ACCEPTATION A02 — LOT L5 COMPLET (L5a + L5b + L5c) — 2026-09-06

> **Étape 6/7 du pipeline (09 §3).** Gardien de la spécification A02, **en lecture seule sur le
> code**. Ce fichier est le seul que j'écris, et **je ne le commite pas** (règle du 2026-09-05 :
> « un réviseur qui commite est un écrivain »).
> **Worktree** : `C:/Users/Will/Documents/_axpc`, détaché, `git status` **vide** à l'ouverture
> comme à la clôture.
> **Ordre de lecture appliqué** (CLAUDE.md §0, ligne L5, et rien d'autre — 09 §5.8) :
> `11_CONTRAT_TECHNIQUE` → `07` ligne L5 → `09` ligne P-C + §3/§4/§5.9 → `03` §33 intégral et
> §33.7 → `08` intégral + `TRACABILITE_E1-E47` → `docs/conception/LOT_L5.md`.

---

## 0. L'ÉTAT RÉEL, VÉRIFIÉ PLUTÔT QUE CRU

Le brief annonçait « `main` porte L5a et L5b ; L5c est en file de fusion (PR #52, CI verte,
auto-merge armé) ». **Trois de ces affirmations sont fausses ou périmées au moment de ma passe**,
et je les corrige avant tout jugement parce que la suite en dépend.

| Affirmation du brief | Ce que j'ai mesuré | Commande |
| --- | --- | --- |
| L5c est en file de fusion | **VRAI** — PR #52 `OPEN`, `mergeable: MEREGEABLE`, auto-merge **SQUASH armé** depuis `2026-09-06T06:15:52Z` par `will383842` | `gh pr view 52 --json state,mergeable,autoMergeRequest` |
| CI verte | **FAUX** — le dernier run **terminé** de la PR (`34016800692`) conclut **`failure`** (job « 7 · constructibilité des 4 images / hq », `unauthorized: authentication required`). Un run neuf était `IN_PROGRESS` (lint, unit) pendant ma passe | `gh run view 34016800692` |
| `main` porte L5a et L5b | **VRAI**, mais `main` a avancé pendant ma passe : `4dd1349` → **`d0d54e5`**, et **la CI de `main` est ROUGE** (run `34017217427`) sur DEUX jobs : `shellcheck` (« Valider le Caddyfile avec le vrai binaire Caddy ») et `couverture ≥ 90 %` (un test rouge : `AssertionError: expected 'sauvegarde: NOTIFICATION SORTANTE INA…' not to contain 'passe terminée avec succès'`) | `gh run view 34017217427` |

**Commit contrôlé : `8f936608e055b2b826b3e08c9209c3a9fd326307` (`origin/lot/l5c`).**
`origin/lot/l5c` **contient intégralement** `origin/main` au moment où je l'ai pris
(`git rev-list --left-right --count origin/main...origin/lot/l5c` → `0 71`, puis `1 71` après que
`main` a avancé d'un commit). C'est donc l'arbre qui porte L5 **complet**, et c'est sur lui que
porte tout ce qui suit. **Je le dis explicitement, comme le brief l'exige : L5c N'EST PAS dans
`main`.**

**Mon environnement est hors contrat, et je le déclare avant mes chiffres** : **Node v24.19.0**
contre `>=22.11.0 <23` (11 §1 ; avertissement « Unsupported engine » à chaque appel). Aucun Node 22
n'est installé sur cette machine (`C:/Program Files/nodejs` seul). Mes exécutions sont des
**corroborations**, pas la mesure de référence — la CI mesure sous Node 22.

---

## 1. VERDICT

> # 🔴 **REFUSÉ — VETO**
>
> **Motif principal, unique et suffisant** : **la fusion de PR #52 SUPPRIME de `main` le correctif
> de sécurité F-22 (CRITIQUE), F-23 et F-25**, code **et** tests. Sept symboles de sécurité et
> **27 cas de test** disparaissent du dépôt. L'auto-merge est **armé** : le jour où la CI verdit,
> la régression entre dans `main` sans qu'aucun humain la voie.
>
> **Trois autres bloquants indépendants** : ② la **revue croisée (étape 4) n'est pas close** sur
> l'arbre livré — quatre commits de code, dont celui qui ferme le bloquant B1, sont postérieurs au
> dernier verdict A29 ; ③ la **recette novice A54 de P-C a été jouée et conclut NO-GO** ; ④ les deux
> écrans qui ferment les bloquants d'A29 sont à **7,69 %** et **11,11 %** de couverture de lignes.
>
> **Écarts NON documentés : 1** (le n° V1 ci-dessous). Le droit de veto s'exerce sur celui-là.
> Les trois autres bloquants sont documentés ; ils n'appellent pas le veto, ils appellent le refus
> de l'étape 6.

**Comptes** : critères 07 ligne L5 : **1 coché sur 8** · DoD transverse : **5 lignes sur 10** ·
invariants : **6 tenus sur 8** · code orphelin : **1, documenté** · gloses de traçabilité fausses :
**aucune** · réserves : **4 bloquantes, 11 non bloquantes**.

---

## 2. LE VETO, ÉTABLI PAS À PAS

### V1 — **ÉCART NON DOCUMENTÉ, BLOQUANT** : la fusion de L5c annule le correctif F-22 (CRITIQUE), F-23 et F-25

**Sections engagées** : `docs/securite/VERDICT_A51_L5A.md` (F-22 **CRITIQUE**, « à faire AVANT le
merge de #30 ») · `docs/portes/REVUE_A29_L5a_securite_2026-09-05.md` §1 (« F-22 **FERMÉ** sur tout le
chemin ») · **06 §10.5** · **05 §9.7** · **invariant 7** (« rien n'est jamais silencieusement écrasé
ou supprimé ») · **invariant 8** · **11 §8-4** (« toucher à la sécurité/crypto autrement que
spécifié ») et **11 §8-5** (« désactiver ou skipper un test »). Exigences : **E33**, **E38**, **E43**.

**Ce que F-22 protège**, verbatim du verdict A51 : « un coffre ILLISIBLE se lit “ABSENT”, et
l'application invite alors l'auditeur à détruire sa DEK ». Une seule ligne `meta` altérée — écriture
partielle sur une tablette qui s'éteint, quota atteint, champ requis ajouté au schéma demain — fait
afficher « Préparer cet appareil », et le mot de passe de l'auditeur **écrase l'enveloppe de son
ancienne DEK**. Toutes les lignes restent là, et plus rien n'est lisible, **définitivement**.
A51 le classe CRITIQUE et note qu'il « se déclenche seul, sans aucun attaquant ».

**LA PREUVE, en quatre mesures indépendantes.**

**① Le fichier, commit par commit** — `git show <c>:apps/field/src/local/coffre-appareil.ts` :

| Commit | Rôle | `CoffreIllisibleError` | Lignes |
| --- | --- | --- | --- |
| `0d4daf4` | wip L5a, **avant** correctif | 0 | 183 |
| `ab6dcf5` | **L5a fusionné dans `main` (PR #30)** — porte le correctif | **3** | **286** |
| `a095d8b` | merge `main` → `l5c` (antérieur) | **0** | **183** |
| `4dd1349` | `main`, **parent du merge final** | **3** | **286** |
| **`8f93660`** | **`origin/lot/l5c`, HEAD contrôlé** | **0** | **183** |
| `origin/main` (`d0d54e5`) | tronc actuel | **3** | **286** |

`git log -1 --format=%P 8f93660` → `a095d8b 4dd1349`. Le merge avait donc **sous les yeux** la
version corrigée (`4dd1349`) et a retenu la version d'avant. **Ce n'est pas un conflit non résolu :
c'est une résolution qui a choisi le mauvais côté, et personne ne l'a vu.**

**② Le diff que le squash appliquerait à `main`** —
`git diff --stat origin/main...origin/lot/l5c -- apps/field/src/local/ apps/field/src/app/` :

```
 apps/field/src/app/EcranDeverrouillage.test.tsx |  86 ------
 apps/field/src/app/EcranDeverrouillage.tsx      |  85 +-----
 apps/field/src/app/contexte.test.tsx            |  81 +-----
 apps/field/src/app/contexte.tsx                 |  33 +--
 apps/field/src/local/coffre-appareil.test.ts    | 365 +-----------------------
 apps/field/src/local/coffre-appareil.ts         | 117 +-------
 apps/field/src/local/coffre.test.ts             | 135 ---------
 apps/field/src/local/coffre.ts                  | 201 -------------
 10 files changed, 98 insertions(+), 1075 deletions(-)
```

**③ Les symboles qui disparaissent du dépôt entier** (`git grep -c <symbole> <ref> -- apps/field packages/shared`) :

| Symbole | `main` | `lot/l5c` |
| --- | --- | --- |
| `CoffreIllisibleError` | 5 fichiers | **0** |
| `ParametresKdfHorsBornesError` (F-25) | 5 fichiers | **0** |
| `DonneesSansCoffreError` (2ᵉ ceinture F-22) | 2 fichiers | **0** |
| `verifierPolitiqueMotDePasse` (F-23) | 3 fichiers | **0** |
| `verifierParametresKdf` (F-25) | 3 fichiers | **0** |
| `MotDePasseTropCourtError` (F-23) | 3 fichiers | **0** |
| `AnomalieCoffreError` | 5 fichiers | **0** |

**④ Les tests supprimés** (`grep -cE "^\s*it\("`, `main` contre `l5c`) :

| Fichier | `main` | `l5c` | **perte** | dont nommant F-22/23/25 |
| --- | --- | --- | --- | --- |
| `local/coffre-appareil.test.ts` | 31 | 17 | **14** | 8 |
| `local/coffre.test.ts` | 29 | 23 | **6** | 3 |
| `app/EcranDeverrouillage.test.tsx` | 10 | 6 | **4** | 4 |
| `app/contexte.test.tsx` | 12 | 9 | **3** | 3 |
| **Total** | 82 | 55 | **27** | **18** |

Parmi eux, nommément, ce cas que la CI de `main` a exécuté ce matin (run `34017217427`) :
`✓ F-22 — un coffre ILLISIBLE ne se lit jamais « absent » (verdict A51, CRITIQUE)`.

**POURQUOI AUCUN GARDE-FOU NE L'ATTRAPE, ET C'EST LE POINT LE PLUS IMPORTANT.**
`check:no-skipped-tests` compte les tests **désactivés**, pas les tests **supprimés**.
Le job `couverture ≥ 90 %` **passe** sur `lot/l5c` (6 m 58 s, `✓`) : le code de sécurité **et** ses
tests ayant disparu ensemble, l'arbre est *cohérent* — il est simplement revenu à l'état
pré-correctif. **Une régression qui emporte le code et son test est verte par construction.** C'est
exactement la famille de défaut que ce dépôt traque, appliquée à son propre correctif critique.

**Ce que cette réserve exige pour être levée** (aucune de ces trois n'est un avis, les trois sont
mécaniques) :

1. **Rebaser ou refusionner `lot/l5c` sur `origin/main`** en conservant la version `main` des quatre
   fichiers de sécurité, puis **vérifier par la mesure ③** que les sept symboles sont revenus et par
   la mesure ④ que le compte de `it(` est ≥ 82 sur les quatre fichiers.
2. **Désarmer l'auto-merge de PR #52** jusque-là. Tant qu'il est armé, la régression est à un run
   vert de distance.
3. **Poser un garde de non-régression** : rien dans la CI ne compare le nombre de cas de test avant
   et après une fusion. Je le porte en doute de spécification (§9, D-2) plutôt que d'en faire une
   exigence — il n'est écrit nulle part.

> **Note de méthode, et elle vaut règle.** Ce défaut n'était visible ni dans le diff de la PR
> (13 322 insertions), ni dans la CI, ni dans aucun document de la branche. Il n'est apparu que
> parce que j'ai lu le *contenu* d'un fichier de sécurité au lieu de lire le *rapport* qui le
> déclarait fermé. **La revue A29 du 2026-09-05 affirme « F-22 FERMÉ sur tout le chemin » et elle
> a raison — sur la branche qu'elle a relue.** Elle n'a jamais relu celle-ci.

---

## 3. LES TROIS AUTRES BLOQUANTS

### B2 — **L'étape 4 du pipeline n'est pas close sur l'arbre livré**

**Sections** : 09 §3 (l'étape 6 suit l'étape 4) · 09 §5.6 · CLAUDE.md §4 · précédent A02 §A de
`TRACABILITE_E1-E47.md` : « un contrôle d'acceptation ne se tient pas sur un arbre qui bouge, **ni
avant la revue croisée** ».

- Verdict A29 initial (`docs/portes/REVUE_A29_L5c_2026-09-05.md`, sur `f20b7a0`) : **REFUSÉ**,
  4 bloquants.
- Addendum de rejeu (même fichier, sur **`4cc22dc`**) : **ACCEPTÉ SOUS RÉSERVE**, et il écrit
  noir sur blanc : « **Bloque encore la fusion et la porte P-C : B1** ».
- **B1 a été fermé APRÈS** : `git log --diff-filter=A -- .../EcranFinDeSession.tsx` → **`b2d0a90`**.
  `git cat-file -e 4cc22dc:apps/field/src/ecrans/journee/EcranFinDeSession.tsx` → **absent**.
- **Quatre commits de code L5c sont postérieurs à `4cc22dc`** et n'ont **jamais** été relus :
  `b2d0a90` (B1, M6), `227c2e1` (M5), `ea4265a` (M7), `c1f228d` (M1, M2, M8).
- `docs/ETAT.md`, dernier bloc L5c, le dit lui-même : « **Prochaine action : rejeu A29**, puis A02. »
  Ce rejeu n'a pas eu lieu ; aucun document ne le porte.

**Je ne peux pas signer l'étape 6 sur un incrément dont l'étape 4 est ouverte.** Ce que ça lève :
un troisième passage d'A29 sur `8f93660` (ou sur l'arbre refusionné de V1), rendu par écrit.

### B3 — **La recette novice A54 de la porte P-C a été jouée, et conclut NO-GO**

`docs/portes/RECETTE_NOVICE_L5_2026-09-06.md`, sur `origin/lot/l5-recette-novice` (`4f75c1f`,
**non fusionnée**), base `8f93660` — **le même commit que je contrôle**. Verdict littéral :

> **NO-GO.** « Le critère “novice < 30 min sans aide” n'est **pas mesurable** dans cet
> environnement, et il est **NON TENU sur la partie que j'ai pu jouer** : le novice est arrêté à
> **t+1 min** par un message d'erreur qui lui dit le contraire de la vérité, et **définitivement
> bloqué à t+3 min** sur un écran sans sortie. »

Les trois constats que j'ai **re-vérifiés dans le code** :

| Constat A54 | Vérifié par moi | Emplacement |
| --- | --- | --- |
| Sur « Première utilisation », presser le bouton sans mot de passe rend « **Mot de passe incorrect** » | **CONFIRMÉ par lecture** : `coffre.ts:143-145` — `deriverKek` lève `MotDePasseInvalideError` sur chaîne vide, et l'écran affiche ce message tel quel | `local/coffre.ts:104,145` |
| Cul-de-sac « Auditeur inconnu » sans bouton de retour | **CONFIRMÉ** : l'en-tête d'`App.tsx` ne porte que titre, indicateurs et « Verrouiller » ; `peutRevenir()` n'est consommé que par le geste système — **inexistant en PWA plein écran sur iPad** | `App.tsx`, `app/navigation.ts` |
| « Ce que cet appareil sait faire sans réseau » promet « **et des photos** » | **CONFIRMÉ** : `EcranAccueil.tsx:109`. La même mention a été retirée du cockpit (M6 d'A29) et **pas** de l'accueil L5a | `app/EcranAccueil.tsx:109` |

**Le critère 07 « test novice < 30 min (A54, guidé strict + grille §33) » est donc mesuré NON TENU.**
Ce n'est pas une absence de preuve : c'est une preuve contraire.

**Et le point d'A54 qui dépasse l'ergonomie mérite d'être au dossier de porte** : la promesse de
photo, non tenue, ne se vit pas comme une fonction manquante mais comme une panne ; l'auditeur prend
alors la photo avec **son téléphone personnel**, et la pièce d'audit sort du coffre chiffré, sort de
l'export de secours et sort de l'invariant 8. **Le coût du manque est RGPD et probatoire, et il est
créé par la promesse, pas par l'absence.** Retirer « et des photos » de `EcranAccueil.tsx:109` est
un geste d'étage 1 qui supprime ce risque tout de suite.

### B4 — **Les deux écrans qui ferment B1 et B2 d'A29 sont livrés quasi sans tests**

**Mesuré par moi**, `coverage/coverage-summary.json` produit par
`npx vitest run --project unit --project interface --coverage --testTimeout=60000` — **103 fichiers,
2151/2151 verts, RC=0** :

| Fichier | lignes | fonctions | branches | fichier de test |
| --- | --- | --- | --- | --- |
| **`ecrans/journee/EcranFinDeSession.tsx`** (302 l.) | **7,69 %** | **0,00 %** | 100 % | **AUCUN** |
| **`ecrans/journee/EcranRestauration.tsx`** (267 l.) | **11,11 %** | **33,33 %** | 100 % | **AUCUN** |
| `ecrans/journee/EcranAujourdhui.tsx` | 93,14 % | 70,58 % | 83,72 % | oui |
| `ecrans/journee/EcranAgenda.tsx` | 96,30 % | 87,50 % | 86,36 % | oui |
| `ecrans/journee/EcranFinDeJournee.tsx` | 98,18 % | 100 % | 88,57 % | oui |
| `ecrans/journee/EcranPilote.tsx` | 97,58 % | 100 % | 92,59 % | oui |
| `sauvegarde/photos.ts` | 26,76 % | 33,33 % | 100 % | partiel |

`grep -rln "EcranFinDeSession\|EcranRestauration" apps/field/src e2e/` → **quatre fichiers**, tous de
production (`App.tsx`, `coquille-l5c.tsx`, et les deux écrans eux-mêmes). **Aucun test, nulle part.**

Ces deux écrans portent **exactement** les deux gestes que la porte P-C nomme :
« **Terminer→note→Valider groupé** » (09 §4, P-C) et « **export de secours créé + restauré** »
(07 ligne L5, 09 §4). Ce sont les seuls chemins par lesquels l'auditeur les atteint.

**C'est la réserve B1 de mon contrôle L5a reproduite à l'identique, un incrément plus tard** :
`app/verrou.ts`, 227 lignes d'un livrable nommé au 11 §6, 0,00 % de couverture, invisible au seuil
faute de glob. La cause est la même : `apps/field/src/ecrans/**` n'est ni dans `cheminsCritiques` ni
dans `cheminsAttendus` de `.github/coverage-critical-paths.json`. **La leçon a été tirée pour un
fichier, pas pour la classe.**

*Pour être juste : `verrou.ts` est aujourd'hui à **100 / 100 / 100 / 100** (mesuré). **B1 de L5a est
fermée, et je la ferme ici par la mesure.***

---

## 4. LES CRITÈRES DU FICHIER 07, LIGNE L5 — COPIÉS MOT POUR MOT, COCHÉS UN PAR UN

> « Démo : écran « Aujourd'hui » §34.2 (agenda agrégé, à-revoir, sync par mission) ; mode avion
> complet sur iPad ET PC ; 1 session de chaque type créée hors ligne ; coupure de courant en pleine
> saisie = zéro perte ; **export de secours créé puis restauré sur un 2e appareil** ; test novice
> < 30 min (A54, guidé strict + grille §33) ; mode écran partagé démontré ; police rendue en mode
> avion »

| # | Critère 07 | Coché | Preuve — ou ce qui manque |
| --- | --- | --- | --- |
| **1** | Écran « Aujourd'hui » §34.2 (agenda agrégé, à-revoir, **sync par mission**) | ❌ | L'écran existe et est bien testé (`EcranAujourdhui.tsx` 93,14 % l.), l'agenda est agrégé (`agenda/jour.ts` 100 % l.). **Mais « sync par mission » est incochable avant L6b** : `PortSync` est **inerte** et rend `indisponible` (`LOT_L5.md` §3.6). C'est la réponse que j'avais déjà donnée en §M.7 du L5a et je ne la change pas. **Aggravation NEUVE mesurée par A54** : `PastilleSyncCoquille` est pilotée par `navigator.onLine` seul et affiche « En attente de synchronisation » alors qu'il n'y a rien à synchroniser, pendant qu'`EcranAccueil` affiche « Hors ligne » sur le même écran. **Deux pastilles, deux réponses opposées, aucune vraie.** La note de conception interdisait nommément « la pastille qui annonce plus qu'elle ne fait » |
| **2** | **Mode avion complet sur iPad ET PC** | ❌ | **`grep -rn "setOffline" e2e/` → AUCUNE OCCURRENCE**, sauf un commentaire de `playwright.config.ts:6`. **Il n'existe aucun scénario E2E hors ligne dans le dépôt**, alors que `LOT_L5.md` §4 le planifiait comme `@critique` (« mode avion intégral `context.setOffline(true)` — session créée, 11 types répondus… »). Le mode avion **réel** est en outre **dû à une machine réelle** (11 §7, limite assumée) |
| **3** | 1 session de **chaque type** créée hors ligne | ⚠️ | Les **six** `kind` existent et sont libellés (`agenda/sessions.ts:46-63`), `modeApplicable` isole le mode à `entretien` (03 §32.6-1). Domaine à **100 % de lignes**. **Mais aucune démonstration** : ni E2E, ni recette. A54 n'a pas pu créer une seule session (aucune identité d'auditeur sans serveur). **Dû à une machine réelle + serveur** |
| **4** | **Coupure de courant en pleine saisie = zéro perte** | ❌ | Aucun test de quelque niveau que ce soit. `LOT_L5.md` §4 le planifiait (« kill de l'onglet en pleine saisie = zéro perte ») ; il n'a pas été écrit. **Et le pack demande plus qu'un kill d'onglet** : 07 dit « coupure de **courant** ». **Dû à une machine réelle** |
| **5** | **Export de secours créé puis restauré sur un 2e appareil** | ❌ | **Le domaine est excellent et je le dis** : `sauvegarde/sauvegarde.ts` **97,10 / 100 / 96,43**, `format.ts` **100 / 100 / 100**, 31 cas dont `@critique une sauvegarde se restaure sur un appareil dont la DEK est DIFFÉRENTE`, refus d'un chiffré altéré, fusion par UUID qui n'écrase jamais une op locale plus récente, aucune sentinelle personnelle dans le fichier sérialisé. **Mais** : ① l'**écran** qui restaure est à **11,11 %** (B4) ; ② 07 §13 exige « restauration sur un **2ᵉ profil navigateur** » — **aucun E2E ne le fait** ; ③ le « 2ᵉ **appareil** » est **dû à une machine réelle** |
| **6** | **Test novice < 30 min (A54, guidé strict + grille §33)** | ❌ | **JOUÉ ET ÉCHOUÉ** — `docs/portes/RECETTE_NOVICE_L5_2026-09-06.md`, **NO-GO**, parcours interrompu à **t+3 min**. Voir B3 |
| **7** | **Mode écran partagé démontré** | ⚠️ | **Le mieux prouvé du lot en test** : `EcranEntretien.test.tsx` — `@critique en écran PARTAGÉ, aucune sentinelle interne n'est dans le DOM — ni texte, ni champ, ni motif` et `@critique la touche « E » bascule le mode, et l'état est affiché en permanence dans les deux sens`. A54 confirme par lecture que les éléments internes sont **retirés du rendu** (`!partage &&`), pas masqués en CSS — c'est la bonne façon. **Mais « démontré » veut dire montré** : A54 n'a pas pu l'atteindre (pas de session). **Dû à une machine réelle** |
| **8** | **Police rendue en mode avion** | ⚠️ | `e2e/polices.e2e.ts` prouve que les `.woff2` sont **émis, servis depuis l'origine, précachés** et qu'`Inter Variable` est **peinte** dans un navigateur réel. A54 confirme : `document.fonts` → `Inter Variable 100 900 loaded`, aucune requête externe. **Ce qui manque, et A54 le dit mieux que moi** : « j'ai prouvé que les `.woff2` sont précachés, **pas** qu'iPadOS la peint après un **cold start hors réseau**, PWA installée ». **KO mineur relevé** : `getComputedStyle(document.documentElement).fontFamily` = **`"Times New Roman"`** — seul `.axn-coquille` pose la police ; latent aujourd'hui (aucun `createPortal`), visible au premier dialogue hors coquille. **Dû à une machine réelle** |

### **BILAN : 0 coché ferme, 1 quasi (n° 8, à un cold start près), 4 explicitement non tenus, 3 dus à une machine réelle.**

Je ne coche aucun critère sur la foi d'un test `jsdom`. La délégation du 2026-09-04 ne dispense
d'aucune preuve, et je l'applique dans les deux sens : **je ne coche pas ce que je n'ai pas vu, et je
ne refuse pas ce qui est réellement prouvé** — c'est pourquoi les n° 3, 7 et 8 sont ⚠️ et non ❌.

### Les exigences propres à la porte P-C (09 §4) — la journée terrain §33.7

| Critère P-C | État | Preuve |
| --- | --- | --- |
| Session planifiée démarrée **en 1 tap** | ⚠️ **non joué** | `EcranAujourdhui.tsx:177-185` → `memoriserSessionCourante` + vue `entretien`, pré-rempli. A54 : « plausible, 1 tap + la coche + Démarrer », **et une réserve** : le bouton est `disabled` tant que l'accord n'est pas coché, **sans un mot pour dire pourquoi** (3ᵉ bouton grisé muet du parcours), et **rien n'est prévu si l'interlocuteur REFUSE** — alors que le refus est un fait d'audit à tracer → doute de spec **D-1** |
| **Aucun verrou en session active de 45 min** | ⚠️ **non joué** | Tenu par construction : `DELAI_INACTIVITE_MS.sessionActive` = 60 min > 45 ; `verrou.ts` à **100 %** de couverture sur les quatre métriques, 42 cas. `multi-appareils.test.tsx` : `@critique <écran> monté 45 min avec une session en cours : ni verrou, ni fermeture, ni navigation` — **sur quatre écrans**. **Réserve d'A54 que je reprends** : le délai ne passe à 60 min que si la session est **`en_cours`** ; une session ouverte et pas encore démarrée (l'auditeur attend son interlocuteur) reste à **15 min** — le cas le plus banal d'une matinée |
| « **Fin de journée** » en un geste | ❌ | L'écran fait les trois choses sous un bouton, par mission depuis la fermeture de B3-A29 (`EcranFinDeJournee.tsx:164`, 98,18 % l.). **DÉFAUT SÉRIEUX relevé par A54, invariant 8** : si le mot de passe est vide ou faux, **la sauvegarde n'est pas produite et `CLE_DERNIER_RITUEL` est écrite quand même** ; `rappelFinDeJournee()` ne lit que cette date, donc **le rappel s'éteint alors que rien n'a été sauvegardé**. §34.2 promet que « l'invariant 8 cesse d'être une discipline de mémoire » ; il le redevient, **en pire : silencieusement**. → réserve **NB-1**, bloquante pour P-C |
| **Terminer → note → Valider groupé** | ⚠️ | La chaîne est complète et bien séparée (`agenda/validation.ts` **100 % l. / 95,83 % br.**, `session/**` 98,77 %). **Mais son unique écran est à 7,69 %** (B4), et A54 relève qu'**il n'existe aucun bouton « Terminer » dans l'écran d'entretien** : sur la dernière question, « Suivant » est grisé et le seul geste est « **Quitter l'entretien** » — un libellé qui dit *abandonner*, pas *finir*. Le novice doit deviner |
| **Grille UX §33 : 4 états écran par écran** | ❌ | Voir §5 |
| Raccourcis complets · ancres visibles · écran partagé · police hors ligne | voir §5 | — |

---

## 5. LA GRILLE UX §33, ÉCRAN PAR ÉCRAN

**§33.2 — les quatre états.** Onze vues déclarées dans `app/vues.ts`. Relevé d'A54, que j'ai
recoupé sur les écrans L5c :

**4 écrans sur 11 tiennent les quatre états · 3 sont KO · 4 sont partiels** (état hors ligne réduit
à la pastille de coquille, sans le « rappel des capacités locales » que §33.2 demande).

Les trois KO, et ils ne sont pas égaux :

1. **`finDeSession` — l'erreur déguisée en vide, et c'est le plus grave.** Dans
   `EcranFinDeSession.tsx`, la lecture est enveloppée dans un `catch { return null; }`, et `null`
   est rendu comme l'état **vide** : « **Aucune session ouverte** ». **Une panne de lecture du
   stockage local annonce donc à l'auditeur que son entretien n'existe pas** — après 45 minutes
   dessus. §33.2 sépare « vide » et « erreur » **précisément** pour que ce message ne puisse pas se
   produire, et l'écran voisin `EcranAujourdhui` fait la bonne chose avec le même motif
   (`undefined` = chargement, `null` = échec, deux états distincts, commentaire à l'appui). **C'est
   une incohérence entre deux écrans du même incrément, sur le fichier qui n'a aucun test.**
2. **`accueil`** — deux états vides concurrents qui se contredisent (« Aucune mission… le
   téléchargement arrive avec la synchronisation » / « Démarrez-en un avec Nouvel entretien »), le
   bouton entre les deux, rattaché à aucun. §33.2 demande **un** état vide « qui dit quoi faire ».
3. **`nouvelEntretien`** — deux états sur quatre **sans issue** : l'état `erreur / Auditeur inconnu`
   et l'état `vide / Aucune unité` n'ont pas d'`actions`, et la coquille n'a pas de bouton retour.

**§33.2, angle mort structurel que je relève en propre** : `multi-appareils.test.tsx` — le fichier
qui porte les garanties §33 de L5c — travaille sur une liste de **quatre** écrans
(`EcranAujourdhui`, `EcranAgenda`, `EcranFinDeJournee`, `EcranPilote`). **`EcranFinDeSession` et
`EcranRestauration` en sont absents.** Ils échappent donc **en bloc** aux quatre gardes `@critique`
du lot : cibles tactiles ≥ 44 px, jetons existants, aucun style en ligne coloré, aucun verrou en
session de 45 min. **Les deux écrans les plus récents sont les seuls que la grille ne mesure pas.**

**§33.1 — fondations.** Police auto-hébergée **OK et mesurée** (`e2e/polices.e2e.ts`, `@critique`,
aucun CDN). Aucune couleur en dur : **vérifié par moi**, `grep -rnE "#[0-9a-fA-F]{3,8}|rgb\(|hsl\("`
sur `apps/field/src` (hors tests) → **sortie vide**. Alerte dans un rouge distinct de l'action
(`#8c0a33` vs `#c24a1b`), contrôlé par `tokens.test.ts`. `prefers-reduced-motion` OK.
**KO mineur** : `html`/`body` sans `font-family` → `Times New Roman` à la racine, latent.

**§33.3 — terrain.** Les **huit** raccourcis sont là (`session/raccourcis.ts`), la règle V2.8
(inactifs dans un champ, Échap rend le focus) est tenue **et testée `@critique`**. Ancres de
cotation visibles : **testé** (`@critique les ANCRES de cotation (guidance figée) sont passées à
l'échelle — visibles, pas mémorisées`). Indicateur « Enregistré » : **testé `@critique`**, allumé
**après** que `ecrireLocal` a rendu la main, et **testé qu'il n'apparaît pas si l'écriture échoue**.
**Partiel** : aucune **découvrabilité** des raccourcis — les libellés portent leur touche
uniquement si `pointeurFin`, et il n'existe aucune aide clavier globale.

**§33.5 — page `/design`.** §33.5 exige « Chacun : états complets + **exemple sur /design** ».
**Aucun fichier, aucune route `/design` n'existe dans le dépôt.** C'est un manquement réel de §33.5,
et il n'a de fiche nulle part → réserve **NB-2**.

**§33.6 — accessibilité.** `@axe-core/playwright 4.13.0` est installé **et enfin utilisé** — la
réserve R5 de mon contrôle L5b est **fermée sur son principe**. Mais le périmètre est étroit :
`e2e/accessibilite-l5a.e2e.ts` balaye **les trois écrans de L5a** (`deverrouillage`, `accueil`,
`stockage`) et son en-tête le dit lui-même (« les TROIS écrans livrés par L5a, **et eux seuls** »).
**Aucun balayage axe sur l'écran d'entretien (L5b) ni sur aucun des sept écrans de L5c.**
La ligne DoD « axe-core vert » est donc **verte sur 3 vues sur 11**.

---

## 6. TRAÇABILITÉ E1-E47 — LES DEUX SENS

### 6.1 Sens 2 — **code → exigences** (le contrôle anti-orphelin)

Les **25 artefacts de code** du diff `origin/main...8f93660` sous `apps/field/src`, relus un par un :

| Artefact | Citation | Rattachement |
| --- | --- | --- |
| `agenda/{sessions,jour,validation,pilote,unites,ecriture-session}.ts` | E12 · E23 · E24 · E6 | juste — M3, §19.1, §25.2-3, §34.2 |
| `ecrans/journee/Ecran{Aujourdhui,Agenda,FinDeJournee,Pilote}.tsx` | E12 · E23 · E24 · E38 · E6 | juste |
| `ecrans/journee/EcranFinDeSession.tsx` | E12 · E24 · E6 | juste (E24 = validation obligatoire §19.1) |
| `ecrans/journee/EcranRestauration.tsx` | E33 · E38 · E6 | juste (E38 = export de secours) |
| `ecrans/journee/{BandeauMiseAJour,coquille-l5c,PastilleSyncCoquille,vue-initiale}` | E23 · E6 · E7 | juste |
| `sauvegarde/{sauvegarde,format}.ts` | E33 · E38 · E6 | juste — 11 §4, invariant 8 |
| `local/{coffre,coffre-appareil}.ts` | E33 (· E38) | juste |
| `App.tsx`, `app/{vues,contexte,EcranDeverrouillage}` | E13 · E23 · E33 · E6 | juste |
| **`sauvegarde/photos.ts`** | E6 · E7 | **voir ci-dessous** |

**Gloses fausses : AUCUNE.** Les vingt-cinq citations résistent à l'ouverture de la section citée.
`check:tracabilite` → **RC=0**, 1008 citations, 460 fichiers.

**UN ORPHELIN FONCTIONNEL, ET IL EST DOCUMENTÉ : `apps/field/src/sauvegarde/photos.ts`.**
`check:graphe-modules` (RC=0) le nomme en tête de ses 25 modules « dont le seul consommateur est un
test ». **Il est en réalité pire que ça, et je le mesure** : `photos.test.ts:21` n'importe que
`COTE_MAXIMAL_PX`, `dimensionsCibles`, `QUALITE_JPEG`, `TYPE_SORTIE` — **`compresserPhoto` n'a
strictement AUCUN consommateur**, ni de production ni de test (`grep -rn "compresserPhoto"
apps/ packages/ e2e/` → une seule ligne, sa propre définition, `photos.ts:141`).

**Et la chaîne entière est absente**, confirmé par mes trois mesures indépendantes :
`type="file"` → **une** occurrence, `EcranRestauration.tsx:174`, `accept={EXTENSION_SAUVEGARDE}` —
c'est le fichier `.axionbackup`, **pas** une photo · `capture=` → **zéro** · `kind: 'photo'` →
**zéro** (seul `TYPES_DE_PIECE` de `formes.ts:91` en porte le mot).

**Ce n'est PAS un veto** : `DECISIONS.md` du 2026-09-05, « La chaîne PHOTO n'a de lot propriétaire
nulle part », arbitre **option 2** — lot **L5d**, ouvert **après P-C** et **avant L6c** — et exige
littéralement : « **le manque se DÉCLARE au contrôle A02 de P-C** plutôt que de disparaître : à cette
date, **03 §17.4 n'est pas tenu** et `photos.ts` est du code sans appelant — les deux doivent être
écrits au dossier de porte, pas découverts à P-E ». **C'est fait, ici même, et c'est cette section.**

**Conséquence sur le fichier 07 que la décision ne dit pas** : la ligne L5 met « **compression
photos R2** » dans le contenu du lot. Elle est livrée comme **bibliothèque sans chaîne** ; le
**contenu** du lot L5 est donc incomplet, indépendamment de l'arbitrage de séquence. Je le note comme
tel et non comme un écart.

### 6.2 Sens 1 — **exigences → code** : ce que L5 complet déplace

| Exigence | Avant L5 | Après L5 (`8f93660`) | Ce qui manque encore |
| --- | --- | --- | --- |
| **E6** — Hors ligne total, PC **et** tablette | partiellement amorcée | **partiellement amorcée** — tous les écrans lisent IndexedDB, le SW précache shell + polices, `/api` exclu de tout cache d'exécution. **Ne passe PAS à `couverte`** : aucun E2E hors ligne (`setOffline` absent), mode avion réel dû à A27 | E2E offline · iPad réel |
| **E12** — Entretiens, à-revoir, notes, ad hoc | partiellement amorcée | **partiellement amorcée** — écran 3 zones, 11 types, notes ×3, ad hoc, hors-parcours, agenda, 6 `kind`, complémentaire | démonstration sur données réelles |
| **E13** — Écran 3 zones, une question à la fois, enregistrement continu | partiellement amorcée | **partiellement amorcée** — structure et contrat JS↔CSS testés ; **la mise en colonnes PEINTE n'est mesurée par aucun test** (`jsdom` n'évalue pas les media queries) | recette d'appareils `LOT_L5.md` §4, lignes 1-3 |
| **E23** — Hyper intuitif, **novice autonome < 30 min** | partiellement amorcée | **EN RECUL DÉCLARÉ** — la recette A54 rend **NO-GO** | B3 |
| **E24** — Validation obligatoire (terminer ≠ valider) | partiellement amorcée | **partiellement amorcée** — la machine à états est à 98,77 % et le domaine `validation.ts` à 100 % ; **son écran est à 7,69 %** | B4 |
| **E33** — Sécurité / chiffrement local | partiellement amorcée | **EN RECUL MESURÉ sur cette branche** — F-22/F-23/F-25 annulés (V1) | V1 |
| **E38** — Sauvegarde terrain (sync ≥ 1×/j + **export testé**) | partiellement amorcée | **la moitié « export » reçoit enfin son objet** : `.axionbackup` conforme au 11 §4, clé dérivée du **mot de passe** (pas de la DEK), 97,10 % de lignes, restauration sur DEK différente testée `@critique`. **L'autre moitié reste L6.** Et le rituel de fin de journée **s'éteint sans avoir sauvegardé** (NB-1) | NB-1 · 2ᵉ appareil réel · sync L6 |
| **E44** — UX/UI 2026-2027, grille §33 | partiellement amorcée | **partiellement amorcée** — tokens, police, raccourcis, ancres, écran partagé, indicateur : tenus et testés. **4 états : 4/11.** **`/design` : inexistante** | §5 |
| **E45** — Cockpit auditeur « Aujourd'hui » | partiellement amorcée | **partiellement amorcée** — l'écran existe et l'arbitrage `vue-initiale` est propre et testé | sync par mission → L6b |

**Aucune exigence ne passe à `couverte` au lot L5.** C'est le constat honnête : L5 est le plus gros
lot du pack et il déplace beaucoup, mais **chacune de ses exigences bute soit sur L6, soit sur une
machine réelle, soit sur les quatre bloquants ci-dessus**.

### 6.3 État du fichier `docs/TRACABILITE_E1-E47.md` lui-même — **un défaut mesuré**

`diff <(sed -n '2278,2738p') <(sed -n '2739,3191p')` → **les sections L (L5b) et M (L5a) sont
présentes DEUX FOIS**, verbatim, à huit lignes près (un encadré de renumérotation). **≈ 910 lignes
dupliquées** dans un fichier déclaré « append-and-amend, jamais réécrit ». Séquelle d'une fusion.
Et **aucune section L5c n'existe** : la matrice s'arrête à L7a. → réserve **NB-3**.

---

## 7. LA DoD TRANSVERSE (CLAUDE.md §5), COCHÉE PAR EXÉCUTION

Toutes les commandes ci-dessous ont été **exécutées par moi**, sur `8f93660`, sous **Node v24.19.0**.

| # | Ligne de la DoD | État | Preuve exécutée |
| --- | --- | --- | --- |
| 1 | lint + typecheck stricts = **0 erreur** | ✅ | `pnpm lint` **RC=0** · `pnpm typecheck` **RC=0** (6 projets) · `pnpm format:check` « All matched files use Prettier code style! » |
| 2 | tous les tests verts, **aucun test skippé** | ✅ **sur ce que j'ai pu jouer** | `npx vitest run --project unit --project interface --coverage --testTimeout=60000` → **103 fichiers, 2151/2151, RC=0**. `check:no-skipped-tests` **RC=0** (132 fichiers) · `check:test-projects` **RC=0** (interface 49 · unit 54 · integration 24 · playwright 5). **Deux passes antérieures ont rendu 1 échec chacune, sur DEUX tests DIFFÉRENTS, tous deux par dépassement de 5 000 ms sous charge ; rejoué en isolation, `garde-fous-invariants.test.ts` passe en 942 ms. Ce sont des flocons de ma machine, pas des tests rouges — et je le dis plutôt que de laisser croire que tout a été vert du premier coup.** **`integration` (24 fichiers) et `e2e` (5) NON JOUÉS par moi** ; la CI de la PR les rend verts (`4 · integration` ✓ 5 m 41 s, `5 · e2e` ✓ 1 m 18 s, run `34016800692`) |
| 3 | **couverture ≥ 90 % sur les modules critiques — mesurée** | ⚠️ **verte sur L5, non concluante ailleurs** | `node .github/scripts/check-coverage.mjs` sur mon rapport. **Modules du lot L5, tous OK avec marge** : `field/src/local/**` **97,88 / 97,88 / 99,01 / 93,73** · `field/src/session/**` **98,77 / 98,77 / 98,41 / 94,43** · `field/src/app/verrou.ts` **100 / 100 / 100 / 100** · `sauvegarde/sauvegarde.ts` **97,10 / 97,10 / 100 / 96,43** · `sauvegarde/format.ts` **100 / 100 / 100 / 100**. Les modules `apps/api/**` sortent sous le seuil **parce que je n'ai pas joué le projet `integration`** — l'échec est de ma mesure, pas du dépôt ; la CI les rend verts. **CE QUE CE CHIFFRE NE MESURE PAS, et c'est B4** : `ecrans/**` n'est dans aucun glob, donc **7,69 % et 11,11 % sont invisibles au seuil** |
| 4 | migrations **up/down exécutées sur staging** | ⚠️ | **Exécutées par moi sur PostgreSQL 16 en conteneur** (`postgres:16`, base créée pour l'occasion) : `db:migrate` → « **15 appliquée(s), schéma à jour** » ; `db:migrate --down-to 0` → « **descente : 15 migration(s) annulée(s)** », 1 table résiduelle (le registre des migrations, attendu). **Ce n'est PAS staging** : `gh run list --workflow=deploy-staging.yml` rend **`[]`** — aucun déploiement de staging n'existe dans l'historique récent. **Delta de schéma de L5c : VIDE** (`git diff --name-only origin/main...HEAD` ne contient ni `drizzle/`, ni `.sql`, ni `schema.ts`) — le risque est donc nul, mais la ligne n'est pas cochée là où le pack la demande |
| 5 | tout écran livré avec ses **4 états** (§33.2) | ❌ | **4 écrans sur 11.** Trois KO, quatre partiels. `finDeSession` rend une **erreur de lecture comme un état vide**. Voir §5 |
| 6 | **axe-core vert** | ⚠️ | `@axe-core/playwright 4.13.0` installé et **enfin appelé** (`e2e/accessibilite-l5a.e2e.ts`, 5 cas ; `accessibilite-l7b.e2e.ts`, 7 cas) — **ma réserve R5 de L5b est fermée sur son principe**. **Mais le balayage couvre 3 vues sur 11** : rien pour l'écran d'entretien (L5b), rien pour les sept écrans de L5c. **Non joué par moi** (Playwright non exécuté) |
| 7 | **`@filrouge` vert sur FIL-TPE ET FIL-GC** | ⚠️ | Vert en CI sur les deux missions (`l1-filrouge` 5 cas, `l3-filrouge` par mission). **Mais NON ALLONGÉ** : `grep -rln "@filrouge"` → `aide/fil-rouge.ts`, `l1-filrouge`, `l3-filrouge`, `socle.e2e.ts`. **Aucun segment L5.** 09 §4bis veut que chaque lot ALLONGE le scénario (« session hors ligne, cotation, à-revoir, photo (L5) ») ; **L5a, L5b et L5c n'y ont rien ajouté**. C'est ma réserve **R6 de L5b, ouverte depuis trois incréments** |
| 8 | **README de l'app à jour** | ❌ | `apps/field/README.md:12` : « **## État au lot L5b** — le SOCLE **et** l'écran d'entretien » ; `:31` : « **PAS encore livré — c'est L5c (A23)** : l'agenda et le cockpit “Aujourd'hui” (§34.2)… ». **L5c EST livré sur cette branche** : 7 écrans, 6 modules `agenda/**`, 3 modules `sauvegarde/**`. **C'est ma réserve R2 de L5b, fermée le 2026-09-03, ROUVERTE à l'identique.** Le README annonce comme à venir ce que le commit contient : il n'est pas incomplet, il est **faux**, et c'est le premier fichier que lit quelqu'un qui arrive |
| 9 | aucun TODO/FIXME sans entrée `DECISIONS.md` / `AMELIORATIONS.md` | ✅ | `grep -rn "TODO\|FIXME"` sur `apps/*/src` et `packages/*/src` → **sortie vide** |
| 10 | **diff schéma-vs-04 = zéro écart** | ✅ **MESURÉ PAR MOI** | `DATABASE_URL=… pnpm schema:diff` → « **✓ diff schéma-vs-04 : ZÉRO ÉCART** — 44 tables · 487 colonnes · 202 contraintes PK/FK/UNIQUE/CHECK · 38 index du §7.1 · 22 index de convention ». `pnpm check:schema-inventaire` → « **aucun objet hors des 8 familles surveillées** » (ni trigger, ni RLS, ni IDENTITY, ni table non permanente) |

### **DoD : 5 lignes vertes sur 10** (1, 2, 9, 10 fermes ; 3 verte sur le périmètre L5) · **2 non tenues** (5, 8) · **3 partielles** (4, 6, 7).

**Les garde-fous du dépôt, tous exécutés, tous RC=0** : `check:pack` (12 fichiers conformes au
sceau) · `check:decisions` (**283** entrées au format) · `check:prose` · `check:invariants` ·
`check:jonction` (51 scripts, 105 variables, 12 fichiers de CI) · `check:graphe-modules`
(206 modules, 17 points d'entrée, aucun import pendu) · `check:porte-journal` (391 fichiers,
porte d'écriture unique) · `check:tracabilite` (1008 citations, 460 fichiers) ·
`check:no-skipped-tests` · `check:test-projects` · `check:isolation-reseau` ·
`check:compose-coolify` · `check:executabilite`.

---

## 8. LES 8 INVARIANTS, UN PAR UN

| # | Invariant | État | Preuve |
| --- | --- | --- | --- |
| **1** | Offline-first, **UUID v7 client**, push idempotent | ✅ pour la part L5 | Toute création locale passe par `uuidv7()` (`agenda/unites.ts:99`, `sessions.ts`) ; **aucune fonction SQL de génération v7** : `grep -rni "uuidv7()" apps/api/drizzle/*.sql` ne rend que **deux commentaires** qui rappellent l'interdiction. Le SW ne met **aucune** réponse `/api` en cache d'exécution. *Push idempotent = L6.* |
| **2** | Aucune référence client dans le code | ✅ | `check:invariants` RC=0 ; le nom de fichier `.axionbackup` est **testé** pour ne porter « NI nom de client NI donnée personnelle » (`sauvegarde.test.ts:569`, `@critique`) |
| **3** | RBAC serveur, financier admin, écritures de sync réservées au propriétaire | ✅ **sans objet ici** | L5c ne touche **aucun** fichier de `apps/api`. Acquis à P-B, non rejoué |
| **4** | **Aucune couleur/taille en dur** | ✅ **mesuré par moi** | `grep -rnE "#[0-9a-fA-F]{3,8}\b\|rgb\(\|hsl\("` sur `apps/field/src` (hors tests) → **vide**. `multi-appareils.test.tsx` : `@critique journee.css n'écrit ni couleur hexadécimale, ni fonction de couleur, ni longueur absolue` et `@critique chaque var(--jeton) des sources L5c est DÉFINI dans tokens.css`. Alerte `#8c0a33` distincte de l'action `#c24a1b` |
| **5** | Interface 100 % française, horodatages **UTC** | ✅ | Une seule horloge : `local/horloge.ts`, `maintenant()` en ISO 8601 UTC, imposée par ESLint. Les `new Date(x)` restants (`EcranAgenda.tsx:66`, `depots/sessions.ts:55`) **parsent une valeur existante** pour l'affichage au fuseau — ils ne lisent pas l'heure. Aucun libellé anglais visible |
| **6** | Le terrain collecte, le siège produit | ✅ | Le terrain ne calcule que sur ses propres lignes (`agenda/jour.ts`) ; ni scoring, ni agrégation multi-sessions, ni DOCX, ni LLM. La compression photo est l'exception délibérée du `LOT_L5.md` §3.5 — et elle n'a pas d'appelant (§6.1) |
| **7** | Toute correction = révision tracée ; **rien n'est jamais silencieusement écrasé ou supprimé** | ❌ **ROMPU, et deux fois** | ① **V1** : 404 lignes de code de sécurité et **27 cas de test** supprimés par une résolution de fusion, sans une ligne de trace nulle part. ② `validation.ts:151` (**M8 d'A29, OUVERT**) : `deverrouillerSession` remet `valideeLe` à `null` — une perte déclarée mais non corrigée |
| **8** | Sauvegarde terrain : sync ≥ 1×/j + **export de secours testé** ; alerte au-delà de 24 h | ⚠️ **la moitié « export » arrive, et elle se sabote au dernier mètre** | L'export `.axionbackup` est réel, conforme au 11 §4, et **bien testé** (31 cas `@critique`). L'alerte 24 h locale existe (`EcranAccueil.test.tsx:261`). **MAIS** : ① le bouton « Fin de journée » **écrit `CLE_DERNIER_RITUEL` même quand la sauvegarde n'a pas été produite**, donc le rappel s'éteint sans que rien ne soit sorti de l'appareil (**NB-1**) ; ② les **deux pastilles de synchronisation se contredisent** et aucune n'est vraie ; ③ l'**écran de restauration est à 11,11 %**. *La sync elle-même est L6.* |

**6 invariants tenus sur 8.** L'invariant **7** est rompu — c'est **V1**. L'invariant **8** est
partiel, et son défaut (NB-1) est du type précis que §34.2 voulait éliminer.

**Interdictions 11 §2, vérifiées `grep` à l'appui** : pas de fonction SQL v7 ✅ · pas de Next.js ✅ ·
pas de Prisma ✅ · pas de CORS ✅ · MinIO non exposé ✅ (`check:isolation-reseau` RC=0) · aucune
donnée personnelle dans pino ✅ (hors périmètre L5c, `check:porte-journal` RC=0) · aucun secret
versionné ✅ (`gitleaks` ✓ en CI) · **aucun test skippé ✅ — mais 27 tests SUPPRIMÉS, ce que ce
garde ne voit pas (V1)**.

---

## 9. RÉSERVES NUMÉROTÉES — ET, POUR CHACUNE, CE QU'ELLE BLOQUE

### Bloquantes pour la porte P-C (4)

| # | Réserve | Bloque | Ce qui la lève |
| --- | --- | --- | --- |
| **V1** | La fusion de PR #52 **annule F-22 (CRITIQUE), F-23, F-25** — 7 symboles, 27 tests, 404 lignes | **la porte ET le merge** | refusion en conservant la version `main` des 4 fichiers + désarmement de l'auto-merge + re-mesure ③ et ④ |
| **B2** | **Étape 4 non close** : 4 commits de code postérieurs au dernier verdict A29, dont celui qui ferme B1 | **la porte** | un 3ᵉ passage A29 écrit, sur l'arbre refusionné |
| **B3** | **Recette novice A54 = NO-GO**, parcours interrompu à t+3 min | **la porte** (c'est un critère 07 **et** 09 §4) | correction des 2 bloquants A54 + rejeu intégral de la recette (09 §4bis : « la porte se rejoue EN ENTIER ») |
| **B4** | `EcranFinDeSession` **7,69 %** et `EcranRestauration` **11,11 %**, aucun fichier de test, **hors de tous les gardes §33** | **la porte** | des tests écrits par un autre agent qu'A23 (09 §5.6), **ou** l'inscription motivée d'`ecrans/**` au seuil — avec un chiffre > 90, jamais avec un glob élargi |

### Bloquantes pour la porte, de moindre ampleur (3)

| # | Réserve | Bloque | Preuve |
| --- | --- | --- | --- |
| **NB-1** | « Fin de journée » écrit `CLE_DERNIER_RITUEL` **même sans sauvegarde produite** → le rappel de l'invariant 8 s'éteint tout seul | **la porte** (invariant 8) | relevé A54 §4 ; `EcranFinDeJournee.tsx`, `rappelFinDeJournee()` |
| **NB-2** | **4 états : 4 écrans sur 11**, dont `finDeSession` qui rend une **erreur de lecture comme un état vide** | **la porte** (09 §4, P-C, nommément) | §5 |
| **NB-3** | **ZAP** : les **deux** conditions posées par `DECISIONS.md` du 2026-09-05 pour P-C sont **non faites**. ① `ZAP_BLOQUANT: 'false'` (`zap-baseline.yml:119`) ; ② la couverture **n'est pas étendue** : `zap-baseline.py -t "${CIBLE}"` **seul**, aucun contexte, aucune URL de départ `/hq` ou `/api`, aucune authentification (`zap-baseline.yml:203-209`) | **la porte** — l'arbitrage dit « le dossier de porte P-C coche donc **deux** choses : la couverture étendue **puis** `ZAP_BLOQUANT='true'` » | mesuré |

### Non bloquantes pour P-C, à porter au dossier (8)

| # | Réserve | Portée |
| --- | --- | --- |
| **NB-4** | **La chaîne photo n'existe pas** : `compresserPhoto` sans aucun consommateur, zéro `capture=`, zéro `kind:'photo'`. 03 §17.4 **non tenu**. Documenté (L5d, après P-C) — **mais `EcranAccueil.tsx:109` promet toujours « et des photos »**, et le bouton « Photo » est grisé **muet** (motif dans l'`aria-label` seul). **Le retrait de la promesse est d'étage 1 et devrait être fait avant la démo** | L5d · **risque RGPD immédiat** |
| **NB-5** | **`@filrouge` non allongé du segment L5** (09 §4bis) — ouvert depuis L5b, trois incréments | L6 |
| **NB-6** | **axe-core ne balaye que 3 vues sur 11** | L5d / L6 |
| **NB-7** | **`/design` n'existe pas** alors que §33.5 l'exige pour chaque composant | Phase 1 |
| **NB-8** | **Aucune ligne L5a NI L5c au compteur du plafond étage 1** (`AMELIORATIONS.md` s'arrête à `L5b`). Le plafond 0,5 j/lot est **invérifiable** : je ne peux ni le cocher ni le contester. C'est **ma réserve NB5 de L5a, jamais fermée** | gouvernance |
| **NB-9** | **`TRACABILITE_E1-E47.md` : ≈ 910 lignes dupliquées** (sections L et M en double) et **aucune section L5c** | gouvernance |
| **NB-10** | Budget A28 « **chiffrement < 50 ms/écriture** » (11 §4) **toujours mesuré nulle part** — ma réserve NB6 de L5a, non fermée. Seule la dérivation < 1 s est bornée, et sur un runner, pas sur un iPad | P-E |
| **NB-11** | Réserves d'A29 **restées ouvertes** au rejeu et non refermées depuis : **M2** (borne « 3 étapes » hors cas nominal), **M3** (aucun filtre `conductedBy`), **M6** (R2 sans appelant), **M7** (régime d'`App.tsx` élargi sans entrée `DECISIONS.md`), **M8** (`valideeLe` effacé sans trace), **m2** (clé React sur le titre de mission) | L5d / L6 |

### Réserves antérieures que je FERME ici, par la mesure

| Réserve | Origine | Fermée par |
| --- | --- | --- |
| **B1 (L5a)** — `app/verrou.ts`, 227 l. à 0,00 % | `CONTROLE_A02_L5a_2026-09-03` | **100 / 100 / 100 / 100** mesuré, 42 cas, glob inscrit dans `coverage-critical-paths.json` |
| **B2 (L5a)** — aucun verdict A51 sur l'incrément de crypto | idem | `docs/securite/VERDICT_A51_L5A.md` (58 848 o.). *Réserve de forme : il vit dans `docs/securite/`, non dans `docs/portes/` comme la réserve le demandait — sans conséquence* |
| **R5 (L5b)** — axe-core absent du dépôt | `CONTROLE_A02_L5b_2026-09-03` | installé **et appelé** — fermée **sur son principe**, rouverte sur son périmètre (NB-6) |
| **R4 (L5b)** — 5 types de saisie jamais rendus | idem | reste une **consigne de recette** (`LOT_L5.md` §4), non une infraction à la DoD. **Toujours due à la recette d'appareils** |

---

## 10. CE QUI RESTE DÛ À UNE MACHINE RÉELLE — la liste exacte

**C'est la partie la plus importante de ce dossier**, et je ne coche aucun de ces points sur la foi
d'un test `jsdom` ou `fake-indexeddb`. La liste ci-dessous fusionne le §5 de la recette A54, la
recette d'appareils de `LOT_L5.md` §4 et mes propres constats.

**A. Dû à un SERVEUR (staging, API montée) — aucune identité d'auditeur ni mission n'existe sans lui**

1. **Embarquement d'une mission FIL-TPE puis FIL-GC**, avec `storage.persist()` **accordé** — et le
   comportement quand il est **refusé** (Safari iPadOS le refuse volontiers) : la mission doit rester
   **NON embarquée**, écran de guidage, jamais un avertissement qu'on clique pour passer outre.
2. **Le cockpit « Aujourd'hui » sur de vraies données** : agenda agrégé multi-missions, à-revoir,
   alertes locales, « Reprendre là où vous vous êtes arrêté » — **critère 07 n° 1**.
3. **Un entretien complet**, du démarrage à la validation, **sur les deux missions fictives**.
4. **1 session de chacun des 6 `kind`**, créée hors ligne — **critère 07 n° 3**.
5. **Les 5 formes de saisie que rendus aucun test n'exerce** (`LOT_L5.md` §4, second point de
   recette) : `money` avec sa devise, `date`, `single_choice`, **`multi_choice` en cochant au moins
   deux options**, `table` — **puis rouvrir la session et vérifier que la valeur relue est celle
   saisie**. Un rendu qui s'affiche sans enregistrer serait invisible à l'œil.
6. **Migrations up/down sur STAGING** (je les ai jouées sur un PostgreSQL jetable ; `deploy-staging`
   n'a **aucun run** dans l'historique récent).
7. **ZAP sur `/hq` ET `/api`, authentification comprise**, puis `ZAP_BLOQUANT='true'` — les deux
   conditions de l'arbitrage du 2026-09-05, dans cet ordre.

**B. Dû à un APPAREIL PHYSIQUE — aucune machine virtuelle ne les donne**

8. **Mode avion RÉEL**, sur **iPad physique** ET sur **desktop** — pas `navigator.onLine` forcé.
   **Critère 07 n° 2.** Limite déjà assumée au 11 §7 (Playwright ne couvre pas le SW sous iOS).
   *A54 a montré que ce n'est pas une formalité : les deux pastilles se contredisent précisément
   parce que personne n'a regardé un écran vrai dans un état vrai.*
9. **La police peinte hors ligne**, PWA **installée** (pas un onglet), après **cold start** sans
   réseau. **Critère 07 n° 8** — la moitié « précachée » est prouvée, la moitié « peinte par iPadOS
   après cold start » ne l'est pas.
10. **Coupure de COURANT en pleine saisie** — arracher l'alimentation, pas fermer l'onglet. Vérifier
    la reprise **exactement sur la question en cours**, aucune réponse perdue. **Critère 07 n° 4.**
11. **Export de secours créé puis restauré sur un DEUXIÈME appareil PHYSIQUE**, mot de passe saisi à
    la main sur clavier virtuel. **Critère 07 n° 5**, et c'est celui qui protège l'invariant 8.
    *(Le domaine est prouvé ; le geste ne l'est pas, et l'écran qui le porte est à 11,11 %.)*
12. **Session active de 45 min sans toucher l'écran** : aucune ressaisie, et le **Wake Lock** tient
    sur Safari (l'API manque avant 16.4 — le code le prévoit, l'appareil décidera). **§33.7.**
13. **Les trois lignes de la recette d'appareils `LOT_L5.md` §4** — et la note dit que **si l'une des
    trois échoue, P-C n'est pas franchie** : ① **iPad paysage ≥ 1024 px : les TROIS zones CÔTE À
    CÔTE**, note saisissable sans ouvrir de panneau ; ② **PC ≥ 1280 px** : idem + rappels de
    raccourcis visibles ; ③ **iPad portrait < 1024 px** : repli en deux panneaux. **`jsdom`
    n'évalue pas les media queries** : la suite éprouve la structure et le contrat JS↔CSS, **jamais
    la mise en colonnes PEINTE**, qui est le livrable-titre de L5b (03 M3.1).
14. **Cibles tactiles au doigt** (≥ 44 px), pas au pixel dans un navigateur de bureau — **et sur les
    deux écrans qu'aucun garde ne mesure** (`finDeSession`, `restauration`).
15. **Mode écran partagé DÉMONTRÉ** devant témoin — **critère 07 n° 7**. Le test prouve que rien
    d'interne n'est dans le DOM ; la démo prouve le geste.
16. **Ancres de cotation visibles sous le curseur**, à l'œil, sur des questions réelles — §33.3.
17. **Recette novice n°1 REJOUÉE EN ENTIER** après correction (09 §4bis : jamais « les points qui
    manquaient »). **Critère 07 n° 6.**

---

## 11. CE QUE JE N'AI PAS PU VÉRIFIER, ET QUI DOIT ÊTRE DIT

1. **`test:integration` (24 fichiers) et `test:e2e` (5) : non joués par moi.** Je m'appuie sur les
   jobs CI verts du run `34016800692` de la PR, et je le dis plutôt que de laisser croire que j'ai
   tout rejoué. Conséquence directe : **`@filrouge` est vert par la CI, pas par moi**, et les
   modules `apps/api/**` sortent sous seuil dans **ma** mesure pour cette seule raison.
2. **Mon environnement est hors contrat** (Node v24 contre 22). Tout vert obtenu localement **répond
   à côté**. Que mes chiffres de couverture sur `local/**` et `session/**` reproduisent l'ordre de
   grandeur attendu est une corroboration, pas une preuve d'équivalence.
3. **Je n'ai pas ouvert de navigateur.** Tout ce que je dis de l'écran vient soit d'un test, soit de
   la lecture du code, soit de la recette A54 — que j'ai recoupée sur ses trois constats principaux
   (§3, B3) mais pas rejouée.
4. **Le rapport ZAP lui-même n'a pas été relu** : je constate la configuration (`-t` seul,
   `ZAP_BLOQUANT: 'false'`), pas le contenu du dernier scan.
5. **Ma part dans B4.** J'ai signé, à l'étape 1bis, une note de conception dont le plan de tests §4
   ne nommait **aucun** test d'écran pour `finDeSession` ni pour `restauration` — pour la bonne
   raison que ces écrans n'existaient pas encore, étant nés de la fermeture des bloquants d'A29.
   **La règle que j'avais tirée de B1 à L5a — « un module nommé au 11 §6 doit avoir une ligne dans le
   plan de tests de sa note » — ne couvre pas les modules nés APRÈS la note.** C'est le trou par
   lequel B4 est passé, et je le nomme plutôt que de le laisser se rouvrir à L5d et à L6.
6. **Je n'ai pas vérifié la branche `lot/l5a-reserves`** (PR ouverte, « les six réserves de sécurité
   fermées, et le coffre passe de 89,51 à 100 pour cent »), hors du périmètre de ma passe. **Si elle
   porte encore du correctif de sécurité non fusionné, V1 est pire que ce que j'ai mesuré**, et A01
   doit le vérifier avant toute refusion.

---

## 12. DOUTES DE SPÉCIFICATION À PORTER EN `DECISIONS.md`

Ce ne sont **pas** des réserves : ce sont des points où le pack ne tranche pas, et je refuse de
deviner (CLAUDE.md §3).

- **D-1 — Le REFUS de participation d'un interlocuteur.** L'écran de démarrage exige la coche
  d'accord et n'offre **aucun chemin** si l'interlocuteur refuse. Or un refus est un **fait d'audit**
  (§27.4 traite le « non communiqué » ; il ne dit rien du refus d'entretien). Faut-il le tracer ?
  **Williams** — c'est un choix produit, et il a un effet RGPD.
- **D-2 — Aucun garde ne compare le nombre de cas de test avant et après une fusion.**
  `check:no-skipped-tests` voit les tests **désactivés**, jamais les tests **supprimés**. V1 est
  passé exactement par là. Un tel garde n'est **écrit nulle part** dans le pack : je ne peux pas
  l'exiger, je le propose. **A01.**
- **D-3 — Le verrou à 15 min sur une session ouverte mais pas encore `en_cours`.** 05 §9.7 nomme
  deux délais (15 hors session / 60 pendant une session `en_cours`) et 03 §33.7 parle d'une « session
  active de 45 min ». Une session **planifiée et ouverte**, l'auditeur attendant en salle, tombe dans
  le premier cas. Est-ce voulu ? **A01.**
- **D-4 — `apps/field/src/ecrans/**` doit-il entrer au seuil de couverture ?** Le doute est déjà
  tracé au 2026-09-03 et attend **A01** ; B4 lui donne son chiffre : deux écrans à 7,69 % et 11,11 %,
  invisibles au garde. Le motif « à extraire un jour » vaut pour le calendrier, pas pour le principe.
- **D-5 — `LOT_L5.md` §5-3** (« qui écrit `packages/shared/src/sync.ts` ? ») : toujours **aucune
  entrée** au registre, alors que `local/contrat-sync.ts:4` **affirme** citer « l'arbitrage A01 sur le
  point `LOT_L5.md` §5-3 ». Le code cite un arbitrage absent du registre. Ma réserve de L5a, non
  fermée. « Une décision non tracée dans ce format n'existe pas » (11 §9bis).

---

## 13. CE QUE CE LOT A DE MEILLEUR — parce que c'est ce qui rend ses défauts lisibles

Un contrôle qui ne dit que ce qui manque déforme. Trois choses méritent d'être écrites :

- **L'export de secours est le meilleur module du lot, et peut-être du dépôt.** La clé dérive
  réellement du **mot de passe** et non de la DEK d'appareil — c'est **ce qui le rend restaurable
  ailleurs**, et le test le prouve sur un appareil à DEK différente. Un chiffré altéré est refusé.
  Une op locale plus récente n'est **jamais** écrasée. L'import **annonce** les opérations qu'il ne
  réinjecte pas, « il ne les tait pas ». Le nom du fichier est **testé** pour ne porter aucun nom de
  client. 97,10 % de lignes, 31 cas.
- **L'arbitrage `vue-initiale` est un modèle de la conduite qu'on attend.** Deux écrans portaient le
  titre « Aujourd'hui » ; plutôt que d'en retirer un depuis un fichier déclaré append-only, la règle
  a été **écrite, isolée dans une fonction pure, testée sur ses deux cas**, et le fichier explique
  pourquoi elle ne touche pas la reprise instantanée. 100 % sur les quatre métriques.
- **Les corrections d'A29 sont arrivées par les tests d'A27**, et le code le dit lui-même (« c'est
  le test d'A27 qui l'a attrapé », « ce garde manquait, et A27 l'a mesuré »). Le croisement 09 §5.6
  fonctionne exactement comme il doit. C'est aussi ce qui rend **B4** frappant : les deux écrans
  écrits **après** le départ des testeurs sont les deux seuls sans tests.

---

## 14. VERDICT ET SUITE DE LA CHAÎNE

> # 🔴 REFUSÉ — VETO sur V1
>
> **Critères du fichier 07, ligne L5** : **0 coché sur 8** (1 quasi, 3 dus à une machine réelle,
> 4 non tenus). **DoD transverse** : **5/10**. **Invariants** : **6/8** — le **7** est rompu.
> **Traçabilité sens 1** : aucune exigence ne passe à `couverte`. **Traçabilité sens 2** : aucune
> glose fausse, **un orphelin fonctionnel (`photos.ts`), documenté**.
> **Écarts NON documentés : 1 — V1.**
> **Réserves : 4 bloquantes majeures (V1, B2, B3, B4) + 3 bloquantes de moindre ampleur
> (NB-1, NB-2, NB-3) + 8 non bloquantes.**

**Ordre des actions, et V1 passe avant tout le reste :**

1. **A01 — immédiatement : DÉSARMER L'AUTO-MERGE DE PR #52.** Tant qu'il est armé, `main` est à un
   run vert de perdre le correctif d'un constat CRITIQUE. C'est la seule action de cette liste qui
   ne peut pas attendre demain.
2. **A20/A24 — refusionner `lot/l5c` sur `origin/main`** en conservant la version `main` des quatre
   fichiers de sécurité ; re-mesurer les sept symboles et le compte de `it(`.
3. **A29 — troisième passage** sur l'arbre refusionné, couvrant les quatre commits jamais relus
   (`b2d0a90`, `227c2e1`, `ea4265a`, `c1f228d`).
4. **A20 — affecter B4** à A26 ou A27 (jamais à A23 — 09 §5.6) : tests des deux écrans.
5. **A22/A23 — étage 1, avant la démo** : retirer « et des photos » de `EcranAccueil.tsx:109` ;
   rendre visible le motif du bouton « Photo » ; corriger le message « Mot de passe incorrect » du
   premier écran ; NB-1 (`CLE_DERNIER_RITUEL`) ; `finDeSession` erreur ≠ vide ; un bouton de retour
   dans la coquille.
6. **A52/A51 — ZAP** : étendre la couverture à `/hq` et `/api`, **puis** `ZAP_BLOQUANT='true'`.
7. **A54 — rejouer la recette novice EN ENTIER** (09 §4bis).
8. **A01 — poser les entrées `DECISIONS.md`** de D-1 à D-5, et **arbitrer A-008 (AAD)** sur la
   prémisse corrigée par A51 : *« la menace suppose un attaquant qui a déjà passé le verrou et le
   coffre » est FAUX — écrire dans IndexedDB n'exige ni l'un ni l'autre.* J'ajoute l'argument que le
   code m'a donné et que je n'ai vu écrit nulle part : **`coffre.ts:228-232` rend délibérément la
   même `MotDePasseInvalideError` pour « mauvaise clé » et pour « chiffré altéré »** — c'est le bon
   choix contre un oracle, mais il a un corollaire que personne n'a tiré : **sur une corruption
   réelle d'enveloppe de DEK, l'auditeur lit “mot de passe incorrect” et ne verra JAMAIS le
   “Ne créez PAS de protection sur cet appareil” de `DonneesSansCoffreError`.** Le garde-fou existe
   (sur `main`) et le chemin qui y mène est fermé par le message. **Sans AAD ni intégrité de
   l'en-tête d'index, l'application ne peut pas distinguer une altération d'une faute de frappe —
   et c'est cette indistinction, pas la substitution théorique, qui coûtera une journée de collecte.**
9. **Williams — P-C ne se tient pas dans cet état.** Quand elle se tiendra, elle se jouera **en
   entier** (09 §4bis), avec la liste du §10 déroulée sur du matériel.

---

*Passe effectuée le 2026-09-06 par **A02**, gardien de la spécification, sur `origin/lot/l5c`
@ `8f93660`, en **lecture seule sur le code**. Aucun fichier du dépôt n'a été modifié ; ce document
est déposé **non commité**, conformément à la règle du 2026-09-05. Toutes les commandes citées ont
été exécutées ; ce qui est déduit est écrit « déduit ». Aucune ligne datée antérieure d'aucun
fichier de traçabilité n'a été touchée.*
