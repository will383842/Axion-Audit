{
  "//": [
    "tsconfig RACINE — ne compile rien : il ne sert qu'à donner un projet aux",
    "fichiers de configuration TypeScript de la racine (vitest.config.ts,",
    "vitest.workspace.ts, eslint.config.js), pour qu'ESLint puisse les analyser",
    "avec les règles TYPÉES au lieu de les rejeter comme « hors projet ».",
    "Le code applicatif est couvert par les tsconfig de chaque espace de travail."
  ],
  "extends": "./tsconfig.base.json",
  "compilerOptions": { "types": ["node"], "lib": ["ES2023"] },
  "include": ["vitest.config.ts", "vitest.workspace.ts", "scripts/**/*.ts"]
}
